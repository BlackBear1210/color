# GRAY (color) — 세션 시작 브리핑

> 이 파일은 **Claude Code 가 세션을 열 때 자동으로 읽는다.** 짧게 유지할 것.
> 자세한 것은 전부 `docs/` 에 있고, 여기는 **어디를 봐야 하는지**만 적는다.
> 최종 갱신: 2026-08-18 · 도형

---

## 지금 상태 (60초)

- **2D 흑백 다크판타지 퍼즐 플랫포머.** 플레이어는 검정/흰색 중 하나. 물감총으로 지형을 칠한다.
  ★핵심 규칙: 몸에 닿은 지형이 **내 색과 다르면 즉사**.
  ★[2026-08-30 변경] **안 칠한 지형은 무색이 아니라 검정**이다 — 화면이 검정이니까.
    흰색 플레이어는 안 칠한 바닥에서 죽는다. 회색만 안전.
    규칙 전문: `docs/색판정_규칙_2026-08-30.md` · 코드: `scripts/스마트월드/색규칙.gd`
    (지금은 2층방 지형 + 칠할 수 있는 장애물에 켜져 있다)
  ★[2026-08-18] **빛도 이 규칙 안에 들어왔다.** 굳은 빛은 밟을 수 있고, 색이 다르면 죽는다.
- 엔진: **Godot 4.6.3-stable** · 브랜치 **dev_4** · 지형은 SmartShape2D(SS2D).
  ⚠ 콘솔 exe 는 **한글 경로에서 본체를 못 찾는다** → `C:\Users\Public\godot46\` 에 복사해 쓴다.
- 게임 켜기: F5 → 로비 → **시작** (= 집 · 거실)
- 스테이지 **7개**가 **통로로** 이어진다 (포탈 없음):
  `거실(5) → 굴뚝(6) → 지붕(7) → 숲의 초입(1) → 뒤틀린 나무들(2) → 늪으로(3) → 폐수로(4)`
  ⚠ 번호는 파일 이름표일 뿐이고 **진행 순서는 표의 줄 순서**다.
- ★순서·챕터·배경색·BGM 톤은 전부 `scripts/스마트월드/챕터.gd` 의 **표 하나**에 있다.
  씬 파일에는 순서 정보가 없다.

## 무엇부터 읽나

| 상황 | 문서 |
|---|---|
| **작업을 시작한다** | `docs/다음작업_프롬프트.md` ★ 여기부터. 검사 명령·할 일·지뢰밭이 전부 있다 |
| 카메라를 만진다 | `docs/카메라_공간전환_가이드.md` |
| 새 스테이지를 만든다 | `docs/레벨디자인_4스테이지_2026-08-17_도형.md` + `docs/레벨디자인_가이드.md` |
| **하수도에 새 맵을 만든다 (분기형)** | `scenes/world_2_클로드/가이드라인_스테이지2_분기형맵_2026-09-12.md` ★ FBWG 식 갈림길·모양 어휘·SS2D 붙이기 규칙 · 아스트라용 프롬프트는 옆 `프롬프트_아스트라_스테이지2_맵제작.md` · ★[2026-09-14] 빌더는 **`tools/하수도_빌더_공통.gd`** 를 쓴다(`build_하수도_2-1.gd`·`2-2.gd` 가 본보기). 기준값·검산·일방통행·편집상태 인스턴스가 다 여기 있다 |
| **하수도 챕터가 어떤 게임인가** | `docs/맵분석_2026-09-07_Claude_하수도_2-1_2-7.md` ★ 재미·지루함·고칠 순서 |
| **최근에 뭘 했나** | `docs/작업기록_2026-09-17_Claude_2-3_재제작_호퍼실4_막다른길3.md` ★ **2-3 「관을 따라서」 v2 새로 지음** — 12,000×3,200 · 호퍼실 4 · 2 층 순환 · 막다른 길 3(되돌아와야 함) · 빙 도는 길 1 · 지름길 0 · 발수 4. 주행검사 정답 43 + 헛걸음 71 단계 사망 0 · 레벨검사 54/56. ★공통 빌더가 이제 **하수도 프리팹**을 찍고 `공중선반()`·`내부무늬()`·`호퍼(출구유체)`·`회전톱()` 이 있으며 저장이 `[editable]` 을 남긴다. ⚠ 회전톱은 헤드리스 검사에서 거의 안 움직인다(실시간 delta) — 사람이 봐야 한다 |
| 그 전 | `docs/작업기록_2026-09-17_Claude_2-2_하수도외관_선반_무늬_물너비.md` ★ **2-2 를 2-1 외관 기준으로**: 흰 탑 발판 9 장 → 공중선반(ledge_v03) · 내부 반대색 무늬 4 곳 · S01 물 64→448(이중 전환 우회 차단) · 세로 맵 배경. ⚠ **탑 발판 일방통행이 HEAD 에서 사라져 있던 것 복구**(에디터 재저장이 편집 불가 인스턴스 내부 덮어쓰기를 버린다 → `[editable path=]` 필요). 도구 `tools/apply_stage22_sewer_look.py`(dry-run 기본) · 검사 `tools/test_stage22_sewer_look.py`. **엔진 미실행** — AGENTS.md §3 |
| 그 전 | `docs/작업기록_2026-09-14_Claude_하수도_쇠사슬_지지대.md` ★ **쇠사슬·삼각 지지대·체인 고정구 장식 키트**(충돌 없음 · 길이 조절 · 끝점 추적). 사용법 `docs/하수도_쇠사슬_지지대_사용안내.md` · 검사 `tools/test_하수도_지지구조.gd` 17/17 · 아트는 프로시저 임시판 |
| 그 전 | `docs/작업기록_2026-09-14_Claude_2-1_2-2_재제작_프롬프트정정.md` ★ **하수도 기준값 통일**(점프 20칸=320 · 줌 1.0 · 치명낙하 1500 — 아스트라 2-10·2-11 기준) · **2-1·2-2 재제작**(하강 수로 3방 / 수직 갱도+허브 호퍼) · 아스트라 프롬프트 오류 11건 정정 · 새 공통 빌더 `tools/하수도_빌더_공통.gd`. ⚠ 2-3 주행검사는 HEAD 에서도 이미 실패(무관) |
| 그 전 | `docs/작업기록_2026-09-08_Claude_STEP16_계단실_건축장식_비주얼패스.md` ★ 계단실을 건축 공간으로. 카메라 `시선_오프셋`(0,300)으로 **아래층이 한 화면에** · 기둥/난간/소핏 26개 · 유령 오칠 복구 검증(소프트락 아님). ⚠ `지형.gd` 에 `장식_전용` 신설 |
| 그 전 | `docs/작업기록_2026-09-08_Claude_STEP15_STAGE2_공간구조_전면재설계.md` ★ STAGE 2 전면 재설계. **카메라 줌 1.35 → 0.85**(레퍼런스와 동일) · 긴 연속 바닥 중심 · 통행 26/26 · 경로 29,260px. 공간감은 `tools/진단_공간감.gd` 로 잰다 |
| 그 전 | `docs/작업기록_2026-09-08_Claude_STEP13_집전체_조명_노멀맵_비주얼패스.md` ★ STAGE 2 조명을 STEP 12 대형 레이아웃에 맞춰 **전면 재배치**(광원 12 · 빛기둥 3 신설). 자리는 전부 **원화 실측** 근거. 실행 화면 12 장은 `docs/스크린샷_STEP13_2026-09-08/` |
| 그 전 | `docs/작업기록_2026-09-07_Claude_stage_2-8_골격.md` ★ **stage_2-8 「갈래」 3층 다경로** 골격 · **움직이는 발판** 신규. ⚠ 층 간격 560 > 치명 낙하 520 → **모든 틈이 L1 물까지 관통**한다 |
| 그 전 | `docs/작업기록_2026-09-07_Claude_페인트분사기.md` ★ **페인트 분사기** 신규 — 지형을 스스로 칠하는 장애물. ⚠ 장치 페인트는 **E 로 회수 불가**(내 물감이 아니다) |
| 그 전 | `docs/작업기록_2026-09-07_Claude_차오르는물_웅덩이.md` ★ **웅덩이** 신규 · 물이 차오른다 · **물 위로 떨어지면 안 죽는다**(색이 맞아야) |
| 그 전 | `docs/작업기록_2026-09-07_Claude_하수도_2-4_2-7_주행검사_수리.md` ★ 하수도 **2-1~2-7 전부** 직접 태워 관통 확인·수리 |
| 그 전 | `docs/작업기록_2026-09-06_Claude_하수도_2-1_2-3_주행검사_수리.md` ★ 하수도 2-1~2-3 을 **직접 태워** 관통 확인·수리 · 새 도구 `tools/하수도_주행검사.gd` |
| 그 전 | `docs/작업기록_2026-09-05_Claude_STEP6_2층방_실제광원기반_조명.md` ★ 근거 없던 `방_중앙_채움` 제거 · **원화 밝기를 실측해서** 창문 3 + 문 1 로 재배치. 왼쪽 +5~19 % · 중앙 −15 % · 전체 +8 % |
| 그 전 | `docs/작업기록_2026-09-05_Claude_테스트씬_복구_조명노멀맵_공식랩.md` ★ `테스트_2층방_노멀맵.tscn` 이 안 보이던 원인 = **스테이지 사본이라 지형 24개 중 20개가 카메라 밖**. 고정 카메라 Lab 으로 재구축 |
| 그 전 | `docs/작업기록_2026-09-05_Claude_STEP4_5_조명미세조정_재질표준_플레이어보조광.md` ★ **플레이어 보조광**(0.35 / 300 · 그림자 없음) · 재질 노멀 세기 LOCK(WOOD 1.6 · GRASS 0.9 · IRON 1.4) · 그림자 알파 0.60 · 실험실 3구역 |
| 그 전 | `docs/작업기록_2026-09-05_Claude_조명표준_HUD확대_그림자_노멀맵확대.md` ★ **조명 표준 확정**(height 128 · ADD · 반경 700 · 어둠 0.62) · HUD 1.35배 + 초상 담기 · LightOccluder2D · 노멀맵 4재질 |
| 그 전 | `docs/작업기록_2026-09-05_Claude_STEP3_색동기화_액체동역학.md` ★ 게이지 액체 **관성·충격** 연출 · `player_anim` 시트 접두어를 `선택색()` 으로 (경계에서 애니 리셋되던 것) |
| 그 전 | `docs/작업기록_2026-09-05_Claude_STEP2_공통HUD_초상_구게이지.md` ★ **HUD 전면 교체**. 얼굴배지·캡슐바 삭제 → 공통 씬 `scenes/ui/페인트_HUD.tscn` (초상 + 구형 게이지). 색 출처 = `자유색` |
| 그 전 | `docs/작업기록_2026-09-05_Claude_페인트명중_물이펙트_공통화.md` ★ **명중 이펙트 = 원본 water_05~08 애니메이션**. 스테이지가 아니라 **모든 발사체의 공통 출구**(`ActionFX.명중()`)에 붙였다 |
| 그 전 | `docs/작업기록_2026-09-04_Claude_유령블러_스플래시_캡슐HUD_전경.md` ★ 유령블러 수정 · **캡슐 HUD(흑백 스위치)** · 페인트 물스플래시 · 긴 발판(INSIDE 레퍼런스) |
| 그 전 | `docs/작업기록_2026-09-03_Claude_4칸확장_부분칠_배경레이어.md` ★ **4칸 테스트 씬** · 색칠 `칠하기_방식`(전체칠/부분칠만/안칠해짐) · 배경 Parallax 4레이어 |
| 그 전 | `docs/작업기록_2026-09-03_Claude_배경깊이_나무벽돌_투명발판.md` ★ 배경 `깊이`(z_index) · 집 재질 = **나무(방 안) / 벽돌(벽·기둥)** · **투명 발판 키트** 신설 |
| 그 전 | `docs/작업기록_2026-09-02_Claude_배관_그리기높이.md` ★ 배관 인스펙터에 **`깊이`(z_index)** · 기본 3 = 물 위 |
| 그 전 | `docs/작업기록_2026-09-02_Claude_SS2D_템플릿_점배열_공유.md` ★ 키트 Template 26 개를 **인스턴스마다 독립**시켰다. ⚠ **새 Template 을 만들면 `tools/생성_템플릿_점독립.gd` 를 돌릴 것** |
| 그 전 | `docs/작업기록_2026-09-02_Claude_유체_판정_실루엣폴리곤.md` ★ 물 판정 = **그림 실루엣 폴리곤**(프레임마다 다름) · 인스펙터 "물 판정 범위" 없어짐 |
| 그 전 | `docs/작업기록_2026-08-29_Claude_SS2D_흑백대칭_흰색키트.md` ★ 색 시스템 흑백 대칭 · **흰색 키트 6종** · 짝 없으면 반전 대체 |
| 집 4방 · 키트 Template 도입 | `docs/작업기록_2026-08-25_도형_집챕터_v2_키트템플릿_칠하기_빛경계.md` 칠해야 열리는 유령 발판 · 빛=색경계 |
| 집 4방 1차(원화 배경 도입) | `docs/작업기록_2026-08-25_도형_집챕터_원화배경_4방재구성.md` |
| 지형 타일셋·작업자 키트 | `docs/작업기록_2026-08-25_도형_SS2D_v2타일셋_작업자키트.md` |
| **맵을 찍는다 (지형)** | `scenes/집/스마트 매쉬 assets/README_지형찍기.md` ★ Template 복제해서 점만 편집 (2026-09-02: 그냥 여러 번 꽂아도 각자 독립) |
| **투명(유령) 발판을 놓는다** | `scenes/집/스마트 매쉬 assets/GHOST_투명발판/README_투명발판.md` ★ 칠해야 밟히는 발판 Template 3 종 |
| **배경 레이어(Parallax 4겹)** | `assets/background/stage1_layers/README_배경레이어.md` ★ 임시 목업 · 디자이너 PNG 오면 texture 만 교체 |
| **4칸 확장 테스트 씬** | `scenes/집/테스트_2층방_4칸_배경레이어.tscn` ★ 챕터표 밖 · 단독 실행 |
| **지금 뭐가 꼬여 있나** | `docs/프로젝트_점검_2026-08-18_도형_병합_오류_꼬임.md` ★ |
| 파일이 어디 있나 | `docs/파일_분류_인덱스.md` |

## 반드시 지킬 규칙 (이 저장소의 규약)

1. 코드를 고치면 **왜 그렇게 했는지 한글 주석**을 반드시 붙인다.
2. 작업이 끝나면 `docs/작업기록_<날짜>_<이름>_<요약>.md` 를 남긴다.
3. 손대기 전과 후에 **검사 13개**를 돌려 하나도 안 깨졌는지 확인한다
   (명령은 `docs/다음작업_프롬프트.md` §1 에 복붙용으로 있다).
4. 씬을 통째로 다시 만드는 도구(`tools/build_*.gd`)는 **평소에 돌리지 않는다.**
   에디터에서 손본 값이 전부 날아간다.
5. 도구는 **멱등**이어야 한다. "기존 값 + 여유" 같은 누적 금지 — 항상 내용물에서 다시 계산.
6. 읽어온 씬의 노드에 **owner 를 다시 박지 않는다.** 새로 만든 노드에만 준다
   (어기면 다음 로드 때 노드가 두 벌이 되어 씬 교체 시 세그폴트).
7. 셰이더(.gdshader) 안에는 **한글을 쓸 수 없다.** 조용히 셰이더가 안 나온다.
8. `git pull` 먼저, 충돌은 **파일 단위**로 해결. 커밋 전 `git status` 로
   **내가 안 지운 파일이 D 로 잡혀 있는지** 확인 (2026-08-07 에 남의 작업 55개가 사라진 적 있다).

> ⚠ 지뢰밭 전체 목록(전부 실제로 한 번씩 터진 것)은 `docs/다음작업_프롬프트.md` §5 에 있다.
> **새 작업 전에 그것부터 읽을 것.**

## 자주 고치는 곳

| 하고 싶은 것 | 파일 |
|---|---|
| **빛의 기준값(height·세기·반경·그림자)** | ★[2026-09-05] `scripts/스마트월드/조명표준.gd` — **여기가 유일한 출처**. 광원 만드는 6곳이 전부 `조명표준.적용()` 을 부른다 |
| **플레이어를 따라다니는 보조광** | ★[2026-09-05] `scripts/플레이어_보조광.gd` + `Player.tscn` 의 자식 노드. 기준값은 `조명표준.보조광_*`(0.35 / 300). ⚠ **그림자를 켜지 않는다** — 켜면 점프할 때마다 그림자·노멀 하이라이트가 같이 움직여 화면이 부산스러워진다 |
| **재질별 노멀맵 세기** | ★[2026-09-05 LOCK] `tools/생성_지형_노멀맵.gd` 의 표(WOOD 1.6 · GRASS 0.9 · IRON 1.4 · BRICK 수제). 기록은 `조명표준.gd` 아래쪽 표 |
| **스테이지 광원 배치 · CanvasModulate** | ★[2026-09-05] `tools/조명_배치.gd` 의 **배치표**. 씬을 손으로 고치지 말고 표를 고치고 도구를 돌린다(멱등). ⚠ 광원 한 줄마다 **"원화 어디가 밝아서 여기인가"** 를 적는다 — 근거 없는 채움광 금지 |
| **발판에 쇠사슬·지지대 장식을 단다** | ★[2026-09-14] `scenes/장식/하수도_지지구조/` 3 종. 절차는 `docs/하수도_쇠사슬_지지대_사용안내.md` §1·§2. ⚠ 천장 고정구를 승강기 자식에 넣으면 체인 길이가 안 변한다 · 벽 지지대는 고정 발판 전용 |
| **"이 물이 누굴 죽이고, 어느 길이 막다른 길인가" 를 그림으로** | ★[2026-09-17] `python tools/맵그래프.py` → `graphify-out/맵그래프.html`. 2-1~2-3 씬과 주행검사 공략표를 읽어 **실제 좌표 위에** 지형·물·호퍼·격자·가시·CP 를 놓고 호퍼→물 · 물→덮인 바닥(색별 생존/사망) · 레버 · 정답/헛걸음 경로(클릭) · 다음 스테이지 선을 그린다. graphify 는 .tscn/.gd 를 안 읽어서 이 관계를 모른다 |
| **하수도 씬을 짓거나 고친다** | ★[2026-09-14] `tools/하수도_빌더_공통.gd` — 기준값 3개(점프 20칸·줌 1.0·치명낙하 1500)를 여기서만 박는다. 지형은 `b.지형()`·`b.유령()`·`b.공중발판()`(일방통행), 검산은 `b.검산(프레임, 빈공간들)`(겹침·구멍·격자·변). ⚠ 인스턴스는 `GEN_EDIT_STATE_INSTANCE` 로 — 아니면 흰 물이 회색으로 저장된다. 공략은 `tools/하수도_주행검사.gd` 에 **정답 + 헛걸음 두 벌**(`"stage_2-N 헛걸음"` 키) |
| **"이 방 어디에 빛이 그려져 있나"** | ★[2026-09-08] `tools/측정_원화_광원자리.gd` — 배경 원화를 격자로 잘라 밝기를 재고 **월드 좌표**로 준다. 광원 자리를 눈대중으로 찍지 말 것. `-- --씬=<경로> --노드=<배경노드>` 로 아무 스테이지·아무 배경판이나 잴 수 있다 |
| **방향이 있는 빛(창문·지붕 틈)** | ★[2026-09-08] PointLight2D 는 지형을 밝히고, `빛기둥`(모드 0 = 연출)이 **어디서 오는 빛인지**를 그린다. 둘을 짝으로 놓는 것이 표준. ⚠ 모드 1·2 는 `색경계` 그룹에 들어가 **색 판정에 관여**한다 — 연출용으로 쓰지 말 것 |
| **바닥에 눕는 빛(원형 후광 방지)** | ★[2026-09-08] 발광체 `종류 = 2`(반사) = `scale.y 0.32` 로 눌린 납작한 빛. 계단 tread·바닥을 스치는 웅덩이가 된다 |
| ⚠ **흰색 발판이 순백으로 뭉갠다** | 흰 지형 **바로 위**에 세기 1.0 넘는 광원을 두지 말 것. 빛은 **검정 지형을 읽히게 하려고** 놓는다 (STEP 13 §5) |
| **빛을 막는 벽(그림자)** | ★[2026-09-05] `tools/그림자_가림막.gd` 의 `대상표`. 콜리전을 **읽기만** 해서 형제 `빛가림`(LightOccluder2D)을 만든다 |
| **지형 노멀맵 켜기/끄기** | ★[2026-09-05] `scripts/스마트월드/노멀맵_표.gd` (재질 전체) · 지형 인스펙터 `연출 > 노멀맵_사용` (하나씩) |
| **★조명·노멀맵을 먼저 확인하는 곳** | ★[2026-09-05] `scenes/집/테스트_2층방_노멀맵.tscn` **F6** — 공식 검증 랩. 4재질 + Player + 환경광 + 보조광 + 그림자가 한 화면에. 키: `←→` 광원 · `N` 노멀 · `E` 환경광 · `F` 보조광 · `S` 그림자 · `Shift` 흑백. 씬을 고치려면 `tools/build_노멀맵_테스트씬.gd` 의 배치표 |
| **"맵이 큰데 왜 답답하지"** | ★[2026-09-08 STEP 15] `tools/진단_공간감.gd -- <씬>` — 물리 공간을 64px 격자로 찍어 **채움 비율 · 발판 길이 분포 · 수직 겹침 · 한 화면에 보이는 발판 수 · 카메라 실효 시야**를 숫자로 준다. TileMap 씬과 SS2D 씬을 같은 잣대로 비교할 수 있다 |
| **밟히지 않는 장식 지형을 놓는다** | ★[2026-09-08 STEP 16] 지형 인스펙터 `연출 > 장식_전용` = ON. 콜리전이 완전히 꺼진다(텍스처·노멀맵은 그대로라 빛에 반응). ⚠ 인스턴스 씬은 **자식을 지워도·자식 속성을 덮어써도 저장이 안 된다** — 반드시 **노드 자신의 export** 로 켤 것. 쓰는 곳: `tools/patch_STAGE2_STEP16_계단실.gd` |
| ⚠ **씬을 읽어서 손보는 도구** | ★[2026-09-08 STEP 16] `공통.주인_지정()` 을 부르면 안 된다. 이미 owner 가 있는 노드에 다시 박는 것이 되어 **세그폴트**가 난다(CLAUDE.md §6). 새로 만든 노드에만 직접 owner 를 준다 |
| ⚠ **플레이어는 160px 밖에 못 뛴다** | `player.gd` 타일_크기 16 × 점프_높이_칸 10 = **높이 160 · 거리 240**. 낙하 중 수평 이동은 700px 낙하에 197px 가 한계. → **넓은 공간은 점프 간격이 아니라 "긴 연속 바닥"으로 만든다** (STEP 15 §2) |
| STAGE 2 지형을 다시 짓는다 | ★[2026-09-08] `tools/patch_STAGE2_STEP15.gd` 의 **지형표**. 좌표를 고치면 `tools/시험_STAGE2_런타임.gd` 의 구간표도 **같이** 고쳐야 한다(어긋나면 거기가 먼저 FAIL 한다) |
| 테스트 씬이 안 보인다 | `tools/진단_테스트씬.gd` — **카메라가 보는 사각형 안에 지형이 몇 개인가**를 숫자로 찍는다. "리소스는 멀쩡한데 카메라가 딴 데를 본다"를 잡는 유일한 검사 |
| 조명 값을 여러 조건으로 한 번에 비교 | `scenes/테스트/Lighting_Standard_Lab.tscn` F6 (3구역: 환경만 / +보조광 / +그림자) · 촬영은 `tools/촬영_인게임.gd` |
| **HUD 크기·초상 프레이밍** | ★[2026-09-05] `페인트_HUD.gd` 의 `배율`(1.35) · `초상_담을_비율`(0.80) · `여백`(34,26) |
| 스테이지 순서·챕터·배경색·BGM | `scripts/스마트월드/챕터.gd` ★ |
| 카메라 추적·줌·리밋 | `scripts/proto/proto_camera.gd` |
| 굴뚝/갱도에서 화면 조이기 | `scripts/proto/카메라_공간.gd` |
| 스테이지 전환 연출 | `scripts/스마트월드/장면전환.gd` |
| 사망 판정 | `scripts/스마트월드/월드.gd` 의 `_사망_판정()` |
| **빛(지붕 구멍·창문)의 색·굳음 규칙** | `scripts/스마트월드/빛기둥.gd` |
| **창문/커튼 규칙** | `scripts/스마트월드/창문커튼.gd` |
| **물 판정 모양(그림 실루엣)** | `scripts/스마트월드/유체_판정모양.gd` ★손으로 고치지 말 것 — `tools/생성_유체_판정폴리곤.gd` 가 굽는다 |
| **웅덩이 · 차오르는 물** | `scripts/스마트월드/웅덩이.gd` (씬: `웅덩이.tscn`) · 유체도 인스펙터 **차오름** 그룹으로 차오른다 |
| **페인트 분사기(지형을 스스로 칠함)** | `scripts/스마트월드/페인트분사기.gd` (씬: `페인트분사기.tscn`) · 규칙은 `페인트_코어.gd` 의 `장치_명중_처리()` |
| **물 위로 떨어지면 안 죽는 규칙** | `월드.gd` 의 `_낙하를_받아주나()` ★`hit_from_inside` 를 끄면 물에 닿는 순간 죽는다 |
| **배관이 물 앞/뒤 어디에 그려지나** | 배관 인스펙터 `깊이` (기본 3 = 물 위) · `배관_SS2D_끝마개.gd` · `배관.gd` |
| **집 안 배경 — 원화(PNG) 꽂는 자리** | `scripts/스마트월드/실내배경.gd` 의 `그림_아래` ★ 비우면 예전 코드 그림 |
| **배경이 지형 앞으로 나온다** | 실내배경 인스펙터 `깊이`(z_index). 지형은 z 0 이라 **음수면 무조건 뒤**. 앞바닥 띠 = −20 |
| **투명 발판이 어떻게 보이나** | 지형 인스펙터 **"유령 발판"** 그룹 5 값 · 셰이더는 `지형_페인트.gdshader` 의 GHOST MODE |
| **페인트 명중 튀는 연출** | ★[2026-09-05] `scenes/effects/PaintImpactEffect.tscn` + `scripts/effects/페인트_명중효과.gd` — 원본 water_05~08 랜덤 재생. **스폰 자리는 `player_action_fx.gd` 의 `명중()` 단 한 곳**(발사체 3종이 전부 여기로 온다). 프레임은 `tools/생성_물감명중_프레임.gd` 가 굽는다 |
| 명중 연출을 옛 손그림으로 되돌리기 | `Player > ActionFX` 인스펙터 `명중_연출 = 손그림_스플래시` (`물감_스플래시.gd`) |
| **좌상단 HUD(초상 + 구형 게이지)** | ★[2026-09-05] 씬 `scenes/ui/페인트_HUD.tscn` **하나**를 월드.gd·stage_lab.gd 가 같이 쓴다. 컨트롤러 `scripts/ui/페인트_HUD.gd` · 셰이더 `shaders/HUD_구.gdshader` · `shaders/HUD_액체.gdshader` |
| HUD 초상 크기·자리 | `페인트_HUD.tscn` 의 `초상_그림` scale/position (실제 `player_frames.tres` 를 그대로 재생) |
| HUD 가 읽는 색 | `player.선택색()` = `자유색`. ⚠ `player_color`(대표색)는 매 프레임 덮어써져서 쓰면 깜빡인다 |
| **게이지 액체 출렁임·충격** | ★[2026-09-05] `페인트_HUD.tscn` 루트 인스펙터 **"액체 동역학"** 7 값. 방향이 반대로 느껴지면 `기울기_방향` 만 −1 로 |
| ⚠ 강제색 구역 안에서 HUD 와 몸 색이 달라 보임 | 버그 아님 — `색경계` 가 몸 색을 강제한다. HUD 는 **고른 색**을 보여준다(설계 선택 A). 작업기록 STEP 3 §7 참고 |
| **큰 지형을 전부는 못 칠하게** | 지형 인스펙터 `칠하기_방식 = 부분칠만` (+ 벽이면 `안칠한_바탕도_색이다` 끄기) |
| **집 4방(2층방·거실·부엌·굴뚝) 배치** | `tools/build_집_배경스테이지.gd` ★ 좌표는 **원화 픽셀**로 적혀 있다 |
| 집 3장(거실·굴뚝·지붕) 옛 배치 | `tools/build_원본_집.gd` |
| 지형 점 생성 규칙 | `scripts/스마트월드/지형규칙.gd` |
| 공중에 뜬 지형 찾기·고치기 | `tools/지형_다듬기.gd` |

## graphify

This project has a knowledge graph at graphify-out/ with god nodes, community structure, and cross-file relationships.

★[2026-09-17] **graphify-godot 포크**(`C:\Users\아버지\graphify-godot` · `pip install -e`)로 바꿨다 — 업스트림은 `.gd`/`.tscn`/`.tres` 를 안 읽어 게임 코드가 그래프에 없었다. 이제 함수 호출(calls) · 상속(inherits) · preload/ext_resource(imports_from) · 씬 인스턴스(instances) · 시그널(emits) 이 들어 있다(코드 3,900 노드 · 전체 7,400).
- 갱신: `graphify update .` (`~/.local/bin/graphify.exe` 를 포크 것으로 바꿔 두었다) 또는 `python -m graphify update .`. Git Bash 에서는 `PYTHONUTF8=1` 을 앞에 붙여야 한글이 안 깨진다.
- 보기: 전체 `graph.html` 은 너무 커서 브라우저가 멈춘다 → `python tools/그래프_부분뷰.py` (scripts+scenes = `graph_game.html` 1,500 노드) · `--tools` (`graph_code.html`) · `--focus 월드.gd --hops 2` (`graph_focus.html`). 서버: `.claude/launch.json` 의 `graph` (http://localhost:8765).
- `.graphifyignore` 가 `docs/백업_*` 사본을 뺀다(같은 이름의 진짜 파일과 섞인다).
- 포크에 로컬 패치 2 줄: id 에 한글 보존(`extract.py`·`build.py` 의 `[^\w]`) · `MAX_NODES_FOR_VIZ 12000`. 포크를 다시 받으면 이 둘을 다시 넣어야 한다.
- 맵 **게임플레이** 관계(물이 누굴 죽이나 · 경로)는 여전히 `tools/맵그래프.py` 가 따로 그린다 — 코드 그래프와 다른 층위.

Rules:
- For codebase questions, first run `graphify query "<question>"` when graphify-out/graph.json exists. Use `graphify path "<A>" "<B>"` for relationships and `graphify explain "<concept>"` for focused concepts. These return a scoped subgraph, usually much smaller than GRAPH_REPORT.md or raw grep output.
- If graphify-out/wiki/index.md exists, use it for broad navigation instead of raw source browsing.
- Read graphify-out/GRAPH_REPORT.md only for broad architecture review or when query/path/explain do not surface enough context.
- After modifying code, run `graphify update .` to keep the graph current (AST-only, no API cost).

# 작업보고 — 2026-06-30 · stage 3·4 레벨 재설계 (플레이어 실측 기반)

> 작성: Claude / 대상: color 팀(도형·성진 외). GitHub Desktop 공유용.
> 핵심: **지형 모듈이 플레이어 크기에 안 맞아 점프/간격이 엉성하던 문제**를 실측값으로 재설계.

---

## 1. ★ 플레이어 실측값 (모든 맵 제작자 필독 — 이걸로 간격을 잡으세요)
`Player.tscn` / `player.gd` 에서 추출:

| 항목 | 값 |
|---|---|
| 충돌 크기 | CapsuleShape 32r·142h **× scale 0.3** → **실제 ≈ 19w × 43h px** |
| 이동 속도 | 390 px/s |
| 점프 초속 | -600 / 중력 1200(상승)·2880(하강) |
| **최대 점프 높이** | **≈ 150px** (= 약 3.5 캐릭터 키) |
| **최대 점프 거리** | **≈ 320px** (= 약 7 캐릭터 폭) |
| 발 위치 | 플레이어 원점에서 아래로 ≈ 54px (배치 시 참고) |

### 설계 안전값 (여유 포함)
- **수평 간격(갭) ≤ 190px** (최대 320이지만 여유)
- **위로 점프(고도 상승) ≤ 110px**
- **아래로 낙하**: 높이 제한 없음(자유 낙하) — 단 수평 갭은 ≤190 유지
- **착지 발판 폭 ≥ 60px** (전력 점프는 ~300px 날아가니, 작은 발판 연속배치는 넘겨버림 주의)

### 모듈 스케일 가이드 (모듈 원본이 플레이어보다 7~30배 큼 → 반드시 축소)
| 모듈 | 원본 px | 권장 scale | 결과 |
|---|---|---|---|
| 바닥(검/흰)1 | 1000×310 | 0.5~0.55 | 폭 500~550 |
| 바닥(검/흰)2 | 1450×295 | 0.5 | 폭 725 |
| 발판2 | 720×222 | 0.45~0.5 | 폭 324~360 |
| 공중발판1~3 | 312~512 | 0.5~0.55 | 폭 170~280 |
| 가시1~4 | 720×208 | 0.2~0.25 | 높이 42~52(점프 회피 가능) |

> ※ 모듈 `centered=false`(좌상단 원점) + 윗면이 거의 y=0(크롭됨). **position.y = 걷는 면 높이**.

---

## 2. 이번에 한 일

### (A) stage_3 재설계 — `scenes/world_1/stage_3/stage_3.tscn` (덮어씀)
- 기존: 모듈이 scale 1(거대)이라 플레이어 대비 간격/점프가 엉성 → **게임 맵이라 보기 어려움**.
- 재설계: 불벽 추격(H5) 테마 유지 + **플레이어 실측 기반 간격**. 바닥0.55·공중발판0.5·가시0.25.
  바닥 기준선 y=400, 낙사 y=780, camera_zoom 1.5(플레이어가 더 크게 보임).
- 요소: 색지형(T1) 흰발판 구간, 가시(H1), 톱니(H2 가로·세로), 낙하블럭(H3), 체크포인트 2, 포탈.
- **검증: reach_sim 자동주행 SIM_OK (포탈 도달).**
- ⚠ 이 파일은 도형님 커밋(`스테이지3 구성완`) 위에 덮였습니다. **이전 버전은 git 에 있음**(`git checkout` 으로 복구 가능).

### (B) stage_4 재설계안 — `scenes/world_1/stage_4/stage_4_재설계.tscn` (신규, 덮지 않음)
- stage_4.tscn 은 **작업자가 빌드 중**이라 덮지 않고 **별도 제안 파일**로 작성. 검토 후 교체/병합용.
- "내리막" 테마: **넓은 계단형 바닥이 좌→우로 점점 낮아짐**(하강감) + 톱니3·가시4·낙하블럭2 = stage_3보다 촘촘(난이도 ↑).
- 넓은 발판이라 전력 점프해도 안전 착지. **reach_sim SIM_OK 검증.**
- 채택하려면: 이 파일 내용을 stage_4.tscn 으로 옮기거나, 루트 노드명을 `stage_4` 로 바꿔 교체.

### (C) 검증 도구 신규 — `tools/reach_sim.gd` (팀 공용)
- 플레이어와 동일 물리 프록시가 맵을 자동 주행하며 **간격/고도차가 실제 점프로 통과되는지** 검증.
- 사용: `Godot --headless --path . -s res://tools/reach_sim.gd -- <씬경로> <포탈x> <낙사y> <시작x> <시작y>`
- 결과: `SIM_OK 포탈도달` = 통과 가능 / `SIM_FAIL 낙사 x=…` = 그 지점 간격 점검 필요.
- ※ 장애물·색 로직은 무시(지형 기하만). 전력 점프 기준이라 작은 발판 연속은 FAIL 날 수 있음(실제론 점프 조절 가능).

---

## 3. 건드린 것 / 안 건드린 것
- **기존 게임 코드 미수정**: player·terrain_image·auto_collision·obstacle 스크립트, SceneManager 등 그대로.
- 추가/수정: 스테이지 씬 2개(.tscn) + 검증 도구 1개(tools/reach_sim.gd, 헤더에 설명 주석).
- 모든 신규 .tscn 상단에 `;` 주석으로 설계 의도·수치 기록.

---

## 4. ⚠ 포탈로 Stage 5 못 가는 문제 (별도, 코드 미수정 — 팀 처리 필요)
원인 2가지:
1. **`scenes/world_1/stage_5/stage_5.tscn` 파일이 없음** (stage_6 은 있는데 5는 비어있음).
2. **`autoload/SceneManager.gd` 의 `STAGES` 딕셔너리에 5·6 미등록** (현재 1~4 만).
   → 포탈 빈칸이면 `next_stage()` → `load_stage(5)` → STAGES[5] 없음 → 에러만 나고 정지.
**해결**: stage_5.tscn 생성 + STAGES 에 아래 추가(들여쓰기 주의):
```gdscript
5: "res://scenes/world_1/stage_5/stage_5.tscn",
6: "res://scenes/world_1/stage_6/stage_6.tscn",
```
(※ "기존 코드 건드리지 말 것" 지침 때문에 제가 직접 STAGES 를 수정하지 않았습니다. 팀에서 적용 바람.)

---

## 5. 사용 가능한 요소 현황 (맵 설계 참고 · 요소 카탈로그 기준)
- **✅ 바로 사용**: T1 색지형, H1 가시, H2 톱니, H3 낙하블럭(BlockSpawner+FallingBlock), H5 불벽추격, H6 서치라이트, P2 투명블럭
- **🆕 미구현(쓰려면 제작 필요)**: T2 회색감속, T3 이동발판, T4 무너지는발판, T6 점프대, P1 색박스(씬 없음), 강하경사(특별), 세로추격(특별)
- ⚠ **회색경사로는 삭제 대상**(요소 카탈로그): 회색=이제 감속존(T2, 미구현). stage 2·3 의 기존 회색경사로 구간 재설계 필요.

---

## 6. 다음 작업 제안
- stage_4_재설계.tscn 검토 후 채택 여부 결정.
- stage_5 생성(테마 "그려진 발판" = P2 투명블럭) + STAGES 등록 → 포탈 연결 복구.
- 각자 만든 맵은 `reach_sim.gd` 로 완주 검증 후 커밋 권장.
- 모든 맵은 위 1번 실측값(간격≤190·고도≤110·모듈 축소)을 기준으로.

# 작업 보고 — 색 게이지 · 스테이지전환 버그 · 분사 페인트 · 스테이지4 (2026-06-28 2차)

> 색상반전 2D 플랫포머 / Godot 4.6.3
> 대상: 같이 작업 중인 다른 작업자 / 새 세션. "무엇이 바뀌고 추가됐는지" 추적용.

---

## ① 상단 색 에너지 게이지 (HUD)
- **요청**: 유튜브 레퍼런스처럼 상단에 플레이어 색(흑/백) 게이지를 만들고 쏠수록(빛과 함께) 줄게.
- **구현**: `scripts/ui/hud.gd` 에 코드로 게이지 생성(`_build_color_gauge`/`_update_color_gauge`).
  - 위치: 상단 중앙. 구조: 검정 테두리 → 회색(0.45) 트랙 → **플레이어색 채움**(검정/흰색 둘 다 회색 위에서 대비됨).
  - 매 프레임 `player.get_energy_ratio()`(0~1)로 채움 폭, `player.get_player_color()`로 색 갱신.
  - `scripts/player.gd` 에 접근자 `get_energy_ratio()`/`get_player_color()` 추가. 게이지는 발광(빛)과 같은 `_shoot_energy` 를 읽으므로 **빛과 함께 줄고 회복**된다.

## ② 스테이지 전환 시 UI(STAGE n) 안 바뀌는 버그 — 수정
- **원인**: `scripts/ui/clear_popup.gd` 가 포탈의 `next_stage_path` 가 있을 때 `get_tree().change_scene_to_file()` 를
  **직접 호출**해 `SceneManager.load_stage()` 를 우회 → `SceneManager.current_stage` 가 갱신 안 됨 → HUD 가 이전 번호 유지.
- **수정**:
  - `SceneManager.go_to_stage_by_path(path)` 신규: 경로를 STAGES 에서 **번호로 역추적**해 정식 `load_stage(n)` 로 라우팅
    (current_stage·타이밍·페이드 일괄). STAGES 에 없으면 페이드 전환만.
  - `clear_popup._change_to()` 가 이 함수를 호출하도록 변경 → 다음 스테이지에서 STAGE 번호 정상 갱신 + 페이드 일관.

## ③ 페인트가 "이미지 덧씌움" → "분사(스프레이)"로 재구현
- **원인 2가지**:
  1. **conform 안 됨**: stage_1 은 비주얼(`MapVisual/TerrainSprite`)과 물리(`MapPhysics`)가 분리돼 있어, 총알이 맞는 물리
     바디의 자식에서 지형 그림을 못 찾아 폴리곤 클립으로 폴백 → 그림과 어긋남.
  2. **느낌**: 미리 그린 splat PNG 한 장을 찍어 '스탬프' 같았다.
- **수정**:
  - **그룹 기반 그림 찾기**: 지형 비주얼 Sprite2D 를 `paint_surface` 그룹에 등록
    (`terrain_image.gd` 자동 + `stage_1.tscn` 의 `TerrainSprite` 에 `groups=["paint_surface"]` 수동).
    `bullet.gd._find_surface_sprite(impact)` 가 '탄착점을 덮는 그림'을 찾아 알파 클립 → **비주얼/물리 분리 스테이지도 정확히 칠해짐**.
  - **절차적 분사 비주얼**(`paint_mark.gd`): 미리 그린 스탬프는 숨기고(판정 JudgmentZone 은 유지),
    **중심 블롭 + 흩뿌린 물방울(중심 밀집·바깥 희박) + 흘러내림(드립, 월드 수직)** 을 매번 무작위로 생성.
    부드러운 원형 물방울 텍스처(코드 생성, 공유 캐시) 사용, 각 물방울을 지형 알파에 클립.
  - 색 사망 판정 로직(`player._check_color_death`)은 **건드리지 않음**(JudgmentZone 계층 유지) → 게임플레이 안전.

## ④ 스테이지 4 — "색칠 심화" (약 2분)
- `scenes/world_1/stage_4/stage_4.tscn` 신규 + `SceneManager.STAGES[4]` 등록.
- 흐름: 도입(걷기·페인트 학습) → 흑/백 토글 건너기(가시) → 기어 회피 → **공중 색전환 점프**(흰→검→흰, 아래 낙사)
  → **회색경사로 등반**(색총으로 칠해 오름) → 상단 숨고르기 → 마무리/포탈.
- 세로 메트릭은 stage_3(검증됨)과 동일(바닥윗면≈560, 점프높이≈150/틈≤230). 충돌은 Platform.tscn 으로 깔아 **확실히 플레이**.
- 도입부에 새 Badland 흑색 Arch 지형(`Ground_Flat`/`Curve_Hill`)을 비주얼+**분사 페인트 테스트 표면**으로 올림.

---

## 검증 (Godot 4.6.3 헤드리스)
- 구문: paint_mark/bullet/terrain_image/SceneManager 통과.
- 부팅 에러 0: **stage_1 / 2 / 3 / 4** (각 150프레임).
- gray_slope 페인트 테스트 통과(마크 생성 정상). ※ `test_death` 는 stage_3 의 옛 좌표(흰타일)를 쓰는 **낡은 테스트**라
  현 추격 stage_3 에선 timeout — 사망 판정 로직 변경 없음(회귀 아님).

## 미검증 (에디터에서 사람이 직접 — 피드백용)
- 색 게이지/빛 밝기 체감, 분사 페인트가 흑/백 배경에서 보이는 정도, stage_4 난이도·길이·점프 간격 손맛.
- stage_4 의 Arch 지형 배치(겹침/높이)는 비주얼 확인 후 미세조정 예정.

## 다음(피드백 후)
- 흰색 Arch 지형 생성(color="WHITE") → stage_4~ 에 흑/백 지형 색칠 동선.
- stage_4 피드백 반영 → 같은 공식으로 stage 5~10.
</content>

extends SceneTree
## ============================================================================
## [2026-09-06 신규] 하수도 스테이지 **공략 주행** 검사기
## ----------------------------------------------------------------------------
## 실행:
##   Godot --headless --path . -s res://tools/하수도_주행검사.gd -- <씬경로>
##   (인자가 없으면 2-1 · 2-2 · 2-3 을 차례로 돈다)
##
## ▣ 왜 또 만들었나 — 기존 도구 둘로는 답이 안 나온다
##   · `레벨검사.gd` 는 **지형만** 본다. 색 규칙(반대색 지형·물에 닿으면 즉사)과
##     탄약은 전혀 안 본다. 검사를 통과해도 흰 바닥에 검정으로 내려서면 죽는다.
##   · `측정_스테이지_플레이타임.gd` 의 직진 봇은 총도 못 쏘고 색도 못 바꾼다.
##     퍼즐 스테이지에서는 첫 관문 앞에서 죽고 끝난다.
##   → 이 도구는 **사람이 푸는 순서(공략)를 그대로 적어 두고 진짜 물리로 태운다.**
##     지형·색·물·탄약이 한꺼번에 걸리므로, 통과하면 "이 경로로는 깰 수 있다" 가
##     실측으로 보장된다. 반대로 어디서 죽었는지도 좌표로 나온다.
##
## ▣ 공략은 코드에 적는다 (§공략표)
##   맵을 고치면 공략도 같이 고쳐야 한다. 그게 **의도한 풀이가 아직 성립하는지**를
##   확인하는 유일한 방법이다.
##
## ▣ 입력 흉내의 규칙 (측정_스테이지_플레이타임.gd 과 같다)
##   `Input.action_press()` 는 약 2 프레임 뒤에야 `is_action_just_pressed` 로 보인다.
##   상승 중에 점프를 떼면 JUMP_CUT_MULTIPLIER(0.4)가 점프를 잘라 버리므로
##   **정점을 지난 뒤에만** 뗀다.
##
## ⚠ 이 도구는 씬을 고치지 않는다. 오직 태워 보고 보고만 한다.
## ============================================================================

const 물리틱 := 60.0
## 걸음 목표에 이만큼 들어오면 도착으로 본다. 24 로 두면 도약 지점이 최대 24px
## 밀려 갱도 점프가 통째로 어긋난다(발판 옆구리를 들이받는다).
const 도착_여유 := 10.0
const 기본대상 := [
	"res://scenes/world_2_클로드/stage_2-1.tscn",
	"res://scenes/world_2_클로드/stage_2-2.tscn",
	"res://scenes/world_2_클로드/stage_2-3.tscn",
	"res://scenes/world_2_클로드/stage_2-4.tscn",
	"res://scenes/world_2_클로드/stage_2-5.tscn",
	"res://scenes/world_2_클로드/stage_2-6.tscn",
	"res://scenes/world_2_클로드/stage_2-7.tscn",
]

# ── 색 상수 (ColorDefs) ──────────────────────────────────────────────────────
const 검정 := 0
const 흰색 := 1

# ============================================================================
# §공략표 — "사람이 이렇게 푼다" 를 그대로 적은 것
# ----------------------------------------------------------------------------
#   ["가", x]                        그 x 까지 걸어간다 (벽·구덩이를 만나면 알아서 점프)
#   ["뛰기", 도약x, 착지x, 착지y]        도약x **가장자리**까지 걸어가 착지x 쪽으로 점프.
#                                     착지y 를 적어 두면 "그 높이에 실제로 올라섰는가"
#                                     까지 본다 — 이게 없으면 발판을 놓치고 바닥에
#                                     떨어져도 성공으로 읽는다(실제로 겪었다).
#   ["뛰기색", 도약x, 착지x, 착지y, 색]   위와 같되 **공중에서** 색을 바꾼다
#                                     (착지 바닥 색이 도약 바닥과 다를 때의 유일한 수단)
#   ["뛰기색", …, 색, "즉시"]           정점을 안 기다리고 뜨자마자 바꾼다 (도약 직후 반대색 물을 지날 때)
#   ["색", 색]                        제자리 색 전환 (회색 바닥·호퍼 위에서만 안전하다)
#   ["칠", 노드이름, 색]                그 대상을 필요횟수만큼 쏜다 (탄약 소모)
#   ["쉬", 프레임]                     그냥 기다린다 (물이 페인트를 되돌리는지 볼 때)
#
# ★도약x 는 **발판 가장자리 − 몸 반폭(22)** 이다. 여기서 뛰지 않으면 포물선이
#   30~50px 짧아져 다음 발판 옆구리를 들이받는다. 갱도처럼 좌우로 번갈아 오르는
#   구조는 이 30px 이 통과와 추락을 가른다.
# ============================================================================
const 공략 := {
	# ★[2026-09-14 재제작] 2-1 v2 — 하강 수로 3 방. 기준: 점프 320 · 줌 1.0 · 치명낙하 1500.
	#   좌표 근거는 tools/build_하수도_2-1.gd 의 표. 맵을 고치면 여기도 같이 고친다.
	"stage_2-1": [
		# ── 방 A: 회색 물(1152) · 검정 물(1536) 을 걸어서 지난다 ──
		["가", 1590.0],
		# ── F1 막다른 복도(갤러리 밑) — L1 턱(1616~1872·896)을 자동 점프로 넘어 레버까지 ──
		["가", 3040.0],
		["레버", "레버_수문"],                     # 직선 레버 — 갤러리 물 → 복도 물로 돌린다
		["확인", "W3_갤러리_물"],
		["가", 1590.0],                            # 되돌아온다 (복도 물 W3b 는 검정이라 무해)
		# ── F3 사다리: L1(896) → L2(768) → L3(640) → L4(512) ──
		["뛰기", 1594.0, 1700.0, 896.0],
		["뛰기", 1850.0, 2000.0, 768.0],
		["뛰기", 1974.0, 1840.0, 640.0],
		["뛰기", 1850.0, 2060.0, 512.0],
		# ── L4 → 흰 갤러리 (224 틈 · 공중 전환) ──
		["뛰기색", 2330.0, 2620.0, 512.0, 흰색],
		["가", 3800.0],                            # 갤러리 (물은 복도로 돌아갔다)
		# ── 샤프트로 떨어진다 → 흰 웅덩이가 받는다 (896 낙하 · 치명 1500 안) ──
		["걸어", 3980.0, 1536.0],
		["가", 4480.0],                            # 구덩이 턱(128)을 자동 점프로 올라간다
		# ── 구덩이 1: 마른 발판 G2 를 흰색으로 칠하고 건넌다 ──
		["칠", "B_G2_마른발판", 흰색],
		["뛰기", 4794.0, 5100.0, 1408.0],
		["뛰기", 5178.0, 5400.0, 1408.0],
		# ── 슬롯 B (가시) → 검정 구간 ──
		["뛰기색", 5482.0, 5700.0, 1408.0, 검정],
		["가", 6100.0],                            # S1 턱(5856~6048) 자동 점프
		["가", 7000.0],                            # 램프(30°)를 걸어 내려간다
		# ── 방 C ──
		["뛰기", 7082.0, 7330.0, 1792.0],          # 가시 구덩이(7104~7296)
		["뛰기색", 7530.0, 7720.0, 1792.0, 흰색],  # 슬롯 C1 → 흰 구간
		["가", 8000.0],                            # 흰 물(7936)을 흰색으로 지난다
		["뛰기색", 8042.0, 8300.0, 1792.0, 검정],  # 슬롯 C2(8064~8192) → 검정 구간
		["칠", "C_G3_검정물_발판", 검정],           # 검정 물 아래 — 검정 페인트는 안 지워진다
		["뛰기", 8426.0, 8760.0, 1792.0],          # G3 [8672,8864] — 꽉 찬 점프로 딱 닿는다
		["가", 9000.0],                            # G3 오른끝~바닥(8896) 32px 은 걸어 넘는다
	],
	# F1 헛걸음: 갤러리부터 올라가 검정 물에 막히는 걸 보고 내려온다 → 복도 → 레버 → 다시 올라간다
	"stage_2-1 헛걸음": [
		["가", 1590.0],
		["뛰기", 1594.0, 1700.0, 896.0],
		["뛰기", 1850.0, 2000.0, 768.0],
		["뛰기", 1974.0, 1840.0, 640.0],
		["뛰기", 1850.0, 2060.0, 512.0],
		["확인", "W3_갤러리_물"],                  # L4 에서 보인다 — 흰 갤러리 위의 검정 물
		["걸어", 2460.0, 1024.0],                  # L4 오른끝에서 그냥 떨어진다 (검정 바닥)
		["가", 3040.0],
		["레버", "레버_수문"],
		["가", 1590.0],
		["뛰기", 1594.0, 1700.0, 896.0],
		["뛰기", 1850.0, 2000.0, 768.0],
		["뛰기", 1974.0, 1840.0, 640.0],
		["뛰기", 1850.0, 2060.0, 512.0],
		["뛰기색", 2330.0, 2620.0, 512.0, 흰색],
		["가", 3800.0],
		["걸어", 3980.0, 1536.0],
		["가", 4480.0],
		["칠", "B_G2_마른발판", 흰색],
		["뛰기", 4794.0, 5100.0, 1408.0],
		["뛰기", 5178.0, 5400.0, 1408.0],
		["뛰기색", 5482.0, 5700.0, 1408.0, 검정],
		# F1 선반(ⓐ+ⓓ): S1 → S2 유령(검정 2발) → 선반 → 오른끝 틈으로 떨어져 돌아온다
		["뛰기", 5834.0, 5950.0, 1280.0],          # S1 턱 [5856,6048]
		["칠", "B_S2_유령칸", 검정],
		["뛰기", 6026.0, 6150.0, 1152.0],          # S2 [6112,6240]
		["뛰기", 6134.0, 5980.0, 1024.0],          # 선반 [5792,6016]
		["걸어", 6080.0, 1408.0],                  # 선반 오른끝(6016)~S2(6112) 틈으로 낙하
		["가", 7000.0],
		["뛰기", 7082.0, 7330.0, 1792.0],
		["뛰기색", 7530.0, 7720.0, 1792.0, 흰색],
		["가", 8000.0],
		["뛰기색", 8042.0, 8300.0, 1792.0, 검정],
		["칠", "C_G3_검정물_발판", 검정],
		["뛰기", 8426.0, 8760.0, 1792.0],          # G3 [8672,8864] — 꽉 찬 점프로 딱 닿는다
		["가", 9000.0],                            # G3 오른끝~바닥(8896) 32px 은 걸어 넘는다
	],
	# ★[2026-09-14 재제작] 2-2 v2 — 수직 갱도 · 허브 호퍼 · 흰 탑 · F1 발코니.
	#   격자: 왼열 [2032,2256] · 오른열 [2336,2560] · 층마다 4 장 + 허브 호퍼(1856~2560). S01 물 기둥 x 2296 · 허브 물 x 1904.
	"stage_2-2": [
		# ── 시작(2560, 통로) → 오른쪽 F1 발코니: r1 턱 → r2 → r3 → 발코니 → 밸브 ──
		["가", 3300.0],                            # 발코니 밑을 지나 r1 턱 앞까지
		["뛰기", 3306.0, 3400.0, 3456.0],          # r1 턱 [3328,3520]
		["뛰기", 3498.0, 3660.0, 3328.0],          # r2 [3616,3808]
		["뛰기", 3638.0, 3480.0, 3200.0],          # r3 [3328,3520]
		["뛰기", 3350.0, 3200.0, 3072.0],          # 발코니 [2752,3264] (왼쪽으로 뛰어오른다)
		["레버", "레버_수문"],                     # 원형 밸브 — S01 흰 물이 멈춘다
		["확인", "S01_흰물"],
		# ── 내려온다: 발코니 오른끝에서 그냥 떨어진다 (올라온 사다리와 다른 길) ──
		["걸어", 3290.0, 3584.0],
		# ── S01 (검정 그대로): p1 R → p2 L → p3 R → p4 L → 허브 호퍼(2938) ──
		#   128 위 격자엔 **내려오는 쪽** 포물선(도약 +250 근처)이 닿는다. 격자는 일방통행이라 밑에서 뚫고 오른다.
		["가", 2200.0],
		["뛰기", 2200.0, 2450.0, 3456.0],          # p1 R [2336,2560]
		["뛰기", 2358.0, 2100.0, 3328.0],          # p2 L [2032,2256]
		["뛰기", 2234.0, 2450.0, 3200.0],          # p3 R
		["뛰기", 2358.0, 2100.0, 3072.0],          # p4 L
		["뛰기", 2234.0, 2450.0, 2944.0],          # 허브 L1 호퍼 (기둥 2264~2328 오른쪽에 내린다)
		# ── 허브 L1 호퍼(색 규칙 밖) 오른쪽(≈2,480)에 내려앉는다. 흰 물 기둥은 왼끝(1872~1936) ──
		["가", 2450.0],
		["색", 흰색],
		["칠", "S12_p6", 흰색], ["칠", "S12_p7", 흰색], ["칠", "S12_p8", 흰색], ["칠", "S12_p9", 흰색],
		# ── S12 (흰 물 · 흰 몸): p6 L → p7 R → p8 L → p9 R → 허브 호퍼 L2 (왼쪽에 내려앉는다) ──
		["뛰기", 2400.0, 2150.0, 2816.0],          # 오른쪽에서 왼열 p6 로 (착지 ≈ 도약 −250)
		["뛰기", 2234.0, 2450.0, 2688.0],
		["뛰기", 2358.0, 2100.0, 2560.0],
		["뛰기", 2234.0, 2450.0, 2432.0],
		["뛰기", 2358.0, 2100.0, 2304.0],
		# ── 허브 L2: 검정 물 기둥(왼끝)에서 떨어진 왼쪽 자리에서 검정으로 ──
		["가", 2150.0],
		["색", 검정],
		# ── S23 (검정 물 · 검정 몸): p11 R → p12 L → p13 R → p14 L → p15 R(L3 바닥 높이) ──
		["뛰기", 2150.0, 2400.0, 2176.0],
		["뛰기", 2358.0, 2100.0, 2048.0],
		["뛰기", 2234.0, 2450.0, 1920.0],
		["뛰기", 2358.0, 2100.0, 1792.0],
		["뛰기", 2234.0, 2450.0, 1664.0],
		# ── L3 복도: 유령 발판(흰 웅덩이가 지운다) 대신 격자(128)로 건넌다 ──
		["가", 2860.0],
		["칠", "L3_유령발판", 검정],
		["쉬", 30],
		["확인", "L3_유령발판"],                   # 웅덩이가 지웠는지 눈으로 확인용
		["뛰기", 2858.0, 3180.0, 1664.0],          # 격자 [3136,3264] 로 256 점프
		["뛰기", 3242.0, 3400.0, 1664.0],
		["가", 3850.0],
	],
	# F2 빙 도는 길: 밸브를 잠근 뒤 왼쪽 흰 탑으로 L2 까지 오르고 허브에서 S23 을 탄다 (총 0발)
	"stage_2-2 탑": [
		["가", 3300.0],
		["뛰기", 3306.0, 3400.0, 3456.0],
		["뛰기", 3498.0, 3660.0, 3328.0],
		["뛰기", 3638.0, 3480.0, 3200.0],
		["뛰기", 3350.0, 3200.0, 3072.0],
		["레버", "레버_수문"],
		["걸어", 3290.0, 3584.0],
		# ── 홀을 가로질러(물은 꺼졌다) L0 슬롯을 뛰며 흰색으로 → 탑 ──
		["가", 1830.0],
		["뛰기색", 1814.0, 1600.0, 3584.0, 흰색],
		["가", 1080.0],
		["뛰기", 1062.0, 960.0, 3456.0],           # l1 턱 [768,1040]
		["뛰기", 1018.0, 1200.0, 3328.0],          # l2 [1136,1408]
		["뛰기", 1158.0, 980.0, 3200.0],           # l3
		["뛰기", 1018.0, 1200.0, 3072.0],          # l4
		["뛰기", 1158.0, 980.0, 2944.0],           # l5
		["뛰기", 1018.0, 1200.0, 2816.0],          # l6
		["뛰기", 1158.0, 980.0, 2688.0],           # l7
		["뛰기", 1018.0, 1200.0, 2560.0],          # l8
		["뛰기", 1158.0, 980.0, 2432.0],           # l9
		["뛰기", 1018.0, 1300.0, 2304.0],          # l10
		["가", 1810.0],
		# ★L2 허브 왼끝(1872~1936)엔 검정 물이 떨어진다. 흰 바닥(≤1856)에선 못 바꾸니 **뛰자마자** 검정으로.
		["뛰기색", 1834.0, 2100.0, 2304.0, 검정, "즉시"],
		["가", 2150.0],
		["뛰기", 2150.0, 2400.0, 2176.0],
		["뛰기", 2358.0, 2100.0, 2048.0],
		["뛰기", 2234.0, 2450.0, 1920.0],
		["뛰기", 2358.0, 2100.0, 1792.0],
		["뛰기", 2234.0, 2450.0, 1664.0],
		["가", 2860.0],
		["뛰기", 2858.0, 3180.0, 1664.0],
		["뛰기", 3242.0, 3400.0, 1664.0],
		["가", 3850.0],
	],
	"stage_2-4": [
		# 물저장고 6개는 **길을 막지 않는다** — 출구 물이 전부 바닥 위(y 934~959)에서 멈춘다.
		# 그래서 통행은 걷기 + 색 전환 한 번이 전부다(§검사 소견 참고).
		["가", 5200.0],
		["뛰기", 5268.0, 5450.0, 1008.0],       # 검정 바닥 → H5 색전환 발판(호퍼 = 색 규칙 밖)
		["색", 흰색],                            # ★호퍼 위라 안전하게 바꾼다
		["뛰기", 5518.0, 5700.0, 1100.0],       # 흰 바닥으로
		["가", 8180.0],
	],
	"stage_2-5": [
		["레버", "L1_원형_첫밸브"],                 # 흰 길막 물을 잠근다
		["가", 1600.0],
		["레버", "L2_직선_갈래선택"],                # 흰 갈래 → 검정 배수 갈래
		["가", 2900.0],
		["레버", "L3_원형_양자택일"],                # 두 번째 흰 길막을 끈다
		["가", 5200.0],
		["뛰기", 5268.0, 5450.0, 1008.0],       # H5 색전환 발판
		["색", 흰색],
		["뛰기", 5518.0, 5700.0, 1100.0],       # 흰 구간
		["레버", "L4_원형_반대색건너"],               # 흰 길의 검정 물문을 잠근다
		["레버", "L5_직선_검정방향"],                # 검정 폐수를 혼합기로 돌린다
		["레버", "L6_원형_흰공급"],                  # 기본 닫힘 — 열어야 회색이 나온다
		["가", 8180.0],
	],
	"stage_2-6": [
		# 양동이는 배우는 대상이고, 통행 자체는 걷기 + 색 전환 한 번이다.
		# ⚠ 이 스테이지의 양동이는 물줄기까지 못 간다 — 물받이 호퍼가 앞을 막는다.
		#    (작업기록 2026-09-07 §양동이 호퍼 참고) 그래서 공략에 밀기를 넣지 않았다.
		#    밀기 동작 자체는 2-7 공략(박스 2 · 양동이 2)이 검증한다.
		["가", 3900.0],
		["뛰기색", 3968.0, 4160.0, 1100.0, 흰색],   # 검정→흰 구간 (전환 틈 100px)
		["가", 8180.0],
	],
	"stage_2-7": [
		["가", 900.0],                              # 관문 1 — 버튼을 밟으면 문이 내려간다
		["가", 1800.0],
		["밀기", "박스_1", 3000.0],                  # 관문 2 — 박스를 버튼에 남긴다
		["가", 4300.0],
		["밀기", "양동이_검정_1", 5500.0],             # 관문 3 — 물을 채운 양동이만 버튼을 누른다
		["가", 6300.0],
		["밀기", "양동이_검정_2", 7200.0],             # 관문 4 — 양동이가 한쪽 버튼을 누르고
		["가", 7600.0],                             #          플레이어가 다른 버튼을 밟는다
		["가", 8300.0],
		["밀기", "박스_2_귀환길", 8500.0],             # 관문 5 — 움직이는 땅을 끌어온다
		["쉬", 200],                                # 950px 을 420px/s 로 끌어오는 데 2.3초 걸린다
		["가", 8640.0],
		["뛰기", 8642.0, 8800.0, 1100.0],           # 틈(8,664~8,739)을 건넌다
		["가", 10380.0],
	],
	# ── [2026-09-17 v2] 2-3 「관을 따라서」 — 호퍼실 4 · 막다른 길 3 · 빙 도는 길 1 (build_하수도_2-3.gd) ──
	"stage_2-3": [
		# A 방: 선반 계단 → 호퍼 H1(색 규칙 밖)에서 흰색 → 흰 물을 지나 흰 바닥
		["가", 850.0],
		["뛰기", 850.0, 1100.0, 1728.0],           # a1 [960,1216]
		["뛰기", 1100.0, 1350.0, 1600.0],          # a2 [1216,1472]
		["뛰기", 1350.0, 1600.0, 1472.0],          # H1 [1472,1728] 왼쪽 절반(공급 물 1648~1712 에 안 닿는다)
		["색", 흰색],
		["걸어", 1780.0, 1856.0],                  # 흰 공급 물을 흰 몸으로 지나 오른끝에서 흰 바닥으로
		["가", 2530.0],
		["뛰기색", 2530.0, 2760.0, 1856.0, 검정],  # 슬롯 2(가시) → 검정 갤러리
		# B 방 갤러리: 구멍 셋을 읽는다. h1(검)·h2(흰)은 뛰어 넘고 h3(검+흰=회)로 내려간다
		["가", 3240.0],                            # 굴뚝 구멍(2944~3136 · 격자 80 아래)은 걸어서 지난다
		["뛰기", 3240.0, 3550.0, 1856.0],          # h1 [3264,3520]
		["가", 4260.0],
		["뛰기", 4260.0, 4580.0, 1856.0],          # h2 [4288,4544] — 흰 물 구멍
		["가", 5100.0],
		["걸어", 5140.0, 2560.0],                  # h3 왼 가장자리로 떨어진다 — 회색 출구 물은 안전
		["확인", "B_H4_출구물"],
		# C 방 F2: 선반 지그재그(유령 c2·c4 검정 2 발) → 상층 → 톱 → 틈 g3 → 호퍼 H5 에서 흰색 → 흰 상층 → 떨어지며 검정
		["가", 5900.0],
		["칠", "C_c2", 검정],
		["칠", "C_c4", 검정],
		["뛰기", 5900.0, 6150.0, 2432.0],          # c1 R [6128,6400]
		["뛰기", 6150.0, 5900.0, 2304.0],          # c2 L [5760,6032] (유령)
		["뛰기", 5900.0, 6150.0, 2176.0],          # c3 R
		["뛰기", 6150.0, 5900.0, 2048.0],          # c4 L (유령)
		["뛰기", 5900.0, 6150.0, 1920.0],          # c5 R — 상층 슬래브(6400)에 붙어 있다
		["가", 6560.0],                            # 64 턱 → 상층
		["가", 7790.0],                            # 회전톱(7200 · 세로 왕복) 밑을 지난다
		["뛰기", 7790.0, 7990.0, 1856.0],          # 틈 g3 [7808,7936] → H5 왼쪽 [7936,8096]
		["색", 흰색],                               # 호퍼 위 = 색 규칙 밖
		["가", 8300.0],                            # 흰 공급 물줄기(8128~8192)를 지나 흰 상층
		["가", 8690.0],
		["뛰기색", 8690.0, 9000.0, 2560.0, 검정],  # 오른끝에서 704 떨어지며 검정 → 물 너머 복도
		# D 방: 320 낙하 → 검정 → [틈] 흰(유령 G_D1 · 흰 물 밑) → [틈] 검정(유령 G_D2 · 회색 물) → 출구
		["가", 9590.0],
		["걸어", 9640.0, 2880.0],
		["가", 9890.0],
		["뛰기색", 9890.0, 10100.0, 2880.0, 흰색],  # 슬롯 1 → 흰 바닥(H6 흰 물 밑)
		["칠", "D_G_D1", 흰색],                     # 흰 물 밑 유령 — 흰 페인트는 안 지워진다
		["쉬", 20],
		["확인", "D_G_D1"],
		["가", 10670.0],
		["뛰기색", 10670.0, 10880.0, 2880.0, 검정],  # 슬롯 2 → 검정 바닥(H7 회색 물 밑)
		["칠", "D_G_D2", 검정],
		["쉬", 20],
		["확인", "D_G_D2"],
		["가", 11470.0],
	],
	# 헛걸음 1: A 방 F1 선반(관을 따라 올라간다 · 유령 G_A 1 발 · 막다른 곳) → 되돌아와 정답
	#          B 방 h1(검정 물)로 떨어져 주머니 P1(막다른 곳) → 굴뚝 격자로 갤러리 복귀 → 정답
	#          C 방 구덩이로 터널(물 밑 · 막다른 곳) → 굴뚝으로 물 앞 복도 복귀 → 정답
	"stage_2-3 헛걸음": [
		["가", 850.0],
		["뛰기", 850.0, 1100.0, 1728.0],
		["뛰기", 1100.0, 1350.0, 1600.0],
		["뛰기", 1350.0, 1600.0, 1472.0],          # H1 (검정 그대로)
		["칠", "A_G_A", 검정],
		["뛰기", 1500.0, 1250.0, 1344.0],          # a4 [1216,1472] 왼쪽 위로
		["뛰기", 1240.0, 990.0, 1216.0],           # a5 [960,1216]
		["뛰기", 985.0, 740.0, 1088.0],            # 유령 G_A [704,960]
		["가", 480.0],                             # F1 선반 [384,704] · CP_A_F1 — 막다른 곳
		["걸어", 340.0, 1552.0],                   # 선반 왼끝(384)에서 입구 통로 지붕(1552)으로 떨어진다
		["걸어", 740.0, 1856.0],                   # 지붕 오른끝(704)에서 바닥으로
		["가", 850.0],
		["뛰기", 850.0, 1100.0, 1728.0],
		["뛰기", 1100.0, 1350.0, 1600.0],
		["뛰기", 1350.0, 1600.0, 1472.0],
		["색", 흰색],
		["걸어", 1780.0, 1856.0],
		["가", 2530.0],
		["뛰기색", 2530.0, 2760.0, 1856.0, 검정],
		["가", 3240.0],
		["걸어", 3280.0, 2560.0],                  # h1 왼 가장자리로 떨어진다 — 검정 출구 물 → P1
		["확인", "B_H2_출구물"],
		["가", 3650.0],                            # CP_P1 · 막다른 곳
		["가", 3030.0],                            # 굴뚝 오른벽 밑(2304~2560)을 지나 굴뚝 안으로
		["뛰기", 3030.0, 2950.0, 2432.0],          # 왼벽(2944)에 몸을 붙이고 똑바로 오른다
		["뛰기", 2968.0, 2950.0, 2304.0],
		["뛰기", 2968.0, 2950.0, 2176.0],
		["뛰기", 2968.0, 2950.0, 2048.0],
		["뛰기", 2968.0, 2950.0, 1936.0],
		["가", 3240.0],                            # 80 턱 → 갤러리
		["뛰기", 3240.0, 3550.0, 1856.0],
		["가", 4260.0],
		["뛰기", 4260.0, 4580.0, 1856.0],
		["가", 5100.0],
		["걸어", 5140.0, 2560.0],
		["가", 6300.0],
		["걸어", 6440.0, 2976.0],                  # 구덩이 [6400,6528] → 터널
		["가", 7400.0],                            # CP_터널 · 막다른 곳
		["가", 7600.0],                            # 벽(7680)까지 걸으면 "가" 가 알아서 뛰어 첫 격자에 올라가 버린다 → 앞에서 선다
		["뛰기", 7600.0, 7690.0, 2848.0],          # 오른벽(7680)에 붙어 오른다
		["뛰기", 7658.0, 7690.0, 2720.0],
		["뛰기", 7658.0, 7720.0, 2560.0],          # 벽 꼭대기(2560)를 넘어 복도로 바로 올라선다(2592 격자는 사람용 발판)
		["가", 7500.0],                            # 굴뚝 구멍(32 아래 격자)을 되밟아 복도(물 앞)로 · 되돌아간다
		["가", 5900.0],
		["칠", "C_c2", 검정],
		["칠", "C_c4", 검정],
		["뛰기", 5900.0, 6150.0, 2432.0],
		["뛰기", 6150.0, 5900.0, 2304.0],
		["뛰기", 5900.0, 6150.0, 2176.0],
		["뛰기", 6150.0, 5900.0, 2048.0],
		["뛰기", 5900.0, 6150.0, 1920.0],
		["가", 6560.0],
		["가", 7790.0],
		["뛰기", 7790.0, 7990.0, 1856.0],
		["색", 흰색],
		["가", 8300.0],
		["가", 8690.0],
		["뛰기색", 8690.0, 9000.0, 2560.0, 검정],
		["가", 9590.0],
		["걸어", 9640.0, 2880.0],
		["가", 9890.0],
		["뛰기색", 9890.0, 10100.0, 2880.0, 흰색],  # 슬롯 1 → 흰 바닥(H6 흰 물 밑)
		["칠", "D_G_D1", 흰색],                     # 흰 물 밑 유령 — 흰 페인트는 안 지워진다
		["쉬", 20],
		["확인", "D_G_D1"],
		["가", 10670.0],
		["뛰기색", 10670.0, 10880.0, 2880.0, 검정],  # 슬롯 2 → 검정 바닥(H7 회색 물 밑)
		["칠", "D_G_D2", 검정],
		["쉬", 20],
		["확인", "D_G_D2"],
		["가", 11470.0],
	],
}

var _씬: Node2D
var _p: CharacterBody2D
var _월드: Node
var _코어: Node
var _사망: int = 0
var _마지막위치: Vector2
var _실패: String = ""
var _수다 := false


func _initialize() -> void:
	Engine.max_fps = 0
	_실행()


func _실행() -> void:
	_수다 = Array(OS.get_cmdline_user_args()).has("--수다")
	var 인자: Array = Array(OS.get_cmdline_user_args()).filter(
		func(a): return not String(a).begins_with("--"))
	var 대상: Array = 기본대상.duplicate() if 인자.is_empty() else 인자
	print("\n════════ 하수도 공략 주행 검사 ════════")
	print("※ 공략표(tools/하수도_주행검사.gd §공략)를 실제 물리로 태운다. 색·물·탄약 전부 산다.\n")
	var 모두통과 := true
	for 경로 in 대상:
		if not await _한판(String(경로)):
			모두통과 = false
	print("")
	quit(0 if 모두통과 else 1)


func _한판(경로: String) -> bool:
	var ps := load(경로) as PackedScene
	if ps == null:
		print("  %s — 로드 실패" % 경로.get_file())
		return false
	_씬 = ps.instantiate() as Node2D
	root.add_child(_씬)
	await physics_frame
	await physics_frame
	await physics_frame

	_p = _씬.get_node_or_null("Player") as CharacterBody2D
	_월드 = _씬
	_코어 = _씬.get_node_or_null("페인트코어")
	_사망 = 0
	_실패 = ""
	_마지막위치 = _p.global_position

	var 이름: String = String(_씬.name)
	# ★[2026-09-14] 공략은 한 씬에 여러 벌일 수 있다 — "stage_2-1" 과 "stage_2-1 헛걸음" 처럼
	#   씬 이름 뒤에 한 칸 띄고 이름표를 붙인다(프롬프트 §5: 정답 경로 + F1 헛걸음 복귀 경로 두 벌).
	#   첫 벌은 여기서, 나머지는 씬을 새로 띄워 차례로 돈다.
	var 열쇠들: Array = []
	for k in 공략.keys():
		if String(k) == 이름 or String(k).begins_with(이름 + " "):
			열쇠들.append(String(k))
	열쇠들.sort()
	var 열쇠: String = String(이름) if 열쇠들.has(String(이름)) else (String(열쇠들[0]) if not 열쇠들.is_empty() else String(이름))
	var 단계들: Array = 공략.get(열쇠, [])
	print("── %s%s ─────────────────────────────" % [이름, "" if 열쇠 == 이름 else "  [" + 열쇠.trim_prefix(이름 + " ") + "]"])
	if 단계들.is_empty():
		print("   공략이 없다 — 건너뛴다")
		_정리()
		return true

	for i in 단계들.size():
		var 단계: Array = 단계들[i]
		var 이전 := _p.global_position
		var ok := await _단계_실행(단계)
		var 지금 := _p.global_position
		var 표시 := "%2d. %-28s (%.0f,%.0f) → (%.0f,%.0f)" % [
			i + 1, _단계_이름(단계), 이전.x, 이전.y, 지금.x, 지금.y]
		if ok and _실패.is_empty():
			print("   ✔ " + 표시)
		else:
			print("   ✖ " + 표시 + "   ← " + (_실패 if _실패 != "" else "시간초과"))
			print("   ── 여기서 막혔다. 남은 단계는 돌리지 않는다.")
			_정리()
			return false

	var 끝 := _씬.get_node_or_null("끝도달_검사점") as Node2D
	var 결과 := true
	if 끝 == null:
		print("   ⚠ 끝도달_검사점 이 없다 — 끝 지점 판정을 못 한다")
	else:
		var d := _p.global_position.distance_to(끝.global_position)
		if d <= 220.0:
			print("   ★ 끝도달_검사점(%.0f,%.0f) 까지 %.0fpx — 도달" % [
				끝.global_position.x, 끝.global_position.y, d])
		else:
			print("   ✖ 끝도달_검사점 까지 %.0fpx 남았다 — 끝까지 못 갔다" % d)
			결과 = false
	print("   사망 %d 회 · 남은 탄약 %s" % [
		_사망, str(_코어.get("남은_탄약")) if _코어 else "-"])
	_정리()
	# 나머지 벌(헛걸음·탑 등)을 같은 씬으로 이어서 돈다
	for k in 열쇠들:
		if k == 열쇠:
			continue
		if not await _한판_이름표(경로, k):
			결과 = false
	return 결과


## 같은 씬을 다시 띄워 이름표가 붙은 공략 한 벌을 돈다 (`_한판` 과 같되 열쇠를 지정한다).
func _한판_이름표(경로: String, 열쇠: String) -> bool:
	var ps := load(경로) as PackedScene
	_씬 = ps.instantiate() as Node2D
	root.add_child(_씬)
	await physics_frame
	await physics_frame
	await physics_frame
	_p = _씬.get_node_or_null("Player") as CharacterBody2D
	_월드 = _씬
	_코어 = _씬.get_node_or_null("페인트코어")
	_사망 = 0
	_실패 = ""
	_마지막위치 = _p.global_position
	var 단계들: Array = 공략.get(열쇠, [])
	print("── %s  [%s] ─────────────────────────────" % [경로.get_file().get_basename(), 열쇠.trim_prefix(경로.get_file().get_basename() + " ")])
	for i in 단계들.size():
		var 단계: Array = 단계들[i]
		var 이전 := _p.global_position
		var ok := await _단계_실행(단계)
		var 지금 := _p.global_position
		var 표시 := "%2d. %-28s (%.0f,%.0f) → (%.0f,%.0f)" % [
			i + 1, _단계_이름(단계), 이전.x, 이전.y, 지금.x, 지금.y]
		if ok and _실패.is_empty():
			print("   ✔ " + 표시)
		else:
			print("   ✖ " + 표시 + "   ← " + (_실패 if _실패 != "" else "시간초과"))
			print("   ── 여기서 막혔다. 남은 단계는 돌리지 않는다.")
			_정리()
			return false
	var 끝 := _씬.get_node_or_null("끝도달_검사점") as Node2D
	var 결과 := true
	if 끝 != null:
		var d := _p.global_position.distance_to(끝.global_position)
		if d <= 220.0:
			print("   ★ 끝도달_검사점 까지 %.0fpx — 도달" % d)
		else:
			print("   ✖ 끝도달_검사점 까지 %.0fpx 남았다" % d)
			결과 = false
	print("   사망 %d 회 · 남은 탄약 %s" % [_사망, str(_코어.get("남은_탄약")) if _코어 else "-"])
	_정리()
	return 결과


func _정리() -> void:
	_입력_해제()
	_씬.queue_free()
	_씬 = null


func _단계_이름(단계: Array) -> String:
	match String(단계[0]):
		"가":     return "가 x=%.0f" % float(단계[1])
		"뛰기":   return "뛰기 %.0f→(%.0f,%.0f)" % [float(단계[1]), float(단계[2]), float(단계[3])]
		"뛰기색": return "뛰기색 %.0f→(%.0f,%.0f) %s" % [float(단계[1]), float(단계[2]),
			float(단계[3]), ("흰색" if int(단계[4]) == 흰색 else "검정")]
		"색":     return "색 → %s" % ("흰색" if int(단계[1]) == 흰색 else "검정")
		"칠":     return "칠 %s" % String(단계[1])
		"쉬":     return "쉬 %d" % int(단계[1])
		"확인":   return "확인 %s" % String(단계[1])
		"걸어":   return "걸어 %.0f→y%.0f" % [float(단계[1]), float(단계[2])]
		"레버":   return "레버 %s" % String(단계[1])
		"밀기":   return "밀기 %s → %.0f" % [String(단계[1]), float(단계[2])]
	return String(단계[0])


func _단계_실행(단계: Array) -> bool:
	match String(단계[0]):
		"가":     return await _가기(float(단계[1]))
		"뛰기":   return await _뛰기(float(단계[1]), float(단계[2]), float(단계[3]), -1)
		"뛰기색": return await _뛰기(float(단계[1]), float(단계[2]), float(단계[3]), int(단계[4]),
			단계.size() > 5 and String(단계[5]) == "즉시")
		"색":     return await _색바꾸기(int(단계[1]))
		"칠":     return _칠하기(String(단계[1]), int(단계[2]))
		"쉬":     return await _쉬기(int(단계[1]))
		"확인":   return _확인(String(단계[1]))
		"걸어":   return await _걸어(float(단계[1]), float(단계[2]))
		"레버":   return await _레버(String(단계[1]))
		"밀기":   return await _밀기(String(단계[1]), float(단계[2]))
	_실패 = "모르는 단계"
	return false


# ============================================================================
# 이동
# ============================================================================
## 목표 x 까지 걸어간다. 벽에 막히거나 앞에 바닥이 없으면 점프한다.
## 자동점프 = false 면 **절대 안 뛴다.** 발판 가장자리로 걸어갈 때 쓴다 —
## 가장자리에서는 "앞에 바닥이 없다" 가 언제나 참이라, 켜 두면 도약 지점에
## 닿는 순간 제멋대로 뛰어 다음 점프를 통째로 날린다.
func _가기(목표x: float, 최대프레임: int = 900, 자동점프: bool = true) -> bool:
	var 방향 := "move_right" if 목표x > _p.global_position.x else "move_left"
	Input.action_press(방향)
	var 누름 := false
	var 정체 := 0
	var 이전x := _p.global_position.x
	for i in 최대프레임:
		await physics_frame
		if not _살아있나():
			Input.action_release(방향)
			if 누름: Input.action_release("jump")
			return false
		if 자동점프 and not 누름 and _p.is_on_floor() and (_p.is_on_wall() or not _앞에_바닥(방향)):
			if _수다:
				print("      · 자동 점프 x=%.0f (벽=%s 앞바닥없음=%s)" % [
					_p.global_position.x, str(_p.is_on_wall()), str(not _앞에_바닥(방향))])
			Input.action_press("jump")
			누름 = true
		elif 누름 and not _p.is_on_floor() and _p.velocity.y >= 0.0:
			Input.action_release("jump")
			누름 = false
		var x := _p.global_position.x
		if absf(x - 목표x) < 도착_여유:
			# ★상승 중이면 정점까지 기다렸다 뗀다. 여기서 그냥 떼면
			#   JUMP_CUT_MULTIPLIER(0.4)가 점프를 160→40px 로 잘라 버려
			#   방금 시작한 자동 점프가 구덩이 한복판에 떨어진다(2-3 관문 4에서 실제로 겪음).
			while 누름 and not _p.is_on_floor() and _p.velocity.y < 0.0:
				await physics_frame
				if not _살아있나():
					Input.action_release(방향)
					Input.action_release("jump")
					return false
			Input.action_release(방향)
			if 누름: Input.action_release("jump")
			# ★땅에 닿을 때까지 기다린다. 공중에서 끝내면 다음 단계가 공중에서 시작해
			#   "칠한 유령 발판이 아직 안 굳었는데 뛰어 통과해 버리는" 일이 생긴다
			#   (지형.gd 이 collision_layer 를 set_deferred 로 바꾸기 때문).
			var 착지대기 := 0
			while not _p.is_on_floor() and 착지대기 < 240:
				await physics_frame
				착지대기 += 1
				if not _살아있나():
					return false
			return true
		if absf(x - 이전x) < 0.5:
			정체 += 1
			if 정체 > 150:
				_실패 = "x=%.0f 에서 막혔다" % x
				break
		else:
			정체 = 0
			이전x = x
	Input.action_release(방향)
	if 누름: Input.action_release("jump")
	if _실패.is_empty():
		_실패 = "x=%.0f 까지 못 갔다" % 목표x
	return false


## 도약x 까지 걸어간 뒤 착지x 쪽으로 점프한다.
## 공중색 >= 0 이면 정점을 지난 뒤 색을 바꾼다 (착지 바닥 색이 다를 때).
## 즉시 = 뜨자마자 색을 바꾼다(정점을 기다리지 않는다). 도약 직후 지나는 물 기둥이 반대색일 때 쓴다.
func _뛰기(도약x: float, 착지x: float, 착지y: float, 공중색: int, 즉시: bool = false) -> bool:
	if not await _가기(도약x, 600, false):
		return false
	# 발이 완전히 땅에 붙을 때까지 기다린다 — 공중에서 점프를 눌러 봐야 씹힌다
	var 대기 := 0
	while (not _p.is_on_floor() or 대기 < 6) and 대기 < 180:
		await physics_frame
		대기 += 1
		if not _살아있나():
			return false
	var 방향 := "move_right" if 착지x > _p.global_position.x else "move_left"
	Input.action_press(방향)
	Input.action_press("jump")
	var 색바꿈 := 공중색 < 0
	var 떴다 := false
	for i in 240:
		await physics_frame
		if not _살아있나():
			Input.action_release(방향)
			Input.action_release("jump")
			return false
		if not _p.is_on_floor():
			떴다 = true
		if _수다 and i < 14:
			print("        t%3d (%.0f,%.0f) vy=%.0f floor=%s jump=%s" % [
				i, _p.global_position.x, _p.global_position.y, _p.velocity.y,
				str(_p.is_on_floor()), str(Input.is_action_pressed("jump"))])
		# 정점을 지난 뒤에 점프를 뗀다 (JUMP_CUT 방지)
		if 떴다 and _p.velocity.y >= 0.0 and Input.is_action_pressed("jump"):
			Input.action_release("jump")
		if not 색바꿈 and 떴다 and (즉시 or _p.velocity.y >= 0.0):
			await _색누르기(공중색)
			색바꿈 = true
		if 떴다 and _p.is_on_floor():
			# 착지한 순간이 판정 시점이다. 예전에는 목표 x 에서 40px 넘게 벗어나면
			# 계속 걸었는데, 그러면 착지 뒤 200 프레임을 그 방향으로 더 달려
			# (1,300px!) 엉뚱한 자리에서 다음 단계가 시작됐다.
			Input.action_release(방향)
			Input.action_release("jump")
			# ★높이까지 봐야 한다. x 만 보면 **발판을 놓치고 아래 바닥에 떨어진 것**을
			#   성공으로 읽는다(갱도에서 7칸을 통째로 헛디디고도 전부 ✔ 로 나왔다).
			if absf(_p.global_position.y - 착지y) > 30.0:
				_실패 = "발판을 놓쳤다 — y %.0f 에 내려야 하는데 %.0f" % [
					착지y, _p.global_position.y]
				return false
			return true
	Input.action_release(방향)
	Input.action_release("jump")
	if _실패.is_empty():
		_실패 = "착지 x=%.0f 에 못 내렸다" % 착지x
	return false


func _앞에_바닥(방향: String) -> bool:
	var 부호 := 1.0 if 방향 == "move_right" else -1.0
	var 공간 := _p.get_world_2d().direct_space_state
	# ★레이를 **발 위 20px 에서** 시작한다. 발바닥(원점)에서 쏘면 이미 바닥 면
	#   안이거나 아래라, Godot 레이는 도형 안에서 시작하면 아무것도 안 맞는다
	#   (hit_from_inside = false) → "앞에 바닥이 없다" 가 되어 평지에서도 계속 뛴다.
	var 발 := _p.global_position + Vector2(40.0 * 부호, -20.0)
	var q := PhysicsRayQueryParameters2D.create(발, 발 + Vector2(0.0, 80.0))
	q.exclude = [_p.get_rid()]
	q.collision_mask = _p.collision_mask
	if not 공간.intersect_ray(q).is_empty():
		return true
	# ★[2026-09-14] 레이는 **일방통행(one_way_collision) 발판을 못 본다.** 2-2 의 격자·탑 선반이
	#   전부 일방통행이라, 그 위를 걷는 동안 "앞에 바닥 없음" 으로 읽혀 제멋대로 뛰었다.
	#   몸을 40px 앞에 두고 아래로 80px 밀어 보는 test_move 는 실제 이동과 같은 판정이라 일방통행도 본다.
	var 앞 := Transform2D(0.0, _p.global_position + Vector2(40.0 * 부호, -8.0))
	return _p.test_move(앞, Vector2(0.0, 80.0))


# ============================================================================
# 색 · 페인트
# ============================================================================
func _색바꾸기(목표: int) -> bool:
	if int(_p.get("player_color")) == 목표:
		return true
	await _색누르기(목표)
	for i in 10:
		await physics_frame
		if not _살아있나():
			return false
	if int(_p.get("player_color")) != 목표:
		_실패 = "색이 안 바뀌었다 (경계에 걸쳐 있나?)"
		return false
	return true


## toggle_color 를 두 프레임 눌러 준다. 한 프레임만 누르면 is_action_just_pressed 를 놓친다.
func _색누르기(_목표: int) -> void:
	Input.action_press("toggle_color")
	await physics_frame
	await physics_frame
	Input.action_release("toggle_color")


## 총을 쏜 것과 같은 상태를 만든다. 총알 궤적은 흉내내지 않는다 —
## `총.gd` 이 하는 일(발사_소모 → 명중_처리)을 그대로 부른다.
func _칠하기(이름: String, 색: int) -> bool:
	var 대상 := _찾기(_씬, 이름)
	if 대상 == null:
		_실패 = "%s 를 못 찾았다" % 이름
		return false
	var 횟수 := int(대상.call("필요횟수")) if 대상.has_method("필요횟수") \
		else int(대상.get("필요횟수"))
	# ★총알 색은 **플레이어 색**이다(`총.gd`). 검정인 채로 흰 페인트를 쏠 수 없다.
	#   이걸 안 보면 공략이 "칠할 수 없는 색을 칠했다" 로 조용히 통과해 버린다.
	var 지금색 := int(_p.get("player_color"))
	if 지금색 != 색:
		_실패 = "%s 를 %s 으로 칠하려는데 플레이어는 %s 이다" % [
			이름, ("흰색" if 색 == 흰색 else "검정"),
			("흰색" if 지금색 == 흰색 else "검정")]
		return false
	var 좌표: Vector2 = (대상 as Node2D).global_position
	for i in maxi(횟수, 1):
		if _코어 and not bool(_코어.call("쏠_수_있나")):
			_실패 = "탄약이 없다 (%s 를 칠하는 중)" % 이름
			return false
		if _코어:
			_코어.call("발사_소모")
			var r: String = _코어.call("명중_처리", 대상, 색, 좌표)
			if _수다:
				print("      · %s %d발째 → %s" % [이름, i + 1, r])
		else:
			대상.call("명중", 색, 좌표)
	if _수다:
		var 진행 = 대상.get("_진행")
		print("      · %s 필요=%d 검정발=%s 흰발=%s 긴변=%s 전체가능=%s" % [이름, 횟수,
			str(진행.횟수(0)) if 진행 else "?", str(진행.횟수(1)) if 진행 else "?",
			str(대상.get("_긴변")),
			str(대상.call("전체_색칠_가능")) if 대상.has_method("전체_색칠_가능") else "?"])
	if _수다 and 대상.has_method("현재색"):
		print("      · %s 칠한 뒤 현재색=%d" % [이름, int(대상.call("현재색"))])
	return true


## 뛰지 않고 걸어서 간다. 가장자리에서 그냥 **떨어져 내려갈 때** 쓴다 —
## 자동 점프를 켜 두면 위 발판 밑면에 머리를 박고 제자리에 떨어진다(2-2 발판 7).
func _걸어(목표x: float, 착지y: float) -> bool:
	if not await _가기(목표x, 900, false):
		return false
	if absf(_p.global_position.y - 착지y) > 30.0:
		_실패 = "y %.0f 에 내려야 하는데 %.0f" % [착지y, _p.global_position.y]
		return false
	return true


## 레버 앞까지 걸어가 E 를 누른다. `월드.gd` 의 E 중재와 같은 경로를 쓰되,
## **`닿아있나()` 가 참일 때만** 조작한다 — 멀리서 조작하면 검사가 거짓말이 된다.
func _레버(이름: String) -> bool:
	var n := _찾기(_씬, 이름)
	if n == null:
		_실패 = "%s 없음" % 이름
		return false
	var 위치: Vector2 = (n as Node2D).global_position
	if not await _가기(위치.x, 900):
		return false
	for i in 6:
		await physics_frame
		if not _살아있나():
			return false
	if not bool(n.call("닿아있나")):
		_실패 = "%s 에 손이 안 닿는다 (플레이어 x=%.0f · 레버 x=%.0f)" % [
			이름, _p.global_position.x, 위치.x]
		return false
	n.call("조작")
	for i in 10:
		await physics_frame
		if not _살아있나():
			return false
	return true


## 양동이·박스를 목표 x 까지 민다. 몸으로 밀어야 하므로 자동 점프는 끈다.
func _밀기(이름: String, 목표x: float) -> bool:
	var n := _찾기(_씬, 이름)
	if n == null:
		_실패 = "%s 없음" % 이름
		return false
	var 물체 := n as Node2D
	var 방향 := "move_right" if 목표x > 물체.global_position.x else "move_left"
	# 먼저 물체의 반대쪽으로 가서 붙는다
	var 뒤 := 물체.global_position.x + (-90.0 if 방향 == "move_right" else 90.0)
	if not await _가기(뒤, 900):
		return false
	Input.action_press(방향)
	var 이전 := 물체.global_position.x
	var 정체 := 0
	for i in 1800:
		await physics_frame
		if not _살아있나():
			Input.action_release(방향)
			return false
		var x := 물체.global_position.x
		if _수다 and i % 30 == 0:
			print("      · %s x=%.0f · 플레이어 x=%.0f vx=%.0f 벽=%s 충돌수=%d" % [
				이름, x, _p.global_position.x, _p.velocity.x, str(_p.is_on_wall()),
				_p.get_slide_collision_count()])
		if absf(x - 목표x) < 26.0:
			Input.action_release(방향)
			return true
		if absf(x - 이전) < 0.3:
			정체 += 1
			if 정체 > 240:
				_실패 = "%s 가 x=%.0f 에서 안 밀린다 (목표 %.0f)" % [이름, x, 목표x]
				break
		else:
			정체 = 0
			이전 = x
	Input.action_release(방향)
	if _실패.is_empty():
		_실패 = "%s 를 목표까지 못 밀었다" % 이름
	return false


## 노드가 지금 실제로 어떤 상태인지 찍는다 (유령이 단단해졌는가 등).
func _확인(이름: String) -> bool:
	var n := _찾기(_씬, 이름)
	if n == null:
		_실패 = "%s 없음" % 이름
		return false
	var 색 := int(n.call("현재색")) if n.has_method("현재색") else -9
	var 레이어 := -1
	var 폴리 := n.get_node_or_null("StaticBody2D") as CollisionObject2D
	if 폴리:
		레이어 = 폴리.collision_layer
	var 덧 := ""
	if "물참" in n:
		덧 = " 물참=%s 물색=%s" % [str(n.get("물참")), str(n.get("물색"))]
	print("      · %s 현재색=%d 레이어=%d 밟을수있나=%s%s · 위치=(%.0f,%.0f)" % [이름, 색, 레이어,
		str(n.call("밟을_수_있나")) if n.has_method("밟을_수_있나") else "?", 덧,
		(n as Node2D).global_position.x, (n as Node2D).global_position.y])
	return true


func _쉬기(프레임: int) -> bool:
	for i in 프레임:
		await physics_frame
		if not _살아있나():
			return false
	return true


# ============================================================================
# 사망 감지
# ============================================================================
## 월드가 리스폰시키면 좌표가 한 프레임에 크게 튄다. 그걸로 죽음을 잡는다.
## (`월드.gd` 은 사망 신호를 안 쏘고 `_리스폰()` 에서 위치만 되돌린다)
func _살아있나() -> bool:
	var 지금 := _p.global_position
	var 튐 := 지금.distance_to(_마지막위치)
	var 직전 := _마지막위치
	_마지막위치 = 지금
	if 튐 > 220.0:
		_사망 += 1
		# ★죽은 자리는 **리스폰 뒤 좌표가 아니라 직전 프레임 좌표**다.
		#   리스폰 좌표를 찍으면 언제나 체크포인트가 나와 원인 파악이 안 된다.
		_실패 = "죽었다 — (%.0f,%.0f) 에서 (%.0f,%.0f) 로 되돌아감%s" % [
			직전.x, 직전.y, 지금.x, 지금.y, _죽인것(직전)]
		return false
	return true


## 죽은 자리에서 몸과 겹치는 것을 훑어 "무엇이 죽였나" 를 이름으로 돌려준다.
## 좌표만 나오면 원인을 못 찾는다 — 벽인지 물인지 발판인지가 안 보인다.
func _죽인것(자리: Vector2) -> String:
	var 모양 := RectangleShape2D.new()
	모양.size = Vector2(44.0, 96.0)
	var q := PhysicsShapeQueryParameters2D.new()
	q.shape = 모양
	q.transform = Transform2D(0.0, 자리 + Vector2(0.0, -48.0))
	q.collision_mask = 1 | 8 | 32          # 지형 + 유령 + 유체
	q.collide_with_areas = true
	q.collide_with_bodies = true
	q.exclude = [_p.get_rid()]
	var 목록: Array[String] = []
	for 항목 in _p.get_world_2d().direct_space_state.intersect_shape(q, 16):
		var n := 항목.get("collider") as Node
		while n != null and not (n.has_method("현재색") or n.has_method("반대색인가")):
			n = n.get_parent()
		if n == null:
			continue
		var c := -1
		if n.has_method("현재색"):
			c = int(n.call("현재색"))
		var 이름색: String = "무색" if c < 0 else ["검정", "흰색", "회색"][mini(c, 2)]
		var 표 := "%s(%s)" % [n.name, 이름색]
		if not 목록.has(표):
			목록.append(표)
	return "" if 목록.is_empty() else "  ← 닿은 것: " + ", ".join(목록)


func _찾기(뿌리: Node, 이름: String) -> Node:
	if 뿌리.name == 이름:
		return 뿌리
	for c in 뿌리.get_children():
		var r := _찾기(c, 이름)
		if r != null:
			return r
	return null


func _입력_해제() -> void:
	for a in ["move_left", "move_right", "jump", "toggle_color"]:
		if Input.is_action_pressed(a):
			Input.action_release(a)

# 색상 반전 2D 플랫포머 — 협업 작업지시서 (멀티 작업자용)

> Godot 4.6.3 / 작성일: 2026-06-21
> 이 문서는 "여러 작업자(또는 여러 AI 세션)가 동시에 일을 나눠 맡기" 위한 **작업 패키지 + 프롬프트 모음**이다.
> 작업자는 자기 패키지(W-A ~ W-E)만 읽어도 독립적으로 일할 수 있도록 작성했다.
> 먼저 루트의 `프로젝트_개발정리.md`, `맵_레벨디자인_시스템.md` 를 숙독할 것.

---

## 0. 절대 규칙 (모든 작업자 공통)

1. **비주얼 / 충돌 분리**: 보이는 그림(`Sprite2D`)과 밟는 면(`CollisionPolygon2D`)을 반드시 분리한다.
   - 그림: 울퉁불퉁/디테일 자유.
   - 충돌: 일부러 매끄럽게. **정점 최소 / 이음새 없는 폴리곤 1개 / 작은 수직 단차 금지 / 경사는 `floor_max_angle` 이하**.
   - 자동 베이크(`opaque_to_polygons`)로 만든 조각난 폴리곤을 **바닥 동선에 두지 않는다**. (조각 이음새의 수직 모서리가 발 걸림의 원인)
2. **프로퍼티명 `color_state` 절대 불변**: `player.gd` 가 `get("color_state")` 로 직접 읽는다. 이름·타입(int: 0=BLACK,1=WHITE,2=GRAY)을 바꾸면 색 판정이 전부 깨진다.
3. **코드 주석 필수**: 새로 만들거나 고친 코드/씬에는 **무엇을·왜** 고쳤는지 한국어 주석을 단다. (날짜 + 이유)
4. **모듈 원점 = 좌상단, 256px 그리드 스냅**. 모듈은 `scale`/좌우반전/`color_state` 만으로 재활용한다(충돌 새로 그리지 않음).
5. **검증 의무**: 한 작업을 끝낼 때마다 헤드리스로 오류 0 을 확인한 뒤 다음으로 넘어간다.
   - 구문 검사: `Godot_v4.6.3-stable_win64.exe --headless --path <proj> --check-only -s res://<script>.gd`
   - 씬 로드: `... --headless --path <proj> res://<scene>.tscn --quit-after 120`
   - Godot exe: `C:\Users\김도형\Desktop\Godot_v4.6.3-stable_win64.exe`
6. **지형 에셋(모듈/맵 폴리 씬)은 `res://scenes/지형파일셋/` 폴더에 모은다.** (설계서의 `terrain_modules` 와 동일 개념, 폴더명만 한글)

---

## 1. 물리 레이어 / 색 규칙 빠른 참조

| 레이어 | 비트 | 용도 |
|---|---|---|
| 1 player | 1 | 플레이어 본체 |
| 2 platform_black | 2 | 검정 지형 충돌 |
| 3 platform_white | 4 | 흰색 지형 충돌 |
| 4 platform_gray | 8 | 회색 지형 충돌 |
| 5 bullet | 16 | 총알 |
| 6 paint_detector | 32 | PaintMark 판정존 |
| 7 kill_zone | 64 | DeathDetector(발판 색 판정) |
| 9 obstacle | 256 | 장애물 |

- BLACK 플레이어: 검정/회색 위 안전, 흰색은 통과 후 **사망**.
- WHITE 플레이어: 흰색/회색 위 안전, 검정은 통과 후 **사망**.
- GRAY 지형: 못 올라감(미끄러짐). 단, 페인트로 칠한 부분만 밟고 올라갈 수 있음.

---

## 2. 작업 분할 & 의존 관계

```
W-A 플레이어/충돌 튜닝 ──┐ (선행: 모든 이동 검증의 기준)
						  ├─> W-E 샘플 스테이지 조립
W-B 모듈 키트 10종 ──────┤
W-C 회색 그라데이션 경사로 ┤
W-D map1/2/3 바디 폴리 ───┘
```
- **W-A 가 먼저** 끝나야 W-B/E 의 발 걸림 판정이 의미 있다.
- W-B, W-C, W-D 는 **서로 독립**이라 동시에 진행 가능.
- W-E 는 W-A/B/C 산출물을 조립하므로 **마지막**.

---

## 3. 작업 패키지 (각자 이 절만 읽으면 됨)

### W-A — CharacterBody2D 충돌 튜닝  ⟨파일: `scenes/player/Player.tscn`, `scripts/player.gd`⟩
**목표**: 평지/경사/계단에서 발 걸림 0.
**할 일**
- Player(CharacterBody2D)에 다음 설정 (값은 플레이테스트로 미세조정):
  - `floor_snap_length` 16~32 (작은 턱에 바닥 붙음 유지)
  - `floor_max_angle` ≈ 46° (라디안 0.8029) — 의도한 경사를 바닥으로 인정
  - `floor_constant_speed = true` — 경사 속도 일정
  - `safe_margin` 소폭↑ — 미세 모서리 무시
  - `floor_stop_on_slope` 는 player.gd 가 회색 지형 여부로 동적 제어(건드리지 말 것)
- ⚠ Player 노드 `scale=0.3` 이라 px 값이 줄어든다. 최종값은 에디터 플레이로 확인.
**완료 기준**: 평지·경사·계단을 걸을 때 턱턱 걸림이 없다. 헤드리스 구문/로드 오류 0.

### W-B — 모듈 키트 10종 제작  ⟨산출: `scenes/지형파일셋/모듈/*.tscn`⟩
**목표**: 재사용 PackedScene 10종. 각 모듈은 깨끗한 충돌 1개.
**모듈 목록**: 1 Ground_Flat · 2 Slope_Up · 3 Slope_Down · 4 Platform_Float · 5 Ledge_Edge · 6 Pillar · 7 Ceiling · 8 Step_Small · 9 Gap_Bridge · 10 Curve_Hill
**내부 구조 (platform.gd 재사용)**
```
TerrainModule (StaticBody2D, script=platform.gd)
├── Visual (Sprite2D)          ← 아트 PNG (없으면 임시 Polygon2D 프리뷰)
├── CollisionPolygon2D         ← 손으로 깔끔히 찍은 충돌 1개 (조각 금지)
├── DeathDetector (Area2D)     ← 자식 CollisionPolygon2D, death_zones 그룹
└── SnapPoints/Entry, /Exit (Marker2D)  ← 이어붙임 기준점
```
**규칙**: 원점=좌상단, 256 그리드. `color_state` export 로 색 재활용. 경사 모듈은 `scale.x` 만 늘이기 기본(과한 `scale.y` 는 경사각 초과).
**완료 기준**: 10종 모두 헤드리스 로드 오류 0. 충돌은 모듈당 매끈한 폴리곤 1개.

### W-C — 회색 그라데이션 경사로  ⟨산출: `scenes/지형파일셋/회색경사로/GrayGradientSlope.tscn` + 스크립트 + 셰이더⟩
**목표**: 회색→궁금증 유발→그냥 오르면 미끄러짐→플레이어 색 총을 맞은 부분만 그 색에서 회색으로 **그라데이션**으로 칠해지고, 칠한 부분만 밟고 오를 수 있음.
**요구 동작**
- 평소: `color_state = GRAY`. 표면에 회색 그라데이션 비주얼(왜 회색? 궁금증).
- 안 칠하고 오르면: 기존 player.gd 회색 미끄럼 로직대로 미끄러져 내려감.
- BLACK 플레이어가 BLACK 총을 쏜 부위: 그 지점에서 **검정 → 회색**으로 자연스러운 그라데이션.
- WHITE 플레이어가 WHITE 총을 쏜 부위: 그 지점에서 **흰색 → 회색**으로 그라데이션.
- 칠해진 부분: 해당 색 플레이어가 **밟고 오를 수 있음**(물리 충돌 활성). (기존 PaintMark 의 동적 StaticBody 충돌 패턴 재사용)
**완료 기준**: 총 쏘기 전 미끄러짐 / 쏜 부위만 그라데이션 + 등반 가능. 오류 0.

### W-D — map1/2/3 바디별 폴리곤  ⟨산출: `scenes/지형파일셋/맵폴리/*.tscn`⟩
**목표**: `scenes/world_1/map1·map2·map3/` 의 지형 PNG 각각을 **바디(덩어리) 단위로** 깔끔한 `CollisionPolygon2D` 1개씩 채운다. (조각 자동 베이크 금지)
**대상 예**: Platform_Black/White_xx, Slope_Body/Surface_xx, Platform_Thorn_xx(장애물 레이어), Cogwheel/Vine(장애물) 등.
**완료 기준**: 각 PNG 가 1개의 매끈한 폴리곤 충돌을 갖는 TerrainImage(혹은 platform 기반) 씬으로 존재. 오류 0.

### W-E — 샘플 스테이지 조립  ⟨산출: `scenes/world_1/stage_3/stage_3.tscn` 갱신 또는 신규⟩
**목표**: W-B 모듈만 배치·크기·반전·색으로 한 스테이지 완성(충돌 새로 안 그림).
**규칙**: 앞 모듈 Exit 높이 = 다음 모듈 Entry 높이(바닥 끊김 없음). 난이도 비트: 도입(안전)→상승→절정→보상(안전발판)→출구(Portal). 절정에 기존 장애물(Gear/Spike/Thorn/FallingBlock) + 회색경사로 페인트 퍼즐 배치. 같은 모듈을 크기·반전·색만 바꿔 최소 3가지로 재사용.
**완료 기준**: 처음~포탈 클리어 가능. 발 걸림 0. 오류 0.

---

## 4. 진행 현황 체크리스트

- [x] W-A 플레이어 튜닝 — `Player.tscn` floor_snap_length/max_angle/constant_speed/safe_margin 적용. 헤드리스 로드 OK. (발 걸림 체감은 에디터 플레이로 최종확인 권장)
- [x] W-B 모듈 10종 — `scenes/지형파일셋/모듈/*.tscn`. 생성기 `tools/build_modules.gd`. 10/10 구조·색전환 검증 OK.
- [x] W-C 회색 그라데이션 경사로 — `scenes/지형파일셋/회색경사로/GrayGradientSlope.tscn` + `scripts/gray_gradient_slope.gd` + `shaders/gray_slope_paint.gdshader`. paint/reset 기능검증 OK. (총=플레이어 색으로 변경: `player.gd._shoot`)
- [x] W-D map1/2/3 바디 폴리 — `scenes/지형파일셋/맵폴리/<map>/*.tscn` 40개. 생성기 `tools/build_map_polys.gd`. 각 씬 '단일 폴리곤' 검증 OK.
- [x] W-E 샘플 스테이지 — `scenes/world_1/stage_3/stage_3.tscn` (모듈만 조립). 로드·레이어·런타임오류0 검증 OK.
- [x] 죽음/리스폰 — 검정 플레이어가 흰색 지형 위 → 사망 → 리스폰 → death_count+1, 헤드리스 시뮬 OK.

### 검증 스크립트 (헤드리스, tools/) — 필요없으면 삭제 가능
`test_gray_slope.gd` · `test_modules.gd` · `test_stage3.gd` · `test_death.gd` · `test_map_polys.gd`
생성기: `build_modules.gd` · `build_map_polys.gd`

### 남은 폴리시(다음 작업자용)
- 에디터에서 stage_3 직접 플레이(F6)로 발 걸림/미끄럼/등반 손맛 미세조정.
- map 폴리는 '볼록 껍질'이라 오목 지형(아치 등)은 과덮음 가능 → 그런 PNG만 손으로 폴리곤 1개 수정.
- 실제 회색 경사로 아트 PNG 가 오면 `GrayGradientSlope` 의 Visual(Polygon2D) 를 Sprite2D 로 교체.

---
*이 문서는 Claude 가 작성했습니다. 작업 분할·동시 진행·인수인계용.*

# 작업 보고 — 빛/페인트 수정 · 지형 에셋 생성 · 스테이지 3~10 설계 (2026-06-28)

> 색상반전 2D 플랫포머 / Godot 4.6.3
> 대상: 같이 작업 중인 다른 작업자 / 새 세션. "무엇이 바뀌고 추가됐는지" 추적용.
> 관련: [발광에너지_연발제한_시스템.md](발광에너지_연발제한_시스템.md), [가이드라인_제미나이_지형프롬프트.md](가이드라인_제미나이_지형프롬프트.md)

---

## A. 버그 수정 2건 (검증 완료)

### A-1. 발광이 '화면 전체'를 어둡게 (사용자 피드백: 빛이 잘 안 보임)
- **문제**: 기존 호박색 후광이 밝은 배경에서 잘 안 보였고, 어두워질 때 플레이어 주변만 살짝 어두워졌다.
- **해결**: `shaders/energy_darkness.gdshader` 신규 + `player.gd` 에 **전용 CanvasLayer(layer 48) 풀스크린 어둠 오버레이** 추가.
  - 발광이 줄면 **화면 전체가 어두워지고**, 플레이어 주변만 원형으로 밝게 남는다(빛 구멍).
  - 파라미터: `DARK_MIN=0.12`(가득일 때도 은은한 비네트로 빛이 늘 보임) ~ `DARK_MAX=0.75`(바닥).
	빛 구멍 반경: 가득 0.5 → 바닥 0.22 (쏠수록 시야가 좁아짐).
  - `_update_energy_light()` 가 매 프레임 `light_pos`(플레이어 화면좌표)·`darkness`·`light_radius`·`aspect` 갱신.
  - layer 48 = 월드 위 + 테두리글로우(49)·HUD(그 위) 아래 → **어둠이 HUD 텍스트를 안 가림**.

### A-2. 페인트가 '지형 그림'에 정확히 칠해지도록 (사용자 피드백: 물리레이어 안 찍으면 색 안 칠해짐)
- **문제(원인)**: 기존 `paint_clip.gdshader` 는 페인트를 지형의 **CollisionPolygon2D(물리)** 한 조각에 맞춰 잘랐다.
  자동 생성 충돌은 비주얼과 다르거나 여러 조각으로 쪼개져, 페인트가 그림과 어긋나거나 잘렸다.
- **해결**: `shaders/paint_clip_alpha.gdshader` 신규 — 페인트를 **지형 비주얼(Sprite2D 텍스처 알파)** 에 맞춰 오린다.
  - 충돌과 무관하게 "그림이 그려진 픽셀"에만 페인트가 남음 → 물리레이어 정밀도와 무관하게 정확.
  - `bullet.gd._spawn_mark`: 지형의 텍스처 Sprite2D 를 찾아(`_find_terrain_sprite`) `paint_mark.setup_terrain_clip_tex()` 로 넘김.
	텍스처가 없는 기하 발판(Polygon2D)은 **기존 폴리곤 클립으로 자동 폴백**(하위호환 유지).
  - `paint_mark.gd`: `_apply_clip_shader_tex()` 신규. 월드→지형로컬 역변환을 열벡터 3개(inv_x/y/o)로 셰이더에 전달.

> 검증: bullet.gd/paint_mark.gd 구문 통과 · stage_1/2/3 헤드리스 부팅 **셰이더/스크립트 에러 0**.

---

## B. 지형 에셋 — 제미나이 이미지 → 검정 지형 Arch 씬 10종 (생성 완료)

사용자가 [가이드라인_제미나이_지형프롬프트.md](가이드라인_제미나이_지형프롬프트.md) 로 뽑은 10장(`Desktop/모듈 키트/*.jpeg`, 마젠타 배경)을 처리.

### 이미지 품질 검토
| 의도대로(7) | Ground_Flat, Slope_Up, Slope_Down, Platform_Float, Ledge_Edge, Curve_Hill, **Gap_Bridge(아치 구멍 완벽)** |
|---|---|
| 비슷하게(3) | Ceiling·Pillar(천장/기둥이 아닌 사각 바위블록·서로 쌍둥이), Step_Small(둥근 바위) — **고품질 범용 블록이라 사용 가능** |

### 파이프라인 (실행 기록)
1. 10장 → `scenes/지형파일셋/원본이미지/` (영문명 복사).
2. `silhouette_from_image.gd` 설정 변경:
   - `DEFAULTS`: `color:"BLACK"`, `mode:"colorkey"`, `tol:0.2` (마젠타+JPEG 헤일로 제거).
   - `MIN_PIECE_AREA: 600 → 8000` (**우하단 흰 ✦ 워터마크 조각 버림**, 큰 지형 1개만 남김).
3. → `실루엣/<이름>_01_B.png` 10개 (워터마크 제거·윗면 매끈 확인).
4. `--import` → `bake_architecture_tileset.gd` → `실루엣/<이름>_01_B.tscn` 10개(TerrainImage, 각 1폴리).

> **검정 지형 키트 완성**. A-2 페인트 수정이 이 TerrainImage 들에 바로 적용됨(텍스처 알파 클립).

### ⚠ 남은 처리(다음 작업)
- **흰색 버전**: 지금은 전부 검정. 흰 지형이 필요하면 도구 `color:"WHITE"` 로 재실행 → `_W.tscn`(layer 4).
- **Gap_Bridge**: 1폴리 SOLIDS 로 구워져 아치 구멍이 메워졌을 수 있음 → 인스펙터 `Collision Build Mode = SEGMENTS` 로.
- **(기존 버그·범위밖)** baker 가 `도형_타일셋/broken_platform.png.png`(삭제된 파일)를 참조해 ERROR 1줄 — baker `ENTRIES` 에서 그 줄 제거하면 깨끗해짐.

---

## C. 스테이지 3~10 레벨디자인 설계 (구현 예정)

> 목표: 각 ~2분 분량, 1·2스테이지 참고, 갈수록 어렵게(색칠·다양한 움직임·예측 어려운 이동·생각하고 멈칫).
> 기법: [맵_레벨디자인_시스템.md](맵_레벨디자인_시스템.md) 의 "비트 흐름"(도입→상승→절정→숨고르기→출구) + 점프물리(높이≈150/수평≈300, 단차≤130·틈≤230).

| St | 테마 | 핵심 난이도 노브 | 적극 활용 장애물 |
|----|------|----------------|-----------------|
| 3 | (기존) 추격 탈출 | 불벽 추격, 체크포인트 | FireWall, 기어, 가시 |
| 4 | 색칠 입문 심화 | 회색 경사로 등반 + 반대색 발판 건너기 | 가시, 회색경사로 |
| 5 | 점프 정밀 | 공중발판 + 넓은 틈 + 색 전환 점프 | 공중발판, 가시 |
| 6 | 상하 기어 미로 | 상하이동 기어 + 좁은 천장 통로 | 상하기어, 천장 |
| 7 | 페인트 퍼즐 | 회색 게이팅(칠해야 진행) + 제한된 발광(연사 못함) | 회색지형, 가시 |
| 8 | 얼음(회색 미끄럼) | GRAY 미끄럼 구간 + 낙하 타이밍 | 회색지형, 낙하블록 |
| 9 | 복합 추격 | 불벽 + 색전환 + 점프 동시 요구 | FireWall, 기어, 가시 |
| 10 | 종합 시험 | 위 모든 요소 압축 + 최장 길이 | 전부 |

- 공통: 어려운 구간 직후 **안전 발판**(숨 고르기) 배치, 죽음 횟수가 적을수록 자랑되게.
- 지형: B의 검정 Arch 키트 + 흰 버전을 섞어 흑/백 색칠 동선을 구성.
- 각 스테이지 길이 ≈ 6000~9000px(2분 분량), 카메라 `camera_zoom`/limits 측정 배치.

---

## D. 다음 작업 순서 (이어받는 사람용)
1. 흰색 지형 버전 생성(도구 color="WHITE" 재실행) + Gap_Bridge SEGMENTS 처리.
2. 스테이지 4부터 한 개씩 텍스트 .tscn 으로 조립(생성기 -s 는 오토로드 미등록으로 실패 → 텍스트가 정답, 메모리 참고).
3. 각 스테이지 헤드리스 부팅(`godot ... <scene>.tscn --quit-after 60`)으로 에러 0 + 발걸림 0 확인.
4. SceneManager STAGES 에 4~10 경로 등록.

### 검증 명령
```
"<godot>" --headless --path "<proj>" "res://scenes/world_1/stage_N/stage_N.tscn" --quit-after 120
```
</content>

# 작업 보고 — 페인트 안쪽 채움 · 배터리 게이지 · 스테이지 선택 · stage4 가시성 (2026-06-28 4차)

> 색상반전 2D 플랫포머 / Godot 4.6.3
> 대상: 같이 작업 중인 다른 작업자 / 새 세션.

---

## ① 페인트가 "안 칠해지고 가장자리만" → 안쪽까지 솔리드하게 (근본 수정)
- **원인**: `paint_mark._build_spray` 가 물방울을 `SURFACE_LOCAL`(표면 '바깥쪽')에 찍어, 지형 알파 클립에
  거의 다 잘려 **가장자리 얇은 띠만** 보였다(= "많은 지형이 안 칠해짐 + 바깥만 칠해짐").
- **수정**:
  - 물방울을 **지형 안쪽(+Y, local 0~+52)** 에 배치 → 안쪽까지 채워지고 클립에 안 잘림.
  - 물방울 텍스처 코어를 불투명하게(`(1-dist)*2.4`) + 구성을 **단단한 중심 블롭(4~5) + 중간 + 분사 + 드립**으로
    바꿔 한 발이 또렷한 splat 으로 읽히게(직관적 가시성).
  - `alpha_cut` 0.35→0.25, 기하 발판엔 폴리곤 클립 폴백(블록 밖 안 삐짐).
- **시각 검증**(아래 워크플로): Arch 지형 + stage_1 실제 지형(비균등 scale 1.3) 둘 다 **안쪽까지 솔리드하게** 칠해짐 확인.

## ★ 시각 검증 워크플로 (사용자 지적 반영)
- **헤드리스는 화면을 렌더하지 않는다** → "프로그램상 OK"만으로는 함정.
- 이제 **창 모드로 뷰포트를 PNG 로 저장**해 눈으로 확인한다:
  `Godot --path . -s res://tools/<도구>.gd` → `*_out.png`.
- 도구: `tools/paint_visual_test.gd`(지형에 페인트 격자), `tools/scene_shot.gd`(임의 씬 촬영),
  `tools/battery_shot.gd`(게이지 상태), `tools/paint_stage1_test.gd`(불투명 픽셀 위 페인트).
- **교훈: 시각 요소는 반드시 렌더해서 이미지로 확인.**

## ② 색 게이지 → 좌상단 '배터리형'(칸 분할, 손그림)
- `scripts/ui/battery_gauge.gd`(class_name BatteryGauge): 칸 5개, 손그림 지터 외곽(고정 시드라 안 깜빡임),
  배터리 단자, **플레이어 색(흑/백)으로 칸 채움**, 밝은 내부 배경(흑·백 둘 다 대비).
- `hud.gd`: 게이지를 **좌상단**(22,14)으로 옮기고, STAGE/TIME/해골/데스 바를 그 아래(24,84)로 정렬(게임 느낌).
- 검증: 60%검정→3칸 / 80%흰→4칸 / 20%검정→1칸 렌더 확인.

## ③ 스테이지 전환/선택 버그
- **STAGE 번호 안 바뀜**(3차 수정): clear_popup 이 `change_scene_to_file` 직접 호출로 `current_stage` 미갱신 →
  `SceneManager.go_to_stage_by_path`(경로→번호 역추적) 경유로 수정.
- **4스테이지 선택 불가**: `stage_select.gd` 가 `[1,2,3]` 하드코딩 → **SceneManager.STAGES 전체에서 버튼 동적 생성**
  (2열 그리드). 이제 스테이지 4~10 추가 시 선택 화면에 자동으로 나타남.

## ④ stage_4 모듈 이미지가 안 보임
- **원인**: 검정 Arch 지형을 검정 Platform(G1) **위에 겹쳐** 놓아 "검정 위 검정"으로 안 보였다.
- **수정**: G1 제거 → 도입 바닥을 **Arch 지형 3조각(Ground_Flat·Curve_Hill·Ground_Flat, scale 0.55 겹침)** 이
  '비주얼+충돌'을 함께 담당하게 교체. 숲 배경 위로 Badland 실루엣이 보이고, 거기서 새 분사 페인트를 시험 가능.
  (렌더로 지형 표시 + 플레이어 안착 확인.)

---

## 검증 (Godot 4.6.3)
- stage_1/2/3/4 부팅 **스크립트 에러 0**. StageSelect 부팅 0.
- 페인트: Arch + stage_1 실제 지형 렌더로 **안쪽 솔리드 칠 + 실루엣 클립** 확인.
- 배터리 게이지 렌더 확인.

## 미검증 (라이브 플레이로 — 피드백용)
- 실제 총 쏠 때 페인트 손맛(밀도/드립), stage_4 점프 간격·난이도.
- 다음: 흰색 Arch 지형 생성 → stage 5~10.
</content>

class_name ColorDefs
extends RefCounted
## 게임 전체에서 공유하는 색 상수.
## Autoload 없이 class_name 만으로 어디서든 ColorDefs.BLACK 등으로 접근 가능.
##
## 색을 추가할 일이 생기면 이 파일에만 추가하면 됨.

const BLACK: int = 0
const WHITE: int = 1
const GRAY:  int = 2

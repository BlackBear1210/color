# 도형 타일셋 — 작업 인수인계 & 구조화 프롬프트

> 색상 반전 2D 플랫포머 / Godot 4.6.3 / 작성·갱신일 2026-06-17
> 대상: 이 프로젝트를 이어서 작업할 다른 작업자 / 새 Claude 세션
> 짝꿍 문서: [맵_레벨디자인_시스템.md](맵_레벨디자인_시스템.md)(설계 원리), [맵폴리_베타_가이드.md](맵폴리_베타_가이드.md)(상세 사용법)

---

## 0. 한 줄 요약

> **프로젝트에 이미 있는 "디테일 살아있는 흑색 지형 PNG"를 이미지+폴리(충돌)가 묶인 씬으로 자동 변환해
> `도형_타일셋_최신` 에 10종(앞으로 N종) 모아 두고, 이 조각들을 크기·반전·색·배치만 바꿔 불규칙하게
> 이어 붙여 다양한 맵을 빠르게 만든다.**

---

## 1. 핵심 설계 원칙 (반드시 준수)

1. **비주얼과 충돌 분리** — 그림(Sprite)은 디테일 살아있게, 충돌(CollisionPolygon2D)은 알파 외곽선을 따라 자동 생성.
2. **이미지+폴리 = 한 씬** — 아키텍처 1개 = `Arch_XX.tscn` 1개 (Sprite2D + 충돌 + DeathDetector + Entry/Exit 마커).
3. **조립식** — 큰 지형을 통째로 그리지 않고, 작은 아키텍처를 여러 방향·크기로 변형해 불규칙 배치 → 플레이어가 난이도를 느낌.
4. **충돌 모드 규칙**
   - 단일 블록형(평지·언덕·발판·계단 조각): **SOLIDS** → 에디터에서 1스테이지처럼 **무지개색으로 보임**.
   - 동굴·도넛형(가운데가 뚫린 지형): **SEGMENTS(외곽선만)** → 구멍이 메워지지 않음. (단, 무지개 채움으로는 안 보이고 외곽선으로 보임)
5. **`color_state` 프로퍼티명 불변** — player.gd 가 `get("color_state")` 로 직접 읽어 색 사망 판정. 절대 이름 변경 금지.
6. **코드 수정 시 한국어 주석 필수** — `▼ 2026-06-17 ...` 형식으로 "왜 고쳤는지" 명시(다른 작업자 추적용).

---

## 2. 파일 구조 (현재)

```
scenes/도형_타일셋_최신/                   ← ★ 타일셋 본체 (이미지+폴리)
    Arch_01_BlackBlock_A.tscn ~ Arch_10_BrokenPlatform.tscn   (10종)
    도형_타일셋_최신_전체보기.tscn          ← 10종 카탈로그(0.2배로 한눈에)
    조립_템플릿.tscn                        ← 새 맵 시작용 빈 작업장(+Player)
scenes/지형파일셋/                          ← ★ (2026-06-21) 신규 지형 에셋 폴더
    원본이미지/                              ← 핀터레스트 등 '날것 이미지' 투입함
    실루엣/                                  ← 누끼+흑백 변환 결과(PNG). baker 가 자동 스캔/굽기
    모듈/ · 회색경사로/ · 맵폴리/             ← 모듈10종 · 회색 그라데이션 경사로 · 맵PNG 폴리
tools/
  silhouette_from_image.gd                ← ★ (2026-06-21) 임의 이미지→흑/백 실루엣+투명 PNG
  bake_architecture_tileset.gd            ← ★ PNG→(이미지+폴리)씬 자동 생성기
scripts/
  terrain_image.gd      (이미지 지형: Sprite+자동충돌+색판정)  ← Arch 가 사용
  terrain_module.gd     (대안: 단색 폴리곤 모듈 — 아트 없이 프로토타이핑용)
  auto_collision.gd     (알파→폴리 변환 헬퍼. build_mode/노이즈제거 개선됨)
  platform.gd / player.gd (기존 색 판정 로직 — 수정 불필요)
```

---

## 3-누끼. 임의 이미지 → 흑/백 실루엣 + 투명배경 — `tools/silhouette_from_image.gd` (2026-06-21 신규)

핀터레스트/직접 캡처 등 **아무 이미지나** 가져와 지형 PNG(흑색 또는 흰색 실루엣 + 투명배경)로 바꿔
바로 bake 파이프라인에 넣는 단계. 맵을 "찍어내듯" 빠르게 만들기 위한 **맨 앞단**이다.

### ⚠ 먼저 알아둘 것 — Godot 의 한계 (정확한 사실)
- **Godot 엔진에는 'AI 누끼'(복잡한 배경 자동 분리) 기능/공식 에셋이 없다.** AssetLib 에 전용 플러그인 없음.
  - 검색해 나오는 [Sprite Buff](https://spritebuff.com/)(브라우저 도구)도 **flood-fill 방식 → 단색/그린스크린 배경만** 깔끔히 제거된다(사진 배경은 약함).
- 따라서 배경 종류에 따라 **2가지 길**로 나뉜다:

| 배경 상태 | 누끼 방법 |
|---|---|
| **단색/근단색** 배경(또는 이미 투명 PNG) | `silhouette_from_image.gd` 로 **바로** 처리(엔진 내장 Image API) |
| **사진처럼 복잡한** 배경 | 먼저 외부 **AI 매팅**으로 투명 컷아웃 → 그다음 이 도구의 `alpha` 모드로 흑/백만 입힘 |

### 외부 AI 매팅 도구 (복잡한 배경용, 추천순)
1. **rembg** — 무료·로컬·고품질(U2-Net). 한 줄: `pip install rembg[cli]` → `rembg i 입력.png 출력.png`.
   - 폴더 일괄: `rembg p 입력폴더 출력폴더`.  결과(투명 PNG)를 `scenes/지형파일셋/원본이미지/` 에 넣으면 됨.
2. **Photopea** (photopea.com, 무설치 웹/무료) — Select > "Magic Cut"/배경제거 → PNG 내보내기. 자유 곡선 컷도 가능.
3. **remove.bg** (웹/무료 제한) — 빠르지만 장수 제한·워터마크 정책 확인.

### `silhouette_from_image.gd` 사용법
1. 원본 이미지(아무 png/jpg/webp)를 `scenes/지형파일셋/원본이미지/` 에 넣는다.
2. 실행: `"<godot>" --headless --path "<proj>" -s res://tools/silhouette_from_image.gd`
3. 결과(흑/백 실루엣 + 투명 PNG)가 `scenes/지형파일셋/실루엣/` 에 저장된다.
4. 이어서 baker 실행 → Arch 씬 자동 생성(baker `SCAN_DIRS` 에 `실루엣/` 가 이미 등록됨).

**모드(스크립트 상단 `DEFAULTS` / 개별 `ENTRIES`):**
- `auto`(기본): 입력에 투명이 있으면 `alpha`, 없으면 `colorkey` 자동 선택.
- `alpha`: 이미 투명 PNG(rembg/Photopea 결과) → 색만 흑/백으로.
- `colorkey`: 단색 배경 → 네 모서리 색 샘플링 후 '연결된 배경'만 flood-fill 제거(`tol` 로 강도).
- `luminance`: 피사체가 배경보다 어둡/밝을 때 밝기 임계로 분리(`lum`, `lum_dark`).
- `color`: `"BLACK"` 또는 `"WHITE"` (지형 색). 개별 지정 예는 스크립트 `ENTRIES` 주석 참고.

> 전체 파이프라인(찍어내기): **원본이미지 → silhouette_from_image.gd → 실루엣/ → bake_architecture_tileset.gd → Arch 씬 → 조립**

---

## 3-0. 큰 지형을 "모양 기반"으로 조각내기 — `tools/slice_terrain.gd` (다운로드 불필요)

Godot 가 PNG 를 직접 자르므로 GIMP 등 외부 도구가 **필요 없다**.
- **모양 기반**: 알파(불투명)가 연결된 덩어리를 찾아 지형 모양대로 조각낸다(떨어진 바위=각각 1조각).
- **큰 덩어리는 4방향**: bbox 가 `LARGE_PX`(기본 1000) 초과면 좌상/우상/좌하/우하(TL/TR/BL/BR)로 4분할.
- 조각은 흑색 실루엣 + 투명배경 유지 → `scenes/도형_타일셋_최신/sliced/` 에 저장.
- 설정(스크립트 상단): `SOURCES`(자를 원본들), `LARGE_PX`, `LABEL_MAX_DIM`, `MIN_PIECE_PX`.
- 실행: `godot --headless --path <proj> --script res://tools/slice_terrain.gd`
- 자유 곡선으로 "디자인해서" 자르고 싶을 때만 Photopea(브라우저, 무설치)/Krita/GIMP 사용.

전체 파이프라인: **slice_terrain.gd(조각) → `--import`(또는 에디터 열기) → bake_architecture_tileset.gd(폴리)**.
baker 는 `SCAN_DIRS`(기본 `sliced/`) 안의 모든 png 를 자동으로 구워 이미지+폴리 씬을 만든다.

---

## 3. 타일셋 추가/재생성 자동화 (가장 중요한 도구)

새 지형 PNG 를 타일셋에 넣는 법:

1. PNG 준비 — **흑색 실루엣 + 디테일**, 배경은 **투명(알파)** 이어야 함(충돌이 알파 외곽선을 따름).
   - 임포트 탭에서 압축을 **Lossless** 로(안 그러면 `get_image()` 실패해 충돌이 안 생김).
2. `tools/bake_architecture_tileset.gd` 의 `ENTRIES` 목록에 `{"name": "...", "tex": "res://경로.png"}` 추가.
3. 실행:
   ```
   "<godot>" --headless --path "<프로젝트>" --script res://tools/bake_architecture_tileset.gd
   ```
   → `도형_타일셋_최신/` 에 이미지+폴리 씬이 자동 저장된다(SOLIDS, 무지개색).
4. 동굴형이면 생성 후 해당 씬의 인스펙터에서 `Collision Build Mode` 를 **SEGMENTS** 로 바꾼다.

> 이렇게 하면 "그림→배경제거→외곽선 따라 폴리→한 씬" 과정을 PNG 한 줄 추가로 자동화한다.

---

## 4. 맵 제작 워크플로 (레벨 작업자, 반복)

1. `도형_타일셋_최신/조립_템플릿.tscn` 복제 → 새 스테이지로.
2. `도형_타일셋_최신_전체보기.tscn` 으로 어떤 아키텍처가 있는지 확인.
3. 파일시스템에서 `Arch_XX.tscn` 을 **MapPhysics 아래로 드래그** → 인스턴스.
4. 인스펙터에서 **Position / Scale / scale.x=-1(반전) / Color State** 조정.
5. **이어붙이기**: 앞 조각 `Exit` 마커 ≈ 다음 조각 `Entry` 마커 위치가 되게 배치 → 바닥이 안 끊김.
6. 같은 조각을 크기·각도 다르게 **불규칙 반복** → 적은 에셋으로 다양한 난이도.
7. 장애물(Thorn/Gear/Falling/Spike/Portal)도 같은 방식으로 인스턴스.
8. ▶ 실행해 발 걸림 0 · 색 판정 정상 확인.

---

## 5. 지금까지의 작업 내역 (변경 이력)

### ✅ 버그 수정
- **stage_2 동굴 충돌 먹통**: `BlackCave1/2/3` 의 거대 키홀(자기교차) 폴리곤을 SOLIDS 로 구워
  동굴 속이 메워진 게 원인. 해당 3개 폴리곤(`AutoCol9/AutoCol6`)에 `build_mode = 1`(SEGMENTS) 적용 → 동굴 속 충돌 해제.
- `scripts/stage.gd`: KillZone 없는 씬에서 `@onready $경로` 가 에러 → `get_node_or_null` 로 변경(주석 명시).

### ✅ 코드 개선 (모두 주석에 이유 명시)
- `scripts/auto_collision.gd`: `build_mode` 인자(동굴=SEGMENTS) + 초소형 노이즈 폴리곤 제거(`MIN_POLY_AREA`, 발 걸림 예방).
- `scripts/terrain_image.gd`: `collision_build_mode` export 추가.

### ✅ 타일셋 시스템 구축
- `tools/bake_architecture_tileset.gd`: PNG→(이미지+폴리)씬 일괄 생성기(헤드리스 실행).
- `scenes/도형_타일셋_최신/`: **Arch_01~10** (이미지+폴리, SOLIDS 무지개) + 전체보기 + 조립_템플릿.
- (구) `scenes/modules/` 의 단색 기하 모듈은 이미지 방식으로 대체되어 제거. 단색 엔진 `terrain_module.gd` 는 프로토타이핑용으로 보존.

### ✅ 검증 (Godot 4.6.3 헤드리스)
- 수정 스크립트 4종 구문 통과 / 타일셋 10종 생성 성공(폴리 1~13개) / 전체보기·조립_템플릿·stage_1·stage_2 로드 시 에러 0.

### ⏳ 미해결 · 확인 필요 (에디터에서 사람이 직접)
1. **"stage_2 폴리가 1스테이지처럼 무지개로 안 보임"** — 의도된 동작.
   - 작은 폴리(stalactite 등)는 SOLIDS 라 무지개로 보임. **거대 동굴 폴리 3개만 SEGMENTS** 라 외곽선으로만 보인다(충돌은 정상 적용, 헤드리스로 검증됨).
   - 화면의 흰 다이아몬드는 "선택된 폴리의 정점 편집 핸들"일 뿐 오류 아님.
   - **무지개로 보이게 하려면** = 동굴을 한 덩어리 SOLIDS 로 바꿔야 하는데 그러면 구멍이 다시 메워짐.
     → 근본 해법은 **stage_2 동굴을 도형_타일셋_최신 의 SOLIDS 조각들로 재구성**하는 것(다음 작업).
2. stage_2 장애물(Thorn/Gear) 실제 작동, 동굴 내 이동 — ▶ 실행로 확인.
3. 각 Arch 씬을 한 번 열어 무지개 충돌 확인.

### 📋 다음 작업
- **stage_2 동굴을 Arch 조각들로 재구성** → 충돌이 무지개로 보이고 구멍도 안 메워짐(둘 다 해결).
- 큰 PNG 를 더 작은 "계단 1칸" 단위로 쪼개 아키텍처 다양화.
- 자동화 다음 단계: 맵을 JSON/CSV(`arch_id,x,y,scale,flip,color`)로 기술 → 로더가 자동 조립.

---

## 6. 새 작업자에게 주는 지시 프롬프트 (복사용)

```text
[프로젝트] Godot 4.6.3 색상반전 2D 플랫포머 (repo: color)
[목표] 도형_타일셋_최신(이미지+폴리 아키텍처 10종)으로 맵을 빠르고 다양하게 조립. 발걸림/충돌 버그 0.

[반드시 지킬 규칙]
- 비주얼/충돌 분리: 그림은 디테일 OK, 충돌은 알파 외곽선 자동 폴리.
- 아키텍처 1개 = Arch_XX.tscn 1개(이미지+폴리+DeathDetector+Entry/Exit).
- 단일블록형=SOLIDS(무지개로 보임), 동굴형=SEGMENTS(구멍 안 메워짐).
- color_state 프로퍼티명 불변(player.gd가 읽음).
- 코드 수정 시 "▼ 날짜 + 이유" 한국어 주석 필수.
- 이어붙임: 앞 조각 Exit 마커 ≈ 다음 조각 Entry 마커.

[입력]
- 원리: 맵_레벨디자인_시스템.md / 사용법: 맵폴리_베타_가이드.md / 현황·프롬프트: 타일셋_작업_인수인계.md(이 문서)
- 타일셋: scenes/도형_타일셋_최신/Arch_01~10_*.tscn, 전체보기, 조립_템플릿
- 이미지→실루엣: tools/silhouette_from_image.gd  (원본이미지/ → 실루엣/)
- 생성기: tools/bake_architecture_tileset.gd (PNG→씬, SCAN_DIRS 에 실루엣/ 포함)
- 코어: scripts/{terrain_image,auto_collision,platform,player}.gd

[할 일]
0. (신규) 임의 이미지로 새 지형 만들기 — "찍어내기" 파이프라인:
   - 단색배경/투명PNG: scenes/지형파일셋/원본이미지/ 에 넣고 silhouette_from_image.gd 실행 → 실루엣/ 에 흑·백 투명 PNG.
   - 사진처럼 복잡한 배경: 먼저 rembg(`pip install rembg[cli]`; `rembg i in out`) 또는 Photopea 로 누끼 → 원본이미지/ → 위 도구 alpha 모드로 흑/백.
   - ※ Godot 자체엔 AI 누끼 없음. 위 2단계가 표준.
1. 실루엣 PNG(흑/백+투명, 임포트 Lossless)를 bake 실행 → Arch 씬 자동 생성(SCAN_DIRS=실루엣/). 개별 큰 지형은 ENTRIES 에 추가도 가능.
2. 동굴형 조각은 collision_build_mode=SEGMENTS로.
3. 조립_템플릿 복제 → Arch 조각을 position/scale/flip/color로 불규칙 배치해 맵 구성.
4. stage_2 동굴을 SOLIDS Arch 조각으로 재구성(무지개 가시화 + 구멍 메움 동시 해결).
5. 프로젝트를 헤드리스로 로드해 에러 0 확인:
   "<godot>" --headless --path "<proj>" "res://<scene>.tscn" --quit-after 30

[완료 기준]
- 발걸림 0·끊긴 바닥 0·도달불가 0 / 임의 이미지→지형 PNG 10분 내 / 조립만으로 새 스테이지 10분 내 / 헤드리스 로드 에러 0.
```

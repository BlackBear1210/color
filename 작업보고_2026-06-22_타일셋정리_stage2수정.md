# 작업 보고서 — 타일셋 정리 + 이미지 워크플로 + Stage 2 수정

> 프로젝트: color (Godot 4.6.3 색상반전 2D 플랫포머)
> 작성일: **2026-06-22**
> 작업자: Claude (헤드리스 검증 완료)
> ※ 이 문서는 요청대로 **수정 내역 보고용**입니다. 모든 코드/씬 수정에는 한국어 주석(`▼ 2026-06-22 …`)을 달았습니다.

---

## 0. 지시사항 4가지 — 처리 요약

| # | 지시 | 상태 |
|---|------|------|
| 1 | 안 쓰는 타일셋 **완전 삭제**, stage1·2 사용 타일 + map1·2·3 타일셋만 유지 | ✅ 완료 |
| 2 | 이미지→흑백 누끼→타일로 찍어내는 워크플로 이해 + **이미지 저장 위치** 안내 | ✅ 아래 2장 |
| 3 | Stage 2 수정(가시 즉시사망 / 카메라 좌우벽·상하 지형 한정 / 세로 배경 자연스럽게) | ✅ 완료 |
| 4 | 코드 주석 + MD 보고 + **실행해서 오류 확인** | ✅ 헤드리스 오류 0 |

> 참고: 지시문에 **map4** 가 있었지만 프로젝트에 map4 폴더/타일은 **존재하지 않습니다**(map1·map2·`map3`(루트)까지만 있음). 그래서 map1·2·3 만 유지했습니다.

---

## 1. 타일셋 정리 (완전 삭제)

삭제 전, **모든 .tscn/.gd 의 리소스 참조를 전수 스캔**해 "어떤 스테이지·유지 씬도 참조하지 않는 것"만 골라 지웠습니다. 유지 대상(stage1·2 가 쓰는 타일, map1·2·3 전체)은 한 개도 건드리지 않았습니다.

### 🗑️ 삭제한 것

| 위치 | 삭제 내용 | 근거 |
|------|-----------|------|
| `scenes/도형_타일셋_최신/` | `Arch_01~06, 08, 09, 10` (9종) | stage 들이 참조 안 함(카탈로그 씬만 참조) |
| 〃 | `도형_타일셋_최신_전체보기.tscn`, `조립_템플릿.tscn` | 카탈로그/템플릿 — 위 Arch 삭제로 무의미 |
| 〃 | `sliced/` 폴더 전체(40+ 조각 PNG/씬) | 실험용 조각 — 아무 곳도 참조 안 함 |
| `scenes/지형파일셋/모듈/` | 모듈 10종 | 미사용(생성기로 언제든 재생성 가능) |
| `scenes/지형파일셋/맵폴리/` | 맵 폴리곤 40종 | 미사용(생성기로 재생성 가능) |
| 루트 `도형_타일셋/` | `찐 1스테이지 도안.png`, `캐릭터 도안.png`, `ghost_unpainted.png.png`, `broken_platform.png.png` | 구버전 도안/미사용(broken_platform 은 삭제된 Arch_10 만 쓰던 것) |

**용량 변화**: `도형_타일셋_최신` 1.3M → 12K, `지형파일셋` 207K → 12K, 루트 `도형_타일셋` 6.1M → 3.0M.

### ✅ 유지한 것 (이유)

- **`scenes/world_1/map1/`, `map2/`, 루트 `map3/`** — 전부 유지(지시: map1·2·3 타일셋 보존). 용량 대부분(map1 59M·map2 24M·map3 ~63M)이 여기지만, **유지가 지시사항**이라 손대지 않았습니다. (원하시면 PNG 용량 최적화는 따로 해드릴 수 있습니다.)
- **`scenes/도형_타일셋_최신/Arch_07_SlopeBody_A.tscn`** — stage_2 가 실제로 사용(지형 `Ground_B`).
- **`scenes/지형파일셋/회색경사로/GrayGradientSlope.tscn`** — 게임 기믹(회색 경사로) 자산이라 보존.
- **`scenes/지형파일셋/원본이미지/`, `실루엣/`** — 아래 2장의 **이미지 찍어내기 워크플로 입출력 폴더**라 보존(현재 비어 있음).
- **루트 `도형_타일셋/spike.png.png`** — `scenes/obstacles/SpikeObstacle.tscn` 이 아직 사용 중이라 보존.

> ⚠️ 참고로 남겨둔 것(범위 밖이라 안 건드림): 루트 `CharacterAnimations/` 와 `도형_타일셋/CharacterAnimations/` 는 **캐릭터 애니메이션 도안**이라 "타일셋" 범위를 벗어나 그대로 뒀습니다(현재 미사용·중복). 원하시면 이것도 지워드리겠습니다.

---

## 2. 이미지 → 타일 "찍어내기" 워크플로 — 이해 확인 & 저장 위치

### 2-1. 제가 이해한 작업 방식 (맞는지 확인 부탁)

> "맵을 손으로 한 칸씩 그리고 물리레이어를 일일이 찍는" 대신,
> **이미지를 흑/백 실루엣 + 투명배경으로 바꿔 → 원하는 부분만 조각으로 떼어 → 크기·방향·색만 바꿔 빠르게 여러 맵을 조립**한다.

파이프라인(이미 프로젝트에 도구가 다 있습니다):

```
[원본 이미지]                      ← 핀터레스트·캡처 등 아무 이미지
   │  (배경이 복잡하면 먼저 누끼: Photopea 웹 / rembg)
   ▼
원본이미지/ 폴더에 넣기
   │  tools/silhouette_from_image.gd  → 흑/백 실루엣 + 투명배경 PNG (조각 자동분리, 밝기로 흑/백 자동)
   ▼
실루엣/ 폴더 (_B=검정/layer2, _W=흰색/layer3)
   │  tools/bake_architecture_tileset.gd → "이미지 + 충돌폴리(알파 외곽선 자동)" 한 씬으로 굽기
   ▼
Arch_XX.tscn (Sprite + 자동충돌 + 사망판정)
   │  맵 씬에 드래그 → position / scale / scale.x=-1(좌우반전) / color_state 만 조정해 배치
   ▼
완성된 맵   ← 같은 조각을 크기·각도 바꿔 불규칙 반복 = 적은 에셋으로 다양한 난이도
```

핵심 원칙(가이드 문서 기준): **비주얼과 충돌 분리**(그림은 디테일 유지, 충돌은 알파 외곽선 자동), **단일블록=SOLIDS / 동굴형=SEGMENTS**, **`color_state` 프로퍼티명 불변**.

> 관련 도구·가이드(그대로 보존됨):
> `tools/silhouette_from_image.gd`, `tools/bake_architecture_tileset.gd`, `tools/slice_terrain.gd`,
> `누끼_가이드_Photopea.md`, `타일셋_작업_인수인계.md`, `맵_레벨디자인_시스템.md`, `맵폴리_베타_가이드.md`.

### 2-2. 📁 이미지는 여기에 저장하세요 (가장 깔끔)

| 단계 | 폴더 | 무엇을 넣나 |
|------|------|-------------|
| ① 원본 투입 | **`scenes/지형파일셋/원본이미지/`** | 핀터레스트/캡처 등 **날것 이미지**(png/jpg/webp). 누끼 끝난 투명 PNG도 여기. |
| ② 변환 결과 | **`scenes/지형파일셋/실루엣/`** | 도구가 만든 흑/백 실루엣 PNG (자동 생성, 직접 안 넣어도 됨) |
| ③ 완성 타일 | `scenes/도형_타일셋_최신/` | bake 결과 Arch 씬 |

> 즉, **새 이미지는 전부 `scenes/지형파일셋/원본이미지/` 한 곳에** 모으시면 됩니다. (도구들이 이 경로를 기본으로 스캔하도록 이미 설정돼 있어, 폴더만 채우고 도구를 돌리면 됩니다.)
> 게임에 안 쓰는 "보관용 참고 이미지"라면 프로젝트 밖(예: `참고이미지/` 별도 폴더)에 두는 걸 권장합니다 — 그래야 Godot 가 임포트하지 않아 프로젝트가 안 무거워집니다.

---

## 3. Stage 2 수정 (4건)

> 측정: 헤드리스로 stage_2 **실제 지형 충돌 영역**을 계산 → `X[-1201, 1352]`(폭 2553), `Y[-1350, 4048]`(높이 5398), 플레이어 시작 `(-656, 3501)`. 이 수치를 기준으로 카메라/벽/배경을 맞췄습니다. (측정 도구 `tools/measure_stage2_bounds.gd` 보존 — 나중에 맵 바뀌면 재측정용.)

### 3-1. 🩸 가시(Thorn) — 닿으면 즉시 사망 (버그수정)

- **원인**: `ThornPlatform1` 에 **지형 밖 좌표로 잘못 배치된 수동 충돌 `TCol3`** 와 **빈 `Sprite2D2`** 가 붙어 있어, 실제 가시 그림과 판정 위치가 어긋나 "닿아도 안 죽는" 현상이 있었습니다.
- **조치**: 두 군더더기 노드를 제거. 가시 스크립트(`thorn_obstacle.gd`)가 **런타임에 가시 알파 모양 그대로 킬존을 자동 생성**하므로, 색과 무관하게 닿으면 즉시 사망합니다.
- **검증**: 가시 텍스처가 실제로 충돌 폴리곤을 만드는지 헤드리스로 확인 → `Platform_Thorn_01` 폴리곤 2개, `Platform_Thorn_02` 1개 정상 생성(임포트도 Lossless라 OK).
- 파일: `scenes/world_1/stage_2/stage_2.tscn` (ThornPlatform1/2 부분)

### 3-2. 🎥 카메라 — 좌우 벽·상하 지형만 비추기

문제 2개를 잡았습니다.

1. **가로**: Player 의 Camera2D 가 Player(scale 0.3)의 자식이라 0.3 스케일을 상속 → 화면 가시폭이 약 **3840px** 로 지형폭(2553)보다 넓어, **좌/우 벽 너머 빈 배경**이 보였습니다.
   → stage.gd 에 **스테이지별 `camera_zoom`** 을 추가하고 stage_2 에 **`camera_zoom = 1.55`** 부여 → 가시폭 ≈ 2477 < 지형폭 → **좌우 벽 밖이 절대 안 보임**(카메라는 위아래로만 스크롤).
2. **상하**: 기존 `camera_limit_top = -200` 은 **포탈(y=-305)보다 아래**라 위로 올라가면 카메라가 못 따라가던 버그가 있었고, `right=1600 / bottom=4200` 은 지형 밖까지 보여줬습니다.
   → 카메라 한계를 **측정한 지형 끝**에 정확히 맞춤: `left=-1202, top=-1350, right=1352, bottom=4048`.

- 파일:
  - `scripts/stage.gd` — `camera_zoom` export 추가 + `_apply_camera_limits()` 에서 적용(0이면 미설정 → stage_1·3 기존 동작 유지).
  - `scenes/world_1/stage_2/stage_2.tscn` — 루트 노드의 camera_limit_* / camera_zoom.

### 3-3. 🧱 오른쪽 벽 추가

- 기존엔 **왼쪽 벽(LeftWall)만** 있었습니다. 지시("좌우 wall")대로 **RightWall** 을 추가했습니다.
- 좌측 벽과 동일하게 **layer 8(회색 레이어)** 에 둬 **플레이어 색과 무관하게 항상** 막습니다. 안쪽면을 지형 오른끝(x=1352)에 맞춤.
- 파일: `scenes/world_1/stage_2/stage_2.tscn` (`MapPhysics/MapBounds/RightWall`)

### 3-4. 🌌 배경 — 세로 스테이지에 맞게 (늘어짐 제거)

- **원인**: 가로 이미지(2560×1080) 한 장을 세로 약 5400px 에 **늘려서** 덮으려다 보니 세로로 ~5배 확대돼 뭉개졌고, ParallaxLayer 에 **가로≠세로 비균등 스케일(1.85×2.65)** 까지 겹쳐 더 찌그러졌습니다.
- **조치(재작성 #1, 폐기)**: 균등 배율 + `motion_mirroring` 세로 무한 반복(타일) → ❌ **사용자 피드백으로 폐기**.
  - 이유: 가로 숲 그림 한 장을 세로로 이어 붙이니 **위 타일 나뭇잎 ↔ 아래 타일 뿌리**가 맞닿아 "배경이 아래로 빨려 들어가는" 어색한 이음새가 생김(세로로 긴 맵 + 배경 1장은 세로 타일링이 원천적으로 부자연스러움).
- **조치(재작성 #2, 최종) — 세로 고정 백드롭**:
  - `Background2.tscn` — 레이어 비균등 스케일 제거.
  - `scripts/background_stage2.gd` — 배경을 **세로로 반복·스크롤하지 않고 화면을 꽉 채운 채 고정**. 올라가도 같은 숲 한 장이 늘 뒤에 있어 **이음새가 아예 없음**(세로 게임 표준 기법). 깊이감을 위해 레이어마다 **아주 조금만** 세로로 따라가되, 이미지 가장자리(검은 빈공간)가 절대 안 드러나도록 여유분(margin) 안에서 **clamp**. 가로는 ParallaxLayer 가 약하게 시차. 창 크기 변경 시 재계산.
  - 튜닝 노브(인스펙터/스크립트 상단): `oversize`(배경 확대·세로 이동 여유), `*_depth`(레이어별 세로 따라가는 정도). 진짜 큰 세로 시차를 원하면 **세로로 긴(세로형) 배경 이미지**가 필요함.
- 파일: `scenes/background/Background2.tscn`, `scripts/background_stage2.gd`

---

## 4. 실행/검증 결과 (헤드리스)

| 검증 | 결과 |
|------|------|
| 삭제 후 임포트 갱신 | OK |
| `stage_2.tscn` 180프레임 실행 | **스크립트 오류 0** |
| `stage_1.tscn` 실행 | 오류 0 (아래 ※경고 1건은 기존 문제) |
| `stage_3.tscn` 실행 | 오류 0 |
| `main.tscn`(진입점) 부팅 | 오류 0 |

> ※ stage_1 에서 `ThornObstacle 'Thorns': thorn_texture 가 설정되지 않았습니다` **경고**가 뜹니다. 이는 제가 건드리지 않은 **stage_1 의 가시에 텍스처가 안 꽂혀 있는 기존 문제**입니다(오류 아님, 그 가시만 안 보임/판정 없음). 원하시면 stage_1 가시도 고쳐드리겠습니다.

### 사람이 직접 확인하면 좋은 것(에디터/실제 플레이)
- 가시에 실제로 닿아 죽는지(자동 킬존이 보이는 가시와 일치하는지) 한 번 플레이 확인.
- 카메라 줌 1.55 의 화면 크기 느낌(원하시면 1.4~1.7 사이로 조절 가능 — `stage_2.tscn` 의 `camera_zoom` 한 줄).
- 세로 배경 타일 이음새가 거슬리지 않는지(스카이는 거의 안 보이고, 가까운 레이어는 반복될 수 있음).

---

## 5. 수정한 파일 목록

| 파일 | 내용 |
|------|------|
| `scripts/stage.gd` | `camera_zoom` export + 적용 로직 |
| `scenes/world_1/stage_2/stage_2.tscn` | 카메라 한계/줌, RightWall 추가, 가시 군더더기 노드 제거 |
| `scripts/background_stage2.gd` | 세로 무한 타일 배경으로 전면 재작성 |
| `scenes/background/Background2.tscn` | 레이어 비균등 스케일 제거 |
| `tools/measure_stage2_bounds.gd` | (신규·보존) 지형 경계 측정 도구 |
| *(삭제)* | 1장에 정리한 타일셋/도안들 |

# Gemini 지형 생성 프롬프트 가이드 (Bad Island 스타일)

> 색상반전 2D 플랫포머 / Godot 4.6.3 / 작성일 2026-06-28
> 목적: 제미나이(Imagen/Gemini)로 **지형 덩어리를 하나씩** 만들어,
> `silhouette_from_image.gd → bake_architecture_tileset.gd` 파이프라인에 바로 넣을 수 있게 한다.
> 짝꿍 문서: [맵_레벨디자인_시스템.md](맵_레벨디자인_시스템.md), [타일셋_작업_인수인계.md](타일셋_작업_인수인계.md), [누끼_가이드_Photopea.md](누끼_가이드_Photopea.md)

---

## 0. 왜 이렇게 뽑아야 하는가 (먼저 읽기)

우리 파이프라인은 **그림의 "실루엣(모양)"만** 쓰고, 색은 엔진이 흑/백/회색으로 다시 입힌다.
따라서 제미나이에게는 **"색을 예쁘게"가 아니라 "모양이 깨끗하게 분리되도록"** 시키는 게 핵심이다.

지켜야 할 4대 제약 (모든 프롬프트에 공통으로 박아 넣음):

1. **측면 직교 뷰(side view, orthographic)** — 원근/소실점 없는 정측면. 플랫포머는 옆에서 본다.
2. **윗면(밟는 면)은 평평하거나 아주 완만하게** — 뾰족한 돌기·가시·요철 금지.
   → 발 걸림(스내깅)의 원인. *비주얼은 옆·아래에서 디테일을 살리고, 윗면은 매끈하게.*
3. **단색 평면 배경** — 지형에 없는 색(권장: 순수 마젠타 `#FF00FF`).
   → 제미나이는 보통 투명 PNG 를 못 만든다. 단색 배경이면 `colorkey` 모드로 자동 누끼된다.
   → **그림자를 배경에 드리우지 말 것**(그림자가 colorkey 를 망친다). "no cast shadow on background".
4. **지형 덩어리 하나만, 화면 가운데, 잘리지 않게** — 한 번에 한 조각(one isolated chunk).

추가 권장:
- 해상도 **1024×1024 이상**, 정사각 또는 가로형 캔버스.
- **실루엣 가독성 최우선**: 윤곽이 배경과 뚜렷이 구분되게(고대비).
- Bad Island 느낌: 점토(claymation)·손으로 빚은 듯한 질감, 층층이 쌓인 암반(rock strata), 이끼·흙·둥근 유기적 형태.

---

## 1. 재사용 프롬프트 템플릿 (영어 — 이미지 모델은 영어가 안정적)

> 아래 `{{ }}` 부분만 조각마다 바꿔서 한 장씩 생성한다.

```text
A single isolated chunk of {{TERRAIN_TYPE}} for a 2D side-scrolling platformer game,
strict orthographic SIDE VIEW (no perspective, no vanishing point).
Art style: inspired by the game "Bad Island" — hand-sculpted claymation look,
organic earthy stone with layered rock strata, moss and dirt, rounded natural forms,
rich tactile texture, soft top-down lighting.

CRITICAL SHAPE RULES:
- The TOP surface (where a character walks) must be FLAT or very gently rolling —
  absolutely no spikes, no bumps, no protrusions, no overhangs along the top edge.
- Put all the detail and roughness on the SIDES and BOTTOM, never on the walkable top.
- One single connected chunk, centered, fully inside frame, not cropped.

BACKGROUND: solid flat pure magenta (#FF00FF), completely empty,
no gradient, no texture, and NO shadow cast onto the background.
High contrast between the terrain and the magenta background.
Square canvas, 1024x1024, crisp clean silhouette.
```

> ⚠️ 제미나이가 마젠타 배경을 거부하거나 흐리게 칠하면 `solid flat chroma green (#00FF00)` 또는
> `pure white (#FFFFFF)` 로 바꿔 시도. (단 흰 배경은 흰색 지형과 헷갈리니 마젠타/그린 우선)

---

## 2. 조각별 프롬프트 (모듈 키트 10종 대응)

`{{TERRAIN_TYPE}}` 슬롯에 넣을 문구. 모듈 설계서의 10종과 1:1 매칭된다.

| # | 모듈 | `{{TERRAIN_TYPE}}` 에 넣을 문구 | 추가 지시 |
|---|------|------------------------------|----------|
| 1 | Ground_Flat (평지) | `a wide flat ground block of layered rock and soil` | 윗면 완전 평탄, 두께 일정 |
| 2 | Slope_Up (오르막) | `a gentle uphill slope ramp (about 25 degrees)` | "smooth straight ramp on top, angle max 30°" |
| 3 | Slope_Down (내리막) | `a gentle downhill slope ramp (about 25 degrees)` | 좌우 반전으로 재활용 가능 |
| 4 | Platform_Float (공중 발판) | `a small floating platform island with flat top` | 아랫면은 뾰족한 바위뿌리 OK |
| 5 | Ledge_Edge (절벽 끝) | `a cliff edge / ledge with flat top and a sheer vertical drop on one side` | 윗면 평탄, 한쪽만 수직 절벽 |
| 6 | Pillar (기둥/벽) | `a tall vertical rock pillar / wall column` | 수직 장애물, 윗면 좁고 평탄 |
| 7 | Ceiling (천장) | `an overhanging ceiling rock slab for a narrow tunnel` | 아랫면(밟는 천장)도 매끈하게 |
| 8 | Step_Small (계단 한 칸) | `a single small step / ledge, knee-height` | 단차 1칸, 윗면 평탄 |
| 9 | Gap_Bridge (다리) | `a narrow natural stone bridge spanning a gap, flat walkable top` | 가운데가 뚫리면(아치) 동굴형 → SEGMENTS |
| 10 | Curve_Hill (둥근 언덕) | `a low rounded grassy hill with a smooth broad top` | 비주얼 포인트, 윗면 완만한 곡선 |

### 장애물(별도 취급 — 윗면 평탄 규칙 예외)
장애물은 밟는 게 아니라 피하는 것이므로 모양 제약이 다르다. 별도 프롬프트 사용:

| 장애물 | 프롬프트 슬롯 | 비고 |
|--------|-------------|------|
| 가시(Thorn/Spike) | `a cluster of sharp upward stone spikes / thorns` | 뾰족함이 의도된 것 |
| 톱니/기어(Gear) | `a round mechanical stone gear / sawblade` | 회전 장애물, 원형 |
| 덩굴(Vine) | `a hanging thorny vine` | 세로 장애물 |

---

## 3. 받은 이미지를 흑/백/회색/장애물로 가르는 "속성 필터" 매핑 ★

제미나이가 보낸 지형 이미지를 내가(작업자) 처리할 때, **검정·흰색·회색·장애물 각각 무엇을 쓰는지**.
(질문하신 "흑색일 때 어떤 필터, 백색일 때 어떤 필터"의 정답표)

| 지형 종류 | 게임 역할 | silhouette 도구 설정 | 파일명 접미사 | `color_state` | 물리 레이어 (project.godot) |
|----------|----------|---------------------|--------------|---------------|----------------------------|
| **검정 지형** | 검정 플레이어만 밟힘 | `color:"BLACK"`, `mode:"colorkey"`(단색배경) / `"alpha"`(투명PNG) | `_B` | `BLACK` (0) | **Layer 2 platform_black** (값 2) |
| **흰색 지형** | 흰색 플레이어만 밟힘 | `color:"WHITE"`, `mode` 동일 | `_W` | `WHITE` (1) | **Layer 3 platform_white** (값 4) |
| **회색 지형** | 못 밟음/미끄럼, 칠해야 통과 | 도구로 자동 안 됨 → **수동 배치**(GrayGradientSlope 등) | (수동) | `GRAY` (2) | **Layer 4 platform_gray** (값 8) |
| **장애물** | 닿으면 사망(색 무관) | 도구 누끼만, 굽기 후 이름에 Thorn/Gear/Spike 등 | (장애물 이름) | — | **Layer 9 obstacle** (값 256) |

> 핵심: **흑/백을 가르는 "필터"는 `silhouette_from_image.gd` 의 `color` 설정 + 파일명 `_B`/`_W` 접미사**다.
> baker 가 `_W` 면 `color_state=WHITE`(layer 4=흰레이어), `_B` 면 `BLACK`(layer 2=검정레이어)로 자동 굽는다.
> `AUTO` 로 두면 조각 평균 밝기로 자동 판정(밝으면 WHITE)하지만, **회색조 암석은 흰색으로 잘못 빠지기 쉬우니**
> 우리 게임처럼 색이 곧 생사인 경우 **`BLACK`/`WHITE` 를 명시 강제**하는 걸 권장한다.

### 충돌 모드(SOLIDS vs SEGMENTS) 추가 규칙
- **단일 덩어리**(평지/언덕/발판/계단/기둥): `SOLIDS` → 에디터에서 무지개로 보임.
- **가운데 뚫린 형(아치 다리/동굴/도넛)**: 굽기 후 `Collision Build Mode = SEGMENTS` → 구멍 안 메워짐.

---

## 4. 한 조각 처리 풀 워크플로 (요약)

```
[제미나이] 위 템플릿으로 지형 1조각 생성(마젠타 배경)
   ↓  PNG 저장
[작업자] scenes/지형파일셋/원본이미지/ 에 넣기 (검정 쓸 건 파일명에 _B 힌트, 흰색은 _W 힌트 권장)
   ↓
silhouette_from_image.gd  (colorkey 누끼 + 흑/백 실루엣 + 투명 PNG)
   ↓  실루엣/<이름>_NN_B.png 또는 _W.png
--import  →  bake_architecture_tileset.gd  (Sprite+충돌폴리+DeathDetector+Entry/Exit, 물리레이어 자동)
   ↓  Arch 씬
[작업자] 조립_템플릿 / 스테이지에 드래그 → position/scale/flip/color_state 로 불규칙 배치
   ↓
헤드리스 로드로 발 걸림 0 · 색 판정 정상 확인
```

---

## 5. 제미나이에게 보낼 때 체크리스트 (복붙용)

```
□ 측면 직교 뷰(side view, no perspective)인가?
□ 윗면(밟는 면)이 평평/완만한가? 뾰족한 돌기 없는가?
□ 배경이 순수 단색(마젠타 #FF00FF)이고 그림자가 배경에 안 깔렸는가?
□ 덩어리 1개만, 안 잘리고, 가운데 있는가?
□ 1024px 이상, 윤곽 고대비인가?
□ (장애물 제외) 한 번에 1종류만 뽑았는가?
```
</content>
</invoke>

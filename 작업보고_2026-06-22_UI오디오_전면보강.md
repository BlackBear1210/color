# 작업 보고서 — UI / 오디오 / 게임피드백 전면 보강

> 프로젝트: color (Godot 4.6.3) / 작성일 **2026-06-22**
> 헤드리스 검증: main + stage 1/2/3 + MainMenu + StageSelect 전부 **오류·경고 0**.
> 실제 소리/연출/메뉴 조작감은 에디터 플레이로 확인 권장.

---

## 0. 요청 → 처리 ("전부 다 해보자")

제안했던 UI/피드백 항목을 **전부 구현**했습니다.

| 항목 | 상태 |
|------|------|
| 메인 메뉴(타이틀) | ✅ 구현 |
| 일시정지(Esc) 메뉴 | ✅ 구현(전역) |
| 오디오 (BGM 골격 + SFX) | ✅ 효과음 7종 생성·연결 / BGM 재생 골격 |
| 씬 전환 페이드 | ✅ 구현(전 화면 공통) |
| 스테이지 선택 개선(잠금/클리어/베스트) | ✅ 구현 |
| 클리어 화면 개선(기록/신기록/메달) | ✅ 구현 |
| 추격 거리 게이지(stage 3) | ✅ 구현 |
| 체크포인트 | ✅ 구현(stage 3 2개) |

---

## 1. 오디오 시스템

- **AudioManager** (`autoload/AudioManager.gd`, 신규 오토로드): Master/BGM/SFX 버스 + SFX 재생 풀 + 음량 저장(`user://audio.json`).
- **효과음 7종**(`assets/audio/sfx/*.res`): 점프·착지·발사·색전환·사망·포탈·클릭. 음원이 없어 **코드로 합성**(`tools/build_sfx.gd`)했고, 나중에 같은 파일명의 좋은 음원으로 **교체만 하면** 됩니다.
- **연결**: `player.gd`(점프/착지/발사/색전환/사망), `clear_popup.gd`(클리어=포탈음), 메뉴 버튼(클릭).
- **BGM**: `AudioManager.play_bgm("res://assets/audio/bgm/곡.ogg")` 골격만 준비 — **배경음 파일을 넣어주시면** 스테이지별로 틀어드립니다.

> 음량 조절 UI(설정 화면)는 AudioManager 에 `set_master/bgm/sfx` API가 이미 있어, 슬라이더만 붙이면 됩니다(원하면 설정 화면도 추가).

## 2. 메뉴 / 흐름

- **메인 메뉴** (`MainMenu.tscn` + `main_menu.gd`): COLOR 타이틀 + [시작하기 / 스테이지 선택 / 종료]. (DEBUG_STAGE=0 또는 일시정지→메인메뉴로 진입)
- **일시정지(Esc)** (`pause_menu.gd`, 신규 오토로드): 스테이지 중 Esc로 열림 → 게임 멈춤 + [계속하기 / 재시작 / 스테이지 선택 / 메인 메뉴]. 메뉴/클리어 화면에선 안 열림.
- **씬 전환 페이드**: `SceneManager` 가 모든 전환을 **검은 화면 페이드 인/아웃**으로 처리(끊김 제거). 일시정지 중에도 동작(process_mode=ALWAYS).

## 3. 진행상황 저장 / 스테이지 선택 / 클리어

- **저장**(`SceneManager`, `user://progress.json`): 클리어한 스테이지·잠금해제·스테이지별 **베스트 타임**.
- **스테이지 선택**: 잠긴 스테이지는 **비활성화**, 클리어한 스테이지는 **✓ 베스트타임** 표시.
- **클리어 화면**: 클리어 타임 + **신기록(★)** + **등급(금/은/동)** + 죽은 횟수. 마지막 스테이지면 **ALL CLEAR!** + 메인 메뉴 버튼.

## 4. Stage 3 추격 강화

- **추격 거리 게이지**: 화면 하단에 출발→목표 진행도 막대(초록 채움) + **불 위치(빨강 마커)**. 불이 얼마나 따라왔는지 직관적으로 보임. (`FireWall` 의 `track_start_x`/`goal_x`)
- **체크포인트**(`Checkpoint.tscn` + `checkpoint.gd`): 가시 구간 직후·색전환 구간 직후 2곳. 닿으면 부활 지점 갱신(불은 사망 시 시작점으로 리셋되므로 공정).
- (이전 작업) 사망 시 불벽 리셋 + 위급 붉은 비네트 포함.

---

## 5. 새 파일 / 수정 파일

**신규**
- `autoload/AudioManager.gd`, `scripts/ui/pause_menu.gd`, `scripts/ui/main_menu.gd`
- `scripts/obstacles/checkpoint.gd`, `scenes/obstacles/Checkpoint.tscn`
- `tools/build_sfx.gd`, `assets/audio/sfx/*.res`(7종)

**수정**
- `project.godot`(오토로드 AudioManager/PauseMenu 등록)
- `autoload/SceneManager.gd`(페이드·저장·기록)
- `scripts/player.gd`(효과음 5종 연결)
- `scripts/ui/hud.gd`·`Hud.tscn`(스테이지번호·진입배너 — 이전 작업)
- `scripts/ui/stage_select.gd`·`clear_popup.gd`(기록/잠금/메달)
- `scenes/ui/MainMenu.tscn`
- `scripts/obstacles/fire_wall.gd`(추격 게이지)
- `scenes/world_1/stage_3/stage_3.tscn`(체크포인트·게이지 파라미터)

---

## 6. 검증 & 다음 단계

- 헤드리스: 전 씬 오류 0. 합성 SFX 7종 생성 확인.
- **플레이로 확인 권장**: 합성 효과음 톤(거슬리면 조정/교체), 일시정지·페이드 조작감, stage 3 게이지/체크포인트 밸런스.
- **음원 필요**: BGM 파일(있으면 `assets/audio/bgm/`에). 효과음도 더 좋은 무료 음원으로 교체 가능(Kenney, freesound 등).
- 원하면 **설정 화면(음량 슬라이더·전체화면)** 도 바로 붙일 수 있습니다(API 준비됨).

> 모든 신규/수정 코드에 `▼ 2026-06-22` 한국어 주석을 달았습니다.

# 기존 장애물 이식 가이드라인

> 원본 프로젝트: `color_도형이꺼_5월19일 전체 버전`  
> 대상 프로젝트: `프로젝트ver-2`  
> 작성일: 2026-05-27  
> **코드 복사 전에 반드시 읽을 것**

---

## 총알 & 장애물 공통 규칙 (확정)

> **3개 장애물 모두 동일하게 적용**

```
일반 지형에 총알 닿음  →  PaintMark 생성 + 총알 소멸  (기존 동작 유지)
장애물에 총알 닿음     →  PaintMark 없이 총알만 소멸   (신규 규칙)
```

### bullet.gd 수정 (딱 한 곳)

```gdscript
# 수정 전
func _on_body_entered(body):
    if body.is_in_group("paint_bodies"):
        body.call_deferred("update_color", bullet_color)
    else:
        _spawn_mark()   # 모든 지형에 PaintMark 생성
    _safe_free()

# 수정 후
func _on_body_entered(body):
    if body.is_in_group("paint_bodies"):
        body.call_deferred("update_color", bullet_color)
    elif not body.is_in_group("obstacle"):
        _spawn_mark()   # "obstacle" 그룹이면 PaintMark 생성 안 함
    _safe_free()        # 어떤 경우든 총알은 항상 소멸
```

### 각 장애물의 _ready()에 반드시 추가

```gdscript
add_to_group("obstacle")   # 이 한 줄로 총알 PaintMark 생성 차단
```

---

## 장애물 목록

| 장애물 | 원본 파일 | 베이스 노드 | 핵심 동작 |
|--------|---------|-----------|---------|
| 톱니 (Gear) | `obstacle_gear/gear_obstacle.gd` | AnimatableBody2D | 좌우/상하 왕복 이동, 닿으면 사망 |
| 낙하 블록 (FallingBlock) | `blockspawner/falling_block.gd` | RigidBody2D | 위에서 떨어져 닿으면 사망 |
| 부서지는 타일 (BrokenTile) | `broken_tile.gd` | TileMapLayer | 밟으면 0.4초 후 사라짐 |

---

## 1. 톱니 장애물 (Gear Obstacle)

### 원본 동작
- AnimatableBody2D가 `move_and_collide()`로 왕복 이동
- `collision_layer = 2`, `collision_mask = 0`
- 플레이어 사망 로직 **없음** — 이동만 함

### ⚠️ 우리 게임과의 충돌 문제

**문제 1: collision_layer = 2 (platform_black) — 색 편향**
```
원본 설정: collision_layer = 2 = platform_black 레이어
→ BLACK 플레이어만 물리 충돌 (밀림/막힘)
→ WHITE 플레이어는 통과해버림

의도: 색 관계없이 모든 플레이어에게 위험한 장애물
→ collision_layer = 256 (obstacle_body) 로 변경
→ player.gd collision_mask에 256 추가 필요
```

**문제 2: 플레이어 사망 로직 없음**
```
원본 코드에 die() 호출 없음
→ 플레이어가 톱니에 닿아도 아무 일도 안 일어남
→ KillArea (Area2D) 추가로 해결
```

**문제 3: 총알이 톱니에 맞으면 PaintMark 생성됨**
```
→ gear_obstacle.gd에 add_to_group("obstacle") 추가로 해결
→ bullet.gd 수정과 함께 동작 (위 공통 규칙 참고)
```

### 이식 시 수정 방향

**씬 구조**
```
GearObstacle (AnimatableBody2D)   ← gear_obstacle.gd
├── Sprite2D
├── CollisionShape2D               ← collision_layer = 256 (obstacle_body)
└── KillArea (Area2D)              ← 신규 추가: 사망 판정 전담
    └── CollisionShape2D           ← 톱니와 동일한 형태
```

**collision 설정**
```
AnimatableBody2D
  collision_layer = 256   ← layer_9 (obstacle_body)
  collision_mask  = 0

KillArea (Area2D)
  monitoring    = true    ← 플레이어 감지 활성화
  monitorable   = false   ← ColorSensor가 감지할 필요 없음
  collision_layer = 0
  collision_mask  = 1     ← player 레이어만 감지
```

**gear_obstacle.gd 수정**
```gdscript
@onready var kill_area: Area2D = $KillArea

func _ready() -> void:
    _start_position = global_position
    add_to_group("obstacle")                              # ← 추가 (총알 PaintMark 차단)
    kill_area.body_entered.connect(_on_kill_area_entered)

func _on_kill_area_entered(body: Node) -> void:
    if body.is_in_group("player"):
        body.call_deferred("die")   # call_deferred 필수
```

---

## 2. 낙하 블록 (FallingBlock + BlockSpawner)

### 원본 동작
- BlockSpawner(Node2D)가 일정 간격으로 FallingBlock 스폰
- FallingBlock(RigidBody2D)이 중력으로 낙하
- 플레이어에 닿으면 `body.die()` 호출 후 소멸
- y 좌표가 `despawn_y` 초과 시 자동 소멸

### ⚠️ 우리 게임과의 충돌 문제

**문제 1: player 그룹 미등록**
```gdscript
# falling_block.gd
if body.is_in_group("player"):  # ← 이 검사가 항상 false
    body.die()

# player.gd에 add_to_group("player")가 없음
# → 낙하 블록이 플레이어에 닿아도 절대 사망 안 함
# → player.gd _ready()에 add_to_group("player") 추가 필수
```

**문제 2: call_deferred 누락**
```gdscript
# 원본 (오류 발생)
func _on_body_entered(body: Node) -> void:
    if body.is_in_group("player"):
        body.die()       # ← body_entered 콜백 내 직접 호출 → physics flush 오류
        queue_free()

# 수정 후
func _on_kill_area_entered(body: Node) -> void:
    if body.is_in_group("player"):
        body.call_deferred("die")
        call_deferred("queue_free")
```

**문제 3: collision_layer/mask 미설정**
```
.tscn에 collision_layer/mask 미설정 → Godot 기본값(layer=1) 적용
→ player 레이어와 같은 레이어 → 의도하지 않은 충돌
→ collision_layer = 256 (obstacle_body) 로 명시 설정 필요
```

**문제 4: 총알이 낙하 블록에 맞으면 PaintMark 생성**
```
→ falling_block.gd에 add_to_group("obstacle") 추가로 해결
```

### 이식 시 수정 방향

**FallingBlock 씬 구조**
```
FallingBlock (RigidBody2D)   ← falling_block.gd
├── CollisionShape2D          ← collision_layer/mask 설정
├── Sprite2D
└── KillArea (Area2D)         ← 신규 추가
    └── CollisionShape2D
```

**collision 설정**
```
RigidBody2D
  collision_layer = 256       ← layer_9 (obstacle_body)
  collision_mask  = 2|4|8=14  ← 지형 위에서 멈추려면 (black+white+gray)

KillArea (Area2D)
  monitoring    = true
  monitorable   = false
  collision_layer = 0
  collision_mask  = 1         ← player 레이어 감지
```

**falling_block.gd 수정**
```gdscript
@onready var kill_area: Area2D = $KillArea

func _ready() -> void:
    add_to_group("falling_blocks")
    add_to_group("obstacle")                              # ← 추가 (총알 PaintMark 차단)
    kill_area.body_entered.connect(_on_kill_area_entered)

# 기존 body_entered 제거하고 아래로 교체
func _on_kill_area_entered(body: Node) -> void:
    if body.is_in_group("player"):
        body.call_deferred("die")
        call_deferred("queue_free")

func _process(_delta: float) -> void:
    if global_position.y > despawn_y:
        queue_free()
```

**BlockSpawner 수정 사항**
```gdscript
# 수정 전
var block: RigidBody2D = block_scene.instantiate()

# 수정 후
var block: Node = block_scene.instantiate()
get_tree().current_scene.add_child(block)
block.global_position = Vector2(global_position.x, global_position.y + spawn_y_offset)
```

---

## 3. 부서지는 타일 (BrokenTile)

### 원본 동작
- TileMapLayer 기반, 타일 하나하나가 개별 물리 영역
- `PhysicsShapeQueryParameters2D`로 타일 위에 플레이어 감지
- 감지 후 0.4초 뒤 타일 삭제 (`erase_cell`)
- 타일 직접 삭제 → 플레이어가 밟고 있으면 낙하

### 현재 TileSet 충돌 설정 (area.tscn 기준)
```
physics_layer_0/collision_layer = 6   # = 2+4 = platform_black + platform_white
physics_layer_0/collision_mask  = 8   # 불필요 (제거 권장)
```
```
collision_layer = 6이므로:
→ BLACK 플레이어 (mask에 layer2=2 포함) → 밟힘 ✅
→ WHITE 플레이어 (mask에 layer3=4 포함) → 밟힘 ✅
→ 두 색 모두 사용 가능 — 유지
```

### ⚠️ 우리 게임과의 충돌 문제

**문제 1: collision_mask = 8 (불필요)**
```
TileSet의 collision_mask = 8 → platform_gray 레이어와 충돌
타일이 회색 지형과 충돌할 이유 없음
→ collision_mask = 0 으로 수정
```

**문제 2: 총알이 부서지는 타일에 맞으면 PaintMark 생성**
```
타일 collision_layer = 6 (black+white) → 총알 mask(14)에 포함됨
→ 총알이 타일에 닿아 PaintMark 생성됨

→ broken_tile.gd에 add_to_group("obstacle") 추가로 해결
   (collision_layer 변경 불필요 — 플레이어가 밟아야 하므로 유지)
```

### 이식 시 수정 방향

**씬 구조 (area.tscn → BrokenTile.tscn 이름 변경)**
```
Node2D (루트)
└── TileMapLayer (broken_tile.gd)
      └── TileSet 설정:
            physics_layer_0/collision_layer = 6   ← 유지 (양색 모두 밟힘)
            physics_layer_0/collision_mask  = 0   ← 8 → 0 수정
```

**broken_tile.gd 수정**
```gdscript
func _ready() -> void:
    add_to_group("obstacle")   # ← 추가 (총알 PaintMark 차단)
    # 기존 코드는 수정 없음
```

---

## 4. player.gd 필수 수정사항

```gdscript
func _ready() -> void:
    add_to_group("player")   # ← 추가
    # 기존 코드...
```

> ⚠️ 이 한 줄 없으면 Gear KillArea, FallingBlock KillArea의  
> `is_in_group("player")` 검사가 **항상 false → 사망 판정 불가**

---

## 5. project.godot 레이어 추가

```ini
[layer_names]
2d_physics/layer_1 = "player"
2d_physics/layer_2 = "platform_black"
2d_physics/layer_3 = "platform_white"
2d_physics/layer_4 = "platform_gray"
2d_physics/layer_5 = "bullet"
2d_physics/layer_6 = "paint_detector"
2d_physics/layer_7 = "kill_zone"
2d_physics/layer_8 = "ghost_interact"
2d_physics/layer_9 = "obstacle_body"   ← 신규 추가
```

| 레이어 | 비트값 | 용도 |
|--------|--------|------|
| layer_9 (obstacle_body) | 1 << 8 = 256 | Gear, FallingBlock 물리 본체 |

---

## 6. 충돌 마스크 업데이트 정리

layer_9(obstacle_body) 추가로 인해 아래 값들 변경 필요.

| 대상 | 변경 전 mask | 변경 후 mask | 이유 |
|------|------------|------------|------|
| player (BLACK) | 2\|8 = 10 | 2\|8\|256 = 266 | 장애물에 물리적으로 막혀야 함 |
| player (WHITE) | 4\|8 = 12 | 4\|8\|256 = 268 | 장애물에 물리적으로 막혀야 함 |
| bullet | 14 | 14\|256 = 270 | 장애물에 총알이 닿아야 함 |

> BrokenTile은 collision_layer = 6 (black+white) 유지이므로  
> 기존 bullet mask(14)에 이미 포함 → bullet mask 추가 불필요

---

## 7. 장애물별 체크리스트

### 공통 (3개 모두)
```
□ bullet.gd _on_body_entered()에 obstacle 그룹 예외 처리 추가
□ player.gd _ready()에 add_to_group("player") 추가
□ project.godot layer_9 = "obstacle_body" 추가
□ player.gd collision_mask에 256 추가 (BLACK: 266, WHITE: 268)
□ bullet.tscn collision_mask → 270
```

### 톱니 (Gear)
```
□ gear_obstacle.gd add_to_group("obstacle") 추가
□ GearObstacle.tscn collision_layer → 256
□ KillArea (Area2D) 자식 추가
    □ monitoring = true, monitorable = false, collision_mask = 1
□ gear_obstacle.gd KillArea body_entered 연결
    □ body.call_deferred("die") 사용
```

### 낙하 블록 (FallingBlock + BlockSpawner)
```
□ falling_block.gd add_to_group("obstacle") 추가
□ FallingBlock.tscn collision_layer → 256
□ FallingBlock.tscn collision_mask → 14 (지형에 올라설 수 있게)
□ KillArea (Area2D) 자식 추가
    □ monitoring = true, monitorable = false, collision_mask = 1
□ falling_block.gd 기존 body_entered 제거 → KillArea 방식으로 교체
    □ call_deferred("die"), call_deferred("queue_free") 사용
□ block_spawner.gd var block: Node 으로 타입 수정
□ despawn_y 값이 스테이지 KillZone y값보다 큰지 확인
```

### 부서지는 타일 (BrokenTile)
```
□ broken_tile.gd add_to_group("obstacle") 추가
□ TileSet collision_mask = 8 → 0 수정
□ collision_layer = 6 유지 (수정 불필요)
□ PhysicsShapeQueryParameters2D collision_mask = 1 유지 (수정 불필요)
□ area.tscn → BrokenTile.tscn 이름 변경
□ 텍스처 교체 (icon.svg placeholder → 실제 타일 텍스처)
```

---

## 8. 공통 주의사항

```
❌ body_entered 콜백 내에서 die(), queue_free() 직접 호출
   → physics flush 오류 → 반드시 call_deferred 사용

❌ 장애물 collision_layer를 2(black), 4(white)로 설정
   → 색 기반 발판 시스템과 충돌
   → 플레이어 색에 따라 통과/막힘이 달라짐

❌ player 그룹 등록 없이 is_in_group("player") 사용
   → 항상 false → 사망 판정 불가

❌ 장애물을 "death_zones" 그룹에 추가
   → player.gd _check_color_death()가 color_state를 읽으려다 null 반환
   → 판정 오동작 (장애물 사망은 KillArea로 독립 처리)

❌ "obstacle" 그룹 등록 안 하면
   → 총알이 장애물에 닿아 PaintMark 생성됨
```

---

## 9. 파일 경로 이식 규칙

| 원본 경로 | 변경 경로 |
|---------|---------|
| `res://obstacle/obstacle_gear/gear_obstacle.gd` | `res://scripts/obstacles/gear_obstacle.gd` |
| `res://obstacle/obstacle_gear/gear_obstacle.tscn` | `res://scenes/obstacles/GearObstacle.tscn` |
| `res://obstacle/blockspawner/block_spawner.gd` | `res://scripts/obstacles/block_spawner.gd` |
| `res://obstacle/blockspawner/BlockSpawner.tscn` | `res://scenes/obstacles/BlockSpawner.tscn` |
| `res://obstacle/blockspawner/falling_block.gd` | `res://scripts/obstacles/falling_block.gd` |
| `res://obstacle/blockspawner/FallingBlock.tscn` | `res://scenes/obstacles/FallingBlock.tscn` |
| `res://obstacle/broken_tile.gd` | `res://scripts/obstacles/broken_tile.gd` |
| `res://obstacle/broken_tile/area.tscn` | `res://scenes/obstacles/BrokenTile.tscn` |

---

## 10. 수정 불필요한 것

```
✅ broken_tile.gd PhysicsShapeQueryParameters2D collision_mask = 1  → player layer 일치
✅ broken_tile.gd BREAK_DELAY = 0.4  → 게임 시스템과 무관
✅ gear_obstacle.gd move_speed, move_distance @export  → 에디터 조정 가능
✅ block_spawner.gd spawn_interval, spawn_count @export  → 에디터 조정 가능
✅ FallingBlock despawn_y @export  → 에디터 조정 가능
✅ 세 장애물 모두 ColorSensor/DeathDetector 시스템과 직접 충돌 없음
```

---

*이 문서는 이식 작업 완료 후 실제 동작 확인 결과에 따라 업데이트 필요.*

# 맵 찍기 프롬프트 (다음 Claude 작업자용)
> 이 파일 전체를 복사해서 Claude 에게 붙여넣고, 맨 아래 "이번에 만들 스테이지" 칸만 채우면
> 우리 프로젝트 규칙에 맞는 스테이지 .tscn 을 검증까지 마쳐 찍어 줍니다.

---

## 너(Claude)에게 주는 지시

너는 Godot 4.6.3 색반전 2D 플랫포머 `color` 프로젝트의 레벨 디자이너다.
아래 규칙을 **반드시** 지켜 스테이지 `.tscn` 을 텍스트로 직접 작성하고, reach_sim 으로 검증해라.

### A. 절대 규칙
1. **기존 코드(특히 페인트/플레이어/총알 로직) 절대 수정 금지.** 새 스크립트가 필요하면 별도 파일로만.
2. 모든 .tscn 상단에 `; ▼ <날짜> <설명>` 한국어 주석. 좌표를 바꾼 이유도 주석으로.
3. **미구현 장애물은 `scenes/obstacles/Placeholder.tscn` 인스턴스로 표시**(아래 D). 절대 가짜로 구현하지 말 것.
4. 만들고 끝내지 말고 **reach_sim 으로 직접 통과 검증**(아래 E). 실패하면 좌표 고쳐 재검증.

### B. 플레이어 실측 (모든 배치의 근거)
- 실제 크기 ≈ 19×43px. 이동 390 / 점프 −600 / 중력 1200·하강 2880.
- **점프 높이 ≈150px / 수평 ≈320px.** 발 = 원점 아래 54px.
- 안전값: 가로 간격 ≤190 · 상승 단차 ≤100 · 착지 폭 ≥60.
- 걸어서 강하(내리막)할 땐 낙차 110px 당 가로 간격 ≤60(안 그러면 갭에 빠짐).
- 모듈 스케일: 바닥 0.5~0.6 / 공중발판 0.5 / 가시 0.2~0.25.

### C. 씬 골격(이 구조를 그대로 따른다)
```
[gd_scene format=3 uid="uid://<고유문자열>"]
; 주석
[ext_resource ... scripts/stage.gd id="1_stage"]
[ext_resource ... scenes/background/Background.tscn id="2_bg"]
[ext_resource ... scenes/player/Player.tscn id="3_player"]
[ext_resource ... scenes/ui/Hud.tscn id="4_hud"]
[ext_resource ... 지형/장애물/Placeholder 모듈들 ...]
[sub_resource type="RectangleShape2D" id="kz"]  size = Vector2(16000, 300)
[node name="stage_N" type="Node2D"]
  script = ExtResource("1_stage")
  camera_limit_left/top/right/bottom + camera_zoom   ← D-카메라 규칙
[node name="Background" ...][node name="Hud" ...]
[node name="MapPhysics" type="Node2D"]
  └ KillZone(Area2D)+CollisionShape2D  ← 낙사선
  └ 지형 모듈 인스턴스들(B1바닥 …)
[node name="Obstacles" type="Node2D"]
  └ 실제 장애물 + Placeholder + Checkpoint + Portal(+Visual+Label, next_stage_path)
[node name="Player" ... position=...]
```
- **검정 스파인 재사용:** 횡스크롤이면 `scenes/world_1/stage_5/stage_5.tscn` 의 MapPhysics(B1~B11) 블록을 그대로 복붙(윗면 y=400, 간격 ≤190, reach_sim 통과 보장). 그 위에 장애물/플레이스홀더만 얹어라.
- **카메라:** Camera2D 는 Player(scale0.3) 자식 → 유효줌 = camera_zoom×0.3.
  세로 가시 절반 = `648/(camera_zoom×0.3)/2`. 플레이어가 끝까지 따라오게:
  `limit_top ≤ 최상단플레이어y − 그값`, `limit_bottom ≥ 최하단플레이어y + 그값`.
  횡스크롤 표준: `top=-220, bottom=880, left=-300, right=(포탈x+700), zoom=1.5`.
  내리막/수직은 강하 범위에 맞춰 top/bottom 을 넉넉히(stage_4·8 참고).

### D. 실제 구현된 장애물 vs 플레이스홀더
- **인스턴스 가능(씬 있음):** 가시(`지형파일셋/씬/가시/가시1~4.tscn` 또는 ThornObstacle) · 톱니(`obstacles/GearObstacle.tscn`, `move_vertical=true` 가능) · 낙하블럭(`obstacles/BlockSpawner.tscn`+`FallingBlock.tscn`) · 불벽(`obstacles/FireWall.tscn`, `start_delay/track_start_x/goal_x`) · Portal · Checkpoint.
- **나머지 전부(T2~T8, H4·H6·H7·H8·H9, P1~P10, 특별기믹)는 미구현 → Placeholder 사용:**
```
[node name="PH_이름" parent="Obstacles" instance=ExtResource("<placeholder id>")]
position = Vector2(x, y)
code = "P4"                         ; 요소 카탈로그 코드
obstacle_name = "토글 발판"
note = "동작 명세 + 배치 지시(프로그래머가 읽음)"
box_size = Vector2(폭, 높이)        ; 권장 충돌 크기(플레이어 19x43 기준)
box_color = Color(r, g, b, 1)       ; ★ 알파 포함 4개 인자 필수!
```
- **주의:** .tscn 의 `Color()` 는 인자 4개(`Color(1,0.5,0.1,1)`). 3개면 파싱 에러난다.
- 흰/장식 발판은 **점프·달리기 통로의 머리 높이에 두지 말 것**(통로 막혀 SIM 실패). 통로 밖/위로 분리.

### E. 검증(반드시 실행)
```bash
GODOT="C:/Users/김도형/Desktop/Godot_v4.6.3-stable_win64.exe"
# 인자: <씬경로> <포탈x> <낙사y=킬존 윗변(=킬존 position.y − height/2)> <시작x> <시작y>
"$GODOT" --headless --path . -s tools/reach_sim.gd -- "res://scenes/world_1/stage_N/stage_N.tscn" <포탈x> <낙사y> <시작x> <시작y>
```
- `SIM_OK` 면 통과. `SIM_FAIL 낙사 x=…` 면 그 x 근처 간격을 좁히거나 발판을 추가.
- `SIM_TIMEOUT 막힘 x=…` 면 그 x 에서 무언가(머리 높이 발판/벽)가 막은 것 → 치워라.
- reach_sim 은 **횡스크롤 전용**(오른쪽 자동주행). **수직 맵은 점프 물리(B)로 직접 계산**하고, 부팅만 확인:
  `"$GODOT" --headless --path . "res://…stage_N.tscn" --quit-after 40` (에러줄 없으면 OK).

### F. 마무리
- 포탈 `next_stage_path` 로 다음 스테이지에 연결(마지막이면 비워서 ALL CLEAR).
- 무엇을 만들었고 어디에 어떤 플레이스홀더를 왜 뒀는지 짧게 보고.

---

## 참고 문서(읽고 시작)
- `스테이지_요소_카탈로그.md` (요소 코드 T/H/P, 구현 여부, 난이도)
- `스테이지_상세_월드1~4.md` (스테이지별 구간 흐름)
- `작업보고_2026-06-30_스테이지4-15_레벨디자인_플레이스홀더시스템.md` (현재 상태/규칙 총정리)
- 모범 예시: `scenes/world_1/stage_5/stage_5.tscn`(횡), `stage_4`(내리막+카메라), `stage_8`(수직)

---

## ✏️ 이번에 만들 스테이지 (이 칸만 채워서 보내세요)
- 스테이지 번호/이름:
- 난이도:
- 맵 구조(횡/수직/경사/전방향):
- 핵심 요소(카탈로그 코드, 예: P4+H1):
- 구간 흐름(문서에서 복사):
- 특별 이벤트/테마:
- 다음 스테이지(포탈 연결 대상):

# 인수인계 프롬프트 — color (배드랜드 지형 타일셋 v3 완료 → 스테이지 빌드 단계)

> 작성: 2026-06-29 (v3 갱신) / 대상: 다음 작업자(사람 또는 AI 세션)
> 이 문서 하나만 읽으면 작업을 이어갈 수 있도록 정리. GitHub Desktop 으로 공유됨.

---

## 0. 게임 한 줄 설명
검정(BLACK) ↔ 흰색(WHITE) 색 전환 2D 플랫포머 "color" (Godot 4.6.3).
같은 색 지형만 밟힘, 반대색은 통과 후 사망. 회색(GRAY) 지형은 색을 칠해야 밟힘.
- 프로젝트: `C:\Users\김도형\Documents\GitHub\color`
- Godot: `C:\Users\김도형\Desktop\Godot_v4.6.3-stable_win64.exe`

## 1. 현재 상태
- **Stage 1·2**: 손그림 유기적 지형. 유지(건드리지 말 것).
- **Stage 3**: 임시 네모 발판(Platform.tscn) + 불벽 추격. 교체 대상.
- **Stage 4**: 도입부만 존재. ⚠ 아직 **옛 폴더 `지형 모듈/`** 의 Ground_Flat·Curve_Hill 참조 중
  → 스테이지 재빌드 때 아래 v3 한글 모듈로 교체할 것.
- **Stage 5~10**: 씬 없음(신규).
- **★ 이번 세션 완료**: 배드랜드 지형 타일셋 **v3** — 민무늬 순수 실루엣, 한글 이름, 이미지/씬 분리,
  회색경사로·공중발판·가시·크랙 포함.

## 2. ★ 타일셋 v3 — 민무늬 순수 실루엣 (정식 세트)

### 스타일 (사용자 확정: B·민무늬)
- v2 의 회색 명암·베이크 크랙·덩굴이 "원하는 느낌과 멀어짐" → 제거.
- v3 = **통짜 단색 실루엣(검/흰/회)** + **윗면 평탄(발 안 걸림)** + 아래로 뾰족하게 끊기는 끝.
  (예시: map1 의 Platform_Black/Slope_Body 같은 깔끔한 순수 검정 실루엣)
- 크랙·가시는 **별도 에셋**으로 분리.

### 폴더 구조 (이미지 ↔ 씬 분리)
```
scenes/지형파일셋/
├── 이미지/                     ← PNG 만 (맵 제작자가 미리보기로 고름)
│   ├── 바닥(검)1.png … 크리스탈기둥(흰)1.png   (지형 66장)
│   ├── 가시/  가시1~4.png        (빨강 즉사)
│   └── 크랙/  크랙1~10.png       (map1 Crack 복사)
└── 씬/                         ← .tscn 만 (스테이지에 인스턴스)
	├── 바닥(검)1.tscn … (지형 66개)
	├── 가시/  가시1~4.tscn       (ThornObstacle 기반)
	└── 크랙/  크랙1~10.tscn      (Sprite 데칼, 충돌 없음)
```
- 지형 .tscn = `StaticBody2D(terrain_image.gd) + Sprite2D + CollisionPolygon2D(auto_baked) + DeathDetector + Entry/Exit`.
- 색→레이어: **(검)=layer2/cs0**, **(흰)=layer4/cs1**, **(회)=layer8/cs2(칠해야 밟힘)**. 전부 SOLIDS 1폴리, 윗면 평탄 검증 완료.
- 로드 검증: 검/흰/회 지형 + 가시 + 크랙 인스턴스화 성공(한글+괄호 이름 정상).

### 이름 ↔ 모양 표 (번호로 변형 구분)
| 카테고리 | 번호별 모양 |
|---|---|
| 바닥(검/흰) | 1=넓은평지, 2=아주긴평지, 3=두꺼운블록(절벽), 4=바위많은바닥 |
| 경사로(검/흰) | 1=오르막완만, 2=오르막급, 3=내리막완만, 4=내리막급, 5=긴완만, 6=평지→경사전환 |
| 회색경사로(회) | 1=내리막, 2=오르막 (회색=색칠해야 밟힘) |
| 발판(검/흰) | 1=작은, 2=넓은 (지상 디딤) |
| 공중발판(검/흰) | 1=넓은타원, 2=둥근, 3=작고울퉁, 4=큰섬, 5=기울어진 (각각 다른 모양) |
| 선반(검/흰) | 1=좌측절벽, 2=우측절벽, 3=협곡선반 |
| 천장(검/흰) | 1=평평, 2=종유석, 3=오버행 |
| 기둥(검/흰) | 1=긴, 2=굵은 |
| 벽(검/흰) | 1=절벽벽, 2=동굴벽 |
| 계단(검/흰) | 1=신전계단 / 단차1=작은단 |
| 다리(검/흰) | 1=아치다리 |
| 크리스탈발판/기둥(검/흰) | 1 |
| 가시(빨강) | 1~4 (촘촘/성김/넓음/작음, 즉사) |
| 크랙(데칼) | 1~10 (map1 번개형 균열, 시각용·충돌 없음) |

## 3. 내가 만들거나 고친 파일 (전부 주석 달림)
- `tools/generate_terrain_silhouettes.gd` — **v3 생성기**. 헤더에 v3 변경 이유 주석.
  민무늬 통짜 렌더(`_render`), 한글 이름·번호(`_specs`/`_path_for`), 가시(`_b_thorn`),
  공중발판 변형(blob skew/lump). 출력 = 이미지/ (가시는 이미지/가시/).
- `tools/bake_architecture_tileset.gd` — `▼ 2026-06-29 v3` 주석과 함께 ①SCAN_DIRS 를 `이미지/` 로
  변경 ②한글 색표기 `(흰)`→WHITE, `(회)`→GRAY 인식 추가. (비재귀 스캔이라 이미지/가시·크랙 하위폴더는 안 구움)
- 가시 씬(씬/가시/가시N.tscn) — ThornObstacle 구조 직접 작성(런타임 자동 킬존).
- 크랙 씬(씬/크랙/크랙N.tscn) — Sprite2D 데칼.
- 문서: 이 파일 + `지형_제작_계획_배드랜드.md`.

기존 게임 코드(player/terrain_image/auto_collision/thorn_obstacle 등)는 **미수정**.

## 4. 파이프라인 (지형 재생성/추가)
형태 조정: `generate_terrain_silhouettes.gd` 의 `_specs()` 파라미터 수정
(`T`/`thick`/`spikes`/`slen`, 경사 `T0`/`slope`(±0.6 이내), 기둥 `hw`/`twist`, 발판 `rx`/`ry`/`skew`/`lump`).
실행:
```
Godot --headless --path <proj> -s res://tools/generate_terrain_silhouettes.gd   # 1) PNG → 이미지/
Godot --headless --path <proj> --import                                          # 2) 임포트(baker가 load() 함)
Godot --headless --path <proj> -s res://tools/bake_architecture_tileset.gd       # 3) .tscn → 이미지/
# 4) 새로 구운 .tscn 을 이미지/ → 씬/ 로 이동 (mv 이미지/*.tscn 씬/). ext_resource 는 res:// 절대경로라 안전.
```
> 베이커 로그의 `broken_platform.png.png` 에러 1줄은 기존 ENTRIES 스테일 참조(무관, 무시).

## 5. 절대 규칙
1. `color_state` 프로퍼티명 절대 변경 금지(player.gd 가 `get("color_state")` 로 읽음).
2. **걷는 지형 윗면은 반드시 평탄**(요철은 아래로). 윗면 요철 = 발 걸림 = 점프지옥.
3. 색→레이어: 검=2, 흰=4, 회=8, 가시/장애물=256. 베이커가 (검)/(흰)/(회) 로 자동.
4. **지형 픽셀 색은 시각용일 뿐** — player.gd 는 color_state '변수'로만 사망 판정, 충돌은 알파로만 생성.
5. 코드 수정 시 `▼ 날짜+이유` 한국어 주석 필수. 새 스테이지는 SceneManager.STAGES[] 등록.

## 6. 남은 작업 (다음) — 우선순위
지형/가시/크랙 에셋 완료. 남은 건 **스테이지 빌드 + 신규 기믹**.

### (권장) 먼저: Stage 3 리메이크 (라이브 검증)
네모 Platform(G1~G4) → 씬/ 의 한글 모듈로 교체. 신규 기믹 불필요.
- 바닥=바닥(검/흰)1·2, 단차=단차1, 발판=발판2/공중발판, 색전환=같은 모듈 (검)/(흰),
  절벽=선반1·2, 가시=씬/가시/가시1~4. 동선·기존 추격(FireWall 175px/s)·체크포인트 유지.
- 배치 팁: 모듈 Sprite `centered=false`(좌상단 원점), Exit 마커=오른쪽 끝 → 다음 모듈 Entry(0,0)를
  이전 Exit 위치에 맞추면 바닥이 매끄럽게 이어짐.

### 그 다음 Stage 4~10 (의존성 순)
| 스테이지 | 테마 | 쓸 모듈(예) | 선행 신규 기믹 |
|---|---|---|---|
| 4 스키 | 연속 강하 | 경사로5, 경사로4, 경사로6, 회색경사로1·2 | 없음 |
| 5 빛과 어둠 | 동굴 | 바닥4, 벽2, 천장2 | 회전 서치라이트(light_obstacle.gd rotation_speed)+암전 셰이더 |
| 6 신전 추격 | 신전 | 바닥3, 계단1, 기둥2 | ColorButton+ColorDoor, Boulder(fire_wall.gd 응용) |
| 7 거울의 방 | 크리스탈 | 크리스탈발판1, 크리스탈기둥1, 발판1 | ToggleBlock+ToggleSwitch |
| 8 바람의 협곡 | 협곡 | 선반3, 공중발판4, 공중발판1·5 | FlowZone(Area2D 방향성 힘) |
| 9 미궁 | 복합 | 위 혼합 | 없음 |
| 10 색의 심장 | 종합 | 전 모듈 | CrumblePlatform |

## 7. 검증
- 구문: `Godot --headless --path <proj> --check-only -s res://script.gd`
- 스테이지: `Godot --headless --path <proj> "res://scenes/world_1/stage_N/stage_N.tscn" --quit-after 120`
- **헤드리스 에러0이어도 화면은 직접 렌더해 봐야 함.** 완료 기준: 발 걸림 0(걸어서 끝까지)·색 판정 정상·포탈 클리어.

## 8. 함정·주의
- **옛 폴더 `지형 모듈/`(v2)·`실루엣/`(v1) 은 레거시.** stage_4 가 아직 `지형 모듈/` 참조 → 재빌드 후 정리 권장.
- 생성기는 임포트 불필요, **베이커는 load() 라 임포트 필수**.
- 지형파일셋 산출물은 git untracked 가능 → 커밋 권장(과거 유실 사고 있었음).
- 경사 `slope` 0.6 초과 시 floor_max_angle(약 46°) 넘어 못 걸어 올라갈 수 있음.

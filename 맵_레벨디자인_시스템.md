# 맵 레벨디자인 시스템 설계서

> 색상 반전 2D 플랫포머 / Godot 4.6 / 작성일: 2026-06-17
> 목적: ① 발 걸림(스내깅) 문제 해결 ② 적은 에셋으로 무한히 다양한 맵을 빠르게 조립 ③ 예쁜 비주얼과 "장애물 회피형 플랫포머"의 손맛 유지

---

## 0. 한 줄 요약

> **"보이는 그림(울퉁불퉁)"과 "밟는 충돌면(매끄러움)"을 분리하고, 미리 깨끗하게 만들어 둔 10종 모듈을 크기·반전·조합만으로 배치한다.**

디자이너는 그림만, 레벨 작업자는 모듈 배치만, 충돌은 모듈에 1회만 박아둔다 → 발 걸림이 구조적으로 사라지고 맵 제작 속도가 수십 배 빨라진다.

---

## 1. 문제 진단

### 1-1. 증상
- 지형을 울퉁불퉁하게 그릴수록 캐릭터 발이 걸려서 안 움직임.
- 새 스테이지마다 새 지형을 그리는 방식은 2스테이지 만에 한계 도달.

### 1-2. 발 걸림의 진짜 원인 (기술적)
현재 충돌은 `AutoCollision.bake_into()` → `BitMap.opaque_to_polygons()` 로 생성된다.
이 함수는 **알파 모양 하나를 여러 개의 볼록(convex) 폴리곤 조각으로 분해**한다.

```
울퉁불퉁한 PNG 하나
   ↓ opaque_to_polygons()
[조각A][조각B][조각C][조각D] ...  ← 바닥에 이 조각들이 다닥다닥 붙음
        ↑           ↑
     이음새(seam)마다 미세한 "수직 모서리"가 생김
```

`CharacterBody2D.move_and_slide()` 의 캡슐이 바닥을 걸어갈 때, 이 **이음새의 수직 모서리("고스트 충돌")** 에 발끝이 턱턱 걸린다. 그림이 울퉁불퉁할수록 정점이 많아지고 → 미세 수직면이 늘어나 → 더 심하게 걸린다.

**즉, 문제는 "그림이 울퉁불퉁해서"가 아니라 "충돌면이 울퉁불퉁한 조각들로 쪼개져서"다.** 그림과 충돌을 분리하면 그림은 마음껏 울퉁불퉁해도 된다.

---

## 2. 해결책: 모듈 키트 + 충돌면 분리 (2대 원칙)

### 원칙 A — 비주얼과 충돌을 분리한다 (발 걸림 해결)

| 레이어 | 무엇 | 규칙 |
|--------|------|------|
| **비주얼 레이어** | 디자이너가 그린 예쁜·울퉁불퉁한 지형 아트 (Sprite2D) | 충돌 없음. 디테일·질감·바위 자유롭게 |
| **충돌 레이어** | 실제로 밟는 면 (CollisionPolygon2D) | 일부러 **매끄럽게**. 정점 최소, 완만한 경사, 작은 수직 단차 금지 |

> 셀레스트·할로우나이트 등 모든 상용 2D 플랫포머가 쓰는 표준 기법이다. 플레이어 눈엔 울퉁불퉁한 바위가 보이지만 발은 매끈한 선 위를 미끄러진다.

**충돌면을 매끄럽게 만드는 3가지 방법** (택1, 권장순):

1. **(권장) 모듈마다 충돌 폴리곤을 손으로 1회 깔끔하게 작성**
   - 조각이 아니라 **하나의 연속된 폴리곤 1개**로. 이음새 자체가 없어짐.
   - 정점 수 최소화, 경사는 `floor_max_angle` 이하 완만하게.

2. **충돌 전용 마스크 PNG를 따로 받기**
   - 디자이너가 "예쁜 아트 PNG" + "매끈한 실루엣 PNG(충돌용)" 두 장을 준다.
   - 충돌용 PNG에만 `opaque_to_polygons` 를 돌리고 `simplify(epsilon)` 를 크게(예: 8~12) 줘서 정점을 확 줄인다.

3. **자동 베이크 후 폴리곤 병합+단순화** (엔지니어링 비용 큼, 후순위)

**추가로 CharacterBody2D 튜닝** (player.gd / Player.tscn):
- `floor_snap_length` ↑ (예: 16~32) → 작은 턱에서 바닥 붙음 유지, 덜렁거림 방지
- `floor_max_angle` 적정값 (예: 46°) → 의도한 경사는 바닥으로 인정
- `floor_constant_speed = true` → 경사에서 속도 일정
- `safe_margin` 살짝 ↑ → 미세 모서리 무시
- `platform_on_leave` / 벽 충돌은 별도 레이어로 관리

---

### 원칙 B — 10종 모듈 키트로 조립한다 (확장성·다양성)

사용자가 제안한 "1~10 패턴" 아이디어를 구체화한 것. **각 모듈 = 재사용 가능한 PackedScene**.

#### 모듈 구성 (예시 10종)
| # | 모듈 이름 | 용도 |
|---|----------|------|
| 1 | `Ground_Flat` | 평지 바닥 (기본 단위) |
| 2 | `Slope_Up` | 완만한 오르막 |
| 3 | `Slope_Down` | 완만한 내리막 |
| 4 | `Platform_Float` | 공중 발판 (점프 목표) |
| 5 | `Ledge_Edge` | 낭떠러지 끝(절벽) |
| 6 | `Pillar` | 기둥/벽 (수직 장애) |
| 7 | `Ceiling` | 천장 (좁은 통로) |
| 8 | `Step_Small` | 한 칸 계단 |
| 9 | `Gap_Bridge` | 구멍 위 좁은 다리 |
| 10 | `Curve_Hill` | 둥근 언덕 (비주얼 포인트) |

#### 모듈 1개의 내부 구조
```
TerrainModule (StaticBody2D, platform.gd 재사용)
├── Visual (Sprite2D)         ← 예쁜 아트 (울퉁불퉁 OK)
├── CollisionPolygon2D        ← 미리 깔끔하게 작성한 충돌 1개 (이음새 없음)
├── DeathDetector (Area2D)    ← 기존 색 판정 구조 그대로
└── SnapPoints (Marker2D들)   ← 모듈끼리 이어 붙일 기준점 (Entry/Exit)
```

#### 조립 규칙 (레벨 작업자가 지키는 것)
- **그리드 기준**: 모든 모듈 원점은 좌상단, 256px 그리드에 스냅.
- **변형 허용**: `scale.x` / `scale.y` 늘이기·줄이기, `flip(scale.x = -1)` 좌우 반전. → 10종으로 사실상 수백 가지 형태.
- **색 지정**: 모듈의 `color_state`(BLACK/WHITE/GRAY) export 만 바꾸면 같은 모양이 모든 색 지형으로 재활용.
- **이어붙이기**: 앞 모듈 Exit SnapPoint 높이 = 다음 모듈 Entry SnapPoint 높이 → 바닥 높이가 끊기지 않아 발 걸림·낙하 사고 방지.
- **충돌은 절대 손대지 않는다**: 모듈에 박힌 깨끗한 충돌만 사용. 새로 그리지 않음.

> ⚠️ scale.y 를 과하게 늘이면 경사가 가팔라져 `floor_max_angle` 을 넘길 수 있다. 경사 모듈은 X만 늘이는 걸 기본으로.

---

### 원칙 C — 난이도·보상 설계 (손맛 유지)

스테이지를 **"비트(beat)의 흐름"** 으로 작곡한다:

```
도입(안전, 조작 학습) → 상승(난이도↑) → 절정(가장 어려운 구간) → 보상/숨고르기 → 출구(포탈)
```

**난이도 조절 노브**:
- 구멍(gap) 폭 → 점프 정밀도
- 공중에서 강제 색 전환 요구 (반대색 발판 위 통과)
- 페인트 퍼즐 게이팅 (회색 지형을 칠해야 진행)
- 장애물 밀도·타이밍 (기어·가시·낙하블록)
- GRAY 미끄럼 구간 (얼음판 컨트롤)

**보상감 설계**:
- 어려운 구간 직후 반드시 "숨 고르는 안전 발판" + 시각적 변화(배경/색)로 성취감.
- 죽음 횟수(HUD)가 이미 있으므로, 적은 죽음 클리어 = 자랑거리로 연결.

**예쁜 비주얼 유지**:
- 충돌면을 깎아도 아트는 그대로 → 비주얼 자유도 100% 유지.
- Parallax 배경 + Details(장식) 레이어 + 일관된 팔레트(흑/백/회 대비)로 특유의 미니멀 감성 강화.

---

## 3. 권장 작업 파이프라인

```
[디자이너]
  10종 모듈 아트 PNG 그림 (+ 필요시 충돌용 매끈 실루엣 PNG)
        ↓
[엔지니어 / 1회성]
  모듈 PackedScene 10개 제작 → 깨끗한 충돌 박기 + SnapPoint 세팅
        ↓
[레벨 작업자 (반복)]
  모듈을 끌어와 크기·반전·색·배치만으로 스테이지 조립
        ↓
[플레이테스트]
  발 걸림 0 확인 → 난이도 비트 조정 → 장식·장애물 추가
```

핵심: **무거운 작업(충돌·모듈)은 1회, 반복 작업(맵 조립)은 가볍게.**

---

## 4. 기존 코드와의 연결점

- `platform.gd` / `terrain_image.gd` 의 `color_state`, `DeathDetector`, `death_zones` 그룹 구조 **그대로 재사용** → player.gd 색 판정 로직 수정 불필요.
- 기존 `MapVisual` / `MapPhysics` 노드 분리 구조와 정확히 일치 → 자연스럽게 확장.
- `auto_collision.gd` 는 "충돌 전용 마스크 PNG"에 epsilon 크게 줘서 계속 활용 가능 (원칙 A-2).

---
*이 문서는 디자이너·레벨 작업자 배포용입니다.*

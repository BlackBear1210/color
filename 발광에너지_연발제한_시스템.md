# 발광(에너지) 기반 색깔총 연발 제한 시스템

> 색상반전 2D 플랫포머 / Godot 4.6.3 / 작성일 2026-06-28
> 대상: 이 프로젝트를 이어 작업할 다른 작업자 / 새 세션
> 관련 코드: `scripts/player.gd`, `scripts/bullet.gd`

---

## 1. 왜 만들었나 (문제)

플레이어가 색깔총을 **너무 빠르게 연발**하면, 같은 순간에 `PaintMark`(페인트 자국)가
`call_deferred` 로 우르르 생성되면서 **지형 색칠 로직이 꼬였다**(겹침·색 판정 혼선·노드 폭증).

해결 방향으로 단순히 쿨다운만 늘리면 "왜 안 쏴지지?"가 직관적이지 않다.
→ **플레이어 주위에 빛(에너지)을 띄우고, 쏠수록 그 빛이 줄어들며, 다 쓰면 회복될 때까지 못 쏘게** 했다.
"빛 = 탄약"이라는 게임 내적(diegetic) 표현이라 플레이어가 자연스럽게 연사를 자제하게 된다.

---

## 2. 설계 결정 (사용자 확정 2026-06-28)

| 쟁점 | 결정 | 이유 |
|------|------|------|
| 빛 색을 흑/백과 연결? | **아니오 — 중립 에너지광(호박색)** | 회색은 게임상 '못 밟는 지형'을 뜻해 혼동 위험. 흑/백별 다른 표현은 학습 부담. → '색 상태' 채널과 '탄약' 채널을 분리 |
| 에너지 0이면? | **발사 실제 차단(디제식 탄약)** | 연발을 물리적으로 막아 페인트 로직 폭주를 근본 차단 |
| 추가 안전장치 | **같은 색 PaintMark 중복 생성 방지** | 한 지점에 마크가 무한 스택되는 것 방지(bullet.gd) |

---

## 3. 동작 방식

```
[매 물리 프레임]
  _shoot_energy += SHOOT_ENERGY_REGEN * delta   (최대 1.0까지 회복)

[좌클릭(shoot) 입력]
  발사 조건 = 입력 있음 AND 쿨다운 종료 AND _shoot_energy >= SHOOT_ENERGY_COST
	→ 만족: 총알 발사 + _shoot_energy -= SHOOT_ENERGY_COST
	→ 불만족(에너지 부족): 발사 안 됨(회복 대기)

[에너지광 비주얼]
  매 프레임 _update_energy_light():
	빛 위치 = 플레이어 위치
	빛 반경 = 70px(바닥) ~ 200px(가득)   ← 에너지 비례
	빛 밝기(알파) = 0.10(바닥) ~ 0.55(가득)
  → "쏠수록 빛이 작아지고 어두워짐, 가만히 있으면 다시 커짐"
```

### 수치(튜닝 노브) — `scripts/player.gd` 상단
| 상수 | 기본값 | 의미 |
|------|--------|------|
| `MAX_SHOOT_ENERGY` | 1.0 | 에너지 최대치 |
| `SHOOT_ENERGY_COST` | 0.34 | 1발당 소모(가득에서 약 3발) |
| `SHOOT_ENERGY_REGEN` | 0.45 | 초당 회복(바닥→가득 약 2.2초) |
| `ENERGY_LIGHT_COLOR` | `(1.0, 0.82, 0.45)` | 중립 호박색(흑/백/회색과 안 겹침) |

> 연사를 더 빡세게/느슨하게: `COST`↑ 또는 `REGEN`↓ = 더 빡셈. 반대면 느슨.

---

## 4. 구현 위치 (코드 지도)

### `scripts/player.gd`
- **상수/상태**: `MAX_SHOOT_ENERGY` 등 + `var _shoot_energy`, `var _energy_light`.
- **`_energy_light_setup()`** (신규): 방사형 그라데이션 텍스처 → `Sprite2D`(ADD 블렌드, `top_level=true`).
  - `top_level=true` 이유: 플레이어 `scale`(0.3)의 영향을 안 받고 월드 좌표로 그리기 위함(총알 트레일과 동일 패턴).
- **`_update_energy_light()`** (신규): 에너지 비율로 빛 위치·반경·밝기 갱신.
- **`_physics_process()`**: 에너지 회복 + 발사 게이트 + 매 프레임 빛 갱신.
- **`_respawn()`**: 리스폰 시 에너지 가득 회복.

### `scripts/bullet.gd`
- **`_spawn_mark()`**: 반경 내에 **같은 색 마크가 이미 있으면 새로 만들지 않고 `return`**(중복 스택 방지).

> 모든 신규/수정 코드에 `▼ 2026-06-28 ...` 한국어 주석으로 "왜" 명시(다른 작업자 추적용).

---

## 5. 검증 (2026-06-28, Godot 4.6.3 헤드리스)

- `bullet.gd` 단독 구문 검사: 통과.
- `player.gd` 는 `AudioManager`(오토로드) 참조라 단독 검사에선 경고 — **실제 씬 실행으로 검증**.
- stage_1 / stage_2 / stage_3 헤드리스 부팅: **스크립트 에러 0**(120프레임).

### 에디터에서 사람이 직접 확인 필요(미검증)
- 호박색 빛이 흑/백 배경 위에서 잘 보이는지(특히 밝은 흰 지형 위 ADD 블렌드 가독성).
- 연사 손맛(COST/REGEN 체감) 튜닝.

---

## 6. 다른 디자인으로 바꾸고 싶을 때

- 빛 색/모양: `ENERGY_LIGHT_COLOR` 와 `_energy_light_setup()` 의 그라데이션만 수정.
- 빛 대신 게이지 바(HUD): `_update_energy_light()` 를 HUD 갱신 호출로 교체하면 됨(에너지 값은 `_shoot_energy`).
- 빛이 너무 약하면: `_energy_light_setup()` 의 블렌드를 ADD 대신 일반(MIX)로 바꾸고 알파를 올리면 어떤 배경에서도 선명해짐.
</content>

# 지형 제작 계획 — 배드랜드(BADLAND) 스타일 (Stage 3~10)

작성: 2026-06-29 / 대상: color (Godot 4.6.3)

> ## ✅ 지형 에셋 생성 완료 (2026-06-29)
> Leonardo 대신 **Godot 절차적 생성**으로 24종 전부 제작 완료.
> (이유: 파이프라인이 어차피 순수 흑/백 실루엣으로 납작하게 만들므로 형태만 직접 생성)
> - 생성기: `tools/generate_terrain_silhouettes.gd` (노이즈 기반, 재실행으로 형태 재조정 가능)
> - 산출물: `scenes/지형파일셋/실루엣/<이름>_01_B.png` · `_01_W.png` (24종 × 2 = 48장)
> - 베이킹: `bake_architecture_tileset.gd` 실행 완료 → 동일 폴더에 `<이름>_01_B.tscn`/`_W.tscn` 48개
>   (각: StaticBody2D + Sprite2D + CollisionPolygon2D(auto_baked) + DeathDetector + Entry/Exit)
> - 충돌 레이어: _B → layer 2(BLACK), _W → layer 4(WHITE) 정상. 전부 SOLIDS 단일 폴리곤.
> - 형태에 막힌 구멍이 없도록 설계 → SEGMENTS 후처리 불필요(Cave_Ceiling/Gap_Bridge 포함).
>
> **남은 일**: 이 모듈들을 stage_3~10 .tscn 에 배치(스테이지 빌드) + 신규 기믹 제작.
> 형태를 바꾸고 싶으면 생성기의 `_specs()` 파라미터(amp/freq/slope 등)만 고쳐 재실행→재베이킹.

---


> 규칙: **Stage 1·2 지형은 그대로 둔다.** Stage 3부터 모든 지형을
> 배드랜드 스타일 실루엣 모듈로 교체/신규 제작한다.

---

## 0. 핵심 원칙 — "형태만 만들면 된다"

`tools/silhouette_from_image.gd`가 입력 이미지를 **순수 검정/흰색 실루엣 + 투명배경**
으로 변환한다. 즉 **색·그림자·질감·디테일은 전부 버려지고 "윤곽선(실루엣)"만 남는다.**

→ Leonardo에서 신경 쓸 것은 단 하나: **읽기 쉬운 유기적 덩어리 실루엣**.
   배드랜드 느낌 = 비대칭·풍화된·울퉁불퉁·뒤틀린 자연 형태 (반듯한 네모 금지).

입력 배경은 3가지 중 하나면 됨(도구가 자동 처리):
1. **투명 PNG(alpha)** — Leonardo "Transparency" 기능 → mode "alpha" (가장 깔끔, 권장)
2. **단색 배경(colorkey)** — 평평한 마젠타 `#FF00FF` 배경 → mode "colorkey", tol 0.2
3. 명암(luminance) — 어두운 피사체 / 밝은 배경 → mode "luminance"

---

## 1. 제작할 지형 모듈 목록 (Leonardo 생성 대상)

각 모듈은 소스 이미지 **1장**만 생성하면 됨.
BLACK·WHITE 두 버전은 같은 소스로 파이프라인을 color 설정만 바꿔 **2번 돌리면** 자동 생성.

비고: SOLIDS = 단일 덩어리(에디터에서 무지개색), SEGMENTS = 동굴/구멍/얇은 형태.

### [A] 공통 베이스 — 재제작/보강 (Stage 3~10 공용)
| 모듈 | 형태 | 사용처 | 충돌 |
|---|---|---|---|
| Ground_Flat | 울퉁불퉁 유기적 바닥, 뿌리 같은 밑면 | 전 스테이지 바닥 | SOLIDS |
| Slope_Up | 오른쪽으로 오르는 비탈 | 3,4,9 | SOLIDS |
| Slope_Down | 오른쪽으로 내려가는 비탈 | 3,4,9 | SOLIDS |
| Platform_Float | 떠 있는 둥근 바위 발판 | 전 스테이지 | SOLIDS |
| Ledge_Edge | 절벽 선반(한쪽 급강하) | 3,8,9 | SOLIDS |
| Step_Small | 작은 단차 턱 | 전 스테이지 | SOLIDS |
| Gap_Bridge | 틈을 잇는 얇은 자연 아치 다리 | 3,9 | **SEGMENTS** |
| Curve_Hill | 둥근 돔형 언덕 | 3,4 | SOLIDS |
| Pillar_Tall | 키 큰 뒤틀린 기둥(나무줄기형) **재제작** | 3,6,9 | SOLIDS |
| Ceiling_Slab | 위에서 내려온 천장 오버행 **재제작** | 3,5,6 | SOLIDS |

### [B] Stage 4 "스키 슬로프"
| 모듈 | 형태 | 충돌 |
|---|---|---|
| Slope_Long_Gentle | 완만한 아주 긴 내리막 | SOLIDS |
| Slope_Long_Steep | 급한 긴 내리막 | SOLIDS |
| Slope_Transition | 평지→경사 부드러운 전환 | SOLIDS |
| (기존 GrayGradientSlope) | 색칠해야 오르는 회색경사로 | 기존 |

### [C] Stage 5 "빛과 어둠" (동굴)
| 모듈 | 형태 | 충돌 |
|---|---|---|
| Cave_Floor | 거친 동굴 바닥(석순 돋은 윗면) | SOLIDS |
| Cave_Ceiling | 종유석 매달린 천장 | **SEGMENTS** |
| Cave_Wall | 키 큰 동굴 암벽 | SOLIDS |

### [D] Stage 6 "바위 추격 신전"
| 모듈 | 형태 | 충돌 |
|---|---|---|
| Temple_Block | 풍화된 고대 신전 석블록(각지나 침식됨) | SOLIDS |
| Temple_Column | 무너진 신전 기둥(금 간 윗부분) | SOLIDS |
| Temple_Stair | 침식된 신전 계단 | SOLIDS |

### [E] Stage 7 "거울의 방" (크리스탈)
| 모듈 | 형태 | 충돌 |
|---|---|---|
| Crystal_Slab | 각진 크리스탈 발판(평평한 윗면) | SOLIDS |
| Crystal_Column | 뾰족한 수직 크리스탈 기둥 | SOLIDS |

### [F] Stage 8 "바람의 협곡"
| 모듈 | 형태 | 충돌 |
|---|---|---|
| Canyon_Ledge | 퇴적층 협곡 바위 선반 | SOLIDS |
| Floating_Rock_Small | 소형 부유 바위 | SOLIDS |
| Floating_Rock_Large | 대형 부유 바위섬(평평한 윗면) | SOLIDS |

### [G] Stage 9 "추격의 미궁" / Stage 10 "색의 심장"
신규 지형 art **불필요**. 위 [A]~[F] 모듈 재조합으로 구성.
(Stage 10의 CrumblePlatform은 지형이 아니라 장애물 → 별도 처리)

> **총 신규 생성 소스 이미지: 약 23장** (각 1장 → 파이프라인이 B/W 2버전 자동 생성)

---

## 2. Leonardo.ai 프롬프트 시트

모든 프롬프트 앞에 아래 **공통 스타일 블록**을 붙인다(영어 권장 — 인식률 높음).

**공통 스타일(복붙):**
```
2D side-scrolling platformer terrain asset, side profile view, bold solid black silhouette,
BADLAND game art style, organic weathered gnarled eroded edges, asymmetric natural form,
clean readable single connected shape, flat solid magenta #FF00FF background, high contrast, centered
```
**Negative prompt(복붙):**
```
3D, photorealistic, top-down view, multiple separate objects, text, watermark, logo, frame,
border, gradient background, soft shadow, glow, blur, busy tiny details, character, creature
```

**모듈별 subject (공통 블록 + 아래 한 줄):**

```
[A 공통]
Ground_Flat        : wide low ground chunk, lumpy rolling top, rooty underside          (가로 16:9)
Slope_Up           : diagonal hillside ramp rising to the right                          (정사각)
Slope_Down         : diagonal hillside ramp descending to the right                      (정사각)
Platform_Float     : small floating boulder platform, rounded rock with flat top         (정사각)
Ledge_Edge         : cliff ledge, flat top with steep drop on the right                  (정사각)
Step_Small         : small low step bump, short stair block                              (가로)
Gap_Bridge         : thin natural rock arch bridge spanning a gap                        (가로 16:9)
Curve_Hill         : smooth rounded dome hill, mound                                     (가로)
Pillar_Tall        : tall twisted vertical pillar, gnarled tree-trunk column, narrow     (세로 9:16)
Ceiling_Slab       : overhanging ceiling slab hanging from the top, drippy underside     (가로 16:9)

[B Stage4]
Slope_Long_Gentle  : very long gentle downhill ski slope, shallow diagonal               (가로 16:9 넓게)
Slope_Long_Steep   : long steep downhill slope, sharp diagonal drop                      (가로/대각)
Slope_Transition   : flat ground curving smoothly into a downhill slope                  (가로)

[C Stage5]
Cave_Floor         : rough cave floor block, jagged stalagmite-topped ground             (가로)
Cave_Ceiling       : cave ceiling with hanging pointed stalactites                       (가로 16:9)
Cave_Wall          : tall vertical rough cave rock wall, cliff face                       (세로 9:16)

[D Stage6]
Temple_Block       : weathered ancient temple stone block, angular eroded ruin           (정사각)
Temple_Column      : broken ancient temple column, cracked crumbled top                  (세로 9:16)
Temple_Stair       : ancient eroded stone temple staircase ascending                     (가로)

[E Stage7]
Crystal_Slab       : horizontal faceted crystal platform shard, flat top                 (가로)
Crystal_Column     : tall pointed vertical crystal cluster column                        (세로 9:16)

[F Stage8]
Canyon_Ledge       : layered sedimentary canyon rock shelf, flat top                     (가로)
Floating_Rock_Small: small rounded floating rock chunk                                   (정사각)
Floating_Rock_Large: large floating rock island, big boulder with flat top               (가로)
```

### Leonardo 사용 절차 (초보용)
1. leonardo.ai 가입(구글 로그인). 무료로 매일 토큰 지급됨.
2. 좌측 **Image Generation** 진입.
3. **Model**: `Leonardo Phoenix` 선택 (프롬프트 충실도 높음).
4. **권장 — Transparency 토글 ON** (Phoenix 지원): 배경 없는 투명 PNG로 출력 → 마젠타 불필요, 파이프라인 mode를 "alpha"로.
   - Transparency를 못 쓰면: 위 프롬프트대로 **마젠타 배경**으로 뽑고 mode "colorkey" 유지.
5. **Aspect ratio**: 위 표의 비율대로 (가로형은 16:9, 세로 기둥은 9:16, 덩어리는 1:1).
6. Number of images 4 → 생성 → 가장 **단순하고 읽기 좋은** 실루엣 1장 선택.
7. 다운로드 → `res://scenes/지형파일셋/원본이미지/` 에 `<모듈명>.png` 로 저장.

> 팁: 같은 모듈을 여러 번 뽑아 형태가 또렷한(구멍/잔가지 적은) 걸 고르면 콜리전이 깔끔.

---

## 3. 파이프라인 실행 (이미지 생성 후)

`인수인계 문서 2절` 그대로:
1. 23장을 `원본이미지/`에 저장.
2. `silhouette_from_image.gd` DEFAULTS → color:"BLACK" (투명PNG면 mode:"auto") → 헤드리스 실행.
3. 같은 설정에서 color:"WHITE"로 재실행 → _W 생성.
   (※ 기존 10종도 color:"WHITE" 재실행으로 흰버전 보강 필요)
4. `bake_architecture_tileset.gd` 실행 → *.tscn 자동 생성.
5. **특수**: Cave_Ceiling, Gap_Bridge → 인스펙터에서 Collision Build Mode = **SEGMENTS**.

---

## 4. 스테이지별 지형 배치 메모 (PDF 인수인계 반영)
- **Stage 3 "추격"**: 현재 네모 Platform(G1~G4) → Ground_Flat/Curve_Hill/Ledge_Edge/Pillar로 교체. FireWall 175px/s 추격 동선 유지(좌→우 6720px).
- **Stage 4 "스키"**: 평지 없이 Slope_Long 연속 강하. 대각선 미끄럼 동선.
- **Stage 5 "빛과 어둠"**: Cave 모듈 + 회전 서치라이트 + 암전 셰이더. 좁은 통로.
- **Stage 6 "신전"**: Temple 모듈 + 굴러오는 바위 추격 + ColorButton/Door. 2페이즈.
- **Stage 7 "거울"**: Crystal 모듈 + ToggleBlock(색 반전 발판).
- **Stage 8 "협곡"**: Canyon_Ledge + Floating_Rock + FlowZone(바람 힘).
- **Stage 9 "미궁"**: Cave/Temple/Crystal 혼합 재조합 (신규 지형 0).
- **Stage 10 "심장"**: 전 모듈 재조합 3페이즈 + CrumblePlatform.

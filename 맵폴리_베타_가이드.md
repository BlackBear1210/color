# 맵폴리 (베타) — 지형 모듈 키트 가이드 & 작업자 인수인계

> 색상 반전 2D 플랫포머 / Godot 4.6 / 작성일 2026-06-17
> 짝꿍 문서: [맵_레벨디자인_시스템.md](맵_레벨디자인_시스템.md) (설계 원리), 이 문서(실제 사용법·자동화·인수인계)
>
> ⚠ **2026-06-17 갱신**: 단색 기하 모듈(`scenes/modules/`) 방식은 **이미지+폴리 아키텍처 타일셋**
> (`scenes/도형_타일셋_최신/`)으로 대체되었습니다. 최신 워크플로·현황은
> **[타일셋_작업_인수인계.md](타일셋_작업_인수인계.md)** 를 우선 참고하세요. 아래는 설계 배경 보존용입니다.

---

## 0. 이 키트가 푸는 문제

1. **발 걸림(스내깅)** — 울퉁불퉁 지형에 `opaque_to_polygons()` 를 돌리면 충돌이
   여러 조각으로 쪼개져 이음새마다 미세 수직면이 생기고 발이 걸렸다.
2. **동굴 충돌 먹통(stage_2 버그)** — 구멍 뚫린 도넛형 PNG 를 채움(SOLIDS)으로 구우면
   구멍이 메워져 플레이어가 지나갈 동굴 속이 막혔다.
3. **맵 제작 속도** — 스테이지마다 새 지형을 그리니 2스테이지 만에 한계.

**해결:** *비주얼과 충돌을 분리한 "깨끗한 단일 폴리곤 모듈" 10종*을 만들어 두고,
크기·반전·색·위치만 바꿔 조립한다.

---

## 1. 구성 파일

| 파일 | 역할 |
|------|------|
| `scripts/terrain_module.gd` | 모듈 엔진. `shape`(폴리곤 1개)에서 비주얼+충돌 자동 생성. 색/반전 처리 |
| `scenes/modules/Module_01~10_*.tscn` | 10종 지형 모듈 (아래 표) |
| `scenes/modules/맵폴리_베타.tscn` | **조립 작업장** — 모듈을 끌어와 맵을 만드는 템플릿 |
| `scripts/auto_collision.gd` | (개선됨) 자동 베이크 헬퍼 — 동굴용 SEGMENTS 모드 + 노이즈 폴리곤 제거 |

### 10종 모듈
| # | 파일 | 용도 |
|---|------|------|
| 1 | Module_01_GroundFlat | 평지 바닥(기본 단위) |
| 2 | Module_02_SlopeUp | 완만한 오르막(≈14°) |
| 3 | Module_03_SlopeDown | 완만한 내리막 |
| 4 | Module_04_PlatformFloat | 공중 발판 |
| 5 | Module_05_LedgeEdge | 낭떠러지 끝(L자 절벽) |
| 6 | Module_06_Pillar | 기둥/벽(수직 장애) |
| 7 | Module_07_Ceiling | 천장(좁은 통로) |
| 8 | Module_08_StepSmall | 한 칸 계단 |
| 9 | Module_09_GapBridge | 구멍 위 좁은 다리 |
| 10 | Module_10_CurveHill | 둥근 언덕(곡선 비주얼) |

---

## 2. 모듈 1개의 구조 (terrain_module.gd)

```
Module_XX (StaticBody2D, terrain_module.gd)
├── Visual (Polygon2D)      ← shape 로 자동 생성. 단색 미리보기.
│   └─ 또는 VisualSprite(Sprite2D) ← Terrain Texture 를 넣으면 아트로 교체
├── Collision (CollisionPolygon2D) ← shape 로 자동 생성. "밟는 면".
├── Entry (Marker2D)        ← 이어붙임 기준점(왼쪽 바닥 높이)
└── Exit  (Marker2D)        ← 이어붙임 기준점(오른쪽 바닥 높이)
```

- **`shape`**: 구멍 없는 단순 폴리곤 1개. 비주얼과 충돌이 이걸 공유한다 → 이음새 0.
- **`color_state`**: BLACK(0)/WHITE(1)/GRAY(2). 같은 모양을 모든 색 지형으로 재활용.
  - player.gd 가 `color_state` 를 직접 읽어 사망 판정하므로 **프로퍼티명 변경 금지**.
- **`terrain_texture`**: 넣으면 그 그림이 보이고, **충돌은 여전히 `shape`** 를 따른다
  → 그림은 마음껏 울퉁불퉁해도 발은 매끈하다(설계 원칙 A).
- **`collision_build_mode`**: SOLIDS(기본)/SEGMENTS. 동굴형 모듈만 SEGMENTS.

---

## 3. 레벨 작업자 사용법 (반복 작업)

1. `scenes/modules/맵폴리_베타.tscn` 을 복제해 새 스테이지 파일로 시작.
2. 파일시스템에서 `Module_0X_*.tscn` 을 씬으로 **드래그 → MapPhysics 아래에 인스턴스**.
3. 인스펙터에서:
   - **Position**: 256px 그리드에 맞춰 배치.
   - **Scale**: `scale.x` 로 길이, `scale.y` 로 높이. ⚠ 경사 모듈은 `scale.x` 만.
   - **Scale.x = -1**: 좌우 반전.
   - **Color State**: 지형 색 지정.
4. **이어붙이기(중요)**: 앞 모듈의 `Exit` 마커 위치 = 다음 모듈의 `Entry` 마커 위치가
   되도록 배치하면 바닥 높이가 끊기지 않는다(낙하/걸림 사고 방지).
   예) 평지 Exit(512,0) → 오르막 Entry(0,128) 이면, 오르막을 y로 −128 올려 맞춘다.
5. 장애물은 `scenes/obstacles/` 의 씬(Thorn/Gear/Falling/Spike/Portal)을 같은 방식으로 인스턴스.
6. ▶ 실행해 발 걸림 0 · 색 판정 정상 확인.

> **충돌은 절대 손대지 않는다.** 모듈에 박힌 깨끗한 충돌만 쓴다.

---

## 4. 자동화 로드맵 (다음 단계)

지금은 "모듈을 손으로 끌어다 배치"하지만, 다음처럼 점진적으로 자동화할 수 있다.

| 단계 | 무엇 | 구현 난이도 |
|------|------|------------|
| **A. 스냅 자동정렬** | 모듈을 놓으면 가장 가까운 Exit/Entry 에 자동 흡착하는 `@tool` 에디터 플러그인 | 중 |
| **B. 팔레트 패널** | 에디터 독(dock)에 모듈 버튼 10개 → 클릭으로 커서 위치에 인스턴스 | 중 |
| **C. 데이터 기반 맵** | 맵을 JSON/CSV(`module_id, x, y, scale, color`) 로 기술 → 로더가 런타임/에디터에서 조립 | 중상 |
| **D. 절차적 생성** | 난이도 비트(도입→상승→절정→보상→출구)를 규칙으로 정의 → 모듈 시퀀스 자동 배치 | 상 |
| **E. 검증 자동화** | 조립된 맵을 헤드리스로 실행해 "발 걸림/끊긴 바닥/도달 불가" 자동 점검 | 상 |

권장 순서: **C(데이터 기반)** 를 먼저 깔면 B·D·E 가 모두 그 위에 자연스럽게 얹힌다.
JSON 한 줄 = 모듈 하나. 디자이너가 스프레드시트로 맵을 짜는 그림이 목표.

핵심 원칙은 동일: **무거운 작업(모듈·충돌)은 1회, 반복 작업(조립)은 데이터로.**

---

## 5. 현재 진행 상황 (2026-06-17 기준)

### ✅ 완료
- **stage_2 동굴 충돌 먹통 버그 수정**
  - 원인: `BlackCave1/2/3` 의 거대한 키홀(자기교차) 폴리곤을 SOLIDS 로 구워 동굴 속이 메워짐.
  - 조치: 해당 3개 폴리곤(`AutoCol9/AutoCol6`)을 `build_mode = 1`(SEGMENTS)로 변경.
- **`auto_collision.gd` 개선** (주석에 변경 이유 명시):
  - `build_mode` 인자 추가(동굴형 SEGMENTS 지원).
  - 초소형(노이즈) 폴리곤 제거(`MIN_POLY_AREA`) → 고스트 충돌/발 걸림 예방.
- **`terrain_image.gd` 개선**: `collision_build_mode` export 추가 → 동굴 PNG 도 안전하게 베이크.
- **모듈 키트 1차 완성**: `terrain_module.gd` + Module_01~10 + `맵폴리_베타.tscn`(조립 예시 4모듈).

### ⏳ 확인 필요 (Godot 에디터에서 직접 검증 요망 — 코드로는 검증 불가)
1. `stage_2.tscn` 실행 → 동굴 내부 이동 가능 여부, 바닥/벽 정상 충돌 여부.
   - SEGMENTS 전환 후에도 미세하게 걸리는 곳이 있으면, 해당 동굴은 **모듈 방식으로
     재구성**하거나 충돌을 손으로 단순화하는 것이 정석(설계 원칙 A-1).
2. stage_2 장애물(Thorn/Gear) 작동 확인. ※ `ThornPlatform1/2` 아래의 빈 `Sprite2D2`
   노드는 텍스처 없는 잔재이므로 삭제 권장(동작에는 무해).
3. 각 모듈 씬을 한 번씩 열어 `Visual`/`Collision` 이 자동 생성되는지 확인 후 저장(Ctrl+S).

### 📋 다음 작업
- 모듈에 **실제 아트 PNG** 연결(`Terrain Texture`) — 디자이너 에셋 필요.
- 동굴형 모듈(Cave) 추가 + SEGMENTS 검증.
- 자동화 4장의 **C단계(데이터 기반 로더)** 착수.

---

## 6. 다른 작업자에게 전달하는 구조화 프롬프트

> 아래 블록을 그대로 복사해 새 작업자/세션에 지시하면 동일 작업을 이어갈 수 있다.

```text
[프로젝트] Godot 4.6 색상반전 2D 플랫포머 (repo: color)
[목표] "맵폴리(베타)" 지형 모듈 키트로 맵을 빠르고 다양하게 조립 + 발걸림/충돌 버그 제거.

[반드시 지킬 규칙]
- 비주얼과 충돌을 분리한다(원칙 A): 그림은 울퉁불퉁 OK, 충돌(shape)은 단순·매끈.
- 지형은 scripts/terrain_module.gd 기반 모듈을 쓴다. shape 는 "구멍 없는 단순 폴리곤 1개".
- color_state 프로퍼티명은 절대 바꾸지 않는다(player.gd 가 직접 읽음).
- 동굴/도넛형 지형은 collision_build_mode = SEGMENTS(외곽선만)로. 평지·언덕은 SOLIDS.
- 코드를 고치면 "왜 고쳤는지" 한국어 주석을 날짜와 함께 남긴다(예: ▼ 2026-06-17 ...).
- 모듈 이어붙임: 앞 모듈 Exit 마커 = 다음 모듈 Entry 마커 위치로 바닥 높이를 맞춘다.

[작업 입력]
- 설계 원리: 맵_레벨디자인_시스템.md
- 사용법/현황: 맵폴리_베타_가이드.md (이 문서)
- 기존 코드: scripts/{terrain_module,auto_collision,terrain_image,platform,player}.gd
- 모듈: scenes/modules/Module_01~10_*.tscn, 조립 템플릿 scenes/modules/맵폴리_베타.tscn

[해야 할 일]
1. Godot 에디터에서 stage_2 와 맵폴리_베타.tscn 을 실행해 발걸림/충돌/색판정을 검증.
2. 문제 발견 시 원인을 적고, 모듈 방식 재구성 또는 충돌 단순화로 수정(주석 필수).
3. 모듈에 실제 아트 PNG 를 Terrain Texture 로 연결(충돌은 shape 유지).
4. 새 지형 유형이 필요하면 Module_11+ 로 같은 패턴(단순 폴리곤 1개)으로 추가.
5. 자동화 C단계(JSON/CSV → 모듈 로더) 설계·구현.

[완료 기준]
- 대상 스테이지에서 발 걸림 0, 끊긴 바닥 0, 도달 불가 0.
- 모듈만 조립해 새 스테이지 1개를 10분 내 구성 가능.
```

# 작업 보고서 — Stage 3 (특별 추격 스테이지) 제작

> 프로젝트: color (Godot 4.6.3) / 작성일 **2026-06-22**
> 헤드리스 검증 완료(스크립트 오류 0). 실제 손맛/난이도는 에디터 플레이로 확인 권장.

---

## 0. 요청 → 처리 요약

| 요청 | 처리 |
|------|------|
| 플랫포머처럼 레벨 디자인 | 가로 '탈출' 플랫포머로 설계(점프 물리 기반 좌표 계산) ✅ |
| 1·2스테이지 장애물 **전부** 포함 | 가시(Thorn)·톱니(Gear)·포탈·흑/백 색전환 지형 모두 배치 ✅ |
| 새 장애물: 톱니(좌우/위아래) + 회색 경사로 | 기어에 **상하 이동** 추가 + 좌우/상하 톱니 각각 배치 + 회색 경사로 배치 ✅ |
| 특별 기믹: 왼쪽에서 추격하는 **불의 장벽**(닿으면 사망) | **FireWall** 신규 제작·배치 ✅ |

---

## 1. 레벨 흐름 (왼 → 오른쪽)

```
[시작] 불벽◀◀ ... 플레이어 ▶ 도망
 G1(긴 바닥) → 가시2개(점프) → 좌우톱니(타이밍) → 흑/백 색전환 3칸(W→B→W, 아래는 낙사)
   → 위아래톱니(타이밍) → 회색경사로(색총으로 칠해 등반·256px) → 높은단 → 포탈(클리어)
```

- **불의 장벽**: 시작 1.4초 뒤 왼쪽에서 **175px/s** 로 전진. 세로로 화면 전체를 덮어 점프로 못 넘음 → 계속 전진해야 생존. 닿으면 색 무관 즉시 사망.
- **색전환 구간**: 흰칸은 흰색일 때만, 검은칸은 검정일 때만 밟힘(반대색이면 통과→낙사). `Up`으로 전환하며 통과.
- **회색 경사로**: 그냥 오르면 미끄러짐 → 색총으로 칠한 부위만 등반 가능(256px 상승이라 점프로는 못 올라가게 설계).

---

## 2. 새로 만든 것 / 수정한 것

### 🆕 불의 장벽 (FireWall)
- `scripts/obstacles/fire_wall.gd` — Area2D. `start_delay` 뒤 `advance_speed` 로 오른쪽 전진, 플레이어 닿으면 `die()`. `add_to_group("obstacle")`(색총 무효), 물리콜백 `call_deferred` 준수.
- `scenes/obstacles/FireWall.tscn` — 세로 킬판정 + **코드 아트 불꽃**(그라데이션 Polygon2D + 화염/불씨 CPUParticles2D 2종). 별도 이미지 에셋 불필요.
- 튜닝: `advance_speed`(전진 속도), `start_delay`(시작 유예), `stop_at_x`(특정 x에서 멈춤).

### 🔧 톱니 상하 이동 추가
- `scripts/obstacles/gear_obstacle.gd` — `move_vertical` 불리언 추가. `false`=좌우(기존), `true`=위아래 왕복. (기존 stage_2 기어 동작 영향 없음 — 기본값 false.)

### 🏗️ Stage 3 씬 재작성
- `scenes/world_1/stage_3/stage_3.tscn` — 위 레벨을 좌표로 배치. 플랫폼은 `Platform.tscn`(색 블록)을 `scale`로 크기 조절해 사용.
- 카메라: `camera_zoom = 1.3`(가시영역 약 886×498), 한계 `left=-500`(시작 시 불 보이게)/`top=80`/`right=6950`/`bottom=860`.
- 포탈: 기본 `Portal.tscn` 은 비주얼이 없어 Visual(보라 다이아)+Label 을 붙임. 도착 시 `stage_1` 로 이동(클리어 루프 — 원하면 경로 변경 가능).

---

## 3. 배치한 장애물 좌표 (튜닝 참고)

| 노드 | 위치 | 비고 |
|------|------|------|
| Thorn1/2 | (1609,480)/(2279,480), scale 0.45 | 바닥 위 일부만 차지, 점프로 넘김 |
| GearH (좌우) | (3220,470) move 230, speed 110 | G4 통로 가로지름 |
| GearV (위아래) | (5340,540) move 150, speed 110, `move_vertical` | G5 통로 막음 |
| GraySlope | 원점 (5800,304) | x5800~6312 에서 y560→304 상승(256px) |
| Portal | (6720,232) | 높은 단 위, 도착=클리어 |
| FireWall | front 시작 (-320,0) | 1.4s 뒤 175px/s 추격 |

---

## 4. 검증 (헤드리스, Godot 4.6.3)

| 대상 | 결과 |
|------|------|
| stage_3 직접 실행 220프레임 | **오류 0**, 파일 유지 |
| main.tscn / stage_1 / stage_2 회귀 | 오류 0 (gear 수정 영향 없음) |

### 직접 플레이로 확인/튜닝하면 좋은 것
- **난이도 밸런스**: 불벽 속도(175)·시작유예(1.4s) — 너무 빠르면/느리면 `FireWall` 인스턴스 값 조정.
- **톱니 타이밍**: 좌우/상하 톱니의 `move_speed`/`move_distance` 로 통과 난이도 조절.
- **가시 위치**: scale 0.45 로 배치했으나, 발판 위 정확한 안착은 눈으로 한 번 확인 권장(살짝 뜨면 position.y 조정).
- **회색 경사로 등반**: 추격 중 총으로 칠해 오르는 게 가능한 페이스인지(필요시 슬로프를 짧게/완만하게).

---

## 5. 파일 목록

| 파일 | 내용 |
|------|------|
| `scripts/obstacles/fire_wall.gd` | 신규 — 추격 불벽 로직 |
| `scenes/obstacles/FireWall.tscn` | 신규 — 불벽 씬(코드 아트 불꽃) |
| `scripts/obstacles/gear_obstacle.gd` | 수정 — `move_vertical`(상하 이동) 추가 |
| `scenes/world_1/stage_3/stage_3.tscn` | 재작성 — 추격 스테이지 본체 |

> 모든 신규/수정 코드에 `▼ 2026-06-22` 한국어 주석을 달았습니다.

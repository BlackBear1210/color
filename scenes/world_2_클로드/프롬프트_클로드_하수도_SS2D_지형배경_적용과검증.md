# 클로드 작업 프롬프트 — 하수도 SS2D 지형·배경 적용과 검증

> **2026-09-17 최신 자산 기준 — 반드시 먼저 읽기**
> [오퍼스 현재 지형 디자인 인계](필독_오퍼스_현재지형디자인_인계.md)를 먼저 읽고 실제 `stage_2-1.tscn` 참조를 확인한다.
> 기본 지형은 **sewer_masonry_v02**, 공중 선반은 **sewer_ledge_v03**이다. 이 문서 아래의 v01/옛 WALL 전용 안내는 과거 설명이며 최신 자산 선택에 사용하지 않는다.
> 내부 무늬 2종·맞물림 4종, 2-1 적용 노드/전용 재질, 페인트 연결, 배치 조건, 미검증 항목이 위 문서에 있다. 다른 모델의 대화를 알고 있다고 가정하지 않는다.

작성: 2026-09-14 · 아스트라. 기존 `프롬프트_아스트라_스테이지2_맵제작.md`를 새 지형 작업에 맞춰 구체화한 인계 문서다.
이 파일을 읽으라는 사용자 요청과 함께 클로드에게 전달한다. 문서의 명령 자체가 기존 맵 전체 수정·엔진 실행·커밋·푸시 허가를 새로 부여하지는 않는다.

## 1. 당신이 할 일과 하지 않을 일

당신은 GRAY의 하수도 지형·배경을 실제 맵에 적용하는 담당자다. 목표는 새 SS2D를 배치해도 색칠·회수·접촉 사망·점프 동선이 유지되고, 배경과 플레이 지형이 구분되는 것이다.

**작업의 첫 단위는 사용자와 정한 맵의 짧은 한 구간이다.** 그 구간에서 저장/재로드, 주행, 색칠, 화면 확인을 끝낸 다음 같은 방식으로 확대한다. 시험 씬이 통과했다고 기존 2-1~2-11 전체를 한 번에 교체하지 않는다.

- 대상 맵 번호나 구간이 아직 없다면 그것만 먼저 묻는다. 임의로 stage_2-1을 대상이라고 정하지 않는다.
- 기존 맵 외관 교체 요청이면 시작·도착·동선·장치 연결은 유지한다. 이미 답한 항목은 재질문하지 않는다.
- 새 맵 또는 구조 개편 요청이면 §3의 질문을 먼저 한다.
- 장애물 디자인은 사용자가 따로 진행한다. 이번에는 기존 장애물 기능·연결을 보존하고, 발견한 문제는 기록한다. 새 톱·가시·물 표현을 임의로 전면 개조하지 않는다.
- 스테이지1은 다른 팀원 범위다. 집·스테이지1·챕터 순서를 바꾸지 않는다.
- 사용자가 손으로 고치는 씬을 빌더로 재생성하지 않는다. 편집된 맵의 변경은 노드·속성 단위로 한다.
- 일상적인 복사·정적 확인·허가 범위 내 수정마다 승인을 반복해서 묻지 않는다. 대상/설계가 미정인 경우와 실행 권한이 없는 경우를 구분한다.

## 2. 작업 전에 읽을 자료와 충돌 해결

1. `AGENTS.md`, `CLAUDE.md`, `docs/다음작업_프롬프트.md`의 검사·지뢰밭.
2. `docs/색판정_규칙_2026-08-30.md`, `scripts/스마트월드/색규칙.gd`.
3. `docs/아트기준_스테이지2_v01/흑백지형_게임연결_2026-09-14.md`.
4. 이 문서와 같은 폴더의 `검증자료_하수도_SS2D_2026-09-14/` 및 §11의 검증 범위.
5. 대상 맵, 대상 빌더, `tools/하수도_빌더_공통.gd`의 실제 현재 코드.
6. 새 맵이면 `가이드라인_스테이지2_분기형맵_2026-09-12.md`, `맵 기본 구조 틀.pptx`, `하수도_오브젝트_사용법.md`, `README_맵제작_인계.md`도 읽는다.

이전 아스트라 프롬프트를 그대로 실행하면 다음 충돌이 있으므로 반드시 구분한다.

| 이전 문구/오해 | 이번 새 지형 작업의 적용 기준 |
|---|---|
| WALL 템플릿 4종만 사용 | 기존 맵 지침의 옛 자산 제한이다. 이번 지형은 §4의 sewer_masonry_v01 흑백 재질과 하수도_벽돌지형.gd를 사용한다. 다른 옛 벽돌 재질을 임의로 혼합하라는 뜻은 아니다. |
| 공통 빌더를 쓰면 새 지형도 자동 적용 | 현재 공통 빌더의 지형()은 여전히 기존 WALL 템플릿을 읽는다. 새 템플릿 경로·전용 스크립트가 실제 저장 씬에 들어갔는지 확인해야 한다. |
| 검정 65%를 면적으로도, 테두리 길이로도 계산 | 두 지표는 다르다. 기존 맵 외관 교체에는 비율을 맞추려고 구조를 바꾸지 않는다. 새 맵의 기존 65:35 목표를 사용할 때는 현재 색비율_보고()의 노출 테두리 기준으로 기록하고 면적 수치라고 부르지 않는다. |
| 점프 20칸 = 높이 320 | 공통 빌더 기준 높이는 10칸=160, 거리는 20칸=320이다. |
| 공중 발판은 모두 일방통행 | 새 시험 템플릿은 밑면·옆면도 충돌하는 일반 발판이다. 일방통행이 필요한 구간만 의도적으로 설정하고, 아래에서 통과할 때 색 사망까지 별도로 검증한다. |
| 모든 변은 90px 이상 | 기존 생성기의 제약이다. 새 얇은 발판은 두께 48px로 검증했다. 생성기가 거부하면 제약/템플릿 차이를 설명하고 해당 경로만 조정한다. 게임 동선을 바꾸거나 검사 전체를 끄지 않는다. |
| 아스트라는 엔진을 실행할 수 없다 | 모델 이름에 따른 제한이 아니다. 현재 세션의 사용자 실행 허가와 저장소 규약으로 판단한다. 이 인계의 실행 성공은 미래 세션의 실행 허가를 대신하지 않는다. |
| 이전 문서에 검증됨이라고 적힌 장애물 | 최신 대상 맵에서 그 연결·상태까지 통과했다는 뜻이 아니다. 요청된 기능별로 실제 검사한다. |

최신 사용자 지시가 우선이다. 문서 간 충돌을 발견하면 적용한 해석과 근거를 한 줄 남긴다. 서로 모순되는 숫자를 조용히 섞지 않는다.

## 3. 새 맵을 만들 때만 먼저 확정할 설계

시작 위치 / 도착 위치 / 주 진행 방향 / 외곽 형태 / 규모·목표 시간 / 주요 장치와 난이도를 묻는다. 가로형·좌하단 시작·우상단 출구를 자동 기본값으로 삼지 않는다. 세로 상승·하강·대각선·평행사변형 모두 가능하다.

PPT의 여러 목적지는 선택 후보이며 출구를 전부 놓으라는 뜻이 아니다. 실제 파일을 읽는다. 시작과 진행 방향이 모순되면 사용자에게 그 모순만 확인한다.

설계는 다음 표부터 작성한다.

| 구역 | 들어오는 방향·색 | 플레이어가 알아차릴 단서 | 선택/행동 | 장치 상태 변화 | 도착·복귀 위치 | 실패했을 때 복구 |
|---|---|---|---|---|---|---|
| 예: 갈림길 허브 | 왼쪽, 검정 | 오른쪽 높은 문과 왼쪽 버튼이 보임 | 왼쪽에서 길을 연다 | 문 열림 | 다른 낮은 길로 허브 복귀 | 버튼 유지 또는 재작동 가능 |

갈림길 수를 채우는 것보다 선택 이유를 먼저 쓴다. 막다른 길 끝에는 정보·스위치·지름길 개방 등 보상을 둔다. 매번 같은 길을 되짚게 하지 않는다. 물을 그냥 장식으로 놓고 물 퍼즐이라고 보고하지 않는다.

## 4. 사용해야 하는 자산: 경로와 역할

모든 res://는 이 프로젝트 루트를 기준으로 한다.

### 가져다 쓸 씬

폴더: `res://scenes/테스트/하수도_지형_v01/`

| 형상 | 검정 | 흰색 |
|---|---|---|
| 얇은 공중 발판 | 공중_얇은발판.tscn | 공중_얇은발판_흰색.tscn |
| 두꺼운 공중 발판 | 공중_두꺼운발판.tscn | 공중_두꺼운발판_흰색.tscn |
| 기울어진 공중 발판 | 공중_경사발판.tscn | 공중_경사발판_흰색.tscn |

- `색칠_사망_플레이시험.tscn`: 실제 Player·월드·페인트코어가 있는 시험 씬. 기능 확인용.
- `지형_레이어_시험.tscn`: 배치 관찰용. 이 씬만 보고 직접 플레이를 완료했다고 하지 않는다.
- 큰 연속 바닥·꺾인 벽의 형태는 시험 씬의 `연속_경사_꺾임`, `수직벽_모서리`를 참고한다. 작은 템플릿을 복사한 뒤 점을 늘려 큰 지형으로 만들어도 된다.

### 재질과 스크립트

- `res://assets/textures/smartshape/sewer_masonry_v01/masonry_black.tres`
- `res://assets/textures/smartshape/sewer_masonry_v01/masonry_white.tres`
- 같은 폴더 `black/`, `white/`: fill.tres, top.tres, side.tres, corner.tres.
- `res://scripts/스마트월드/하수도_벽돌지형.gd`: 공용 지형.gd 상속, 아틀라스 UV 준비·흑백 짝 연결·근접 반사.
- `res://shaders/하수도_벽돌_페인트.gdshader`: 전용 페인트/반사 셰이더.
- `res://assets/background/stage_2/layers_v01/far_wall.png`, `mid_arches.png`.
- `res://shaders/sewer_background_fog.gdshader`: 가까운 배경 가장자리를 먼 벽으로 흐리는 셰이더.

**금지:** 재질만 일반 SS2D에 넣고 색칠 연결까지 완료했다고 판단하기. 노드의 스크립트가 하수도_벽돌지형.gd인지 확인한다. 기존 지형.gd를 전용 코드로 통째로 바꿔 다른 맵에 영향을 주지 않는다.

## 5. SS2D를 안전하게 배치·편집하는 순서

1. 원본 템플릿을 보존한다. 해당 맵 전용 변형은 다른 이름의 씬으로 저장하거나 맵 인스턴스에 필요한 속성만 덮어쓴다.
2. 한 덩어리로 연속된 플랫폼은 SS2D 하나로 만든다. 경사→평지→단차를 각각 별도 SS2D 3~4개로 겹쳐 조립하지 않는다.
3. 별도 색칠 단위·움직이는 장치·실제 끊긴 공중 발판은 분리할 수 있다. 분리 이유를 지형표에 쓴다.
4. Node2D Scale은 (1,1), 부모 스케일도 확인한다. 길이/높이는 SS2D 점으로 수정한다. 벽돌 그림 전체를 늘이지 않는다.
5. 점은 교차 없는 닫힌 외곽으로, 시험 템플릿과 같은 시계 방향을 사용한다. 영길이 변, 내부 교차, 겹친 점을 만들지 않는다. SS2D close_shape가 만든 닫힘 점과 임의 중복점을 구분한다.
6. 월드 좌표 표를 만든 뒤 로컬 좌표로 변환한다. 노드 위치를 더한 점을 다시 로컬 점으로 넣어 이중 이동시키지 않는다.
7. StaticBody2D/CollisionPolygon2D와 collision_polygon_node_path 연결을 유지한다. 그림 점만 고치고 예전 충돌 다각형을 남기지 않는다. 저장 후 재로드해서 충돌이 따라오는지 확인한다.
8. 재질을 공유해도 칠한 상태는 인스턴스별이어야 한다. A를 칠한 뒤 같은 템플릿 B가 바뀌면 불합격이다. 공유 .tres를 맵 한 군데 수정 목적으로 변경하지 않는다.
9. 한글 속성 키를 tscn에 직접 쓸 때는 따옴표가 필요하다. 예: `"시작상태" = 1`, `"위치별_판정" = true`. 저장된 텍스트와 런타임 값을 둘 다 확인한다.
10. 새 Resource를 만들면 필요한 local_to_scene/복제 관계를 유지한다. 이미 읽은 노드의 owner를 일괄 재설정하지 않는다. 저장/재로드 시 노드가 두 벌로 생기는지 검사한다.

지형표에는 노드 경로·역할·시작 색·전용 스크립트·재질·외곽점·최대 길이·전체/부분칠·일방통행 여부를 기록한다.

### 헷갈리기 쉬운 두 색 번호

| 용도 | 검정 | 흰색 |
|---|---|---|
| 지형의 시작상태 enum | 1 | 2 |
| 플레이어/총알 색, 현재색() 반환 | 0 | 1 |

숫자 1을 모든 곳에서 흰색이라고 사용하지 않는다. 가능하면 해당 코드의 enum/상수를 쓴다. 흰색 지형은 흰색 재질과 시작상태2를 함께 설정한다. 검정 재질에 상태만 바꿔 에디터 외관을 검정으로 남기지 않는다.

## 6. 게임 규칙과 배치 검산

- 검정 몸은 검정 지형에 안전, 흰 지형 접촉 시 사망. 흰 몸은 반대다. 바닥뿐 아니라 벽·천장·경사·모서리까지 해당한다.
- 안 칠한 일반 지형은 검정이다. 회색 지형을 안전 구간으로 추가하지 않는다. 물·호퍼 등 예외 오브젝트는 각 스크립트의 계약을 확인한다.
- Shift를 밟고 있는 반대색 표면에서 누르면 즉사할 수 있다. 색 전환은 몸 전체가 기존 바닥·옆면·천장에서 떨어지는 구간으로 설계한다.
- 색 경계 틈은 기존 가이드의 80px 이상, 실전 예시 128px를 참고한다. 틈이 있다는 사실만으로 안전하지 않다. 뛰는 동안 머리나 측면이 다른 색에 닿는지 본다.
- 하수도 공통 기준: Player 점프_높이_칸=10.0, 점프_거리_칸=20.0; 월드 카메라_줌=1.0, 치명_낙하거리=1500.0. 씬에 명시한다.
- 높이160·거리320은 최대 운동 특성이다. 실제 배치는 여유를 둔다. 기존 가이드의 단차110, 수평 틈272 이하를 출발점으로 삼고 실제 주행으로 확인한다.
- 정지 머리 공간112 이상과 점프 중 머리 공간은 다르다. 플레이어 키 약96에 점프 높이160이 더해진다. 천장 아래 점프는 발 위치만으로 검산하지 않는다.
- 경사 권장30도 이하. 얇은 발판 끝·경사→평지 접합에서 걸림, 떨림, 순간 사망을 검사한다.
- 낙하1500이라고 모든 구덩이가 안전한 것은 아니다. 바닥 도착 후 돌아갈 수 없고 죽지도 않으면 소프트락이다. 재시작 버튼이 있다는 이유로 정상 동선이라고 처리하지 않는다.
- 전체칠 기본 최대 긴변은576이다. 큰 연속 바닥은 부분칠될 수 있다. 큰 바닥을 잘게 쪼개 규칙을 피하지 않는다. 전체칠이 퍼즐에 꼭 필요하면 그 발판을 독립된 작은 단위로 설계한다.
- 필요 발수는 현재 `필요횟수()`로 읽는다. 문서의 예전 발수나 손으로 계산한 횟수를 실제 값보다 우선하지 않는다.
- E 회수로 발판이 원래 색으로 돌아가므로, 그 위에서 회수하면 의도된 사망이 날 수 있다. 안전 지점에서 회수하는 해법을 설계한다.
- 장치가 칠한 페인트와 플레이어가 쏜 페인트의 회수 규칙은 다를 수 있다. 분사기 페인트를 회수 가능 자원으로 계산하지 않는다.
- 움직이는 승강기는 전용 움직이는발판 기능을 쓴다. 새 StaticBody2D SS2D를 움직였다는 사실만으로 플레이어 탑승 운반이 된다고 보지 않는다.

일방통행을 추가하면 아래에서 충돌이 빠져도 월드의 색 감지 질의는 따로 동작할 수 있다. 아래 통과·옆 접촉·위 착지에 대해 검정/흰색을 각각 검사한다. 사망을 없애기 위해 전역 색 판정을 끄지 않는다.

## 7. 외관 기준과 배경 적용

### 지형

검정은 젖은 어두운 벽돌, 흰색은 같은 계열의 밝은 도장이다. 상면·옆면은 얇고 고정된 각도로 읽히게 한다. 카메라 방향에 따라 측면이 바뀌는 3D 기능을 추가하지 않는다.

반복 fill, 상면, 측면, 모서리 재질을 사용한다. 확대해 접합부 중복 띠·빈 삼각형·이중 테두리·벽돌 비율 변형을 확인한다. 경사 부분의 벽돌 줄이 회전해야 하는지 고정되어야 하는지를 마음대로 매번 바꾸지 않는다. 시험 씬 외관을 기준으로 통일한다.

검정 발판 전체에 상시 흰 선을 더하지 않는다. 근접반사_세기 기본0.10, 반경190이다. 실제 반사는 플레이어 가까운 외곽 약1~2픽셀에만 생긴다. 흰색 판정을 밝기만으로 정하지 말고 현재색/위치색으로 확인한다.

**현재 한계:** 반사 코드는 충돌 다각형의 처음 최대32개 선분을 처리한다. 점이 많은 지형, 뒤집힌 Scale, 복잡한 변형은 이번 검사 범위 밖이다. 32개를 넘었다고 SS2D를 여러 개로 쪼개지 말고, 불필요한 점을 줄이거나 반사 코드 확장과 추가 검증이 필요하다고 보고한다. 대형 맵의 프레임 성능은 아직 측정하지 않았다.

전용 페인트 셰이더는 공용 셰이더 수식에서 파생했다. 공용 페인트 수식이 변경되면 비교해 같이 반영해야 한다. 셰이더 파일에는 한글을 쓰지 않는다.

### 배경

시험 씬의 `먼벽`과 `아치배관` Parallax2D 구조를 참고한다. 각각 scroll_scale (0.25,0.25), (0.60,0.60)이며 플레이 지형은 일반 월드 좌표에 있다. 배경을 지형의 자식으로 넣어 지형과 같이 움직이거나, 반대로 지형을 Parallax2D 안에 넣지 않는다.

현재 먼 그림은 3.2배/위치(-1600,-1000), 가까운 그림은1.8배/위치(-500,-400)다. **시험 씬 전용 배치값이지 모든 맵의 정답이 아니다.** 큰 맵에 그대로 복사하면 끝이 드러날 수 있다. 가까운 그림의 끝은 sewer_background_fog로 먼 벽에 섞인다. 먼 벽 자체는 화면을 채워야 한다.

카메라 중심 이동 범위 + 가장 넓은 줌에서 반시야 + 앞보기/흔들림/전환 여유를 계산한다. 반시야=뷰포트 크기/(2×줌). 1920×1080, 줌1이면960×540이다. 배경은 각 Parallax2D의 캔버스 변환까지 포함해 판단한다. 단순히 맵 폭보다 이미지 폭이 크다고 통과시키지 않는다.

왼쪽/오른쪽/위/아래/네 모서리, 시작/도착에서 정지·점프·낙하·카메라 이동을 확인한다. 평행사변형 외곽의 바깥 삼각형도 포함한다. 실제 내부 방/의도된 구덩이를 외부 공백으로 오인해 막지 않는다.

검정 배경 기둥과 검정 발판이 겹치면 명도만으로 분리되지 않을 수 있다. 이때 배경 위치·안개·명암을 조정한다. 지형 전체에 흰 테두리를 추가해 해결하지 않는다. 배경 회색은 장식이고 안전한 게임 지형을 뜻하지 않도록 충돌을 만들지 않는다.

## 8. 기존 빌더와 새 자산을 연결할 때

공통 빌더 전체를 새 템플릿으로 치환하면 기존 맵 재생성 결과까지 바뀐다. 새 자산을 선택하는 명시적 옵션이나 대상 맵 전용 어댑터를 검토한다. 기존 기본 동작과 검사 경로를 보존한다.

구체적으로 확인할 것:

1. 반환된 모양 노드가 실제 SS2D이며 하수도_벽돌지형.gd를 사용하는가?
2. 시작상태와 흑백 재질이 일치하는가?
3. set_point_array/close_shape 후 그림·CollisionPolygon2D가 같은 영역인가?
4. 기존 검사기의 역할 메타데이터·지형표·좌표 검산이 계속 유지되는가?
5. pack/save 후 새로 load/instantiate한 씬에도 상태·재질·콜리전·NodePath가 남는가?
6. 같은 입력으로 재실행했을 때 좌표나 외곽 여유가 누적되지 않는가?

기존 코드에 owner 재설정 관례가 있어도 저장소의 중복 노드 금지 규약을 우선 확인한다. 실행해 보지 않은 저장 코드를 안전하다고 가정하지 않는다. 새 맵 생성 승인이 기존 편집 맵 재굽기 승인과 같다고 생각하지 않는다.

## 9. 검사는 네 종류로 나누어 실시한다

### A. 파일·저장 검사

참조 파일 존재, 씬 파싱, 숫자 타입, 한글 속성 따옴표, 색 enum, NodePath, 중복 이름/리소스 ID를 확인한다. 저장 후 새 인스턴스를 로드해 값이 살아 있는지 확인한다. 주석만 바꾸고 문제를 고쳤다고 하지 않는다.

### B. 기능 검사

검정/흰색 × 얇음/두꺼움/경사에 대해 같은색/반대색 바닥·벽·아랫면, 비접촉, 코어 색칠, 실제 총알 비행 명중, 회수, 부분칠 안/밖, 동일 템플릿의 다른 인스턴스 보존을 확인한다.
전체 색이 바뀌었다는 로그뿐 아니라 내부와 모서리의 화면도 함께 확인한다. 새 맵의 부모 이동·좌표·스케일이 달라졌다면 원본 템플릿 통과만 인용하지 말고 그 맵의 인스턴스도 검사한다.

### C. 주행/퍼즐 검사

실제 플레이어 물리와 입력을 사용하는 주행을 별도로 실시한다. 시작부터 구간 끝, 잘못된 길→복귀, 색 변경, 오칠→회수, 사망→재시작의 경로를 기록한다. 점프를 좌표 순간이동으로 대체한 테스트를 주행 통과라고 부르지 않는다.
장치가 있으면 초기/작동/해제/재진입/재시작 상태에서 경로가 성립하는지 확인한다. 양동이 소실, 물을 끈 뒤 재가동 불가, 버튼을 밟아도 길이 안 열리는 경우를 모두 기록한다.

### D. 실제 화면 검사

기본 줌1.0, 가장 넓은 실제 줌, 경사/모서리 확대, 흑백 칠하기 전후, 근접 반사 켬/끔, 카메라 외곽을 캡처한다. 이미지를 직접 열어 본다. headless 성공은 그림 검사가 아니다. 효과 켬/끔 비교 시 HUD·애니메이션·카메라 이동 때문에 생기는 차이는 제거한다.

단계별로 PASS / FAIL / 미실행 / 해당없음을 쓴다. 해당없음이면 이유를 쓴다. 테스트 코드가 없거나 실행이 허용되지 않았으면 미실행이다.

## 10. 버그 판정과 수정 순서

| 현상 | 판정/확인 | 금지하는 임시 해결 |
|---|---|---|
| 반대색 지형에 실제 닿아 죽음 | 정상 규칙일 수 있음. 색·접촉점·기대 동선을 확인 | 색 사망 자체 끄기 |
| 같은색인데 죽음 | 인접 반대색/남은 옛 충돌/좌표 불일치/낙하 원인을 분리 | 주변 충돌 전부 삭제 |
| 닿지 않았는데 죽음 | 몸 전체와 벽/천장 접촉점, 판정 오프셋을 재현 | 플레이어 크기를 임의 축소 |
| 내부는 흰색, 모서리는 검정 | 재질 짝·전용 스크립트·셰이더 연결 버그 | 모서리를 흰 Sprite로 덮기 |
| A를 칠하면 B도 바뀜 | 공유 상태/재질 복제 문제 | 테스트에서 B를 빼기 |
| 저장 후 색이 달라짐 | 상태 enum/직렬화/인스턴스 덮어쓰기 문제 | 런타임 매 프레임 강제 색 지정 |
| 머리 걸림/경사 끝 떨림 | 점·충돌·헤드룸·일방통행 검사 | 점프 수치를 전역 확대 |
| 배경 기둥 밑이 일자로 끊김 | 배경 범위/알파 마감 불합격 | 카메라 전체를 좁혀 맵을 숨김 |
| 총알이 맞아도 안 칠해짐 | 필요횟수/부분칠/레이어/대상 부모/코어 연결 검사 | 필요 발수를 전부1로 변경 |
| 구덩이에서 못 나오고 죽지도 않음 | 소프트락 | 사용자가 재시작하면 된다고 보고 |
| exit code0인데 SCRIPT ERROR 있음 | 실행 실패/불완전 검사 | exit0만 인용해 통과 처리 |

재현 기록 양식: 대상 씬·노드 / 시작 위치·플레이어 색 / 입력 순서 / 장치 상태 / 기대 결과 / 실제 결과 / 접촉점·색 정보 / 로그·이미지 / 수정 파일 / 재검사 결과.

진행을 막는 오류·소프트락·색 판정 불일치를 먼저 고치고, 외곽 틈·식별 문제, 장식 미세 조정 순으로 진행한다. 오래된 공용 검사 실패를 새 지형 탓이라고 단정하지 말고, 변경 전 기준선과 실제 의존성을 비교한다. 반대로 기존 실패라고 이름 붙이고 조사 없이 숨기지도 않는다.

## 11. 이번 인계에서 실제로 확인한 것

2026-09-14, Godot4.6.3, 실제 AMD OpenGL Compatibility 렌더러로 재검사했다.

| 범위 | 결과 | 한계 |
|---|---|---|
| 신규 SS2D 6종 기능 검사 | SEWER_RESULT 132/132 PASS, ERROR/FAIL 없음 | 접촉은 위치를 지정한 실제 물리 질의. 완주 주행은 아님 |
| 실제 총알 | 6종 모두 실제 투사체 비행→명중→색 전환→회수 복구 통과 | 마우스 조준/UI 입력 전체 검사는 아님 |
| 그래픽 | GRAPHICS_RESULT failures=0, ERROR 없음 | 배경 자동 판정은 먼 벽의 화면 커버 범위. 캡처도 별도로 확인 |
| 줌/범위 | 기본1.0 캡처, 줌0.55에서 카메라 중심 (0,0), (1950,0), (0,1100), (1950,1100) 확인 | 다른 크기 맵에 자동 보장되지 않음 |
| 근접 반사 차이 | 407픽셀, 화면 bbox(645,471)-(877,473), 최대 채널차20/255 | 고정 시험 위치의 이미지 비교. 모든 가림 배치를 검증한 것은 아님 |

검증자료 폴더의 로그/PNG는 이 실행 결과다. 이미지가 실제 게임 캡처임을 명시한다. 원화나 생성 컨셉 이미지를 실행 증거로 쓰지 않는다.

아직 확인하지 않은 것: 실제 대상 맵 완주·갈림길 복귀, 새로운 장치 조합, 새 일방통행 설정, 32개 초과 선분 반사, 대형 맵 성능, 모든 해상도/카메라 전환. **템플릿 통과와 맵 완성은 다르다.**

이전 공용 회귀 확인에서 남은 문제(이번 재검사의 대상은 아님): 사망 검사21/25, 챕터38/41, 로비 Nil→String 오류, 페인트v4 47개 통과 후 종료 누수. 일부 옛 검사 파일도 없다. 상세는 `docs/작업기록_2026-09-14_아스트라_하수도_지형검증_근접반사_배경.md`. 이 문서를 전달했다고 공용 프로젝트 전체가 깨끗하다고 보고하지 않는다.

## 12. 재현 명령과 안전한 실행

해당 세션에서 사용자가 엔진 실행을 명시적으로 허용한 경우에만 실행한다. 이번 사용자 테스트 요청으로 아스트라는 아래 검사를 실행했다. 문서의 과거 허가를 미래 작업의 포괄 허가로 사용하지 않는다.

프로젝트 루트 PowerShell에서:

```powershell
& 'C:/Users/Public/godot46/Godot_v4.6.3-stable_win64_console.exe' --headless --path . --disable-crash-handler --script 'res://tools/test_하수도_벽돌_색칠사망.gd' --log-file '.godot/sewer_test.log'
& 'C:/Users/Public/godot46/Godot_v4.6.3-stable_win64_console.exe' --path . --disable-crash-handler --script 'res://tools/test_하수도_벽돌_화면.gd' --log-file '.godot/sewer_graphics.log'
```

두 번째는 실제 그래픽 창을 띄우고 자동 종료한다. 로그에 ERROR, SCRIPT ERROR, FAIL이 없는지 보고 마지막 결과 줄도 확인한다. 시험 도중 종료했거나 제한 프레임으로 강제 종료되었으면 완료 로그를 확인하기 전에는 통과시키지 않는다.

맵 적용 후 `레벨검사.gd`와 `하수도_주행검사.gd`는 현재 코드의 인자와 공략 등록법을 읽고 해당 맵을 명시해서 실행한다. 새 맵 공략이 등록되지 않았는데 도구가 종료했다고 완주 성공으로 해석하지 않는다. 공략이 없으면 실제 입력 기반 공략을 추가하거나 주행 미실행으로 보고한다.

크래시가 발생하면 같은 실행을 반복해 오류창을 계속 띄우지 않는다. 로그를 보존하고 최소 재현으로 원인을 줄인다. Godot 프로세스 전체를 강제 종료하지 않는다. 사용자의 편집기와 테스트 프로세스를 구분한다.

## 13. 제출물과 완료 기준

제출물: 수정 대상 씬/스크립트 목록, 지형표, 변경 전후 캡처, 기능/주행/화면 결과표, 남은 버그·미실행 이유, 작업기록.

- [ ] 대상 맵/구간과 사용자 요청 범위가 명확하다.
- [ ] 한 연속 플랫폼은 SS2D 하나다. 원본 템플릿을 무심코 변경하지 않았다.
- [ ] 전용 스크립트·흑백 재질·시작상태·위치별 판정이 일치한다.
- [ ] 저장 후 재로드해 값·노드 수·충돌을 확인했다.
- [ ] 몸 전체의 접촉·부분칠·실제 총알·회수·독립 인스턴스를 확인했다.
- [ ] 적용 구간을 실제 플레이어 물리로 통과했고 오칠/복귀/재시작도 확인했다.
- [ ] 줌·카메라 끝·반사·흑백 전환 화면을 직접 열어 확인했다.
- [ ] 실행 안 한 기능을 통과로 쓰지 않았다. 기존 공용 실패도 구분했다.
- [ ] git diff에 사용자/다른 팀원의 삭제·재생성·불필요한 공용 변경이 없다.
- [ ] docs/작업기록_<날짜>_Claude_<대상>_지형배경.md를 작성했다.

보고 예시: “대상 A구간의 지형 8개와 배경을 교체했습니다. SS2D 기능 검사 n/n, A→B 주행 및 오칠 복귀 통과. 오른쪽 카메라 끝에서 배경을 확인했습니다. 일방통행은 이 구간에 없어 해당없음. 공용 로비 오류는 미수정입니다.”
실제로 관찰한 수치와 경로로만 채운다. 위 예시의 숫자를 결과로 복사하지 않는다.

## 2026-09-16 필수 주의 — 기본 지형 윗면의 바깥 모서리

- 기본 지형은 `res://scenes/지형/하수도/하수도_기본지형_검정.tscn` / `하수도_기본지형_흰색.tscn`을 사용한다. 집의 TEMPLATE_WALL_SOLID에 외형만 덮어씌우지 않는다. 공중 선반은 같은 폴더의 하수도_공중선반_* 프리팹을 쓴다.
- **윗면을 직사각형 띠처럼 수직으로 잘라 끝내면 안 된다.** 바깥 모서리에서는 뒤쪽 가장자리가 안으로 들어가고 앞쪽 가장자리는 지형 폭까지 이어지는 얕은 사다리꼴로 마감한다. 좌우 끝에 짧은 대각선이 보여야 한다.
- 공통 `sewer_natural_platform.gdshader`가 `ground_platform=true` 및 실제 `ground_edges`로 처리한다. 새 기본 지형에도 이 재질·하수도_자연발판.gd를 유지한다. 개별 씬에 장식 사각형을 덧붙여 가리지 않는다.
- 현재 깊이는 발끝 기준 위아래 약 4px, 뒤쪽 끝의 안쪽 여유는 최대 약 6px다. SS2D 점으로 길이를 조정하고 Scale=1을 유지한다. 작은 지형은 길이에 비례해 여유가 제한된다. 사망·충돌 폴리곤을 대각선 그림에 맞춰 임의로 깎지 않는다.
- 이 처리는 **볼록한 바깥 모서리**만 대상으로 한다. 일직선 중간의 추가 점에는 끝마감을 만들지 않는다. 계단의 오목한 안쪽 접합부(사용자 노랑/파랑 표시)는 별도 미완료 항목이다. 이번 처리를 안쪽 모서리에 복제하지 않는다.
- 점을 편집한 뒤 `ground_edges`가 실제 폴리곤과 맞는지 확인한다. 에디터에 저장된 미리보기 재질의 좌표가 오래되면 런타임과 외형이 다를 수 있으므로 저장/재로드 전후를 비교한다.
- 화면 점검: 검정/흰색, 좌우 바깥 모서리, 짧고 긴 지형, 계단 위 돌출부, 경사 끝을 확인한다. 윗면이 사다리꼴인지, 앞면 아래까지 삼각형 구멍이 뚫리지 않는지, 중간 반복 경계에 홈이 생기지 않는지 확인한다.
- 기능 점검: 중앙/양끝 착지, 색칠/회수, 같은색 생존·반대색 사망을 확인한다. 시각적 가장자리와 충돌면의 작은 차이도 양끝에서 확인해야 한다. 엔진 실행 허가 없이 Godot을 실행하지 않으며, 정적 검사만 했다면 실행/시각 검증 완료라고 쓰지 않는다.

### 2026-09-16 추가 — 벽돌 이음새 기준 배치
- 기본 지형 윗면의 사다리꼴 끝마감을 유지하며, 상단 마감돌이 양 끝에서 애매하게 일부만 잘리는 배치는 금지한다.
- 새 맵은 마감돌 한 개의 기준 폭(현재 약 46.6px)의 정수 배를 우선 고려한다. 점프 거리와 안전 착지 폭이 우선이며, 미관 때문에 기존 충돌면을 임의 이동하지 않는다.
- 공통 셰이더는 연속 직선 구간 길이에 가장 가까운 정수 개의 마감돌을 배치한다. 양 끝은 이음새로 맞추고 돌 너비를 조절한다. 매우 짧은 구간은 너비 조절이 커질 수 있으므로 화면을 확인한다. 같은 직선의 중간 SS2D 점은 합쳐 계산하며, 기울기가 바뀌면 새 구간이다. Scale=1 유지.
- 검정/흰색, 좌우 끝, 짧고 긴 구간에서 마지막 마감돌이 통째로 보이는지, 사다리꼴과 앞면 연결이 자연스러운지 확인한다. 같은 돌 반복이 눈에 띄면 전용 마감돌 변형 아트를 추가한다.
- 자동 정렬 대상은 윗면과 바로 아래 앞쪽 마감돌 한 단이다. 본체의 엇갈린 벽돌 줄까지 하나의 수직선에서 모든 이음새를 맞출 수는 없다. 본체는 줄별 반장/모서리 벽돌 아트가 별도로 필요하며 완료했다고 오해하지 않는다.
- 엔진 실행 없이 정적 확인만 했으면 시각·착지·색칠·사망 검증 완료라고 쓰지 않는다.

### 2026-09-16 추가 — 벽 접합 및 가려진 윗면
- 하수도_자연발판.gd는 같은 부모 노드 아래의 SS2D 점을 비교해 다른 지형에 가려진 윗면을 자동으로 숨긴다. 기본 지형은 한 지형 부모 아래에 배치한다. 다른 부모 아래의 지형은 현재 자동 접합 범위에 포함되지 않는다.
- 지형 점/위치/표시 여부가 바뀌면 최대 0.5초 간격으로 확인하고, 변화가 있을 때만 접합 재질을 갱신한다. 에디터와 실행 모드 모두 적용한다. 미리보기의 ground_edges도 실제 점으로 갱신한다.
- 벽에 닿는 윗면은 자기 선분의 끝에서 멈춘다. 지형 내부에 숨은 윗면은 마감돌과 사다리꼴 잘라내기를 함께 끈다. 본체 벽돌을 유지하고 배경이 보이는 구멍을 만들지 않는다.
- 별도 장식 벽처럼 윗면 전체가 필요 없으면 인스펙터에서 윗면표시=false로 지정한다. 가려진 일부 구간만 없애려는 경우는 자동 접합을 사용한다.
- 색이 다른 이웃도 접합 대상으로 본다. 충돌/색칠/사망 규칙은 변경하지 않는다. 그림이 이어져 보여도 색 경계가 안전해지는 의미는 아니다.
- 주변 가림 선분 256개 또는 자체 선분 64개 한도를 넘는 기본 지형은 잘못된 내부 마감 대신 윗면 전체를 숨긴다. 복잡한 지형은 이를 확인하고 배치를 정리한다. 렌더링 성능은 실행 후 확인한다.
- 검증: 계단의 빨강/노랑 안쪽 접합부, 하늘색처럼 다른 지형 아래에 맞닿은 윗면, 이동/크기 수정 후 재계산, 검정/흰색, 양끝 착지와 색칠 경계를 확인한다. 현재 정적 확인만 했으며 엔진 실행/화면/성능/플레이 검사는 별도로 필요하다.

### 2026-09-16 확정 — 기본 지형의 노출 옆면 마감
- 사용자가 작은 돌출부 시험을 승인했다. 이후 하수도 기본 지형은 검정/흰색 모두 `옆면마감=true`를 기본으로 사용한다. 고정 사각형 시험 범위와 `시험_옆면마감`은 더 이상 사용하지 않는다.
- 실제 SS2D 외곽 중 수직 또는 수직에 가까운 옆선을 따라 끝돌을 배치한다. 공중 선반·보행 경사·밑면에는 이 기본 지형 옆면 마감을 복제하지 않는다.
- 같은 부모 아래의 이웃 SS2D에 닿거나 가려진 구간은 끝돌·음영·줄눈 홈을 제외한다. 일부만 가려지면 그 부분만 제외한다. 검정/흰색 색 경계도 이웃이 닿아 있으면 틈이나 추가 옆면을 만들지 않는다.
- 일직선의 중간 점은 합쳐 계산하여 점을 추가했다고 줄눈이 다시 시작되지 않게 한다. 상단 마감과 연결되고 실제 옆선이 끝나는 바닥에서 종료되어야 한다.
- 비교/예외 처리 시 노드의 `옆면마감`을 끌 수 있다. `윗면표시`와는 별도 옵션이다. 64/256 선분 제한을 넘으면 옆면도 숨기므로 복잡한 맵에서 누락 여부를 확인한다.
- 검수: 노출 좌우면, 전체/부분 접합면, 색이 다른 이웃, 계단 바닥 접점, 긴 벽, 추가 점/크기 변경 후 연결을 확인한다. 정적 가림 계산이 맞아도 실제 렌더링·성능·색칠/사망 검증은 별도로 한다.

## [2026-09-16 필수] 내부 반대색 벽돌 / 불규칙 3색 구간

최신 사용자 확정: 1번 검정 벽 안 흰 벽돌과 흰 벽 안 검정 벽돌 모두 사용. 2번 검–흰–검, 흰–검–흰 모두 사용하며 가로/세로 모두 불규칙한 줄눈 경계. 3번 바닥 잔해는 보류.

제작 전에 `docs/하수도_흑백벽돌_모듈_사용법.md`를 읽는다. 재사용 모듈은 `scenes/지형/하수도/색조합/`, 비교 씬은 `scenes/테스트/하수도_흑백벽돌_비교.tscn`이다.

- 내부 무늬는 장식 합성이다. 별도 콜리전을 추가하지 않는다. 모든 외곽에서 24px 이상 안쪽에 배치한다. 페인트보다 앞에서 합성하여 색칠·회수에 따라 덮이고 복구되어야 한다.
- 내부벽돌 전용 스크립트/바탕색/시작상태=0 설정을 유지한다. 일반 지형의 시작상태만 0으로 바꾸는 방식으로 흰 지형을 만들지 않는다. 에디터 미리보기 재질과 런타임 무늬 배열을 함께 갱신한다.
- 실제 경계는 SS2D 3개로 구성한다. 같은 줄눈 경로를 양쪽에 역순으로 공유한다. 반 장~두 장의 불규칙한 폭을 섞는다. 가로도 적용하며 지퍼 반복과 회색 블렌딩은 금지한다.
- 자식 원점/UV 기준/Scale 1을 유지한다. 공유선 편집은 양쪽 동시에 한다. 같은 부모가 아닌 지형과의 접합은 자동 마감 가림을 가정하지 말고 직접 확인한다.
- 정적 영역 검사는 통과했지만 엔진 실행 검증은 아직 하지 않았다. 문서의 색칠·회수·접촉사망·이음새 통행·성능 검수를 완료하기 전 테스트 완료라고 보고하지 않는다.
## [2026-09-17 우선 적용] 색조합 모듈 배치와 이동 규칙

이 절은 옛 문서의 ‘모든 점 16px 격자’, ‘최소 변 90px’, ‘모든 흑백 경계에 틈’을 아래처럼 구분한다. 이동 경로의 기준과 벽 내부 줄눈 경계를 혼동하지 않는다.

### 설계 수치와 우선순위

- Player에 점프 높이 10칸(160px), 점프 거리 20칸(320px)을 명시한다. 카메라 줌 1.0, 치명 낙하거리 1500px도 씬에 명시한다.
- 권장 올라가는 단차 ≤110px, 가로 간격 ≤272px, 머리 공간 ≥112px. 128px 단차는 기존 설계의 예외이며 포물선·머리 충돌을 실제 플레이로 확인한다. 장식 작업을 이유로 기존 단차를 임의 변경하지 않는다.
- 플레이어 폭44px·키96px 기준으로 실제 착지 구간을 잰다. 외형 마감의 튀어나온 돌 끝을 착지 폭에 포함하지 않는다.
- 착지 직후 반대색 벽에 몸이 닿지 않는지 확인한다. ‘총 점프거리 320’만 보고 320px 틈을 만들지 않는다.
- 기존 장애물 앞뒤 준비 공간, 체크포인트, 레버 시야, 복귀 경로를 보존한다. 장식과 새 색 경계로 통로를 좁히거나 장애물의 색을 가리지 않는다.

### 1번 내부 무늬

검정 안 흰 벽돌과 흰색 안 검정 벽돌은 기존 지형 내부에서만 사용한다. 모든 외곽·구멍·오목한 경계에서 최소24px 이상 떨어뜨린다. 경계 마감 깊이(최대24px)와 겹치지 않도록 필요하면 더 띄운다. 총알 명중·페인트 시드는 기존 지형 하나에서 처리한다. 별도 충돌·사망 영역을 얹지 않는다.

무늬의 양옆과 행 경계는 기존 텍스처의 실제 줄눈 좌표를 따른다. 16px 격자에 장식 무늬를 강제로 맞추지 않는다. 무늬는 길 안내나 안전 발판이 아니므로 반복해서 빽빽하게 깔지 않는다. 우선 한 시야에 1~2개 덩어리로 비교하고 길/장치를 가리면 줄인다(새 난이도 규칙이 아니라 시각 배치 권장값).

### 2번 실제 맞물림

- 검–흰–검과 흰–검–흰 모두 허용. 가로·세로 경계에 반 장~두 장 깊이의 불규칙한 맞물림을 쓴다.
- **벽 내부의 공통 경계:** 틈0·겹침0. 양쪽은 동일한 경로를 역순 공유한다. 줄눈 때문에 16px 배수나 최소90px 변 규칙에서 벗어날 수 있다. 이것은 벽 내부 색 분할에 한정하며, 통행 외곽을 짧은 톱니 모양으로 바꿔도 된다는 뜻이 아니다.
- **플레이어가 지나가는 흑백 표면 전환:** 기존 ≥80px 색 전환 틈/도약 계획을 유지한다. 맞물림의 돌출부까지 실제 접촉색으로 계산한다. 틈을 메우거나 반대색 돌을 착지점으로 밀어 넣지 않는다.
- 배치 전 외곽 실루엣, 지형 합집합 면적, 공유 경계 끝점, 플레이어가 닿는 색을 기록한다. 내부만 바꾸는 작업은 이 값들이 보존되어야 한다.
- SS2D 점·저장 콜리전·실행 콜리전은 같은 폴리곤을 사용한다. 경계 위에 단순 이미지로 다른 색을 덮지 않는다.
- 이웃 가림은 같은 부모의 SS2D끼리 작동한다. 다른 모듈 부모 아래에 숨겨 두고 자동 마감이 될 것이라 가정하지 않는다.

### 2-1 적용 위치와 검사

현재 stage_2-1에는 A_바닥 내부의 흰 벽돌, A_갤러리_슬래브 내부의 검정 벽돌을 적용했다. 기존 바닥/갤러리/샤프트 ㄱ자 경계에 더해 샤프트 흰 벽과 아래 검정 채움의 내부 경계도 맞물린다. 벽 내부 x약2750~3040에 무늬가 있으므로 기존 스크린샷의 복도·레버 주변 시야에서 확인한다.

추가 도구 `tools/apply_stage21_color_inlays.py`는 적용 표식이 있으면 재실행 시 덮어쓰지 않는다. 일반 맵 빌더를 실행해 편집된 씬 전체를 재생성하지 않는다.

정적 검사: `tools/test_stage21_masonry_joint.py` + `tools/test_sewer_color_modules.py`. Godot 허가 후 실행 화면, 두 색 페인트·회수·사망, 기존 점프 구간과 레버·물줄기 퍼즐의 동작을 별도로 확인한다. 정적 검사 통과를 플레이 통과나 FPS 개선의 근거로 사용하지 않는다. 3번 잔해는 계속 보류한다.
## [2026-09-17 사용자 확정] 물 너비로 우회 허용 구간과 필수 퍼즐 구간을 구분

### 목적

흰 바닥 위의 검정 물을 ‘흰색으로 점프 → 공중에서 검정으로 전환 → 물 통과 → 흰색으로 재전환 → 착지’로 넘어갈 수 있다. 검정 바닥/흰 물도 대칭이다. 이것은 허용 구간에서는 의도한 기술 플레이지만, 레버 등의 필수 퍼즐을 건너뛰게 하면 설계 실패다. 모든 물을 넓힐 필요는 없다.

### 배치 전에 물마다 적을 항목

`노드명 / 물색 / 바닥색 / 우회 허용 여부 / 필수 장치와 작동 결과 / 설정 너비 / 실제 유효 판정 폭 / 출발·착지 높이 / 위아래 우회·중간 발판 / 검증 상태`

- **우회 허용형:** 지금과 같은 얇은 물을 유지할 수 있다. 점프 중 두 번 색을 바꾸는 기술을 학습하거나 빠르게 지나가는 구간으로 쓴다. 레버가 있다면 편의 경로 또는 다른 기능이 있어야 한다.
- **필수 퍼즐형:** 물 너비와 주변 지형을 함께 설계하여, 장치를 작동하기 전에는 공중 색 전환만으로 통과할 수 없게 만든다. 필수 장치에 도달하는 길은 물 차단 이전에도 가능해야 하며, 작동 후에는 분명한 통과 경로가 생겨야 한다.

### 너비 산정

1. 현 기준 점프 거리320px·높이160px, 플레이어 폭44px를 사용하되 실제 출발/착지 높이와 이동 기믹을 먼저 확인한다. 낮은 곳으로 떨어지며 이동하면 수평 도달 거리가320px보다 길 수 있다. 272px는 일반 점프 배치의 여유 기준이지, 우회 불가능 판정 기준이 아니다.
2. **같은 높이·추가 이동 수단 없음** 조건에서는 필수 차단 물의 실제 연속 판정 폭이320px를 충분히 넘도록 설계한다. 설정 너비448px(28칸)는 초기 설계 후보로 사용할 수 있으나 보장값은 아니다. 부풀린 그림 크기만으로 통과 불가능이라 판정하지 않는다.
3. 물은 프레임마다 실루엣 판정이 달라진다. 크기.x가448이어도 몸이 지나는 높이의 물줄기는 훨씬 좁을 수 있다. 모든 애니메이션 프레임, 점프 궤적, 판정_여유를 포함하여 가장 쉽게 통과할 수 있는 상황을 검증한다. 플레이어 몸 전체가 물에 들어갔다 나올 때의 색 전환 가능 시점도 확인한다.
4. 물 아래 빈틈, 위쪽 출구를 뛰어넘는 길, 높은 발판에서 대각으로 뛰는 길, 중간 안전 발판, 이동 플랫폼, 낙하 우회가 있으면 폭만 늘려서는 막히지 않는다. 실제 통행 높이를 물이 연속적으로 차단하도록 배치한다.
5. 그림을 가로로 과하게 늘려 물결이 뭉개지면 넓은 폭포용 시각 자산이나 별도 구성으로 만든다. 충돌만 넓혀 보이지 않는 물벽을 만들지 않는다. 여러 물을 나란히 둘 경우 이음새·애니메이션 순간 틈·색 혼합·계산 비용을 검증한다.

### 페인트와 장치까지 포함한 우회 검토

- 바닥을 물과 같은 색으로 칠하고 걸어가기, 물/웅덩이의 혼합으로 회색 만들기, 가동 중인 플랫폼을 이용하기도 검사한다. ‘점프를 못 넘는다’와 ‘레버가 반드시 필요하다’는 다르다.
- 이런 해법도 허용할지 먼저 설계표에 기록한다. 레버가 정말 필수라면 기존 물의 페인트 제거 규칙이나 공간 배치 등 게임에 이미 있는 규칙으로 차단되는지 확인한다. 레버를 강제하려고 색 전환 기능 전체를 끄거나 근거 없이 색칠 불가 지형을 추가하지 않는다.
- 장치 작동 후 물의 그림뿐 아니라 유체 판정/총알 감지도 꺼져야 한다. 꺼진 물이 허공에서 총알을 터뜨리는 알려진 문제는 별도 수정·검증 대상이다. 보이지 않는 물에 총알이나 플레이어가 막히면 통과로 인정하지 않는다.
- 필수 레버를 물 건너편에만 두지 않는다. 물을 넓힌 뒤 레버·체크포인트·도약 준비 구간이 물 안으로 들어가지 않았는지 확인한다. 사망/재시작 시 장치 상태도 일관되어야 한다.

### 완료 기준

필수형은 장치 미작동 상태에서 양방향 점프·빠른 색 전환 두 번·높은 곳에서 낙하·중간 발판·색칠 우회를 실제로 시도해 실패하고, 작동 후 의도 경로가 성공해야 한다. 허용형은 반대로 기술 통과가 가능함을 확인한다. 엔진 실행 없이 너비 숫자만 변경했다면 ‘설계/정적 확인 완료, 실제 우회 차단 미검증’이라고 보고한다.

기존 씬의 물을 일괄448px로 바꾸지 않는다. 위 표로 분류하고 각 퍼즐의 도달성·시각 품질·성능을 확인한 뒤 해당 물만 조정한다.
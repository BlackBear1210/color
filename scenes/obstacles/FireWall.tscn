[gd_scene format=3 uid="uid://dnpu8j7j150to"]

; ▼ 2026-06-22 신규(stage_3): 추격하는 불의 장벽.
;   구조: Area2D(fire_wall.gd) + 세로 킬판정 + 그라데이션 비주얼 + 화염/불씨 파티클(코드 아트, 아트 PNG 불필요).
;   front(불의 앞면) = 원점 x=0. 닿으면 사망. 스크립트가 오른쪽으로 전진.

[ext_resource type="Script" path="res://scripts/obstacles/fire_wall.gd" id="1_fire"]

[sub_resource type="RectangleShape2D" id="kill_shape"]
size = Vector2(200, 1800)

[sub_resource type="Gradient" id="grad_flame"]
offsets = PackedFloat32Array(0, 0.5, 1)
colors = PackedColorArray(1, 0.85, 0.35, 0.9, 1, 0.4, 0.05, 0.7, 0.35, 0.04, 0, 0)

[sub_resource type="Gradient" id="grad_ember"]
offsets = PackedFloat32Array(0, 1)
colors = PackedColorArray(1, 0.85, 0.4, 0.95, 1, 0.45, 0.1, 0)

[node name="FireWall" type="Area2D"]
collision_layer = 0
collision_mask = 1
monitorable = false
script = ExtResource("1_fire")

; 킬 판정: front(x=0)에서 왼쪽으로 뻗는 세로 막대 → 앞면에 닿으면 사망
[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
position = Vector2(-100, 600)
shape = SubResource("kill_shape")

; 비주얼: 왼쪽(어둡고 옅음)→front(밝은 주황) 가로 그라데이션 세로 띠
[node name="FireBody" type="Polygon2D" parent="."]
color = Color(1, 0.45, 0.05, 1)
polygon = PackedVector2Array(-280, -300, 30, -300, 30, 1500, -280, 1500)
vertex_colors = PackedColorArray(0.5, 0.04, 0, 0.12, 1, 0.5, 0.08, 0.78, 1, 0.5, 0.08, 0.78, 0.5, 0.04, 0, 0.12)

; 화염: front 근처에서 위로 솟구치는 큰 불꽃
[node name="Flames" type="CPUParticles2D" parent="."]
position = Vector2(-45, 600)
amount = 90
lifetime = 1.0
emission_shape = 3
emission_rect_extents = Vector2(70, 880)
direction = Vector2(0, -1)
spread = 22.0
gravity = Vector2(0, -260)
initial_velocity_min = 130.0
initial_velocity_max = 300.0
scale_amount_min = 4.0
scale_amount_max = 9.0
color_ramp = SubResource("grad_flame")

; 불씨: 더 작고 빠르게 올라가는 밝은 입자
[node name="Embers" type="CPUParticles2D" parent="."]
position = Vector2(-25, 600)
amount = 55
lifetime = 1.4
emission_shape = 3
emission_rect_extents = Vector2(55, 880)
direction = Vector2(0, -1)
spread = 30.0
gravity = Vector2(0, -180)
initial_velocity_min = 80.0
initial_velocity_max = 210.0
scale_amount_min = 1.5
scale_amount_max = 3.5
color_ramp = SubResource("grad_ember")

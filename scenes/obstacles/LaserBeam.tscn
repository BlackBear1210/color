[gd_scene load_steps=3 format=3]

; 색 레이저 빔 장애물. 같은 색이면 통과, 반대 색이면 닿는 즉시 사망.
; 빔은 이 노드의 원점에서 +Y(로컬 아래) 방향으로 beam_length 만큼 뻗는다.
; 가로로 누운 빔을 만들려면 인스펙터에서 rotation_degrees 를 90 으로 주면 된다.

[ext_resource type="Script" path="res://scripts/obstacles/laser_beam.gd" id="1_laser"]

[sub_resource type="RectangleShape2D" id="RectangleShape2D_kill"]
size = Vector2(12, 400)

[node name="LaserBeam" type="Node2D"]
script = ExtResource("1_laser")
color_state = 0
beam_length = 400.0
beam_width = 12.0

[node name="Visual" type="Polygon2D" parent="."]
color = Color(0.05, 0.05, 0.05, 0.95)
polygon = PackedVector2Array(-6, 0, 6, 0, 6, 400, -6, 400)

[node name="DeathDetector" type="Area2D" parent="."]
collision_layer = 64
collision_mask = 0
monitoring = false
monitorable = true

[node name="CollisionShape2D" type="CollisionShape2D" parent="DeathDetector"]
position = Vector2(0, 200)
shape = SubResource("RectangleShape2D_kill")

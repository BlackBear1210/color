# 파일 분류 인덱스 (2026-08-17 · 23차 갱신)

> ★23차 변경(2026-08-17): **카메라 공간(굴뚝 심리스 전환)** 과 **지형 뿌리내리기**.
> 상세: `작업기록_2026-08-17_도형_카메라공간_굴뚝심리스_지형뿌리내리기.md`
>
> | 파일 | 소유 | 설명 |
> |---|---|---|
> | `scripts/proto/카메라_공간.gd` | 도형 | ★`카메라공간`(Area2D). 굴뚝에 들어가면 리밋을 좌우 벽에 맞춰 좁히고 줌 인. 틈새빛 연출 |
> | `scripts/스마트월드/카메라공간배치.gd` | 도형 | `○○벽_좌`/`○○벽_우` 를 실측해 카메라 공간을 자동 배치 (멱등) |
> | `tools/카메라공간_배치.gd` | 도형 | 위 배치기를 구운 씬에 돌리는 러너 (`--조사` / 적용) |
> | `tools/지형_다듬기.gd` | 도형 | ★공중에 뜬 지형을 찾아 기둥·버팀대·덩어리꼬리로 뿌리를 내린다 (기본 콜리전 없음) |
> | `tools/test_카메라공간.gd` | 도형 | 굴뚝 카메라 자동 검사 15항목 |
> | `docs/카메라_공간전환_가이드.md` | 도형 | 카메라 공간 사용·확장·튜닝 한계 |
> | `docs/레벨디자인_4스테이지_2026-08-17_도형.md` | 도형 | 집 안 / 하수도 / 도시 / 스팀펑크 상세 설계 |
>
> ⚠ 2026-08-07 · 08-08 작업(경계 재구축 · 챕터 전환 · 사망 판정 · 구역 카메라)은
> 이 인덱스에 반영되지 않았다. 해당 `작업기록_2026-08-07/08_*.md` 를 볼 것.

> ★22차 변경(2026-08-06): **스마트월드가 본편 라인이 되었다.**
> · F5 진입점 = `scenes/스마트월드/스마트월드_1-1.tscn` (구 `lobby.tscn`)
> · `스마트월드_테스트.tscn` → **`스마트월드_1-1.tscn`** 개명 · **`스마트월드_1-2.tscn`** 신설
> · 낙하 사망 / 통로 연결 / 레벨 검사기 / 신규 장애물 3종 (아래 표 참고)
> · **저장소 안의 프로젝트 사본 2벌(추적 파일 1,005개 = 전체의 61% · 28MB)을 밖으로 뺐다**
>   → `C:\Users\김도형\Documents\GitHub\_color_정리백업_2026-08-06\`
> 상세: `작업기록_2026-08-06_스마트월드_메인화_낙하사망_통로연결_1-2제작.md`
>
> **22차 신규 파일**
>
> | 파일 | 소유 | 설명 |
> |---|---|---|
> | `scenes/스마트월드/스마트월드_1-1.tscn` | 생성 | ★F5 진입점. 자연→물가. `build_스마트월드_1-1.gd` 산출 |
> | `scenes/스마트월드/스마트월드_1-2.tscn` | 생성 | ★폐수로. 물가→판자촌. 일자 바닥·9,200px. `build_스마트월드_1-2.gd` 산출 |
> | `scripts/스마트월드/낙하_감시.gd` | 도형 | 낙하 **거리** 기반 즉사 + 경고 비네트 |
> | `scripts/스마트월드/장면전환.gd` | 도형 | 암전→씬교체→점등 (VINE 식 · 잉크 와이프 아님) |
> | `scripts/스마트월드/연결통로.gd` | 도형 | 걸어 들어가는 굴. 바닥·천장·뒷벽을 스스로 짓는다 |
> | `scripts/스마트월드/도약대.gd` | 도형 | 밟으면 418px 위로. 수직 레벨용 |
> | `scripts/스마트월드/무너지는바위.gd` | 도형 | 밟으면 부서짐 · **칠하면 단단해짐** |
> | `scripts/스마트월드/색레이저.gd` | 도형 | 빔과 다른 색이면 사망. **물감 0발로 통과 가능** |
> | `tools/지형공통.gd` | 도형 | 두 빌더의 공용 부품(재질·점·노드·owner) |
> | `tools/build_스마트월드_1-1.gd` | 도형 | 구 `build_스마트월드.gd` 대체·개편 |
> | `tools/build_스마트월드_1-2.gd` | 도형 | 1-2 생성기 |
> | `tools/레벨검사.gd` | 도형 | ★실제 물리로 도달성·소프트락·치명낙차 검사 |
> | `tools/test_낙하사망.gd` | 도형 | 낙하 사망·체크포인트 자동 검사 (11항목) |
> | `tools/test_통로전환.gd` | 도형 | 통로 연결·씬 전환 자동 검사 (19항목) |
> | `assets/textures/smartshape/지형_거친암반.tres` | 생성 | center2/left2/right2 타일 재질 (1-2 지형) |
> | `assets/textures/smartshape/지형_덩어리.tres` | 생성 | allaround 타일 재질 (공중 발판·선반) |
>
> **22차 삭제 파일**: `tools/build_스마트월드.gd` · `scenes/스마트월드/스마트월드_테스트.tscn`
> (둘 다 위 신규 파일로 대체 · 백업은 `_color_정리백업_2026-08-06/씬백업/`)

> ★19차 변경(2026-07-30): **타일맵 페인트 v4** 신설 — TileMapLayer 로 맵을 찍기만 하면
> 아틀라스 좌표로 흑/백을 인지해 플랫폼을 자동 검출하고 색칠·회수가 된다.
> v3(플랫폼 노드 단위)와 **공존**하며, `stage_lab.gd` 의 `타일맵_페인트` 를 켠 스테이지에서만 동작.
> 상세: `동현_작업기록_07-30_타일맵_페인트_시스템.md`

> ★18차 변경(2026-07-25): **배경 바이옴 시스템**(`stage_backdrop.gd`) · **통로 연결**(`통로.tscn`,
> 구 `목표문` 대체) · 체크포인트를 **잉크 등불**로 재디자인 · 배경 프롭 16종 추가 ·
> 색칠 실루엣 이탈 버그 수정. 상세: `작업기록_2026-07-25_도형_배경바이옴_지형두께_통로연결.md`

> ★17차 변경(2026-07-24): **페인트 시스템 v3(플랫폼 단위 색칠)** 신설 —
> `shaders/` · `scripts/장애물/` · `scenes/장애물/` · `scenes/스테이지/` · `scenes/페인트/` 폴더 추가.
> 기존 v2(타일맵 셀 단위)는 zone_01/02/world_1 이 그대로 쓰므로 **둘 다 살아 있다.**
> 상세: `작업기록_2026-07-24_도형_페인트v3_타일셋적용_스테이지1-5.md`

> 12차 정정(코드 수정 아님, 문서 오기 수정): `main.tscn` 행이 "F5 기본 씬"으로 잘못 표기돼
> 있던 것을 정정(실제 F5 기본 씬은 10차부터 lobby.tscn). 상세: `작업기록_2026-07-21_...md`.
> 11차 변경: world_1 = 3구역(zone1+2+★zone3 수직 도시) / zone2 경계 = ColorZone 자동 방식
> (SwitchZone 폐기) / 프롭 일러스트+라이팅(assets/textures/props/·light_flicker.gd) /
> 테스트 신설(test_lobby_flow) · world_flow v2 재작성
> 10차 변경: verlet_tail.gd 삭제 / 로비 v2(디오라마) / **F5 시작 씬 = lobby.tscn**
> (project.godot 1줄 — 도형 명시 요청) / 존·월드 벽 제거 재생성

> **찾고 싶은 파일이 있으면 이 문서에서 Ctrl+F.**
> 소유: `팀` = 프로그래머 코드(도형 수정 금지) / `도형` = 레벨·프로토·툴 / `생성` = 스크립트가 만든 산출물(직접 편집 금지).

## 실행 진입점

| 파일 | 소유 | 설명 |
|---|---|---|
| `main.tscn` | 팀 | zone_02 단독 (팀 회귀 테스트용). ⚠F5 기본 씬 아님 — project.godot 의 `run/main_scene` 은 10차부터 lobby.tscn (2026-07-21 정정) |
| `scenes/스마트월드/스마트월드_1-1.tscn` | 생성 | ★★**F5 기본 씬 (22차부터)**. 자연→물가. 출구 통로로 1-2 와 이어진다 |
| `scenes/스마트월드/스마트월드_1-2.tscn` | 생성 | ★폐수로. 일자 바닥·9,200px·레인월드식 수직 구조 |
| `scenes/lobby/lobby.tscn` | 도형 | 구 F5 기본 씬(10~21차). **지금도 살아 있다** — ESC 메뉴 "로비로 돌아가기" 로 진입. 시작 → world_1 / 챕터 1 → 스테이지 선택 |
| `scenes/스테이지/스테이지_선택.tscn` | 도형 | ★(17차) 신규 챕터 입구 — 스테이지 1~5 + zone_04/05 로 바로 진입 |
| `scenes/world_1/world_1.tscn` | 생성 | ★zone1+복도+zone2+다리+★zone3(수직 도시) 통합 심리스 월드 (build_world.gd 산출) |

## scripts/ — 게임 로직

| 파일 | 소유 | 설명 |
|---|---|---|
| `scripts/player.gd` | 팀 | 이동·점프·Shift 색 전환 (무수정 원칙) |
| `scripts/gun.gd` / `bullet.gd` | 팀 | 구형 직선 발사 (프로토 존에서는 런타임 제거됨) |
| `scripts/player_anim.gd` | 팀 | player B 캐릭터 애니메이션 컴포넌트 (커밋 db8f55e) |
| `scripts/color_defs.gd` | 팀 | BLACK/WHITE 색 상수 |

### ★빛 시스템 — 집 챕터 신규 (2026-08-18)

> 이 게임에서 **빛도 색 규칙 안에 있다.** 굳은 빛은 밟을 수 있고, 색이 다르면 죽는다.
> 자세한 근거: [작업기록_2026-08-18_도형_빛기둥_창문커튼_집3분할.md](작업기록_2026-08-18_도형_빛기둥_창문커튼_집3분할.md)

| 파일 | 설명 |
|---|---|
| ★`scripts/스마트월드/빛기둥.gd` | 빛줄기. 모드 3종 — **연출**(그림만) / **경계**(닿으면 반대색 즉사·통과됨) / **발판**(굳어서 밟힌다·레이어 1) |
| ★`scripts/스마트월드/창문커튼.gd` | 창문 + 커튼. 쏘면 커튼이 쳐지고 **자식 빛**이 그 색으로 물든다. `색섞임_회색=false` 로 소프트락 방지 |
| ★`scripts/스마트월드/실내배경.gd` | 실내 배경 4종(거실·굴뚝·지붕·하수도)을 **카메라 y 로 세로 교차**시킨다 |

### scripts/proto/ — 도형의 프로토 시스템 (정식 구현의 참조)

| 파일 | 설명 |
|---|---|
| `paint_system.gd` | 물감 규칙 v2 코어 (내구도·FIFO 회수·회색 영구) — P-A 정식 구현 참조본 |
| `proto_gun.gd` / `proto_bullet.gd` | 포물선 발사 + 조준 궤적 + E 회수 |
| `brush_cursor_ui.gd` | 붓 커서 + 횟수 표시 + 중앙 메시지 |
| `zone_lab.gd` | 존 공용 조립 (총·UI·카메라·모션·라이팅 런타임 부착) + 색 사망 판정 |
| `zone02_lab.gd` | zone_02 단독 씬용 — 색 전환 잠금 감시 (구 방식 프로토 보존 — world_1 은 ColorZone) |
| `world_lab.gd` | ★world_1 로직 v2 — 3구역 감지·카메라 리밋 트윈·체크포인트·클리어 (SwitchZone 잠금 삭제) |
| `light_flicker.gd` | ★(11차 신규) PointLight2D 깜빡임 — 가로등·창문·발광 구슬 |
| `effect_spawner.gd` | ★(16차 신규 2026-07-22) 게임필 이펙트 — 색칠 스플래시·착지 먼지·회색 히트스톱·카메라 킥. paint_system.hit_feedback 구독 + 착지 감지. 파티클 코드생성(아트 0). zone_lab 이 런타임 부착 |
| `proto_camera.gd` | ★카메라 v2 (룩어헤드·플랫폼 스냅·리밋 트윈) |
| `proto_motion.gd` | ★스쿼시&스트레치·기울임 (절차적 2차 모션) |
| `zone_visuals.gd` | ★환경광·플레이어 광원·배경 노멀맵 주입·비네트 |
| `stage_transition.gd` | 잉크 와이프 씬 전환 (`StageTransition.change_scene(...)` 한 줄) |

### ★페인트 v3 — 플랫폼 단위 색칠 (17차 신규, 2026-07-24)

> v2(위 `paint_system.gd`, 타일맵 셀 단위)와 **공존**한다.
> zone_01/02/world_1 = v2 / 스테이지 1~5·zone_04/05 = v3.

| 파일 | 설명 |
|---|---|
| `shaders/ink_spread.gdshader` | ★잉크 번짐 셰이더 — 9슬라이스 + 씨앗 번짐 마스크 + 젖은 광택 |
| `scripts/proto/tile_art.gd` | 디자이너 타일셋 카탈로그(384px 덩어리 아트 5종). **아트 교체 시 여기만 수정** |
| `scripts/proto/paint_platform.gd` | ★칠하는 플랫폼 — 규칙·판정의 주인(내구도·상태·유령 레이어) |
| `scripts/proto/paint_manager.gd` | 회수 FIFO (플랫폼 단위) |
| `scripts/proto/paint_fx.gd` | 스플래시·물감 흐름·카메라 킥 + VFX 스탬프 모드(zone_05) |
| `scripts/proto/paint_drip.gd` | ★물감 흐름 1가닥 (흐름 → 굳음 → 소멸) |
| `scripts/proto/stage_backdrop.gd` | ★(18차) 배경 빌더 — 하늘 바이옴 보간·뒷벽·실루엣 4겹·안개·가로등·구슬·수면·발판장식 |
| `scripts/proto/stage_lab.gd` | 스테이지 공용 조립 (zone_lab 의 플랫폼 버전). ★(19차) `타일맵_페인트` 로 v4 도 켠다 |
| `scripts/proto/stage_hud.gd` | 플랫폼용 붓 커서 UI (v2 의 brush_cursor_ui 대응) |
| `scripts/proto/stage_select.gd` | 챕터 1 스테이지 선택 화면 |
| `scripts/proto/player_shoot_anim.gd` | 발사 모션 (팀 `player_anim.gd` 무수정, 형제 스프라이트 방식) |

### ★타일맵 페인트 v4 — 타일맵 지형 색칠 (19차 신규, 2026-07-30)

> v3(위 `paint_platform.gd`, 플랫폼 노드 단위)와 **공존**한다.
> `stage_lab.gd` 의 `타일맵_페인트` 를 켠 스테이지만 v4 (기본 꺼짐 → 기존 씬 회귀 없음).
> **디자이너는 TileMapLayer 로 맵을 찍기만 하면 된다.** 상세: `동현_작업기록_07-30_타일맵_페인트_시스템.md`

| 파일 | 설명 |
|---|---|
| `scripts/proto/tile_paint_map.gd` | ★`TilePaintMap` — 아틀라스 좌표로 흑/백 인지 · 플러드필 플랫폼 자동검출 · 색칠 · 회수 FIFO |
| `shaders/ink_spread_mask.gdshader` | ★잉크 번짐 **마스크 전용** 셰이더. 오버레이 레이어 알파만 깎아 칸 경계 없이 번지게 함 (⚠`MODEL_MATRIX` 로 월드좌표 계산 필수) |
| `scenes/플랫폼/shared_tileset.tres` | v4 가 쓰는 공용 TileSet (16px 타일, physics_layer_0 = 레이어 1) |
| `scripts/proto/tile_paint_platform.gd` | ⚠**미사용** — v4 초기 시도(3레이어 방식). 대체됨, 삭제 보류 |


### scripts/장애물/ — 한글 장애물 부품 (17차 신규)

| 파일 | 설명 |
|---|---|
| `장애물_공통.gd` | 외곽선 2겹 그리기·가시 폴리곤 도우미 (흑/백 지형 양쪽에서 보이게) |
| `가시.gd` · `회전톱.gd` · `떨어지는블록.gd` | 즉사 함정 (`hazard` 그룹) |
| `움직이는발판.gd` · `무너지는발판.gd` | **PaintPlatform 상속** — 움직이거나 무너지면서 색칠도 되는 발판 |
| `색문.gd` | 지정 색일 때만 통과 (즉사 아님) |
| `통로.gd` | ★(18차) 스테이지 연결 터널. 출구 속빛 = 다음 스테이지 하늘색 (구 `목표문.gd` 대체) |
| `체크포인트.gd` | ★(18차 재디자인) 잉크 등불 — 꺼져 있다 불이 켜짐 |
| `목표문.gd` | ⚠(18차) `통로.gd` 로 대체됨. 파일은 남겨뒀으나 신규 스테이지는 안 씀 |
| `낙하지대.gd` | 낙사 판정 |

## scenes/

| 파일 | 소유 | 설명 |
|---|---|---|
| `scenes/player/Player.tscn` | 팀 | 플레이어 (무수정 원칙 — 존이 런타임으로만 손댐) |
| `scenes/bullet/Bullet.tscn` | 팀 | 구형 총알 |
| `scenes/proto/ProtoBullet.tscn` | 도형 | 프로토 총알 |
| `scenes/lobby/` | 도형 | ★로비 v2 — 흑백 디오라마 + 인게임 캐릭터 2체 (F5 시작 씬) |
| `scenes/obstacles/obstacle_template.tscn` | 도형 | 장애물 뼈대 (복제해서 사용) |
| `scenes/world_1/zone_01·zone_02/` | 생성 | 단독 테스트 존 (build_zones.gd 산출) — 자동 테스트가 사용 |
| `scenes/world_1/stage_1/` | 팀 | 팀 제작 스테이지 1 |
| `scenes/world_1/zone_template/` | 도형 | 새 구역 만들 때 복제용 템플릿 |
| `scenes/페인트/칠하는발판.tscn` | 도형 | ★(17차) 칠하는 발판 부품 — 스테이지에 드래그, `재질`·`크기_칸`만 조절 |
| `scenes/장애물/*.tscn` | 도형 | ★(17차) 장애물 9종. 노드 1개+스크립트 1개 = **끌어다 놓으면 끝**. 사용법: `docs/장애물_카탈로그.md` |
| `scenes/스테이지/스테이지_1~5.tscn` | 생성 | ★(17차) 신규 챕터 (build_stages.gd 산출 — **직접 편집 금지**) |
| `scenes/world_1/zone_04·zone_05/` | 생성 | ★(17차) 셰이더 vs VFX 색칠 비교 실험실 (지형·규칙 동일, 표현만 다름) |
| `scenes/경계/color_zone.tscn` | 신규 | ★다각형 색 강제 구역 (구역 안=흑/백 자동 고정 / 밖=키보드 자유 전환 / 겹침=마지막 진입 우선). 지형에 겹쳐 배치·복제 |

## tools/ — 빌더·테스트 (헤드리스 실행)

```
Godot --headless --path . -s res://tools/<파일>   ← 실행법
```

| 파일 | 설명 |
|---|---|
| `build_zones.gd` | zone_01/zone_02 생성기 |
| `build_world.gd` | ★world_1 생성기 v2 — 3구역·ColorZone 경계·프롭·라이팅 (배치 수정 후 재실행) |
| `generate_normal_maps.gd` | ★타일·배경·프롭 노멀맵 생성 (아트 교체 시 재실행. ⚠새 에셋은 `--headless --import` 먼저) |
| `test_paint_system.gd` | 물감 규칙 자동 테스트 33항목 |
| `test_color_death.gd` | 색 사망 자동 테스트 8항목 (zone_01/02 단독 씬 사용) |
| `test_world_flow.gd` | ★심리스 흐름 자동 테스트 v2 — 3구역·ColorZone 22항목 |
| `test_lobby_flow.gd` | ★(11차 신규) 로비→월드 진입 자동 테스트 5항목 |
| `screenshot_scene.gd` | 범용 씬 캡처 (창모드): `-- <씬> <저장.png> [x] [y]` |
| ★`build_stages.gd` | (17차) 스테이지 1~5 + zone_04/05 생성기. **레벨 수치 표가 여기 있다** — 숫자만 고치고 재실행 |
| ★`test_paint_v3.gd` | (17차) 플랫폼 색칠 규칙 자동 테스트 37항목 |
| ★`test_stages.gd` | (17차) 7씬 구조 검사 + **스폰→목표 도달 가능성 BFS 시뮬레이션** |
| `screenshot_paint.gd` | (17차) 색칠된 상태로 캡처 (실제 `명중()` 호출 — 번짐·흐름이 실전과 동일) |
| `screenshot_zone01.gd` | (구) zone 전용 캡처 — screenshot_scene.gd 로 대체 가능, 페인트 시뮬 기능 때문에 유지 |
| ★`build_원본_집.gd` | (2026-08-18) **집 3장(거실·굴뚝·지붕)의 원본 씬 빌더.** 선반 좌표·창 위치·단차 계산이 전부 여기 주석에 있다. ⚠평소에 돌리지 말 것 |
| ★`test_빛창문.gd` | (2026-08-18) 빛기둥·창문커튼 55항목 + **월드 사망 판정 통합** + **실제 스마트월드_7 씬 검증** |
| ★`shot_빛창문.gd` | (2026-08-18) 창을 칠한 **전/후**를 캡처(창모드). `명중()` 을 코드로 부르고 플레이어 색까지 맞춘다 |
| `스마트월드_체인.gd` | ★[2026-08-18] `-- --만 5,6,7` 로 **일부 스테이지만** 굽는다. 안 주면 예전처럼 전부 굽는다 |
| ★`지형_진단.gd` | (2026-08-19) **"못 지나가는 자리" 전용 진단기.** 레벨검사가 조용히 빼 버리는 끼임 구간을 x 범위·막는 노드 이름·필요 px 까지 찍는다. `-- --전부` |
| ★`지형_보정.gd` | (2026-08-19) 선반 **아래만** 깎기 + 디딤돌 사다리 놓기. **멱등** · `--적용` 없이는 저장 안 함. 유령 발판·통로 옆에는 일부러 안 놓는다(퍼즐 보호) |

## assets/

| 폴더 | 설명 |
|---|---|
| `assets/tileset/` | ★(17차) **디자이너 타일셋** brick/wood/rock/soil/grass — 한 칸 384×384 "플랫폼 덩어리" 아트(흑/백 쌍). 배치는 `scripts/proto/tile_art.gd` 참조. **같은 파일명으로 덮어쓰면 코드 무수정 반영** |
| `assets/player(shoot)/` | ★(17차) 발사 시트(640px 5프레임) + `shoot_frames.tres` |
| `assets/tilesets/terrain_tileset.tres` | ★(구 v2) 타일맵용 공용 타일셋 (zone_01/02/world_1 이 사용) |
| `assets/textures/tiles/` | 타일 원본 (placeholder — 아트 교체 대상) |
| `assets/textures/bg/` | 패럴랙스 배경 3층 (아트 교체 대상) |
| `assets/textures/normal/` | 생성된 노멀맵 (직접 편집 금지 — generate_normal_maps.gd 재실행) |
| `assets/textures/props/` | ★(18차) 배경 프롭 **총 23종**. 11차 7종(어두운 원본, world_1 사용) + 18차 16종(**밝은 실루엣** — 레이어 밝기를 곱해도 안 뭉개지게). 11차 7종 — 풀·발판 캡(흑/백)·가로등·발광 구슬·판잣집·고층 (전부 placeholder, ART 교체 대상) |
| `assets/textures/ui/` | 붓 커서 등 UI |
| `assets/p/` | 팀 캐릭터 player B 시트 (흑/백 × 6동작) + player_frames.tres — ★게임이 실제 사용하는 폴더 |
| `assets/player B/` | ⚠미참조. assets/p 와 파일명은 같지만 **내용이 전부 다른** 시트 (해시 비교) — 어느 쪽이 최신인지 캐릭터 담당자 확인 전까지 보존 |
| `assets/characters/` | 로비 장식용 공룡 SVG 2종 (dino_black/white) |
| `assets/_archive/` | ★미참조 확인 후 보관 처리된 구 에셋 (슬라임·공룡 입벌림·Kenney 프로토) |

## docs/

| 파일 | 설명 |
|---|---|
| `기획서_규칙_플로우차트.md` | ★★팀 기준 문서 (규칙 명세). ⚠1-5 "무색=안전" 이 레벨디자인 가이드 V1 과 모순 — 17차에서 발견, 팀 확정 대기 |
| `작업기록_2026-07-25_도형_배경바이옴_지형두께_통로연결.md` | ★★최신 작업 상세 (18차) |
| `팀_작업배분_제안_2026-07-25.md` | ★★회의용 — 프로그래머2/LD2/ART1 작업 배분 + 결정 안건 3건 |
| `디자이너_아트의뢰_2026-07-25.md` | ★★ART 상세 의뢰서 (명도 규칙·바이옴별 프롭 목록·납품 규격) |
| `작업기록_2026-07-24_도형_페인트v3_타일셋적용_스테이지1-5.md` | 17차 작업 상세 |
| `페인트v3_셰이더_vs_VFX_비교.md` | ★zone_04/05 A/B 비교 + 튜닝 노브 목록 |
| `장애물_카탈로그.md` | ★장애물 9종 사용법·배치 요령 (LD 작업 시 옆에 켜둘 것) |
| `레벨디자인_가이드.md` | LD 기준표 (이코노미·부품·30구역) |
| `작업현황_도형.md` | 도형 작업 로그 (최신이 위) |
| `작업기록_2026-07-19_도형_경계재구축_zone3_비주얼고도화.md` | ★★최신 작업 상세 (경계 재구축·zone3·프롭 라이팅·디버깅 기록) |
| `작업기록_2026-07-18_도형_비주얼고도화_심리스전환.md` | 9차 작업 상세 (시스템 설명·디버깅 기록·아트 워크플로) |
| `경계_색구역_시스템.md` | ★색 구역(경계) 시스템 문서 (로직·사용법·튜닝·제약·검증 + §8-1 world_1 적용·중립 틈 규칙) |
| `파일_분류_인덱스.md` | 이 문서 |
| `다음작업_프롬프트.md` | 세션 인수인계 |
| `로비_에셋_배경_기획_도형.md` | 로비/배경 기획 + CC0 에셋 추천 |
| `게임_기획서_세계관_디자인_도형.md` / `수정된세계_클로드디자인_프롬프트팩.md` | (구) 아트 디렉션 — 명도 규칙표는 유효 |

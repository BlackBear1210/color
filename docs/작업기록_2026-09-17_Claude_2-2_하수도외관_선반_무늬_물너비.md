# 작업기록 2026-09-17 · Claude — stage_2-2 에 2-1 하수도 외관·규칙 적용 (공중선반 · 내부 무늬 · S01 물 너비 · 일방통행 복구)

> 요청(도형님): 인계 문서 6 종 + 맵분석 3 종을 읽고 `stage_2-2.tscn` 을 고쳐라.
> 읽은 것: `AGENTS.md` · `docs/다음작업_프롬프트.md` · `scenes/world_2_클로드/프롬프트_아스트라_스테이지2_맵제작.md`(§0-A · §2026-09-16 · §2026-09-17 색조합·물너비) ·
> `docs/레벨디자인_가이드.md` · `docs/색판정_규칙_2026-08-30.md` · `docs/하수도_흑백벽돌_모듈_사용법.md` ·
> `scenes/world_2_클로드/필독_오퍼스_현재지형디자인_인계.md` ★ · `docs/맵분석_2026-09-07_Claude_하수도_2-1_2-7.md` ·
> `docs/작업기록_2026-09-13_아스트라_하수도2-1부터2-7_게임성분석.md` · `docs/맵분석_2026-09-13_아스트라_world2_2-1부터2-7_개편방향.md` ·
> Codex 09-16/09-17 기록 4 종(2-2 맞물림 · 2-1 무늬 · 물너비 규칙 · 인계).
> **엔진 미실행** — AGENTS.md §3 사용자 안전 예외에 따라 Godot 을 한 번도 켜지 않았다. 아래 "검사" 절에서 정적/미실행을 나눴다.

---

## 0. 세 줄 요약

1. **2-2 의 외관을 2-1 과 같은 기준으로 올렸다** — 흰 탑 공중 발판 9 장을 **공중선반 프리팹(ledge_v03)** 으로, 검정/흰 벽 안에 **반대색 벽돌 무늬 4 곳**, 세로 맵용 **다층 배경**.
2. **게임 규칙 2 건**: S01 흰 물을 64 → **448** 로 넓혀 "점프 중 흰색 → 통과 → 검정" 이중 전환으로 레버를 건너뛰는 우회를 바닥 높이에서 막았다(물너비 규칙 09-17). Player `점프_높이_칸 = 10` 명시.
3. **회귀 1 건 발견·복구**: HEAD 의 2-2 는 흰 탑 발판 9 장의 **일방통행 콜리전이 사라져** 있었다(에디터 재저장이 편집 불가 인스턴스 내부 덮어쓰기를 버린다). 두 칸 위 선반 밑면에 머리가 박혀 F2(탑) 경로가 성립하지 않는 상태였다. 되살리고 `[editable]` 로 고정했다.

---

## 1. 바꾼 파일

| 파일 | 무엇 |
|---|---|
| `scenes/world_2_클로드/stage_2-2.tscn` | 아래 §2 전부. 선반 9 장 외 지형의 점·위치, 장치·체크포인트·출구 블록은 HEAD 와 **바이트 동일**(검사기가 git HEAD 와 비교) |
| `scenes/배경/하수도_다층배경_2_2.tscn` ★신규 | 2-1 배경(`하수도_다층배경_2_1.tscn`)의 4608×4352 세로 맵용 배치 사본. 층·재질·시차 동일, 배수구·아치 자리만 위/아래로 나눔 |
| `tools/apply_stage22_sewer_look.py` ★신규 | 적용 도구. 기본 dry-run · `--apply` 때만 저장 · 표식(`metadata/sewer_look_v1`) 있으면 재기록 안 함(멱등) · 빌더를 돌리지 않는다 |
| `tools/test_stage22_sewer_look.py` ★신규 | 정적 검사(§4) |
| `tools/test_stage22_masonry_joint.py` | HEAD 의 2-2 에서 이미 실패하던 것을 고침 — 에디터 재저장 씬은 프리팹 기본값(`위치별_판정=true`)과 콜리전 덮어쓰기를 지우므로 "있으면 점과 같아야 하고 없으면 통과" 로. 맞물림 재질 검사도 외부 .tres / 씬 안 sub_resource 둘 다 읽게 |
| `CLAUDE.md` · `필독_오퍼스_현재지형디자인_인계.md` | 이 기록 포인터 · 2-2 적용 노드 표(§6-2) |

---

## 2. stage_2-2 에 한 일

### 2-1. 흰 탑 공중 발판 9 장 → 공중선반 프리팹 (`탑_l2` ~ `탑_l10`)

- 전: `하수도_기본지형_흰색`(땅 프리팹) 272×96 직사각 — "두꺼운 벽을 잘라 띄운" 모양(인계 §2 가 금지한 것).
- 후: `하수도_공중선반_흰색` 인스턴스 · `ledge_v03/white` 텍스처(런타임은 `하수도_자연발판.gd` 가 `석조선반=true` 를 보고 ledge 셰이더를 만든다) · 윗면 10px 모따기 + 밑면 깨진 돌 요철(두께 42~60, 프리팹 요철을 선반마다 위상을 돌려 씀).
- **착지면 y · 폭 272 · 노드 position 은 빌더 표 그대로.** 밑면만 얇아졌다. 주행검사 공략(`tools/하수도_주행검사.gd` "stage_2-2 탑")의 도약·착지 좌표는 그대로 유효하다(정적).
- 벽에 붙는 쪽 끝(l3·l5·l7·l9 = 탑_왼벽 x 768 · l4·l8·l10 = L1/L2 흰바닥 x 1408)은 모따기를 빼고 **직각**으로 — 벽 앞 10px 홈과 l10→L2_흰바닥 이음새의 V 자 틈을 막기 위해.
- **★일방통행 복구.** `[node name="CollisionPolygon2D" parent="지형/탑_lN/StaticBody2D" index="0"]` 에 `one_way_collision = true · margin 4` + 점과 같은 polygon. 파일 끝에 `[editable path="지형/탑_lN"]` 9 줄 — 편집 가능 인스턴스의 자식만 에디터가 다시 저장한다(HEAD 에서 사라진 원인).
- 삼각지지대 7 개(`벽고정_지지대` 자식 · 그림뿐 · 충돌 없음): 벽 쪽 끝 24px 안쪽, 밑면 요철 3px 위(2-1 A_L2 와 같은 관례). l2·l6 은 양옆이 트여 있어 안 달았다(사용안내 §1-6 "볼트 뒤에 벽 단서").

### 2-2. 내부 반대색 벽돌 무늬 (1번) 4 곳

| 노드 | 바탕 | 무늬 | 자리(월드) | 왜 여기 |
|---|---|---|---|---|
| `홀_바닥` | 검정 | 흰 벽돌 6 줄 | x 3041~3241 · y 3620~3741 | 시작 화면(2560±960) 바닥. 윗면 3584 에서 35px 아래 |
| `우측_덩어리` | 검정 | 흰 벽돌 5 줄 | x 2618~2696 · y 2345~2451 | 갱도 오른벽(2560~2752) · L2 허브에서 색 바꾸며 서 있는 자리 옆 |
| `탑_왼벽` | 흰 | 검정 벽돌 6 줄 | x 553~682 · y 2756~2882 | 흰 탑 왼벽 · l7~l5 사이 높이 · 벽 오른끝 768 에서 ≥ 48 |
| `L2_흰바닥` | 흰 | 검정 벽돌 4 줄 | x 1512~1759 · y 2345~2430 | L2 열림 흰 바닥 · 윗면 2304 에서 41px 아래 |

- 줄 경계 = `apply_stage22_masonry_joint.ylines()`(2-2 UV 기준 (0, 1792.1) · 0.18), 양옆 = `nearest_joint()` 로 실제 줄눈. 16px 격자에 억지로 맞추지 않았다(§2026-09-17 규칙).
- 노드: `script = 하수도_내부벽돌.gd` · `시작상태 = 0` · `내부_바탕흰색` · `내부_벽돌영역`(로컬 Rect2). 에디터 미리보기 ShaderMaterial 에도 `inlay_count / inlay_rects` 를 **같이** 넣었다(사용법 §1 "둘 다 갱신"). 검사기가 두 배열이 같은지 본다.
- 모든 외곽(오목·구멍 포함)에서 ≥ 24px — 검사기가 폴리곤 변까지 거리를 잰다. 별도 콜리전·사망 영역 없음.
- 한 시야에 최대 2 덩어리(L2 허브에서 우측_덩어리 + L2_흰바닥). 길·장치를 가리지 않는다.

### 2-3. S01 흰 물 너비 64 → 448 — 물 분류표 (프롬프트 §2026-09-17 양식)

| 노드 | 물색 | 바닥색 | 분류 | 필수 장치 · 작동 결과 | 설정 너비 | 실제 유효 판정 폭(정적) | 출발·착지 높이 | 우회 요소 | 검증 |
|---|---|---|---|---|---|---|---|---|---|
| `S01_흰물` (2296, 3104~3584) | 흰 | 검정 홀 바닥 | **필수 퍼즐형** — "막다른 F1 이 열쇠" 가 이 맵의 첫 경험 | `레버_수문`(F1 발코니 · 물 건너편 아님) → 물 꺼짐 → 홀 통행 + 갱도 격자 + 탑 열림 | **448**(28칸) | 바닥띠(v 0.8~1.0) 0.754~0.773 W = **338~346** · 공중 물줄기 심 0.26~0.32 W = 116~143 | 같은 높이(3584→3584) · 추가 이동 수단 없음 | 아래 "우회 검토" | 정적: 338 + 몸 44 = 382 > 320 ✓ · **실행 미검증** |
| `S12_흰물` (1904, 2368~2944) | 흰 | 허브 호퍼(회색·색 규칙 밖) | 우회 허용형 | 없음. 흰 몸이 격자를 칠하고 오르는 F3 지름길의 재료 | 64 | — | 허브 위 | 이중 전환으로 지나가도 설계 의도 안 | 그대로 |
| `S23_검정물` (1904, 1728~2304) | 검정 | 허브 호퍼 | 우회 허용형 | 없음. 검정 몸이 그냥 오른다 | 64 | — | 허브 위 | 동상 | 그대로 |
| `L3_흰웅덩이` (3120, 1792 · 480×112) | 흰 | 검정 구덩이 | 우회 허용형(교육) | 유령 발판 페인트를 지운다 = "격자는 안 지워진다" 를 보여 주는 장치 | 480×112 | — | — | 옆 격자 128 로 256 점프 | 그대로 |

**우회 검토(S01):**
- **점프 중 이중 전환(바닥 높이)**: 검정 몸이 홀 바닥에서 뛰어 흰색 → 물 통과 → 검정 → 착지. 착지점이 바닥 물띠(338~346) 밖이어야 하므로 필요 수평 거리 ≥ 물띠 + 몸 44 = **382~390 > 320**. 8 프레임 전부 계산(`유체_판정모양.gd` 실루엣 · 판정_여유 0). → **차단(정적)**.
- **격자 지그재그 이중 전환(잔존)**: 물이 켜져 있어도 p1(2336~2560 @3456)의 오른쪽 마른 부분은 통로 쪽에서 오를 수 있고, p1→p2→p3 를 지날 때 공중 물줄기 심(116~143 폭)만 흰색으로 통과하면 검정 몸이 허브까지 갈 수 있다. 심을 격자 두 열(2032~2560 = 528) 전체로 넓히려면 W ≈ 1,700 이라 시각적으로 불가. **이 우회는 남아 있다** — 막으려면 p1 자리를 바꾸거나(물 아래로), 물이 켜진 동안 격자를 못 밟게 하는 재설계가 필요. 도형님 결정 사항으로 남긴다.
- **페인트 우회**: 검정 몸이 물 아래 바닥을 흰색으로 부분칠(홀_바닥은 2048 폭 → 자동 부분칠) → 뛰어 흰색으로 착지 → 흰 물 안을 걸어 통과. 448 을 다 덮으려면 여러 발. 게임 규칙 안의 해법이라 **허용으로 기록**(막지 않았다).
- **낙하 우회**: 허브 L1(2944) 에서 왼쪽으로 떨어져 물을 통과 → 물 바닥 = 홀 바닥이라 공중에서 되돌릴 틈이 없다 → 착지 즉사. 차단.
- 레버·CP_시작(2600)·시작 위치(2560, 몸 2538~2582)는 물 밖(오른쪽 물띠 끝 ≈ 2475). 관_S01 출구(2296)는 물 중심 그대로.
- ⚠ 시각: 물 그림 배율이 (1.75, 0.94) 가 된다(2-1 의 300×800 은 (1.17, 1.56)). 프롬프트가 "과하게 늘려 물결이 뭉개지면 폭포용 자산" 이라 했는데 **화면을 못 봤다** — 실행 후 확인 항목.
- ⚠ 물이 켜진 동안 격자 p1~p3 가 물 안에 든다(전에는 64px 기둥이 두 열 사이 틈에만 있었다). `레벨검사.gd` 가 "흰 물 × 검정 격자" 를 색 경계 겹침으로 셀지 모른다 — 실행 항목.

### 2-4. Player `점프_높이_칸 = 10.0` 명시 (2-1 과 동일 · 기본값에 기대지 않는다)

### 2-5. 다층 배경 `하수도_다층배경_2_2.tscn` — 루트에 `하수도배경` 인스턴스(지형보다 앞 = 뒤에 그려진다)

카메라 왼위 S 가 x 0~2688 · y 0~3272 를 움직인다. 장식 위치 = 패럴럭스 좌표(S × ScrollScale + 화면 안 위치)로 계산:
배수구_1 (700, −560 · 0.9) = 위쪽 절반(L3 복도·갱도 위) · 배수구_2 (150, 1500 · 0.85) = 아래쪽 절반(홀·탑 밑동) ·
아치_1 (−400, −300 · ×2) = 거의 항상 왼쪽에 · 아치_2 (1900, 2200 · ×1.8) = 아래 1/3(홀). 먼 벽·안개는 반복. 화면 확인은 미실행.

---

## 3. 이번에 알게 된 것 (지뢰)

1. **에디터 재저장은 편집 불가 인스턴스 내부의 덮어쓰기 노드를 버린다.** 빌더 `공중발판()` 이 `owner` 를 줘서 넣은 `CollisionPolygon2D`(one_way) 가 19fef5a 시점에 이미 없었다. `[editable path=...]` 가 있어야 남는다. 2-1 에 Codex 가 넣은 `CollisionPolygon2D` 덮어쓰기 4 개도 같은 운명이다(다만 그쪽은 폴리곤뿐이라 `collision_update_mode=2` 가 실행 때 다시 구워 **기능 영향 없음**). `tools/하수도_빌더_공통.gd` 의 저장 함수에 `[editable]` 등록(`루트.set_editable_instance(n, true)`)을 넣는 것이 근본 해결 — 이번엔 손대지 않았다.
2. `test_stage22_masonry_joint.py` 는 HEAD 에서 이미 실패하고 있었다(위 이유로 덮어쓰기 노드가 없어서). 고쳤다.
3. 물 실루엣(`유체_판정모양.gd`)은 **바닥띠는 0.75 W, 공중 심은 0.3 W** 다. "448 이면 320 을 못 넘는다" 는 바닥 착지에만 맞고, 공중 발판 사이를 지나는 우회에는 안 맞는다(§2-3).

---

## 4. 검사

**정적 (통과)**
```
python tools/apply_stage22_sewer_look.py            # dry-run · 무늬 24px 여유 · 선반 표 대조 · S01 추정
python tools/test_stage22_sewer_look.py             # §2 전부 + git HEAD 대비 불변 검사
python tools/test_stage22_masonry_joint.py          # 기존 맞물림 1,092 점 · 콜리전 · UV · 참조 (HEAD 에도 통과)
python tools/test_sewer_color_modules.py            # 모듈 37,288 점 (무관하지만 회귀 확인)
```
- sub_resource id 유일 · 모든 `SubResource/ExtResource` 참조 정의됨 · 고아 sub_resource 0 · 모든 `res://` 존재.
- 선반 9 장 외 지형 점·위치, 장치·위험물·체크포인트·출구 블록 HEAD 와 동일(S01 크기 한 줄만 다름).

**엔진 (미실행 · 허가 후 이 순서로)**
```
"C:/Users/Public/godot46/Godot_v4.6.3-stable_win64_console.exe" --headless --path . -s "res://tools/레벨검사.gd" -- "res://scenes/world_2_클로드/stage_2-2.tscn" --간격=8
"C:/Users/Public/godot46/Godot_v4.6.3-stable_win64_console.exe" --headless --path . -s "res://tools/하수도_주행검사.gd" -- "res://scenes/world_2_클로드/stage_2-2.tscn"
```
확인할 것: ① 씬 파싱·셰이더 컴파일 오류 0 ② 탑 경로("stage_2-2 탑")가 일방통행으로 통과 — 특히 l2→l3(1158→980) 에서 l4 밑면에 머리가 안 박히는지 ③ 무늬가 페인트에 덮이고 회수 후 복구 · 무늬 옆 외곽 접촉 사망 정상 ④ S01 켜진 상태에서 홀 바닥 이중 전환 실패 · 레버 후 통과 ⑤ 448 물 그림 뭉개짐 ⑥ 레벨검사 색 경계 겹침 0 ⑦ 배경 4 층 · 아치/배수구 자리 ⑧ 지지대가 발판 뒤(z −1)에 그려지고 걸리지 않음 ⑨ 프레임 시간(마감 캐시 · 물 448).
⚠ 알려진 미수정: 꺼진 유체가 총알을 감지하는 문제(인계 §7) — S01 을 잠근 뒤 격자에 쏠 때 나타날 수 있다. 이번 작업과 무관.

---

## 5. 남은 일 · 도형님 결정

- **격자 지그재그 이중 전환 우회**(§2-3)를 막을지. 막으려면 p1 자리를 물 기둥 아래로 옮기거나 물이 켜진 동안 격자 진입을 못 하게 하는 재설계.
- 색 비율: 09-14 에 59 %(기준 60). 선반이 얇아져 흰 테두리가 줄었을 테니 다시 재면 올라갔을 것 — `색비율_보고()` 는 빌더 안에 있어 실행 없이는 못 잰다.
- l2·l6(양옆 트인 선반)에 쇠사슬 매달기 여부 — 시각 결정.
- `하수도_빌더_공통.저장()` 에 `[editable]` 등록 추가(§3-1).

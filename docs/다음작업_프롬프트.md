# 다음 작업 인수인계 프롬프트

> 사용법: 다음 작업 세션(사람 또는 Claude)을 시작할 때 아래 블록을 통째로 복사해서 첫 메시지로 붙여넣으세요.
> 갱신: 큰 작업이 끝날 때마다 도형이 "현재 상태"와 "다음 할 일"만 고쳐 씁니다. (마지막 갱신: 2026-07-14 7차)

---

```
[프로젝트 컨텍스트]
- 저장소: C:\Users\김도형\Documents\GitHub\color (GitHub: BlackBear1210/color, 작업 브랜치 dev_4)
- 게임: 「GRAY」 — Godot 4.6.3, 2D 퍼즐 플랫포머. 흑백 세계를 수직 상승(30구역).
- ★핵심 규칙 v1.3 (2026-07-14 확정):
  · 발사 무제한(게이지 폐지). 무색 발판마다 "필요 명중 횟수(1~3발) = 내구도"가 있고,
	같은 색을 그만큼 맞히면 그 색 발판이 됨 (타일 커스텀 데이터 paint_hits).
  · 마우스 커서 = 붓. 칠 가능 발판에 올리면 남은 횟수가 작은 숫자(x1~x3)로 표시.
  · E = 회수: 색 무관 전역 FIFO(먼저 칠한 발판부터), 원래 내구도의 무색으로 복귀.
	게이지가 없으므로 회수는 "칠한 길을 되돌리는 지형 조작 도구".
  · 완성된 발판에 반대색 = 회색(영구, 회수·재칠 불가). 유일한 비가역 페널티.
  · ★색 사망(v1.3): 반대색 발판을 밟으면 통과·낙하가 아니라 즉시 사망 → 스폰 색으로
	복원 리스폰. 회색 발판 = 흑·백 모두 밟아도 안 죽는 중립 지형 (일단, 테스트 중).
  · 진행 중(횟수 미달) 발판에 다른 색 = 새 색으로 진행 리셋 (회색 아님, 🟡제안 상태).
  · Shift = 흑백 전환 (전환 존 안에서만 — zone_02 프로토 참조).
  · 흑 발판 군집·백 발판 군집 혼합 배치 금지 (최소 4타일 분리). 회색 발판 = 안전 전환 지대.
- 팀 5명: LD(도형=리드 기획·레벨·검증·파일 정리), P-A(색·총·물감·경계), P-B(카메라·진행·체크포인트·UI),
  ART(배경 일러스트+지형 타일), TB(타일맵 빌더). 기간 7/13~8/4, 매주 일요일 통합 빌드+전원 테스트.

[반드시 먼저 읽을 파일] (전부 docs/ 안)
1. docs/기획서_규칙_플로우차트.md — 규칙 전체 명세 v1.2 + 플로우차트 + 결정 현황 (기준 문서)
2. docs/레벨디자인_가이드.md — 이코노미(v1.2 주의문 포함), 퍼즐 부품 V1~V8, 30구역 골격
3. docs/작업현황_도형.md — 작업 로그(최신이 위). 특히 2026-07-14 6차 "기존 문제점 진단"·7차 절

[현재 상태 — 2026-07-14 (7차) 기준]
- 프로토 (scripts/proto/ — 기존 팀원 코드 무수정): paint_system.gd(무제한·내구도·FIFO·회색),
  brush_cursor_ui.gd(붓 커서+횟수 숫자+E 마커), proto_gun.gd(fired 시그널),
  zone_lab.gd(공룡 장착+색 사망 판정), zone02_lab.gd(전환 잠금 감시).
  자동 테스트 2종: tools/test_paint_system.gd (33항목) + tools/test_color_death.gd (8시나리오)
- 테스트 맵 2개 (직접 실행: 이동 A/D·점프 Space·색전환 Shift·발사 좌클릭·회수 E):
  · zone_01 = 횟수제(1/2/3발 발판)·회색 혼합·FIFO 회수·색 사망 검증. 흑 군집(좌)·백 군집(우) 분리.
  · zone_02 = 색 전환 잠금: 이중 경계선 위=백 지형만/아래=흑 지형만, 전환은 SwitchZone 안에서만.
	경계선 위 회색(중립) 발판 = 안전 전환 지대. 위/아래 구역 틴트 + 패럴랙스 배경 3층.
- 배경: 3층 패럴랙스(z_index -10/-9/-8 — 반드시 음수, 트리 순서에 기대지 말 것. 7차 버그 참조).
- 타일셋: 32px 6타일 — 흑/백/회X/무색1발/무색2발/무색3발(점 개수=필요 횟수),
  커스텀 데이터 paint_hits=1/2/3. ART 타일로 교체해도 로직 무수정.
- 캐릭터: 공룡 도트 20×16 4프레임 (dino_black/white + _open, 반전 외곽선 + 지형과 다른 명도).
  발사 순간 0.18초 입 벌림(페인트가 입에서 나감). 존 씬에서 런타임 장착,
  콜리전도 존에서만 슬라임 크기로 축소 (Player.tscn 무수정 → main.tscn 영향 없음).
- ⚠ 본편(main.tscn/stage_1)은 여전히 구 임시 구현(bullet.gd 회색칠+alternative tile 충돌 소실
  버그). 진단·해결책은 작업현황 6차 6항 — P-A 전달용.
- 결정 현황: ③회수E ⑪붓커서UI ⑫혼합배치금지 확정 / ①아트 ②타일32px ④칠범위 ⑤진행구조
  ⑬클리어자동회수 = 7/15 팀 확정 대기 (도형 의견은 기획서 결정 이력 v1.2 참조).

[너의 역할과 규칙]
- 역할: LD 보조. 레벨 설계·씬 조립·작동 검증·파일 구조 정리·문서 갱신.
- 다른 작업자(P-A/P-B/ART/TB)가 만든 스크립트의 로직은 수정하지 않는다. 오류를 발견하면
  작업현황_도형.md에 기록하고 담당자에게 전달할 내용을 정리한다. (직접 수정은 명백한 오타/경로 오류만)
- 모든 작업 후: docs/작업현황_도형.md 맨 위에 날짜 항목 추가 (무엇을/왜/어떤 파일).
- 파일은 최소로: 새 문서를 만들기 전에 기존 문서에 절이 추가 가능한지 먼저 확인.
- 검증 습관: 씬/코드를 만지면 반드시 헤드리스 실행으로 확인 —
  "C:\Users\김도형\Desktop\Godot_v4.6.3-stable_win64.exe" --headless --quit-after 120 <프로젝트경로>
- 커밋/푸시는 사용자가 GitHub Desktop으로 직접 한다. 작업 완료 시 커밋할 파일 목록만 정리해줄 것.

[다음 할 일 (우선순위)]
1. 실기(창모드) 확인: zone_01 붓 커서·횟수 숫자, zone_02 전환 잠금·패럴랙스·슬라임 크기감.
   문제 있으면 brush_cursor_ui.gd / zone_lab.gd 수치만 조정 (전부 도형 소유 프로토 파일).
2. 7/15 팀 회의 결과 반영: 결정 ①·②·④·⑤·⑬ 확정값을 기획서 3장에 기입.
3. P-A 에게 전달 (작업현황 6차 6항 그대로): ⓐ bullet.gd alternative tile 충돌 소실 버그 원인·해법
   ⓑ paint_system.gd v2 가 확정 규칙의 참조 구현 ⓒ 정식 구현 시 얹을 것 = 발판(오브젝트) 단위
   칠하기·색별 통과 판정·포물선 발사·경계(전환 잠금) 시스템.
4. M1(7/19) 프로토 나오면: 점프 실측(최대 높이/거리 px) → 레벨디자인_가이드 6장에 기입.
5. zone_01/zone_02 를 기준으로 지하 1~4 튜토리얼 구역 그레이박스 착수
   (zone_template 복제 + 내구도 배치표 작성 — 가이드 2장 v1.2 주의문 참조).
6. ART 에게 전달: 슬라임(반전 외곽선 원칙)·타일 6종 배치(점=필요 횟수)·배경 3층 명도
   (원경 55~75 / 중경 30~42 / 근경 15~30%) — 전부 같은 배치로 교체만 하면 됨.
```

# 다음 작업 인수인계 프롬프트 (2026-08-07 갱신 · 도형)

> 다음 세션에서 **아래 회색 블록을 통째로 복붙**하면 바로 이어서 작업할 수 있다.
> 최신 상세: `docs/작업기록_2026-08-07_도형_복구_스마트월드12연결_장애물씬화_바인카메라.md`

---

```text
프로젝트: C:\Users\김도형\Documents\GitHub\color   (Godot 4.6-beta2, 브랜치 dev_4)
엔진: C:\Users\김도형\OneDrive\Desktop\Godot_v4.6-beta2_win64.exe\Godot_v4.6-beta2_win64_console.exe

────────────────────────────────────────────────────────────────
[지금 상태 — 2026-08-07]
────────────────────────────────────────────────────────────────
· F5 → 로비. 메뉴 4 개:
    시작 · 스테이지 1-1 / 1-2   → scenes/world_1/stage_1-1, 1-2.tscn   (동현 타일맵 라인)
    스마트월드 · 1 → 2          → scenes/스마트월드/스마트월드_1.tscn   (통로로 2편까지)
    테스트월드 제작             → 동현 테스트 월드 제작/테스트월드제작.tscn
    기타 스테이지               → scenes/스테이지/스테이지_선택.tscn
  ★lobby.tscn 은 안 건드린다. 버튼은 lobby.gd 가 런타임에 복제해 만든다.

· 스마트월드 1 ↔ 2 는 **통로**로 이어진다 (포탈 없음).
    걸어 들어가면 0.85초 암전 → 씬 교체 → **1.35초에 걸쳐 밝아지며** 걸어 나온다.
    원본(스마트월드_테스트 / 스마트월드_1-2)은 그대로 있고,
    tools/스마트월드_연결.gd 가 **읽어서 통로만 얹어** _1 / _2 로 저장한다.

· 장애물은 **씬**이다 → scenes/스마트월드_장애물/*.tscn (14 종)
    각 씬에 빈 `그림`(Sprite2D · 아트슬롯.gd) 슬롯이 있다.
    ★디자이너는 인스펙터 Texture 칸에 PNG 를 끌어다 놓기만 하면 된다. 코드 수정 없음.
    슬롯이 비면 지금처럼 _draw() 가 코드로 그린다(회귀 없음).

· 카메라: scripts/proto/카메라_연출.gd + 카메라_구역.gd
    ★전환이 **시간이 아니라 플레이어 x 좌표**로 굴러간다.
      멈추면 화면도 멈추고, 되돌아가면 정확히 되돌아온다.
    stage_1-1, 1-2 에 구역 2 개(1-1 줌0.72 / 1-2 줌0.62, 전환폭 1400px)가 심겨 있다.

· 레벨 도구 3 종 (레벨 좌표를 고쳤으면 반드시 돌릴 것)
    tools/레벨검사.gd       — 도달성 · 소프트락 · 치명 낙차 보고 (--지도 / --상세)
    tools/치수_보정.gd      — 점프거리 기준 자동 보정 (--적용 없으면 미리보기만)
    tools/배치_스테이지1.gd — 라벨 자리에 장애물 배치 + 카메라 구역 (멱등)

────────────────────────────────────────────────────────────────
[반드시 지킬 것]
────────────────────────────────────────────────────────────────
 1. 팀 파일 무수정: scripts/player.gd · gun.gd · bullet.gd · player_anim.gd ·
    color_defs.gd · scenes/player/Player.tscn · scenes/world_1/stage_1/stage_1.tscn

 2. ★★owner 규칙 (이걸 어겨서 엔진이 두 번 죽었다)
    · 코드로 **처음부터 만드는** 씬(build_*.gd) → 트리 전체에 owner 를 준다. 그래야 저장된다.
    · **이미 있는 씬을 읽어서 손보는** 도구 → **우리가 새로 만든 노드에만** 준다.
      불러온 노드는 이미 owner 가 맞고, _ready() 가 만든 노드는 owner 가 없어야 정상이다.
      여기에 owner 를 박으면 씬에 **노드가 두 벌** 저장되고, 씬 교체 때
      해제된 짝을 참조해 **signal 11 세그폴트**가 난다.
    · 인스턴스는 **루트에만**. 안쪽 자식에는 절대 주지 않는다.

 3. **셰이더 코드에는 한글 식별자를 못 쓴다.** GDScript 는 되지만 GDShader 는 거부하고,
    셰이더가 통째로 컴파일 실패해 **효과가 조용히 사라진다.**

 4. **GDScript 의 `_set()` 안에서 그 프로퍼티를 다시 대입하지 말 것.**
    `_set` 은 네이티브 프로퍼티보다 먼저 불려서 **무한 재귀 → 스택 오버플로**가 난다.
    Sprite2D 라면 `texture_changed` 신호를 쓸 것.

 5. 지형 함수를 새로 만들면 **감김 방향을 시계 방향**으로. 반대면 콜리전 생성이 실패해
    "Convex decomposing failed!" 와 함께 **밟을 수 없는 지형**이 된다.

 6. 새 PNG·새 class_name 을 추가하면 Godot --headless --path . --import 를 먼저 돌릴 것.

────────────────────────────────────────────────────────────────
[레벨 디자인 수치 — 실측값 (하드코딩하지 말고 도구로 재라)]
────────────────────────────────────────────────────────────────
  플레이어      폭 44px · **키 97px**   ← 2026-08-06 에 47px 에서 바뀌었다
  점프 높이     160px      점프 거리 320px      도약대 상승 418px
  치명 낙하     520px (넘으면 즉사)

  → 올라가는 단차     ≤ 110px  (판정선 128 보다 15px 이상 여유)
  → 가로 간격         ≤ 260px
  → 발판 위 머리 공간 ≥ 105px  (플레이어 키 + 8) ← ★키가 바뀌면 이 값도 바뀐다
  → 통로 안 높이      ≥ 130px

────────────────────────────────────────────────────────────────
[검증 명령 — 커밋 전에 전부]
────────────────────────────────────────────────────────────────
  Godot --headless --path . --import
  Godot --headless --path . -s res://tools/check_스마트월드.gd        # 파싱 30
  Godot --headless --path . -s res://tools/test_페인트v4.gd           # 32
  Godot --headless --path . -s res://tools/test_낙하사망.gd           # 11
  Godot --headless --path . -s res://tools/test_통로전환.gd           # 19
  Godot --headless --path . -s res://tools/test_카메라연출.gd         # 10
  Godot --headless --path . -s res://tools/test_lobby_flow.gd
  Godot --headless --path . -s res://tools/레벨검사.gd -- <씬> [--지도] [--상세]
  # 기존 회귀: test_paint_system / test_color_death / test_world_flow /
  #            test_paint_v3 / test_stages

  # 씬 재생성 (순서 중요)
  Godot --headless --path . -s res://tools/스마트월드_연결.gd
  Godot --headless --path . -s res://tools/치수_보정.gd -- <씬> --적용
  Godot --headless --path . -s res://tools/배치_스테이지1.gd
  Godot --headless --path . -s res://tools/build_장애물씬.gd

  ⚠ SceneTree 테스트 함정 3가지
   1) _init() 에 Engine.max_fps = 60
   2) _init() 에서 add_child 한 노드는 그 프레임에 _ready 가 안 돈다
   3) await 가 있는 함수는 **await 로 불러야** 한다. 안 그러면 코루틴만 만들고
      다음 줄 quit() 이 먼저 실행돼 아무것도 안 하고 끝난다.

────────────────────────────────────────────────────────────────
[남은 일 — 위에서부터 우선순위]
────────────────────────────────────────────────────────────────
 1. 디자이너 이미지 꽂기. 슬롯은 다 파 뒀다 (scenes/스마트월드_장애물/*.tscn 의 `그림`).
 2. 사운드 4종 (색칠 성공 / 회색화 / 착지 / 사망). 12차부터 계속 밀린 최우선 공백.
 3. stage_1-1, 1-2 에 식물 외 장애물 라벨 추가 (대응표에 15종 준비돼 있다).
 4. 스마트월드 3편 — 스마트월드_2 출구의 `다음_씬` 한 줄만 바꾸면 이어진다.
 5. 스마트월드_1 상단 루트(발판_5/6/7)는 도약대로 겨우 닿는다. 실플레이 후 조정.
 6. 지형 시스템 4개(v2 타일맵 / v3 PaintPlatform / TilePaintMap / SS2D) 정리 방향 결정.
    ★이게 정해져야 씬 폴더 정리를 할 수 있다.

────────────────────────────────────────────────────────────────
[팀 협업 — 브랜치 통째 교체 금지]
────────────────────────────────────────────────────────────────
 2026-08-07 에 "dev_4 를 로컬 작업본으로 통째 교체" 커밋 하나로 남의 작업 55개가 지워졌다.
 · 작업 전 git pull → 충돌은 파일 단위로 해결
 · 로컬 폴더를 통째로 덮어쓰지 말 것
 · 커밋 전 git status 로 **내가 안 지운 파일이 D(삭제)로 잡혀 있는지** 확인
```

---

## 부록 · 이전 판 (2026-08-06)

<details>
<summary>펼치기</summary>

# 다음 작업 인수인계 프롬프트 (2026-08-06 갱신)

> 다음 세션에서 **아래 회색 블록을 통째로 복붙**하면 바로 이어서 작업할 수 있다.
> 최신 상세: `docs/작업기록_2026-08-06_스마트월드_메인화_낙하사망_통로연결_1-2제작.md`
> 이전 판(2026-07-24, 페인트 v3 시절)은 이 문서 맨 아래 §부록 에 보관.

---

```text
프로젝트: C:\Users\김도형\Documents\GitHub\color   (Godot 4.6-beta2, 브랜치 dev_4)
엔진 경로: C:\Users\김도형\OneDrive\Desktop\Godot_v4.6-beta2_win64.exe\Godot_v4.6-beta2_win64_console.exe

────────────────────────────────────────────────────────────────
[지금 상태 — 2026-08-06 기준]
────────────────────────────────────────────────────────────────
· F5 로 실행하면 **스마트월드 1-1** 이 뜬다 (project.godot 의 run/main_scene).
  로비는 살아 있다 — ESC 메뉴 → "로비로 돌아가기".

· 본편 라인 = 스마트월드 2 개. 통로로 이어져 있다.
    scenes/스마트월드/스마트월드_1-1.tscn   자연 → 물가   길이 4,400px
    scenes/스마트월드/스마트월드_1-2.tscn   물가 → 판자촌 길이 9,200px  ★신규
  두 씬 모두 tools/build_스마트월드_1-*.gd 가 생성한다. **직접 편집하면 다음 빌드에 날아간다.**

· 페인트 규칙 v4 는 scripts/스마트월드/페인트_코어.gd 한 파일에만 있다(변경 없음).
  (반대색→회색 / 회색은 수동회수 불가·2발 / FIFO 회수 / 스테이지 이동 시 전량 회수)

· 오브젝트 14종: 식물A·식물B·물·연기·통과플랫폼·배관·제어레버·호퍼·물저장고·송풍기·발광체
                 + ★도약대 · ★무너지는바위 · ★색레이저 (2026-08-06 신규)

· ★낙하 사망 — 좌표가 아니라 **한 번에 떨어진 거리**로 죽는다.
    scripts/스마트월드/낙하_감시.gd  ·  기본 치명 거리 520px (= 점프 높이 160px × 3.25)
    55% 지점부터 화면 가장자리 비네트로 경고한다.
    죽으면 **마지막으로 안전하게 서 있던 땅**으로 돌아간다(자동 체크포인트).

· ★스테이지 이동은 포탈이 아니라 통로다.
    scripts/스마트월드/연결통로.gd   지형에 파인 굴. 바닥·천장·뒷벽을 스스로 짓는다.
    scripts/스마트월드/장면전환.gd   암전 0.85초 → 씬 교체 → 점등 1.35초 (VINE 식)
    출구 통로의 `속빛` = 다음 지역 하늘색이라 지나기 전에 다음 분위기가 보인다.

· ★레벨 검사기 — tools/레벨검사.gd
    실제 물리로 지형을 훑어 "못 가는 발판 / 갇히는 곳 / 죽는 낙차" 를 숫자로 보고한다.
    **레벨 좌표를 하나라도 고쳤으면 반드시 돌릴 것.**

────────────────────────────────────────────────────────────────
[반드시 지킬 것]
────────────────────────────────────────────────────────────────
 1. 팀 파일 무수정: scripts/player.gd · gun.gd · bullet.gd · player_anim.gd ·
    color_defs.gd · scenes/player/Player.tscn · scenes/world_1/stage_1/stage_1.tscn
    → 필요하면 scripts/스마트월드/ 에 새 파일을 만들고 **런타임에 조립**한다(월드.gd 방식).

 2. 생성된 씬 직접 편집 금지: scenes/스마트월드/*.tscn · scenes/스테이지/* · zone_* · world_1
    → 각각의 tools/build_*.gd 를 고치고 재실행.

 3. 씬을 코드로 만들 때 **owner 는 인스턴스 노드 자신까지만.**
    자식에게 주면 PackedScene 이 노드를 두 벌 저장한다(Gun2·Placeholder2 사건, 2026-08-02).
    → tools/지형공통.gd 의 `주인_지정()` 을 쓸 것. 이미 처리돼 있다.

 4. **셰이더 코드에는 한글 식별자를 못 쓴다.** GDScript 는 되지만 GDShader 는 거부하고,
    셰이더가 통째로 컴파일 실패해 **효과가 조용히 사라진다.** 셰이더 안은 전부 ASCII.

 5. 한글 변수명은 되지만 'ㄹ' 같은 **자음 한 글자**는 Godot 이 경고한다.

 6. .aseprite 원본은 옛 크기다. PNG 를 직접 손봤으면 aseprite_export.ps1 쓰지 말고 복사만 할 것.

 7. 새 PNG 를 넣으면  Godot --headless --path . --import  를 먼저 돌릴 것.

 8. 지형 함수를 새로 만들면 **감김 방향을 시계 방향으로 맞출 것.**
    반대면 콜리전 생성에서 "Convex decomposing failed!" 가 나고 **밟을 수 없는 지형**이 된다.

────────────────────────────────────────────────────────────────
[레벨 디자인 수치 — 플레이어 실제 물리에서 역산한 값]
────────────────────────────────────────────────────────────────
  점프 높이        160px   (10칸 × 16px)
  점프 거리        320px   (20칸 × 16px)
  도약대 상승      418px   (도약속도 −1500 기준)
  치명 낙하 거리   520px   (넘으면 즉사)

  → 올라가는 단차   ≤ 110px   (판정선 128px 보다 15px 이상 여유를 둘 것 ★)
  → 가로 간격       ≤ 260px
  → 의도치 않은 낙차 ≤ 520px
  → 통로 안 높이     ≥ 100px  (플레이어 콜리전 높이 약 47px)
  → 발판 위 머리 공간 ≥ 52px   (이게 없으면 설 수 없다 — 겹침 사고의 원인 1위)

  ⚠ "명목 간격 125px" 로 잡으면 선반 두께·기울기 때문에 **실측 131px** 이 나온다.
     반드시 레벨검사기로 실측을 확인할 것.

────────────────────────────────────────────────────────────────
[검증 명령 — 커밋 전에 전부 돌린다]
────────────────────────────────────────────────────────────────
  Godot --headless --path . --import                                  # 새 에셋 추가 후 필수
  Godot --headless --path . -s res://tools/check_스마트월드.gd         # 파싱 29
  Godot --headless --path . -s res://tools/test_페인트v4.gd            # 페인트 규칙 32
  Godot --headless --path . -s res://tools/test_낙하사망.gd            # 낙하 사망 11
  Godot --headless --path . -s res://tools/test_통로전환.gd            # 통로 연결 19
  Godot --headless --path . -s res://tools/레벨검사.gd -- res://scenes/스마트월드/스마트월드_1-1.tscn
  Godot --headless --path . -s res://tools/레벨검사.gd -- res://scenes/스마트월드/스마트월드_1-2.tscn --지도

  # 씬 재생성 (덮어쓰기 주의)
  Godot --headless --path . -s res://tools/build_스마트월드_1-1.gd
  Godot --headless --path . -s res://tools/build_스마트월드_1-2.gd

  # 화면 확인 (창모드 — 헤드리스는 렌더 안 됨)
  Godot --path . -s res://tools/screenshot_scene.gd -- <씬> <저장.png> [x] [y]
  Godot --path . -s res://tools/shot_페인트_확인.gd -- 결과.png 2080 700 0.75
  Godot --path . -s res://tools/shot_메뉴_확인.gd  -- 메뉴.png 0.55

  # 기존 회귀 (건드리지 않았어도 한 번씩)
  test_paint_system / test_color_death / test_world_flow / test_lobby_flow /
  test_paint_v3 / test_stages

  ⚠ SceneTree 테스트 함정 2가지 (둘 다 실제로 가짜 실패를 만든 적 있음)
     1) _init() 에 Engine.max_fps = 60   (헤드리스는 fps 무제한 → 시간 기반 로직이 어긋난다)
     2) _init() 에서 add_child 한 노드는 그 프레임에 _ready 가 안 돈다

────────────────────────────────────────────────────────────────
[남은 일 후보 — 위에서부터 우선순위]
────────────────────────────────────────────────────────────────
 1. 사운드 4종 (색칠 성공 / 회색화 / 착지 / 사망).
    12차부터 계속 밀린 최우선 공백. 낙하 사망이 들어간 지금은 낙하 바람소리·착지음이 특히 아쉽다.
 2. 스테이지 1-3 제작. 1-2 출구의 `다음_씬` 한 줄만 채우면 이어진다(지금은 1-1 로 순환).
    바이옴은 판자촌 → 도심으로 (StageBackdrop 팔레트에 이미 있다).
 3. 지형 시스템 4개 정리 방향 결정 (v2 타일맵 / v3 PaintPlatform / TilePaintMap / SS2D).
    ★이게 정해져야 씬 정리를 할 수 있다.
 4. 레벨검사.gd 에 **물감 이코노미 검사** 추가 —
    스테이지를 깨는 데 필요한 최소 발수를 계산해 페인트코어의 `최대_탄약` 과 비교.
    (레벨디자인_가이드 §2 공식을 코드로 옮기는 것)
 5. 실플레이 튜닝 — 무너지는바위 `버티는시간` 0.7초 / 색레이저 `주기` 3.2초 /
    도약대 `도약속도` −1500 / 치명 낙하 520px. 자동 테스트로는 못 잡는 유일한 영역.
 6. 식물B 지대 판정과 scenes/경계/color_zone.gd 통합 (지금 판정이 두 군데).
 7. 물이 배관 경로를 따라 흐르게 (지금은 배관이 색만 따라가고 물은 직선 낙하).

────────────────────────────────────────────────────────────────
[작업 후에 할 것]
────────────────────────────────────────────────────────────────
 · 코드를 고치면 [2026-MM-DD 이름] 주석 + **왜 그랬는지**를 반드시 남긴다.
 · docs/작업현황_도형.md 맨 위에 날짜 항목 추가 + 상세 작업기록 문서 신설.
 · docs/파일_분류_인덱스.md 에 새 파일 등록.
 · 이 프롬프트 파일([지금 상태] 블록)을 갱신.
```

---

## 부록 · 이전 판에서 아직 유효한 것 (2026-07-24 페인트 v3 시절)

스마트월드가 본편이 되었지만 **v2/v3 스테이지는 그대로 살아 있다.** 그쪽을 건드릴 때는 아래가 유효하다.

### 색칠 시스템이 여러 벌 공존한다 (일부러 그렇게 뒀다 — 회귀 방지)

| | v2 (타일맵 셀) | v3 (플랫폼 단위) | v4 (SS2D 지형) |
|---|---|---|---|
| 코어 | `scripts/proto/paint_system.gd` | `scripts/proto/paint_platform.gd` | `scripts/스마트월드/페인트_코어.gd` |
| 조립 | `zone_lab.gd` | `stage_lab.gd` | `scripts/스마트월드/월드.gd` |
| 쓰는 씬 | zone_01 / zone_02 / world_1 | 스테이지 1~5 / zone_04 / zone_05 | **스마트월드 1-1 / 1-2** |
| 색칠 단위 | 타일 1칸 | 플랫폼 1개 | SS2D 지형 1개 |
| 아트 | `terrain_tileset.tres` | 디자이너 타일셋 5종(384px) | `smartshape/` 픽셀 타일 |

### 아직 결정 안 된 기획 안건

**무색 발판은 "유령"인가 "안전한 징검다리"인가** — 문서 2건이 정면으로 반대다.

| 문서 | 서술 |
|---|---|
| `레벨디자인_가이드.md` §3 V1 | 무색 발판 = **칠해야만 밟을 수 있음** |
| `기획서_규칙_플로우차트.md` 1-5 | 무색(칠 가능) 발판 = **안전** |

후자를 따르면 색칠할 이유 자체가 없어진다. 현재 구현은 **가이드(V1) 쪽**이다
(`스마트지형.무색일때_통과` · 기본 켜짐 / `PaintPlatform.무색일때_통과`).
**어느 쪽으로 정하든 이 한 줄만 바꾸면 전 스테이지가 따라온다.** 정한 뒤 문서 한쪽을 고칠 것.


</details>

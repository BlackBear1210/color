# 하수도 2-7 · 눌러 두어라

작업일: 2026-09-06
작업자: Codex

## 만든 것

- `scenes/world_2_클로드/stage_2-7.tscn`
  - 버튼과 무게를 배우는 하수도 2-7을 만들었다.
  - 지형은 전부 검정 SS2D 시멘트이며 회색 지형을 쓰지 않았다.
  - 배관은 `TEMPLATE_PIPE_OPEN_GRAY.tscn` SS2D 인스턴스 5개만 썼고 서로 겹치지 않게 배치했다.
  - 관문 1: 플레이어가 밟는 동안만 문이 내려간다.
  - 관문 2: 색 없는 박스를 버튼에 남겨 문을 계속 내린다.
  - 관문 3: 물을 채운 검정 양동이만 버튼을 누른다.
  - 관문 4: 양동이와 플레이어가 각각 버튼 하나씩을 동시에 눌러 문을 내린다.
  - 관문 5: 박스를 버튼에 남겨 움직이는 땅을 틈으로 끌어와 끝까지 간다.

- `scripts/스마트월드/박스.gd`
- `scenes/집/스마트월드_장애물/박스.tscn`
  - 색이 없고 누구나 밀 수 있는 `CharacterBody2D` 무게 상자다.
  - 구멍에 빠지면 최초 위치로 되돌아와 퍼즐이 잠기지 않는다.
  - `박스` 그룹으로 압력 버튼이 감지한다.

- `scripts/스마트월드/압력버튼.gd`
  - `양동이` 그룹은 `물참 = true`일 때만 버튼을 누르게 했다. 빈 양동이는 무게추가 아니다.
  - `동시_버튼들` NodePath 배열을 추가했다. 한 버튼만 문을 움직이되, 지정한 모든 다른 버튼이 함께 활성일 때만 작동한다.

## 정적 확인

- 2-7 외부 리소스 경로, 버튼 대상 NodePath 6개, 버튼 5개·박스 2개·양동이 2개를 확인했다.
- `배관.tscn` 사용 0개, 회색 지형 상태(`시작상태 = 3`) 0개를 확인했다.
- `git diff --check`와 새 파일의 후행 공백 검사를 통과했다.

## 실행 검사

Godot 실행·헤드리스 검사는 하지 않았다. 이 PC의 Godot 4.6.x 메모리 읽기 충돌 이력과 저장소 안전 규약 때문에,
실제 조작 확인은 충돌 문제가 해결된 환경에서 해야 한다.

# 작업 현황 — 도형 (레벨 디자인 / 씬 통합 / 작동 검증)

> 이 파일은 **도형의 전용 작업 기록**입니다.
> 다른 작업자는 병합 충돌이 나거나 뭔가 갑자기 안 될 때, 이 파일의 최신 날짜 항목부터 읽으면
> "누가 어떤 파일을 왜 건드렸는지"를 바로 확인할 수 있습니다.
> 새 작업은 **맨 위에** 날짜 단위로 추가합니다. (최신이 항상 위)

---

## 📁 프로젝트 구조 맵 (dev_4 기준)

```
color/
├─ main.tscn                  ← 실행 진입점 (Player + Stage1 배치)
├─ project.godot              ← 입력: A/D·화살표=이동, Space=점프, Shift=흑백 전환, 좌클릭=사격
├─ docs/                      ← 문서 전용 (기획서·프롬프트팩·이 파일)
│  ├─ 작업현황_도형.md         ← ★ 이 파일. 도형의 작업 기록
│  ├─ 기획서_규칙_플로우차트.md ← ★★ 팀 기준 문서 (규칙 명세+플로우차트+결정 5건)
│  ├─ 레벨디자인_가이드.md      ← LD 작업 기준표 (이코노미·부품·30구역·씬 규칙)
│  ├─ 다음작업_프롬프트.md      ← 다음 세션 인수인계 프롬프트
│  ├─ 게임_기획서_세계관_디자인_도형.md   ← (구) 아트 디렉션 문서 — 명도 규칙표는 계속 유효
│  └─ 수정된세계_클로드디자인_프롬프트팩.md ← (구) 목업용 프롬프트팩
├─ assets/
│  ├─ tilesets/
│  │  └─ terrain_tileset.tres ← ★ 공용 지형 타일셋 (새 스테이지는 이걸 사용)
│  └─ textures/
│     ├─ 임시타일.svg          ← 임시 타일 텍스처 (Godot 로고, 128×128 = 16px 타일 8×8)
│     └─ tiles/               ← ★ 진짜 타일 이미지가 들어갈 폴더 (흑/백/회 지형)
├─ scenes/
│  ├─ player/Player.tscn      ← 플레이어 (이동·점프 + Shift 흑백 전환)
│  ├─ bullet/Bullet.tscn      ← 총알 (맞은 타일을 회색으로 칠함 = 페인트 로직)
│  ├─ obstacles/              ← ★ 장애물 씬 폴더 (프로그래머 코드 + 디자이너 이미지 조립)
│  │  └─ obstacle_template.tscn  ← 장애물 만들 때 복제해서 쓰는 뼈대 씬
│  └─ world_1/
│     └─ stage_1/stage_1.tscn ← 팀이 만든 스테이지 1 (인라인 타일셋 사용 중)
└─ scripts/                   ← player.gd / gun.gd / bullet.gd / color_defs.gd(색 상수)
```

### 폴더 규칙 (팀 합의용 제안)
- **스테이지 추가**: `scenes/world_1/stage_N/stage_N.tscn` — 지형은 `terrain_tileset.tres`로 찍는다.
- **장애물 추가**: `obstacle_template.tscn`을 `scenes/obstacles/이름.tscn`으로 복제 →
  Sprite2D에 디자이너 이미지, 스크립트 슬롯에 프로그래머 코드 부착 → 스테이지 씬에 인스턴스로 배치.
- **타일 이미지**: `assets/textures/tiles/`에 넣고, `terrain_tileset.tres`에 아틀라스로 등록.
- **문서**: 루트에 두지 말고 `docs/`에.

---

## 🗓 업데이트 로그

### 2026-07-13 (4차) — 커밋·푸시 + 예비 타일셋 32px + 구역 씬 템플릿 (도형)

**1. 정리분+문서 전부 커밋·푸시 완료** (커밋 e822df4 → 병합 c7c64ad → origin/dev_4)
- 푸시 중 팀원의 f9dce6e("팀원 색전환 작업물을 루트로 반영, 중복 Downloads 폴더 제거")와 겹쳤으나
  **서로 동일한 정리를 한 것**이라 충돌 없이 병합됨 (스크립트 내용 diff 0 확인).

**2. 예비(플레이스홀더) 타일셋 — 32px 기준으로 교체**
- `assets/textures/tiles/placeholder_tiles.svg` 신규: 32px 타일 4종 —
  (0,0)흑 / (1,0)백 / (2,0)회색(X 무늬=죽은 지형) / (3,0)무색(점선=칠 가능)
- `terrain_tileset.tres` 재작성: tile_size 32×32, 4타일 전부 풀 사각 충돌.
  ART 타일 완성 시 **같은 배치의 이미지로 교체만** 하면 모든 씬에 자동 반영.
- ⚠ 규격은 기획서 결정②(32px)의 추천값 — 7/15에 다른 규격으로 확정되면 재생성 필요.

**3. 구역 씬 템플릿** `scenes/world_1/zone_template/zone_template.tscn`
- 새 구역 만들 때: 이 폴더를 복제 → `zone_NN`으로 이름 변경 → 지형 찍고 마커 위치만 조정.
- 구성 노드 (전부 그룹 지정 완료, 스크립트는 P-A/P-B가 그룹 기준으로 연결):
  | 노드 | 그룹 | 용도 |
  |---|---|---|
  | TileMapLayer | — | 공용 타일셋으로 지형 찍는 곳 |
  | SpawnPoint (Marker2D) | `spawn_point` | 플레이어 시작 위치 |
  | Checkpoint (Area2D) | `checkpoint` | 부활 지점 (P-B W2 시스템 연결 예정) |
  | SwitchZone (Area2D) | `switch_zone` | 이 영역 안에서만 Shift 전환 허용 (P-A 경계 시스템) |
  | ExitZone (Area2D) | `zone_exit` | 다음 구역 트리거 (P-B 진행 시스템) |
  | KillZone (Area2D) | `killzone` | 하단 낙사 판정 |
  | Obstacles (Node2D) | — | 장애물 인스턴스 배치용 컨테이너 |
- 검증: 임포트 오류 0, 템플릿 씬 단독 실행 0, 전체 게임 실행 0.

### 2026-07-13 (3차) — 기획서·플로우차트 + 레벨디자인 가이드 작성 (도형, W1 ①번 작업)

- 근거 문서 4건(개발 일정표, 스토리 기획안 ACFG, 도형_신우_스토 GRAY, 배경·지형 컨셉 초안) 정독 후 작성:
  - `docs/기획서_규칙_플로우차트.md` — **팀 기준 문서.** 상호작용 전체 명세(발사/회수 FIFO/회색 혼합/
	경계/판정/사망), 플로우차트 6종, **최우선 결정 5건 추천안**(7/15 확정 대기), 유사 게임 비교,
	스토리 통합안(최소 연출 + 엔딩 흑백→컬러, 채도 셰이더 트릭)
  - `docs/레벨디자인_가이드.md` — 물감 이코노미 공식, 소프트락 금지 3규칙, 퍼즐 부품 V1~V8,
	30구역 골격 초안, 가독성 명도표, 구역 체크리스트, 씬 파일 규칙
  - `docs/다음작업_프롬프트.md` — 다음 세션 인수인계용 (복붙 프롬프트)
- 코드는 이번에 하나도 수정하지 않음 (문서 작업만).
- ⚠ 스토리 방향 변경 확정: 회색이 규칙상 페널티(혼합·회수불가)가 되어 GRAY 스토리의 "회색=이해자"
  상징과 충돌 → 대사·컷신 없는 최소 연출 + 엔딩 컬러 전환으로 정리 (기획서 5장).

### 2026-07-13 (2차) — 잘못 커밋된 Downloads 폴더 정리 + 최신 스크립트 제자리 이동 (도형)

- 커밋 213749a("테스트완료")에 프로젝트 사본이 통째로 `Downloads/color-dev_4/color-dev_4/`
  경로로 잘못 커밋되어 있었음. **그 안의 스크립트가 루트보다 최신 버전**이었음
  (색 전환 + 페인트 총알 기능 포함).
- 조치 (코드 내용은 한 줄도 수정하지 않음, 위치만 정리):
  - `scripts/player.gd`, `gun.gd`, `bullet.gd` → Downloads 안의 최신 버전으로 교체
  - `scripts/color_defs.gd` (+.uid) → 루트 scripts/로 이동 (신규 파일)
  - `project.godot` → `toggle_color`(Shift) 입력이 추가된 최신 버전으로 교체
  - `Downloads/` 폴더 → 저장소에서 삭제 (나머지 파일은 전부 루트와 동일함을 diff로 확인)
- 검증: Godot 4.6.3 헤드리스 임포트 + 120프레임 실행 → 오류 0.
- 이로써 현재 게임 기능: 이동/점프(Space, 코요테·점프버퍼), **Shift = 흑백 전환**,
  좌클릭 사격 → **맞은 타일이 회색으로 칠해짐**.

### 2026-07-13 (1차) — dev_4 전환 완료 + 폴더 구조 정리 (도형)

**1. dev_2 → dev_4 브랜치 전환 오류 해결 (GitHub Desktop에서 안 받아지던 문제)**
- 원인: dev_2에서 작업하던 변경분이 브랜치 전환 시 자동 보관(stash)되었다가
  dev_4 최신을 받으며 복원되는 과정에서 `stage_1.tscn`에 **병합 충돌**이 났고,
  충돌이 해결되지 않은 채 남아 있어 GitHub Desktop이 모든 동작을 막고 있었음.
- 조치:
  - `stage_1.tscn` → **팀이 올린 dev_4 최신 버전(커밋 5a70d62)으로 복원** (dev_2 내용 버림)
  - dev_2의 옛 `stage_2.tscn` / `stage_3.tscn` → dev_4에서 **제거**
	(새 규칙으로 다시 만들 예정. 원본은 dev_2 브랜치에 그대로 남아 있어 언제든 참조 가능)
  - 병합 잔여 상태 파일 정리 → 현재 dev_4는 origin/dev_4와 완전 일치
- **다른 작업자 코드는 하나도 수정하지 않았음.** 순수하게 충돌 상태만 풀었음.

**2. 새로 만든 파일 (구조 정리)**
| 파일 | 용도 |
|---|---|
| `docs/작업현황_도형.md` | 이 파일. 도형 작업 기록 |
| `docs/` (기획서 2개 이동) | 루트에 있던 기획 .md를 docs/로 이동 |
| `assets/tilesets/terrain_tileset.tres` | 공용 지형 타일셋. stage_1의 인라인 타일셋과 **동일 설정**으로 분리 제작 |
| `assets/textures/tiles/` | 타일 이미지 넣을 폴더 (현재 비어 있음) |
| `scenes/obstacles/obstacle_template.tscn` | 장애물 조립용 뼈대 (Area2D + Sprite2D + CollisionShape2D, `hazard` 그룹) |

**3. 확인된 사실 / 팀에 공유할 것**
- 현재 타일셋은 8×8칸 중 **(1,0)과 (1,1) 두 칸에만 충돌**이 설정되어 있음.
  다른 칸으로 지형을 찍으면 플레이어가 통과함 → 타일 이미지 확정 후 충돌 설정 필요.
- `임시타일.svg`는 Godot 로고임. 진짜 타일 이미지로 교체 필요.
- 기획서는 "스페이스바 = 흑백 전환"이지만 실제 구현은 **Space=점프 / Shift=전환**으로 정착됨.
  기획서 표기를 실제 구현에 맞출지 팀 확인 필요.
- 총알 페인트가 기획서의 "반대색으로 뒤집기"가 아니라 **항상 회색으로 칠하기**로 변경됨
  (bullet.gd 주석에 명시됨). 회색=감속 지형 규칙과 연결되는지 프로그래머에게 확인 필요.
- stage_1.tscn은 타일셋을 씬 안에 내장하고 있음. 스테이지가 늘어나면 각 씬의 타일셋이
  제각각이 되므로, 팀 합의 후 `terrain_tileset.tres` 공용 사용으로 통일 권장.

---

## ⚠ 충돌 났을 때 확인 순서 (팀원용)

1. 충돌 난 파일이 `.tscn`이면 → Godot을 **끄고** 해결할 것 (에디터가 파일을 다시 써버림).
2. `stage_*.tscn` 충돌 → 이 파일 최신 로그에서 도형이 그 스테이지를 건드렸는지 확인.
3. `scripts/*.gd` 충돌 → 도형은 원칙적으로 스크립트를 수정하지 않음(수정 시 이 파일에 기록됨).
   기록이 없으면 프로그래머끼리의 충돌.
4. 해결 기준: **씬 배치·지형 = 도형 버전 / 스크립트 로직 = 프로그래머 버전**이 기본.
5. 판단이 어려우면 그대로 두고 팀 채팅에 파일명 + 충돌 부분 스크린샷 공유.

---

## ✅ TODO (도형)

- [ ] 타일 이미지 제작·수급 → `assets/textures/tiles/` → 타일셋 등록 + 충돌 설정
	  (명도 기준: 흑 지형 0~10%, 백 지형 90~100%, 회색 감속 45~55% — 기획서 2-3 규칙표)
- [ ] stage_1 실제 플레이 검증 (충돌 없는 타일 밟히는 구간 있는지)
- [ ] 회색으로 칠해진 타일의 게임적 효과 정의 (감속? 통과? — 현재는 색만 바뀜)
- [ ] 흑백 전환 ↔ 지형 상호작용 구현 조율 (검정 상태일 때 검정 지형만 밟는 규칙, OVIVO 방식)
- [ ] stage_2 새 규칙으로 신규 제작 (기승전결 룸 구조 적용)
- [ ] 사망/리스폰(체크포인트) 사양 정리
- [ ] 기억의 조각 수집 요소 프로토타입

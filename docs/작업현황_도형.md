# 작업 현황 — 도형 (레벨 디자인 / 씬 통합 / 작동 검증)

> 이 파일은 **도형의 전용 작업 기록**입니다.
> 다른 작업자는 병합 충돌이 나거나 뭔가 갑자기 안 될 때, 이 파일의 최신 날짜 항목부터 읽으면
> "누가 어떤 파일을 왜 건드렸는지"를 바로 확인할 수 있습니다.
> 새 작업은 **맨 위에** 날짜 단위로 추가합니다. (최신이 항상 위)

---

## 📁 프로젝트 구조 맵 (dev_4 기준)

> ⚠ **2026-07-21 추가**: 아래 맵은 dev_4 초기(7/13) 스냅샷이라 지금은 낡았음(예: 실행 진입점은
> 이미 `main.tscn`이 아니라 `lobby.tscn`, `경계/`·`scenes/proto/` 등 폴더 누락, `verlet_tail`은
> 7/18에 삭제됨). **최신·정확한 파일 지도는 `docs/파일_분류_인덱스.md`를 볼 것.** 이 맵은 역사적
> 스캐폴딩 기록으로만 남겨둠(삭제하지 않음).

```
color/
├─ main.tscn                  ← 실행 진입점 (Player + Stage1 배치)
├─ project.godot              ← 입력: A/D·화살표=이동, Space=점프, Shift=흑백 전환, 좌클릭=사격
├─ docs/                      ← 문서 전용 (기획서·프롬프트팩·이 파일)
│  ├─ 작업현황_도형.md         ← ★ 이 파일. 도형의 작업 기록
│  ├─ 기획서_규칙_플로우차트.md ← ★★ 팀 기준 문서 (규칙 명세+플로우차트+결정 5건)
│  ├─ 레벨디자인_가이드.md      ← LD 작업 기준표 (이코노미·부품·30구역·씬 규칙)
│  ├─ 다음작업_프롬프트.md      ← 다음 세션 인수인계 프롬프트
│  ├─ 게임_기획서_세계관_디자인_도형.md   ← (구) 아트 디렉션 문서 — 명도 규칙표는 계속 유효
│  ├─ 수정된세계_클로드디자인_프롬프트팩.md ← (구) 목업용 프롬프트팩
│  ├─ 작업기록_2026-07-18_도형_비주얼고도화_심리스전환.md ← ★9차 상세 (시스템·디버깅·아트 워크플로)
│  └─ 파일_분류_인덱스.md      ← ★전체 파일 지도 (파일 찾을 땐 여기서 Ctrl+F)
├─ assets/
│  ├─ tilesets/
│  │  └─ terrain_tileset.tres ← ★ 공용 지형 타일셋 (새 스테이지는 이걸 사용)
│  └─ textures/
│     ├─ 임시타일.svg          ← 임시 타일 텍스처 (Godot 로고, 128×128 = 16px 타일 8×8)
│     └─ tiles/               ← ★ 진짜 타일 이미지가 들어갈 폴더 (흑/백/회 지형)
├─ scenes/
│  ├─ player/Player.tscn      ← 플레이어 (이동·점프 + Shift 흑백 전환)
│  ├─ bullet/Bullet.tscn      ← 총알 (맞은 타일을 회색으로 칠함 = 페인트 로직)
│  ├─ obstacles/              ← ★ 장애물 씬 폴더 (프로그래머 코드 + 디자이너 이미지 조립)
│  │  └─ obstacle_template.tscn  ← 장애물 만들 때 복제해서 쓰는 뼈대 씬
│  └─ world_1/
│     ├─ stage_1/stage_1.tscn ← 팀이 만든 스테이지 1 (인라인 타일셋 사용 중)
│     ├─ zone_01/zone_01.tscn ← ★테스트 존 1: 횟수제 칠하기·회색·FIFO 회수 검증
│     ├─ zone_02/zone_02.tscn ← ★테스트 존 2: 색 전환 잠금 (위=백만/아래=흑만) + 패럴랙스 배경
│     └─ world_1/world_1.tscn ← ★★zone1+복도+zone2 통합 심리스 월드 (로비 시작이 여기로)
└─ scripts/                   ← player.gd / gun.gd / bullet.gd / player_anim.gd / color_defs.gd
   └─ proto/                  ← 도형의 프로토: paint_system(v2)·proto_gun·proto_bullet·
								 brush_cursor_ui·zone_lab·zone02_lab·world_lab·
								 proto_camera·proto_motion·verlet_tail·zone_visuals·stage_transition
```

### 폴더 규칙 (팀 합의용 제안)
- **스테이지 추가**: `scenes/world_1/stage_N/stage_N.tscn` — 지형은 `terrain_tileset.tres`로 찍는다.
- **장애물 추가**: `obstacle_template.tscn`을 `scenes/obstacles/이름.tscn`으로 복제 →
  Sprite2D에 디자이너 이미지, 스크립트 슬롯에 프로그래머 코드 부착 → 스테이지 씬에 인스턴스로 배치.
- **타일 이미지**: `assets/textures/tiles/`에 넣고, `terrain_tileset.tres`에 아틀라스로 등록.
- **문서**: 루트에 두지 말고 `docs/`에.

---

## 🗓 업데이트 로그

### 2026-08-07 (23차) — 도형 · 복구 + 스마트월드1↔2 통로연결 + 장애물 씬화 + 바인 카메라

**상세: `docs/작업기록_2026-08-07_도형_복구_스마트월드12연결_장애물씬화_바인카메라.md`**

- **① 사라진 파일 55개 복구.** `1badd2b`("dev_4 를 신우 로컬 작업본으로 통째 교체")가
  머지가 아니라 통째 복사라 남의 작업이 조용히 지워졌다. 8/6 우리 작업 23개 +
  **우리 것이 아닌데 같이 지워진 32개**(stage_1-1.tscn · scenes/플랫폼/ 8개 · 8/1·8/2 작업기록 ·
  aseprite_export.ps1 등)를 `c81df2e` 에서 되살렸다. **신우님 실변경분은 전부 보존.**
  복구 직전 상태는 브랜치 `복구전_백업_20260807` 에 있다.
- **② 스마트월드 1 ↔ 2 통로 연결.** `스마트월드_테스트`→**스마트월드_1**,
  어제 만든 폐수로→**스마트월드_2**. 원본을 재생성하지 않고 **읽어서 통로만 얹어** 저장하므로
  신우님 에디터 편집(칠하기_허용)이 100% 보존된다. 포탈 없음 — 걸어 들어가면
  0.85초 암전, 다음 스테이지에서 **1.35초에 걸쳐 밝아지며** 걸어 나온다.
- **③ 장애물 14종을 씬(.tscn)으로.** `scenes/스마트월드_장애물/`. 각 씬에 빈 `그림` 슬롯이 있어
  **디자이너는 Texture 만 끌어다 놓으면 끝**. 슬롯이 비면 지금처럼 코드가 그린다(회귀 없음).
  `기준_크기` 덕에 몇 px 로 그리든 게임 안 크기는 항상 같다.
- **④ stage_1-1, 1-2 라벨 자동 배치 + 바인식 카메라.** 신우님 라벨 12개 자리에 장애물을 꽂고,
  1-1↔1-2 구역 카메라를 심었다. ★전환이 **시간 트윈이 아니라 플레이어 x 좌표**로 굴러간다 —
  멈추면 화면도 멈추고 되돌아가면 정확히 되돌아온다(Vane 이 혹평받은 지점을 피한 설계).
- **⑤ ★장애물 치수 재검증.** 신우님이 **플레이어 콜리전을 키 47px → 97px** 로 바꿨는데
  검사기가 "머리 공간 52px"를 상수로 박고 있었다 → 씬에서 직접 재도록 고쳤다.
  그리고 **스마트월드1이 통째로 막혀 있었다**(밟는 지형 26개 중 도달 6개) —
  첫 발판 단차가 152px 로 점프(160px) 여유 8px. `tools/치수_보정.gd` 를 만들어
  **도약대 신설 → 발판 하강 → 갇힘 해소** 순으로 자동 보정 → **31개 중 30 도달 · 갇힘 0**.
- **⑥ ⚠엔진이 죽던 크래시 수정.** 통로를 지나면 Godot 이 signal 11 로 죽었다.
  원인은 후처리 도구가 `owner` 를 **트리 전체에 박아** 런타임 생성 노드가 씬에 중복 저장된 것.
  (2026-08-02 Gun2 사고와 같은 계열) 덤으로 `아트슬롯._set()` 무한 재귀도 잡았다.
- **⑦ 로비 재구성 + ESC 게임 종료.** 시작=stage_1-1,1-2 / 스마트월드=1→2 /
  테스트월드 제작 / 기타 스테이지. ESC 에 게임 종료(확인 창)와 전체화면 토글 추가.
- **검증**: 파싱 30/0 · 페인트 32/32 · 낙하사망 11/11 · 통로전환 19/19 ·
  **카메라연출 10/10** · 로비/paint_system/color_death/world_flow 전부 통과 ·
  paint_v3 37/0 · stages 56/0.

---

### 2026-08-06 (22차) — ★스마트월드 본편화 · 낙하 사망 · 통로 연결 · 1-2 제작 · 저장소 정리 (도형·Claude)

**상세: `docs/작업기록_2026-08-06_스마트월드_메인화_낙하사망_통로연결_1-2제작.md`**
**인수인계: `docs/다음작업_프롬프트.md` (전면 갱신)**

- **① F5 진입점이 로비 → 스마트월드 1-1 로 바뀌었다** (`project.godot` 1줄).
  로비는 살아 있다 — ESC 메뉴의 "로비로 돌아가기" 로 그대로 간다. 에디터에서 F6 로도 확인 가능.
- **② ★낙사 규칙 교체 — 좌표가 아니라 "한 번에 떨어진 거리"**(`낙하_감시.gd` 신규).
  기본 520px = 점프 높이(160px)의 3.25배. 착지를 기다리지 않고 **즉시** 죽는다.
  55% 지점부터 화면 가장자리 비네트로 경고하고, 죽으면 **마지막으로 안전하게 서 있던 땅**으로
  돌아간다(자동 체크포인트). → 바닥이 뚫려 있어도 되고 깊은 방도 만들 수 있게 됐다.
- **③ ★포탈 제거, 통로 연결**(`연결통로.gd` · `장면전환.gd` 신규).
  지형에 파인 굴로 걸어 들어가면 화면이 어두워지고(0.85초), 다음 스테이지의 같은 굴에서
  서서히 밝아지며(1.35초) 걸어 나온다. 통로가 **바닥·천장·뒷벽을 스스로 지어서** 뛰어넘어
  갈 수 없다. 출구 속빛 = 다음 지역 하늘색이라 지나기 전에 그쪽 분위기가 보인다(VINE 식).
- **④ ★스마트월드 1-2 "폐수로" 신규** — 일자 바닥 · 길이 9,200px(1-1 의 2.1배) ·
  레인월드식 수직 구조(넓은 홀 ↔ 좁은 굴 ↔ 수직 갱도 ↔ 하층 수로 ↔ 굴뚝 ↔ 구멍 다리).
  stage_1-1(동현 테스트월드)의 타일 데이터를 실제로 풀어 재서 **구조 비례만** 옮겼다.
  지형은 도형님 타일 중 **한 번도 안 쓰였던 변형 타일**(center2/left2/right2/allaround)로
  새 재질(`지형_거친암반` · `지형_덩어리`)을 조립해 1-1 과 룩이 다르다.
- **⑤ ★레벨 검사기 신설**(`tools/레벨검사.gd`). 실제 물리로 지형을 훑어
  **못 가는 발판 / 갇히는 곳 / 죽는 낙차**를 숫자로 보고한다. 이번에 눈으로는 안 보이는
  설계 사고 5건을 잡았다(발판 단차 133px, 선반과 통과플랫폼이 9px 간격으로 겹쳐 머리 공간 없음 등).
- **⑥ 신규 장애물 3종** — 도약대(수직 이동) / 무너지는바위(칠하면 단단해짐) /
  색레이저(물감 0발로 통과 가능한 반사신경 구간).
- **⑦ ★저장소 정리** — `Downloads/도형이꺼/` · `OneDrive/Desktop/새 폴더 (2)/` 두 사본
  (**추적 파일 1,005개 = 전체의 61%, 28MB**)을 저장소 밖 `_color_정리백업_2026-08-06/` 로 옮겼다.
  `.gitignore` 에 재발 방지 규칙 추가. 빈 폴더 2개 삭제.
- **⑧ 버그 2건 수정** — 셰이더에 한글 식별자를 써서 비네트가 조용히 안 나오던 것 /
  `is_on_floor()` 의 stale 값 때문에 허공이 체크포인트로 저장되던 것.
- **검증**: 파싱 29/0 · 페인트 32/32 · 낙하사망 11/11 · 통로전환 19/19 ·
  레벨검사 1-1 31/32 · 1-2 47/48 (나머지는 밟는 물건이 아님) · 기존 회귀 6종 전부 통과.

---

### 2026-07-25 (18차) — ★배경 바이옴 진행 + 지형 두께/디테일 + 통로 연결 + 색칠 이탈 버그 수정 (도형·Claude)

**상세: `docs/작업기록_2026-07-25_도형_배경바이옴_지형두께_통로연결.md`**
**회의 자료: `docs/팀_작업배분_제안_2026-07-25.md` / `docs/디자이너_아트의뢰_2026-07-25.md`**

- **① ★zone_05 색칠 문제 원인 2건 규명·해결**: 도형님이 지적한 "벽돌 옆 마스킹 테이프까지
  칠해지고 가운데가 덜 칠해진다"는 **별개 버그 2개**였다. ⓐ그림 배율을 `크기/350` 으로 잡아
  **투명 여백이 폭에 비례해 커짐**(8칸 발판에서 12.4px 삐져나옴) → `(크기+12)/384` 로 바꿔
  **항상 6px 로 고정**. ⓑVFX 모드가 얼룩 스프라이트를 랜덤 배치 → `clip_children` 은 사각형
  기준이라 실루엣을 못 지키고 덮임도 미보장 → **얼룩을 셰이더 프로파일로 흡수**
  (`profile 1` = 덩어리 노이즈 + 딱딱한 경계). 이제 실루엣 이탈이 **구조적으로 불가능**하고
  100% 에서 반드시 전부 덮인다. paint_fx 의 스탬프 코드 90줄 제거.
- **② 배경 신설 — "바인처럼 이어지지만 변한다"**: `stage_backdrop.gd`. 하늘 네 귀퉁이 색을
  **월드 좌표로 보간** + 프롭 교차 페이드 → **걸어갈수록 다음 바이옴으로 변한다**
  (자연→물가→판자촌→도심→고층). 각 스테이지 끝 바이옴 = 다음 시작 바이옴이라 통로를 지나면
  하늘이 그대로 이어진다.
- **③ ★뒷벽 도입**: 발판이 "허공의 막대기"로 보인 근본 원인은 **뒤가 비어 있어서**였다.
  노이즈 암반 뒷벽 한 장으로 "덩어리에서 파낸 공간"(레인월드 문법)이 됐다.
- **④ 지형 두께**: 공중 발판 1칸→**2칸**, 선반·바닥 **3~4칸**. **윗면 y 고정**이라 17차 점프
  설계(간격 6칸/단차 3칸)가 하나도 안 바뀌고 도달성 BFS 도 그대로 통과. + 발판마다 윗면 풀/배관,
  아랫면 덩굴 자동 부착(색칠과 분리된 스프라이트라 칠해져도 디테일 유지).
- **⑤ 라이팅**: 가로등·발광 구슬(반딧불)·수면 반사 셰이더 + ZoneVisuals(환경광·플레이어 광원·
  노멀맵·비네트) 부착. 환경광을 씬별로 지정 가능하게 열었다(스테이지 0.93 / 기존 존 0.82).
- **⑥ 통로 연결**: 포탈형 `목표문` 폐기 → **터널**. **출구 속빛 = 다음 스테이지 하늘색**이라
  지나가면 방금 본 빛깔의 하늘이 펼쳐진다. 전환도 잉크 와이프 → **어둠 페이드**.
- **⑦ 체크포인트 재디자인**: 깃발(UI 기호) → **잉크 등불**. 꺼져 있다 불이 켜지고, 지나온 길에
  등불이 줄줄이 남는다 = 진행 기록이 세계 안에 남음.
- **⑧ 카메라 줌 1.0 → 0.70** (보이는 영역 1152×648 → 1646×926).
- **검증**: 자동 테스트 **6종 전부 통과**(지형을 두껍게 한 뒤에도 도달성 BFS 56/56),
  8개 씬 헤드리스 **오류 0**. 스크린샷 육안 확인 후 **7건 실제 수정**(배경 지면선·세로 시차·
  프롭 명도·프롭 밀도·회색 명도 규격·뒷벽 이음매·전경 프롭 크기).
- **미검증**: 실플레이 체감 9항목 — `팀_작업배분_제안` §5 체크리스트 참조.

### 2026-07-24 (17차) — ★페인트 v3(플랫폼 색칠) + 디자이너 타일셋 적용 + 스테이지 1~5 신설 (도형·Claude)

**상세: `docs/작업기록_2026-07-24_도형_페인트v3_타일셋적용_스테이지1-5.md`**
**비교 문서: `docs/페인트v3_셰이더_vs_VFX_비교.md` / 부품표: `docs/장애물_카탈로그.md`**

> ⚠ 팀 파일(player/gun/bullet/player_anim/Player.tscn/project.godot/stage_1.tscn) **무수정 유지.**
> 새 챕터는 `scenes/스테이지/` 에 따로 만들었으므로 팀원과 병합 충돌이 나지 않는다.

- **① 디자이너 에셋 실측·적용**: `assets/tileset/*.png` 5장은 32px 타일이 아니라 **한 칸 384×384
  짜리 "플랫폼 덩어리" 아트**임을 확인(PIL 알파 바운딩박스). → 타일맵이 아니라 **플랫폼 1개 =
  스프라이트 1장 + 9슬라이스 셰이더**로 쓰도록 파이프라인 신설(`tile_art.gd`). 아트 교체는
  같은 파일명 덮어쓰기만 하면 코드 무수정. `Shoot` 시트(640px 5프레임)도 발사 모션으로 붙임.
- **② ★페인트 시스템 v3 (7/24 회의 결정 반영)**: 색칠 단위를 **타일 1칸 → 플랫폼 1개**로 승격.
  **필요횟수 = ceil(긴 변 칸수 / 2)** → **8칸 플랫폼 = 4번**(회의 예시와 일치).
  "셰이더냐 다른 방법이냐"는 **역할 분담**으로 결론: **규칙·판정 = CPU**(paint_platform.gd,
  결정론·저장·자동테스트 가능) / **표현 = 셰이더**(ink_spread.gdshader) / **연출 = 노드**(paint_fx).
  칠해진 지점을 씨앗으로 잡아 **노이즈로 퍼지는 잉크 번짐** + 젖은 광택 구현.
- **③ 물감 흐름(Paint Drip)**: 벽·아랫면 명중 시 확률적으로 물감이 흘러내림 → 굳음 → 소멸.
  파티클이 아니라 `_draw()` 폴리곤(노드 1개·드로우콜 1회, 아트 의존 0).
- **④ ★zone_04(셰이더) / zone_05(VFX 스탬프) 비교 실험실**: **지형·규칙이 픽셀 단위로 동일**하고
  `표현_모드` 한 값만 다르다 → 공정한 A/B 비교. 추천안은 "A 기본 + 완성 순간에만 B" (비교 문서 §4).
- **⑤ ★문서 모순 발견·해소**: `레벨디자인_가이드 §3 V1`("무색 = 칠해야만 밟을 수 있음") 과
  `기획서 1-5`("무색 = 안전")가 **정면으로 반대**였고, 구현은 후자를 따르고 있었다.
  → **그러면 색칠할 이유가 없다.** 가이드 V1 을 `무색일때_통과`(기본 켜짐) 옵션으로 구현
  (유령 상태 = 물리 레이어 4번, 총알만 감지). **팀 확정 필요 1순위.**
- **⑥ 장애물 9종 + 스테이지 1~5**: `scenes/장애물/` 한글 부품(가시·회전톱·움직이는발판·
  무너지는발판·색문·떨어지는블록·체크포인트·목표문·낙하지대) — **끌어다 놓으면 끝**.
  스테이지는 기승전결(안전한 소개 → 즉사 규칙 → 회수·회색함정 → 낙사·타이밍 → 종합)로 구성.
  로비에 **"챕터 1 (신규)" 버튼** 추가(lobby.tscn 무수정, 런타임 삽입) → 스테이지 선택 화면.
- **검증**: 자동 테스트 **6종 전부 통과** (기존 4종 회귀 없음 + 신규 `test_paint_v3` 37/37,
  `test_stages` 56/56 — **스폰→목표 도달 가능성 BFS 시뮬레이션 포함**).
  7개 신규 씬 헤드리스 150프레임 **오류 0**. 창모드 스크린샷 9장 육안 확인 후 **7건 실제 수정**
  (무색 평평함·무늬 늘어짐·배경 명도 충돌·잉크 수염·번짐 과속·얇은 발판 얼룩·HUD 정렬).
- **미검증**: 실제 손맛(유령 발판 조준 난이도·무너지는 발판 0.7초·톱 왕복 3.2초·낙사 빈도) — 실플레이 필요.

### 2026-07-22 (16차) — ★게임필 개선 실제 구현: 조준 가시성 + EffectSpawner + 카메라 셰이크 (도형·Claude)

**상세: `docs/작업기록_2026-07-22_도형_게임필개선_조준가시성_이펙트스포너_카메라셰이크.md`**

> ⚠ **이번엔 실제 코드를 수정함**(12~15차는 검토·문서만). 팀 파일(player/gun/bullet/Player.tscn/
> project.godot) 무수정 유지 + 아트 에셋 0 의존(파티클 코드 생성). 수정 코드 전부 `[2026-07-22 도형]`
> 주석 + 이유 명시.

- **① 조준 궤적 가시성 개선(`proto_gun.gd`)**: 배경에 묻히던 점선(사용자 최초 불만·Ch5.3)을
  **2겹 외곽선(어두운 링+밝은 코어)으로 흑/백 지형 모두 가독** + 알파 0.32→0.85 상향 + **지형 명중
  시 빨간 탄착 마커** + 점선 흐름 애니메이션. 시스템은 기존 것 유지(5.9 "새로 만들어라"는 안 따름).
- **② EffectSpawner 신규(`proto/effect_spawner.gd`)**: 비어있던 `paint_system` 시그널 리스너를 채움
  → 색칠 스플래시·착지 먼지·회색화 히트스톱(0.06s)·카메라 킥. 파티클 `top_level`로 월드 좌표 스폰
  (Player 비균등 스케일 찌그러짐 회피) + `one_shot` 자동 정리.
- **③ 카메라 트라우마 셰이크(`proto_camera.gd`)**: `add_trauma()` 공개 API + **리밋과 독립된
  `offset` 흔들림**(구역 밖으로 안 샘). 사망=강(0.6)·색칠=약·착지=낙하 비례.
- **④ 테스트 정리**: 종료 전 존/월드 동기 `free()`. 남는 leaked 경고는 프레임워크 SceneTreeTimer라
  무해 확정(노드 해제로 못 잡음).
- **검증**: 자동 테스트 4종 **전부 통과**(EffectSpawner 조립된 상태) + `world_1.tscn` **실제 창모드
  렌더 부팅 120프레임 오류 0**. 미검증=실제 손맛·시각 밸런스(실플레이 필요, 튜닝 노브는 각 파일
  상단 상수).
- **보류**: Landing Zoom(멀미 경계)·사운드 4종(에셋 결정 필요, M2후반~M3초 권장)·팀 협의 항목·에디터 툴.

### 2026-07-22 (15차) — 오픈AI 바이블 Chapter 4 Part 2 + Chapter 5 실제 코드 대조 검토 (도형·Claude)

**상세: `docs/작업기록_2026-07-22_도형_바이블챕터4part2_챕터5_코드대조검토.md`**

- **코드 변경 없음** — 문서 대조·판정만.
- ★**Chapter 5 사실 오류 발견**: "Jump Buffer / Coyote Time = 팀 협의로 추가"라고 적혀 있으나
  **`player.gd`에 이미 둘 다 구현돼 있음**(COYOTE_TIME·JUMP_BUFFER_TIME + 가변 점프·비대칭 중력까지).
  → 5.2 표·5.8에서 이 두 항목 삭제. 착수하면 이미 있는 걸 재제작+팀 파일 침범이라 금지. (Ch3에서
  player_anim.gd 못 본 것과 같은 "파일 내용 안 읽음" 패턴.)
- ★**가장 가치 높은 실착수 항목 = 조준 궤적 가시성 개선(Ch5 5.3)**: 도형님이 처음 ChatGPT에 던진
  불만("우클릭 포물선 잘 안 보임")과 동일 문제. `proto_gun.gd`가 **일부러 흐리게**(TRAJ_COLOR
  알파 0.32→0.10) 그리는 게 원인. 시스템은 이미 완성(포물선·충돌 끊김·탄착 마커) → "Line2D로 새로
  제작"(5.9)은 오답, **상수/`_draw()` 몇 줄 튜닝(알파 상향·탄착 빨간 마커)이면 끝**. player.gd 무관.
- **이미 완료라 착수 금지**: Look Ahead(proto_camera), 점프 손맛 3종, 기본 애니(land/death),
  씬 전환 헬퍼(stage_transition). **신규 유효**: 조준 가시성·Paint/Landing FX·Camera Shake/
  Landing Zoom·Hit Stop.
- **오디오 우선순위 문서마다 엇갈림**(Part1=autoload 2순위 / Part2=M4 / Ch5=지금) → 색=즉사 장르는
  오디오 피드백이 중요하므로 **최소 사운드 4종을 M4 아닌 M2 후반~M3 초에** 권장.
- Claude Prompt가 계속 "Godot 4.4"(실제 4.6.3)·"이미 있는 걸 만들어줘"체 → **복붙 금지, 항상
  기존 proto 튜닝으로 전환해 지시**.
- Chapter 6(Paint System & VFX) 받으면 이어서 대조 예정.

### 2026-07-22 (14차) — 오픈AI 바이블 Chapter 4 Part 1(Godot Best Practice) 실제 코드 대조 검토 (도형·Claude)

**상세: `docs/작업기록_2026-07-22_도형_바이블챕터4part1_코드대조검토.md`**

- **코드 변경 없음** — 문서(Ch4 Part1) 대 실제 코드 대조만.
- **Chapter 3보다 확연히 개선됨**: 4.1 "현재 확인된 사실" 표(Stage 1개·Autoload 0·Audio 0·Save 0·
  Proto 활용)가 실제와 정확히 일치. "확장성보다 개발속도, 불필요한 매니저 금지" 방침도 이 프로젝트
  성향과 맞음 → **대체로 그대로 믿고 따라도 되는 문서.**
- **짚을 점 3건**: ①문서가 이미 존재하는 `stage_transition.gd`(씬 전환 최소 버전)를 모름 —
  "SceneManager 만들지 마라" 방침은 맞지만 출발점이 0은 아님. ②"첫 autoload=EffectManager" 추천은
  이 프로젝트 최초의 autoload+project.godot 수정이라, **먼저 proto/EffectSpawner로 검증 후 승격**
  권장(팀 통지 후). ③FX 스폰은 Player 비균등 스케일(0.795,0.368) 함정 때문에 **반드시 월드 좌표**로.
- **어제(12·13차) 우선순위와 정합**: Part 1이 그 "빈 곳"에 구현 그릇을 얹어줌 — Paint Splash/
  Landing Dust=4.3 EffectSpawner(★★★★★, 바로 착수), Camera Shake·Hit Stop=EffectManager,
  AudioManager=2순위. **착수 1번(proto/effect_spawner.gd)은 player.gd 무관이라 즉시 가능** —
  이미 비어있는 `paint_system` 시그널 리스너 자리에 연결만 하면 됨.
- Part 2(Editor Tool/Performance/Roadmap) 받으면 이어서 대조 예정.

### 2026-07-22 (13차) — 오픈AI 바이블 Chapter 3(Code Architecture) 실제 코드 대조 검토 (도형·Claude)

**상세: `docs/작업기록_2026-07-22_도형_바이블챕터3_코드대조검토.md`**

- **코드 변경 없음** — 이번엔 문서(Chapter 3) 대 실제 코드 대조만 진행.
- Chapter 3의 권장 항목 다수가 **이미 구현돼 있음을 확인**: InputComponent 역할=`gun.gd`,
  AnimationComponent 역할=`player_anim.gd`(경량 FSM 포함), Paint System의 시그널 3종(`paint_changed`·
  `gray_created`·`hit_feedback`)도 이미 존재. Chapter 3가 "현재 확인된 스크립트"에 `player_anim.gd`를
  나열해놓고도 이걸 못 본 것처럼 "AnimationComponent 신설"을 제안하는 등, **파일명만 보고 내용은
  깊이 안 읽은 흔적**이 뚜렷함.
- **진짜로 비어있는 항목은 2개뿐** — Camera Shake, Paint FX/Sound(이미 있는 시그널에 리스너만
  추가하면 됨) — 어제(12차) 감사에서 뽑은 1·2순위와 **정확히 일치**(교차검증됨).
- ⚠**"Claude Prompt"(Player를 5개 Component+FSM으로 전면 리팩터링) 그대로 실행 금지 판단**:
  `player.gd`는 팀 소유 무수정 대상이고 이미 팀원이 활발히 작업 중(`db8f55e`·`fbde89a`), 게다가
  이미 있는 것을 중복 신설하는 꼴이라 폐기.
- 다음 세션(Chapter 4 — Godot Best Practice) 받으면 이어서 실제 코드와 대조 예정.

### 2026-07-21 (12차) — 전수 점검(오류·코드 꼬임 확인) + 바이블 대비 격차 분석 + 할 일 정리 (도형·Claude)

**상세: `docs/작업기록_2026-07-21_도형_프로젝트점검_할일정리_품질리서치.md`**

- **코드 변경 없음** — HEAD(`aa43a49`) 기준 점검만 수행. 헤드리스 재임포트 오류 0, 자동 테스트
  4종 63/63 전부 재확인(paint 22·death 8·world_flow 21·lobby_flow 5).
- **코드 전수 감사(팀+proto 스크립트 전체 교차 대조)**: 살아있는 신규 버그 0건. 유일한 잠재
  버그(`bullet.gd`의 `create_alternative_tile` 콜리전 미상속 — 6차에서 이미 진단된 것)는
  **완전 죽은 코드로 확인**(`stage_1`은 어느 씬에서도 참조 안 됨 + 모든 실플레이 경로가
  `zone_lab.gd` 상속으로 런타임에 ProtoGun 교체). 과거 사고 패턴 6종(z_index·콜리전 레이어·
  시그널 경합·카메라 스케일 상속·타입추론·네이밍) 전부 재발 없음.
- **문서 오기 2건 정정**(코드 아님): `경계_색구역_시스템.md` §7이 §8-1과 자기모순이던 것,
  `파일_분류_인덱스.md`의 `main.tscn`="F5 기본 씬" 오기(실제는 10차부터 lobby.tscn).
- **Development Bible(Ch.1·2, 오픈AI 상담본) 대비 격차 분석**: 바이블이 지적한 항목 다수가
  이미 구현됨(Tween류·Look Ahead·Glow·포물선 총). **진짜 빈 곳은 구조가 아니라 기능** —
  ★사운드 0건(autoload 섹션 자체가 없음), Hit Stop/Camera Shake/Paint Splash/Landing Dust
  미구현, SaveManager 부재(dev_2 폐기 때 같이 사라짐), 다단계 퍼즐 콘텐츠는 zone3 1개뿐.
- **다음 할 일 우선순위**(상세 문서 §4): Hit Stop → Camera Shake → Paint Splash/Landing Dust →
  사운드 인프라 자리잡기 → 퍼즐 콘텐츠 확충. M2(7/26)·M3(8/2) 전 사운드·퍼즐 착수 권장.

### 2026-07-19 (11차) — zone2 경계 재구축(ColorZone) + ZONE 3 수직 도시 신설 + 일러스트·라이팅 비주얼 고도화 (도형)

**상세: `docs/작업기록_2026-07-19_도형_경계재구축_zone3_비주얼고도화.md`**

- **zone2 경계 재구축**: SwitchZone 수동 잠금 폐기 → `ColorZone`(경계_색구역_시스템.md) 적용.
  경계 위 = 자동 白 / 아래 = 자동 黑. ★중립 틈 28px 규칙(S−20 / S+8) 신설 — 문서 §8-1.
- **ZONE 3 신설**: 회색 다리(y6) → 판자촌 바닥 → 흑/백 밴드 4개를 교대로 지나는 수직 등반
  (발판 20개, 회색 전환 발판 3개) → 옥상 GOAL. 카메라 리밋 세로형 + 줌 0.9.
- **비주얼**: SVG 프롭 7종(풀·발판 캡·가로등·구슬·판잣집·고층) + 노멀맵 자동 생성 확장 +
  PointLight2D 라이팅(가로등·창문·구슬 = 깜빡임 light_flicker.gd / 옥상 달빛) +
  복도 물웅덩이 반사 셰이더. 아트 교체 워크플로 동일(같은 파일명 교체 → 생성기 재실행).
- **검증**: paint 33 + color_death 8 + world_flow 22(v2 재작성) + lobby_flow 5(신규) 전부 통과.
  스크린샷 7장 검증. 디버깅 4건(임포트 순서·셰이더 유니폼·워프-착지 경합·가로등 z) 기록 문서 §5.
- **정리**: `.gitignore` 에 tools/_shots/ 추가. `assets/player B/` 는 미참조지만 assets/p 와
  내용이 다른 시트라 **이동하지 않음** — 캐릭터 담당자 확인 요청 (기록 문서 §6).

### 2026-07-18 (10차) — 실기 피드백 반영: 로비 전면 리디자인 + 열린 골짜기 전환 + 꼬리 제거 (도형)

**상세: `docs/작업기록_2026-07-18_도형_2차_로비리디자인_열린전환.md`**

- **버렛 꼬리 제거** (캐릭터와 안 어울림 피드백) — verlet_tail.gd 삭제, 스쿼시&스트레치는 유지.
- **열린 골짜기 전환**: zone1 우측 벽·zone2 좌측 벽·복도 터널 천장 삭제를
  생성기(build_zones/build_world)에 반영 + 씬 재생성. 두 구역 사이 = 하늘 보이는 회색 중립 길.
- **구역별 카메라 줌 트윈** 추가 (proto_camera `set_region_zoom`): zone2 진입 시 0.95 로
  줌아웃되며 새 지역이 펼쳐지는 연출 (리밋 트윈과 한 호흡, GMTK/Scroll Back 근거).
- **⚠ project.godot 1줄 변경 (도형 명시 요청)**: `run/main_scene` → lobby.tscn.
  이제 F5 = 로비 → zone1 → zone2 전체 흐름. main.tscn 은 회귀용으로 유지.
- **로비 v2 전면 리디자인**: "게임 세계의 축소판 디오라마" — 흑 지형+흑 캐릭터(좌) vs
  백 지형+백 캐릭터(우), 가운데 회색 중립. 인게임 시트(player_frames) idle 재생,
  무색 발판·패럴랙스 도시·노멀맵 광원·부유 먼지·그레인. 구형 반전 오버레이·공룡 폐기.
- 디버깅: 로비 배경이 안 보이던 원인 = **전체 화면 ColorRect(z0) 캔버스에서 음수 z_index 는
  가려진다** → 트리 순서 방식으로 수정 (기록 문서 6장에 상세).
- 검증: 5씬 헤드리스 오류 0 · 자동 테스트 52/52 (paint 33 · death 8 · world 11) ·
  창모드 캡처(로비 v2/골짜기/줌 프레이밍) 육안 확인.

### 2026-07-18 (9차) — 비주얼 고도화 5종 + zone1→2 심리스 전환 + 회귀 2건 해소 (도형)

**⚠ 팀원 코드(player.gd/gun.gd/bullet.gd/Player.tscn/project.godot) 무수정 유지.**
**상세는 `docs/작업기록_2026-07-18_도형_비주얼고도화_심리스전환.md` (시스템 설명·디버깅 기록·아트 워크플로).**

**1. 신규 시스템 (전부 `scripts/proto/` 컴포넌트 — 존이 런타임 조립)**
- `proto_camera.gd` ★카메라 v2: 속도 비례 룩어헤드(달릴수록 전방이 넓게 보임, GDC
  "Scroll Back" 문법) + 수직 플랫폼 스냅 + 낙하 룩다운 + 구역 리밋 트윈.
  Player.tscn 내장 카메라는 런타임 `enabled=false` (파일 무수정).
- `proto_motion.gd` ★착지 스쿼시&스트레치(스프링 감쇠)·달리기 기울임 — 레인월드식
  "물리 상태에서 변형 계산". `verlet_tail.gd` ★버렛 체인 꼬리 (참고 영상의 IK 느낌의 실체).
- `zone_visuals.gd` ★노멀맵 라이팅 리그: 환경광 + 플레이어 광원 + 배경 노멀맵 주입 + 비네트.
  `tools/generate_normal_maps.gd` 가 타일·배경 노멀맵 자동 생성 →
  `terrain_tileset.tres` 를 CanvasTexture(디퓨즈+노멀)로 교체. **아트 교체 시 스크립트 재실행이면 끝.**

**2. ★zone1→zone2 심리스 전환 (바인 방식) — `world_1.tscn` + `world_lab.gd`**
- 씬 교체 없음: zone1+회색 복도+zone2 를 한 씬에 (`tools/build_world.gd` 생성).
- 구역 경계에서 **카메라 리밋 사각형만 1.1초 트윈** → 화면이 다음 구역으로 미끄러짐.
- 구역별 체크포인트(zone2 는 회색 복도 = 반대색 리스폰 루프 원천 차단), zone2 한정 전환 잠금,
  골인(ExitZone) → WORLD 1 CLEAR → 흰 잉크로 로비 복귀.
- 로비 "시작" → world_1 로 연결. main.tscn 은 팀 회귀용 유지 (main_scene 변경은 팀 합의 대기).

**3. 로비 퀄리티 업**: 필름 그레인+비네트 오버레이(셰이더, 에셋 0) + 타이틀 미세 맥동.

**4. ★기존 회귀 2건(7/17 발견, db8f55e 의심) 해소 — 범인은 게임 로직이 아니라 테스트**
- 헤드리스가 ~145fps(무제한)로 돌아 "프레임 수 진행 테스트" vs "실시간 사망 타이머"가
  어긋난 것. 사망→리스폰→색 복원 로직은 정상임을 프레임 로그로 확인. **db8f55e 무죄.**
- 수정: `test_color_death.gd` 에 `Engine.max_fps=60` + 무의미했던 거리 판정(160→60) 강화
  + 시나리오5 를 리스폰 무적(grace) 이후로 이동 → **8/8 통과**.

**5. 파일 정리**: 미참조 에셋 6종(슬라임·공룡 입벌림·Kenney 프로토) → `assets/_archive/`.
전체 지도 신규 문서 `docs/파일_분류_인덱스.md`.

**6. 검증**: 5씬(lobby/zone_01/zone_02/main/world_1) 헤드리스 오류 0 ·
paint 33/33 · death 8/8 · ★신규 world_flow 11/11 · 창모드 스크린샷 육안 확인
(로비/zone1 룩어헤드·꼬리/복도/zone2 전환존·카메라 팬).
**테스트 플레이**: lobby.tscn 실행 → 시작 → zone1 → 복도 → zone2 → GOAL → 로비 (막힘 없음).

### 2026-07-17 (8차) — 포물선 발사(v3) + 조준 궤적 + 흑백 반전 로비 + 잉크 씬 전환 (도형)

**⚠ 기존 팀원 코드(player.gd/gun.gd/bullet.gd/Player.tscn/project.godot) 무수정.**

**1. 포물선 발사 시스템 (proto_gun.gd / proto_bullet.gd v3)**
- 직선 발사 폐기 → **중력 포물선** (포트리스식). 좌클릭 발사, **파워 = 입~마우스 거리 비례**.
- **우클릭 홀드 = 조준 궤적**: 흐릿한 회색 점선(멀수록 더 옅음), 레이캐스트로
  **지형에 닿으면 그 지점에서 끊김**. 발사와 같은 물리 시뮬이라 궤적=실탄도 일치.
- 발사 원점 = 입(MOUTH_POS). look_at 회전 제거. Player 비균등 스케일 때문에
  점이 찌그러지는 문제는 `draw_set_transform_matrix(역행렬)` 로 월드 좌표 드로잉으로 해결.

**2. 흑백 반전 로비 (`scenes/lobby/` 신규)**
- 화면 전체 흑 바탕 + 사선 경계 오른쪽 **통째로 색 반전**(스크린 텍스처 셰이더),
  마우스 원은 XOR 재반전(커서=붓). 외부 에셋 0 = 라이선스 문제 0.
- 시작(→main.tscn 잉크 전환)/설정(볼륨·전체화면, user://settings.cfg)/종료.
- 타이틀 "G R A Y" 는 플레이스홀더. main_scene 지정은 팀 합의 후.

**3. 잉크 와이프 씬 전환 (`scripts/proto/stage_transition.gd` 신규)**
- `StageTransition.change_scene(self, path[, 잉크색])` 한 줄. autoload 불필요(root 자착·자삭).
- 물감 가장자리 노이즈 잉크가 왼쪽에서 덮고 → 씬 교체 → 오른쪽으로 걷힘(전진감).

**4. 문서** `docs/로비_에셋_배경_기획_도형.md` — CC0 에셋 추천(Kenney 1-Bit 등),
스테이지 0~3 배경 생성 프롬프트, 메트로베니아(물감 용량) 설계안, 전환 연출 가이드.

**5. 검증**
- 헤드리스: lobby / zone_01 / main.tscn 전부 **오류 0**. `test_paint_system.gd` 33항목 통과.
- ⚠ `test_color_death.gd` 2건 FAIL 발견 — **이번 변경과 무관** (변경분 stash 후 재실행해도
  동일 실패). 7/14엔 전부 통과 → 그 이후 회귀. 커밋 db8f55e("player B") 유력. 조사 필요.

### 2026-07-14 (7차) — 실기 피드백 반영: 배경 가림 버그 수정 + 색 사망 판정(v1.3) + 공룡 캐릭터 (도형)

**⚠ 기존 팀원 코드(player.gd/gun.gd/bullet.gd/Player.tscn) 여전히 무수정.**

**1. ★배경이 플레이어·지형을 가리던 버그 수정 (실기에서 발견)**
- 원인: 빌더가 Parallax2D 배경을 Player 노드보다 **나중에** 트리에 추가했는데 z_index 가
  전부 0이라, 같은 z 에서는 트리 순서대로 그려져 배경이 플레이어 위를 덮었음.
- 수정: 배경 3층에 **z_index -10/-9/-8** 부여 → 트리 순서와 무관하게 항상 맨 뒤.
  (교훈: 런타임 조립 씬에서는 트리 순서에 기대지 말고 z_index 로 명시할 것)
- zone_01 에도 같은 배경 3층 적용. 배경 SVG 세로 1000px 로 연장(바닥 아래 빈 회색 제거).

**2. 색 사망 판정 — 규칙 v1.3 (사용자 확정, 기획서 1-5·⑥·⑦ 갱신)**
- **반대색 발판을 밟으면 통과·낙하가 아니라 즉시 사망** (OVIVO 방식 폐기 → Ikaruga 방식).
- **회색 = 흑·백 모두 밟아도 안 죽는 중립 지형** (일단. 회수·재칠만 영구 불가).
- 사망 → 스폰 위치 리스폰 + **스폰 시점 색으로 복원** (반대색 리스폰 무한사망 루프를
  구현 중 발견해서 막음). zone_02 잠금 감시는 grace 중 쉬어 색 복원을 오인하지 않음.
- 구현: `zone_lab.gd _check_color_death()` — 매 물리 프레임 발밑 타일(중앙+좌우 발끝) 검사.
  player.gd 무수정 (정식 구현은 P-A — 색별 통과가 아니라 **전 타일 충돌 유지 + 사망 판정**이라
  구현이 오히려 단순해짐: 충돌 레이어 분리 불필요).
- zone_02 에 **경계선 위 회색(중립) 발판** 추가 — 흑으로 올라와 여기서 안전하게 백 전환 후
  백 구역으로 진입하는 "안전 전환 지대" 레벨 문법의 첫 사례.

**3. 캐릭터 = 공룡 도트 (슬라임 폐기)**
- arks dino-characters(https://arks.itch.io/dino-characters) 자동 다운로드가 itch 세션 보호로
  막혀서, 그 스타일 그대로 **20×16 도트 공룡을 직접 제작**: `assets/characters/dino_black.svg`
  `dino_black_open.svg` `dino_white.svg` `dino_white_open.svg` (흑/백 × 입닫힘/벌림 4프레임).
  어차피 입 벌림 프레임·흑백 2색·반전 외곽선은 원본에 없어 재제작 대상이었음.
  원본을 쓰려면 수동 다운로드 (라이선스: 무료·크레딧 @ScissorMarks 표기 권장 — 확인 필요).
- **발사 순간 입을 벌리고 페인트가 입에서 나감**: proto_gun 에 `fired` 시그널 추가 →
  zone_lab 이 0.18초간 입 벌림 프레임으로 스왑. 총구(MOUTH_POS)=입 위치.
- 지형과의 구분: 반전 외곽선(흑 몸=밝은 테두리/백 몸=어두운 테두리) + **몸통 명도를 지형과
  다르게**(흑 지형 4% vs 몸 15% / 백 지형 97% vs 몸 92%) + 배 부분 저대비 셰이드.
- 슬라임(slime_*.svg)은 파일만 남기고 미사용 (백업 후보).

**4. 경계 가시성 개선 (zone_02)**
- 경계선 = **이중선**(어두운 굵은 선 + 밝은 가는 선) — 어떤 배경 위에서도 보임.
- 위/아래 구역 틴트(위 밝게 +10% / 아래 어둡게 -22%) → "백 구역/흑 구역"이 화면 전체로 읽힘.
- SwitchZone 도 이중 테두리 + 반투명 채움. 라벨 전부 초기 카메라 시야 안으로 재배치 + 외곽선 강화.

**5. 검증**
- 신규 자동 테스트 `tools/test_color_death.gd` **8시나리오 전부 통과**
  (흰 발판 밟은 흑=사망·색복원 / 회색=흑백 모두 생존 / 존 안 전환 허용 / 존 밖 전환 잠금).
- 기존 `test_paint_system.gd` 33항목 회귀 통과. main.tscn 회귀 오류 0.
- **창모드 스크린샷으로 육안 확인**: 공룡·배경 뒤로감·경계 이중선·E 마커·x1 호버·칠해짐 팝업 정상.

### 2026-07-14 (6차) — 물감 규칙 v2 (게이지 폐지→횟수제) + 붓 커서 UI + zone_02 + 슬라임 캐릭터 (도형)

**⚠ 이번에도 기존 팀원 코드(player.gd/gun.gd/bullet.gd/Player.tscn)는 한 줄도 수정하지 않음.**
전부 `scripts/proto/` · `tools/` · 에셋 · 생성된 존 씬만 변경.

**1. 규칙 v1.2 확정 (기획서 1장·2장·3장 갱신됨 — 팀 기준 문서 참조)**
- **게이지(흑5/백5) 전면 폐지. 발사 무제한.**
- 대신 무색 발판마다 **필요 명중 횟수(1~3발) = 내구도**. 같은 색을 그만큼 맞히면 칠해짐.
- **붓 커서 UI**: 마우스 커서 = 붓. 칠 가능 발판에 올리면 남은 횟수가 작은 숫자(x1~x3)로 표시.
  반대색 완성 발판 위 = ⚠(회색 경고), 회색 위 = ✕, 다음 회수될 발판 위 = 파란 E 마커.
- 회수(E)는 **전역 FIFO 그대로 유지** — 단, 게이지 복귀가 아니라 "칠한 길을 순서대로
  되돌리는 지형 조작 도구"로 재정의. 회수된 발판은 원래 내구도의 무색으로 복귀.
- 회색 = 영구 (칸 파괴 페널티는 게이지와 함께 삭제 — 회색 그 자체가 유일한 비가역 페널티).
- 진행 중(횟수 미달) 발판에 다른 색을 쏘면 → 회색이 아니라 **새 색으로 진행 리셋** (🟡제안).
- 신규 결정: ⑫흑·백 발판 혼합 배치 금지(최소 4타일 분리), ⑬구역 클리어 자동 회수(신우와 논의).

**2. 타일셋 개편 — 횟수제를 타일로 표현하는 방법 (TB·ART 참고)**
- `placeholder_tiles.svg` 6타일로 확장(192×32): (0,0)흑 (1,0)백 (2,0)회X + **(3,0)무색-1발
  (4,0)무색-2발 (5,0)무색-3발** — 점 개수 = 필요 횟수. 흑/백 타일에는 바인(Vine) 스타일
  저대비 도트 디테일 추가(실루엣은 플랫 유지 — 아트 디렉션 원칙 그대로).
- `terrain_tileset.tres`: 6타일 전부 풀 사각 충돌 + **커스텀 데이터 레이어 `paint_hits`(int)**
  = 3:0→1, 4:0→2, 5:0→3. 코드는 이 값을 읽으므로 **ART 타일로 교체해도 로직 무수정**.
- 내구도가 다른 발판이 필요하면: 같은 행에 타일을 추가하고 `paint_hits`만 지정하면 끝.

**3. 신규/변경 파일**
| 파일 | 내용 |
|---|---|
| `scripts/proto/paint_system.gd` | ★v2 재작성 — 무제한 발사·내구도·FIFO·회색 영구 (P-A 정식 구현의 참조) |
| `scripts/proto/brush_cursor_ui.gd` | ★신규 — 붓 커서 + 횟수 숫자 + 탄착 팝업 + E 마커 + 중앙 메시지 |
| `scripts/proto/proto_gun.gd` | v2 — consume(잔량 확인) 제거, 무제한 발사. E 회수 유지 |
| `scripts/proto/zone_lab.gd` | ★신규(구 zone01_lab.gd 대체) — 존 공용 조립 + 슬라임 캐릭터 장착 |
| `scripts/proto/zone02_lab.gd` | ★신규 — 색 전환 잠금 감시(허용 구역 밖 전환 즉시 되돌림 + 안내) |
| `scenes/world_1/zone_01/zone_01.tscn` | 재생성 — 1발/2발/3발 발판 + 흑 군집(좌)·백 군집(우) 분리 배치 |
| `scenes/world_1/zone_02/zone_02.tscn` | ★신규 — 경계선 위=백만·아래=흑만, SwitchZone(전환 허용 구역), 패럴랙스 배경 3층 |
| `assets/characters/slime_black.svg` / `slime_white.svg` | ★신규 슬라임 캐릭터 16×12 도트, 단색+반전 외곽선 (Kenney 캐릭터 폐기) |
| `assets/textures/ui/brush_cursor.svg` | 붓 커서 아이콘 |
| `assets/textures/bg/bg_far·mid·near.svg` | 패럴랙스 배경 3층 (명도 규칙표 준수: 원경 55~75 / 중경 30~42 / 근경 15~30%) |
| `tools/build_zones.gd` | zone_01+zone_02 생성기 (구 build_zone01.gd 대체) |
| `tools/test_paint_system.gd` | v2 규칙으로 재작성 (33항목) |
| `tools/screenshot_zone01.gd` | v2 API 대응 + zone_02 캡처 인자 추가 |
| 삭제: `paint_gauge_ui.gd`, `zone01_lab.gd`, `build_zone01.gd` | 게이지 폐지로 폐기 (git 이력에 있음) |

**4. 슬라임 캐릭터 & 콜리전 (ART 참고)**
- 요구: 작고(화면 44×30px) 단색이며 지형·배경과 항상 구분될 것 →
  **흑 상태 = 검정 몸+밝은 외곽선 / 백 상태 = 흰 몸+어두운 외곽선** (반전 외곽선이 핵심).
  Shift 전환 시 텍스처 스왑. NEAREST 필터로 도트 또렷.
- 콜리전은 존 씬 런타임에서만 슬라임 크기(화면 44×30)로 축소 — **Player.tscn 파일 무수정**
  (새 RectangleShape2D 인스턴스 대입 방식이라 main.tscn/stage_1 은 영향 없음).
- 후보 에셋: https://arks.itch.io/dino-characters (무료, 4종 도트 공룡) — 다운로드는 라이선스
  확인 후 팀 결정. 현재 슬라임 SVG로도 방향성 검증에는 충분.

**5. 검증 결과**
- `tools/test_paint_system.gd` v2 자동 테스트 **33항목 전부 통과**
  (내구도 1/2/3발 → 진행 중 색 변경 리셋 → 같은 색 낭비 → 회색 영구·큐 제외 →
  FIFO 순서 회수·원래 내구도 복구 → 회수 후 재칠 무제한).
- zone_01 / zone_02 / main.tscn 헤드리스 120프레임 실행 **오류 0**.
- ⚠ 실기(창모드) 확인 필요: 붓 커서 표시, 숫자 가독성, zone_02 전환 잠금 체감 — 다음 세션에서.

**6. 기존 문제점 진단 (P-A 에게 전달할 것 — 직접 수정 안 함)**
- **"칠해진 지형에 총알이 통과되는" 버그의 원인**: `bullet.gd` 의 `_paint_gray()` 가
  `create_alternative_tile()` 로 회색 대체 타일을 만드는데, **Godot 4 에서 alternative tile 은
  원본 타일의 충돌 폴리곤을 상속하지 않는다.** 그래서 칠해진 셀은 충돌이 사라져
  총알(과 플레이어)이 통과한다. 해결: 프로토처럼 **아틀라스 좌표 교체 방식**
  (`set_cell(cell, source, ATLAS_GRAY)`) 을 쓰거나, alternative tile 생성 직후
  `get_tile_data(...).add_collision_polygon()` 으로 폴리곤을 복사해야 함.
  → v2 프로토(paint_system.gd)가 참조 구현.
- 회수 시스템이 "아예 작동 안 됨" 문제: 본편(main.tscn/stage_1)에는 회수가 애초에 미구현
  (E 회수는 zone_01/zone_02 프로토에만 존재). P-A 정식 구현 시 paint_system.gd v2 참조.
- bullet.gd 가 항상 회색으로만 칠하는 것도 임시 구현 — v2 규칙(쏜 색 + 횟수제)으로 재작업 대상.

### 2026-07-13 (5차) — 물감 규칙 확정 구현(프로토) + zone_01 테스트 맵 + 캐릭터 에셋 (도형)

**⚠ 기존 코드(player.gd/gun.gd/bullet.gd/씬)는 한 줄도 수정하지 않음.**
전부 신규 파일이며, zone_01 맵이 런타임에 기존 Gun 노드만 제거하고 프로토 총을 붙이는 방식.

**1. 물감 규칙 v1.1 확정 (기획서 1장·3장 갱신됨)**
- 흑/백 게이지 **분리**, 각 5칸. 발사한 색의 게이지만 차감.
- UI는 **현재 색 게이지만** 표시 — Shift 반전 순간 교체. 상단 중앙, 벡터(도트 아님),
  칸별 번호(1~), 빈 칸 = 격자 무늬.
- **회수 = E 키.** 색과 무관하게 "먼저 쏜 것"부터 전역 FIFO. 먼저 비워진 칸부터 차오름.
- 회색(흑+백 혼합) = 회수 불가(큐에서 제외) + **흑·백 최대 칸 각각 1칸 영구 파괴**(칸이 사라짐).

**2. 신규 파일**
| 파일 | 내용 |
|---|---|
| `scripts/proto/paint_system.gd` | 게이지·FIFO·회색 규칙 코어 (P-A 정식 구현의 참조용) |
| `scripts/proto/proto_gun.gd` / `proto_bullet.gd` | 좌클릭 발사 + E 회수 (E는 액션 미등록 — project.godot 무수정) |
| `scripts/proto/paint_gauge_ui.gd` | 상단 게이지 UI (사양 그대로) |
| `scripts/proto/zone01_lab.gd` | zone_01 조립 (기존 Gun 런타임 제거 → ProtoGun 부착) |
| `scenes/proto/ProtoBullet.tscn` | 프로토 총알 씬 |
| `scenes/world_1/zone_01/zone_01.tscn` | ★테스트 맵 (무색 발판 3곳·고정 흰 발판·기둥) — 직접 실행해서 확인 |
| `tools/build_zone01.gd` / `test_paint_system.gd` / `screenshot_zone01.gd` | 맵 생성기 / 규칙 자동테스트 / 화면 캡처 |
| `assets/characters/proto_player.png` (+jump) | Kenney Platformer Characters (CC0, 크레딧 불필요) — 입 위치에 총구 배치 |

**3. 검증 결과**
- `tools/test_paint_system.gd` 자동 테스트 **21항목 전부 통과** (1~3발 칠하기 → 4+5발 혼합 회색 →
  칸 파괴 → 순차 회수 → 회색 영구 제외 → 낭비 사격 → 잔량 0 거부 → 낮은 칸부터 차오름).
- zone_01 실행 화면 캡처로 육안 확인: 게이지(격자 빈칸·번호·파괴 칸 제거), 회색 X 타일, 칠해진 흑/백 타일.
- 전체 게임(main.tscn) 회귀 실행 오류 0.

**4. 타일 규격 결론: 32×32px 채택 (16px 아님)**
- 이 게임은 "발판 하나의 색 상태"가 곧 규칙이라 **한 칸이 화면에서 크고 또렷해야** 함.
  16px는 현재 카메라 배율에서 색·격자·X 무늬 같은 상태 표시가 뭉개져 가독성이 죽음.
- 30구역을 3주에 채우려면 타일 수가 적을수록 유리 (16px는 같은 면적에 4배 작업).
- 디테일은 타일 해상도가 아니라 "타일 내부 저대비 무늬 + 배경 일러스트 레이어"가 담당
  (아트 디렉션의 실루엣 원칙). 포물선 조준 정밀도와 발판 크기 균형도 32px가 적정.

### 2026-07-13 (4차) — 커밋·푸시 + 예비 타일셋 32px + 구역 씬 템플릿 (도형)

**1. 정리분+문서 전부 커밋·푸시 완료** (커밋 e822df4 → 병합 c7c64ad → origin/dev_4)
- 푸시 중 팀원의 f9dce6e("팀원 색전환 작업물을 루트로 반영, 중복 Downloads 폴더 제거")와 겹쳤으나
  **서로 동일한 정리를 한 것**이라 충돌 없이 병합됨 (스크립트 내용 diff 0 확인).

**2. 예비(플레이스홀더) 타일셋 — 32px 기준으로 교체**
- `assets/textures/tiles/placeholder_tiles.svg` 신규: 32px 타일 4종 —
  (0,0)흑 / (1,0)백 / (2,0)회색(X 무늬=죽은 지형) / (3,0)무색(점선=칠 가능)
- `terrain_tileset.tres` 재작성: tile_size 32×32, 4타일 전부 풀 사각 충돌.
  ART 타일 완성 시 **같은 배치의 이미지로 교체만** 하면 모든 씬에 자동 반영.
- ⚠ 규격은 기획서 결정②(32px)의 추천값 — 7/15에 다른 규격으로 확정되면 재생성 필요.

**3. 구역 씬 템플릿** `scenes/world_1/zone_template/zone_template.tscn`
- 새 구역 만들 때: 이 폴더를 복제 → `zone_NN`으로 이름 변경 → 지형 찍고 마커 위치만 조정.
- 구성 노드 (전부 그룹 지정 완료, 스크립트는 P-A/P-B가 그룹 기준으로 연결):
  | 노드 | 그룹 | 용도 |
  |---|---|---|
  | TileMapLayer | — | 공용 타일셋으로 지형 찍는 곳 |
  | SpawnPoint (Marker2D) | `spawn_point` | 플레이어 시작 위치 |
  | Checkpoint (Area2D) | `checkpoint` | 부활 지점 (P-B W2 시스템 연결 예정) |
  | SwitchZone (Area2D) | `switch_zone` | 이 영역 안에서만 Shift 전환 허용 (P-A 경계 시스템) |
  | ExitZone (Area2D) | `zone_exit` | 다음 구역 트리거 (P-B 진행 시스템) |
  | KillZone (Area2D) | `killzone` | 하단 낙사 판정 |
  | Obstacles (Node2D) | — | 장애물 인스턴스 배치용 컨테이너 |
- 검증: 임포트 오류 0, 템플릿 씬 단독 실행 0, 전체 게임 실행 0.

### 2026-07-13 (3차) — 기획서·플로우차트 + 레벨디자인 가이드 작성 (도형, W1 ①번 작업)

- 근거 문서 4건(개발 일정표, 스토리 기획안 ACFG, 도형_신우_스토 GRAY, 배경·지형 컨셉 초안) 정독 후 작성:
  - `docs/기획서_규칙_플로우차트.md` — **팀 기준 문서.** 상호작용 전체 명세(발사/회수 FIFO/회색 혼합/
	경계/판정/사망), 플로우차트 6종, **최우선 결정 5건 추천안**(7/15 확정 대기), 유사 게임 비교,
	스토리 통합안(최소 연출 + 엔딩 흑백→컬러, 채도 셰이더 트릭)
  - `docs/레벨디자인_가이드.md` — 물감 이코노미 공식, 소프트락 금지 3규칙, 퍼즐 부품 V1~V8,
	30구역 골격 초안, 가독성 명도표, 구역 체크리스트, 씬 파일 규칙
  - `docs/다음작업_프롬프트.md` — 다음 세션 인수인계용 (복붙 프롬프트)
- 코드는 이번에 하나도 수정하지 않음 (문서 작업만).
- ⚠ 스토리 방향 변경 확정: 회색이 규칙상 페널티(혼합·회수불가)가 되어 GRAY 스토리의 "회색=이해자"
  상징과 충돌 → 대사·컷신 없는 최소 연출 + 엔딩 컬러 전환으로 정리 (기획서 5장).

### 2026-07-13 (2차) — 잘못 커밋된 Downloads 폴더 정리 + 최신 스크립트 제자리 이동 (도형)

- 커밋 213749a("테스트완료")에 프로젝트 사본이 통째로 `Downloads/color-dev_4/color-dev_4/`
  경로로 잘못 커밋되어 있었음. **그 안의 스크립트가 루트보다 최신 버전**이었음
  (색 전환 + 페인트 총알 기능 포함).
- 조치 (코드 내용은 한 줄도 수정하지 않음, 위치만 정리):
  - `scripts/player.gd`, `gun.gd`, `bullet.gd` → Downloads 안의 최신 버전으로 교체
  - `scripts/color_defs.gd` (+.uid) → 루트 scripts/로 이동 (신규 파일)
  - `project.godot` → `toggle_color`(Shift) 입력이 추가된 최신 버전으로 교체
  - `Downloads/` 폴더 → 저장소에서 삭제 (나머지 파일은 전부 루트와 동일함을 diff로 확인)
- 검증: Godot 4.6.3 헤드리스 임포트 + 120프레임 실행 → 오류 0.
- 이로써 현재 게임 기능: 이동/점프(Space, 코요테·점프버퍼), **Shift = 흑백 전환**,
  좌클릭 사격 → **맞은 타일이 회색으로 칠해짐**.

### 2026-07-13 (1차) — dev_4 전환 완료 + 폴더 구조 정리 (도형)

**1. dev_2 → dev_4 브랜치 전환 오류 해결 (GitHub Desktop에서 안 받아지던 문제)**
- 원인: dev_2에서 작업하던 변경분이 브랜치 전환 시 자동 보관(stash)되었다가
  dev_4 최신을 받으며 복원되는 과정에서 `stage_1.tscn`에 **병합 충돌**이 났고,
  충돌이 해결되지 않은 채 남아 있어 GitHub Desktop이 모든 동작을 막고 있었음.
- 조치:
  - `stage_1.tscn` → **팀이 올린 dev_4 최신 버전(커밋 5a70d62)으로 복원** (dev_2 내용 버림)
  - dev_2의 옛 `stage_2.tscn` / `stage_3.tscn` → dev_4에서 **제거**
	(새 규칙으로 다시 만들 예정. 원본은 dev_2 브랜치에 그대로 남아 있어 언제든 참조 가능)
  - 병합 잔여 상태 파일 정리 → 현재 dev_4는 origin/dev_4와 완전 일치
- **다른 작업자 코드는 하나도 수정하지 않았음.** 순수하게 충돌 상태만 풀었음.

**2. 새로 만든 파일 (구조 정리)**
| 파일 | 용도 |
|---|---|
| `docs/작업현황_도형.md` | 이 파일. 도형 작업 기록 |
| `docs/` (기획서 2개 이동) | 루트에 있던 기획 .md를 docs/로 이동 |
| `assets/tilesets/terrain_tileset.tres` | 공용 지형 타일셋. stage_1의 인라인 타일셋과 **동일 설정**으로 분리 제작 |
| `assets/textures/tiles/` | 타일 이미지 넣을 폴더 (현재 비어 있음) |
| `scenes/obstacles/obstacle_template.tscn` | 장애물 조립용 뼈대 (Area2D + Sprite2D + CollisionShape2D, `hazard` 그룹) |

**3. 확인된 사실 / 팀에 공유할 것**
- 현재 타일셋은 8×8칸 중 **(1,0)과 (1,1) 두 칸에만 충돌**이 설정되어 있음.
  다른 칸으로 지형을 찍으면 플레이어가 통과함 → 타일 이미지 확정 후 충돌 설정 필요.
- `임시타일.svg`는 Godot 로고임. 진짜 타일 이미지로 교체 필요.
- 기획서는 "스페이스바 = 흑백 전환"이지만 실제 구현은 **Space=점프 / Shift=전환**으로 정착됨.
  기획서 표기를 실제 구현에 맞출지 팀 확인 필요.
- 총알 페인트가 기획서의 "반대색으로 뒤집기"가 아니라 **항상 회색으로 칠하기**로 변경됨
  (bullet.gd 주석에 명시됨). 회색=감속 지형 규칙과 연결되는지 프로그래머에게 확인 필요.
- stage_1.tscn은 타일셋을 씬 안에 내장하고 있음. 스테이지가 늘어나면 각 씬의 타일셋이
  제각각이 되므로, 팀 합의 후 `terrain_tileset.tres` 공용 사용으로 통일 권장.

---

## ⚠ 충돌 났을 때 확인 순서 (팀원용)

1. 충돌 난 파일이 `.tscn`이면 → Godot을 **끄고** 해결할 것 (에디터가 파일을 다시 써버림).
2. `stage_*.tscn` 충돌 → 이 파일 최신 로그에서 도형이 그 스테이지를 건드렸는지 확인.
3. `scripts/*.gd` 충돌 → 도형은 원칙적으로 스크립트를 수정하지 않음(수정 시 이 파일에 기록됨).
   기록이 없으면 프로그래머끼리의 충돌.
4. 해결 기준: **씬 배치·지형 = 도형 버전 / 스크립트 로직 = 프로그래머 버전**이 기본.
5. 판단이 어려우면 그대로 두고 팀 채팅에 파일명 + 충돌 부분 스크린샷 공유.

---

## ✅ TODO (도형)

### 17차(2026-07-24) 이후 최우선

- [ ] ★**팀 결정 ①**: 무색 발판 = "유령(칠해야 밟힘)" vs "안전한 징검다리"
      — 문서 2건이 반대. 후자면 색칠할 이유가 없어짐. `PaintPlatform.무색일때_통과` 한 줄
- [ ] ★**팀 결정 ②**: zone_04(셰이더) vs zone_05(VFX) — 직접 번갈아 보고 결정
      (`docs/페인트v3_셰이더_vs_VFX_비교.md`, Claude 추천 = A 기본 + 완성 순간만 B)
- [ ] **실플레이 튜닝**: 유령 발판 조준 난이도 / 무너지는발판 0.7초 / 회전톱 왕복 3.2초 /
      스테이지 4~5 낙사 빈도 (자동 테스트로는 못 잡는 유일한 영역)
- [ ] 사운드 4종 (색칠 성공·회색화·착지·사망) — 12차부터 계속 밀린 최우선 공백
- [ ] 일시정지 메뉴 + 체크포인트 재시작 (소프트락 최후 탈출구) → 이게 있어야 "E 회수 필수" 퍼즐 가능
- [ ] 디자이너 얼룩 텍스처로 교체 (`paint_fx.gd` `_얼룩_텍스처()`)
- [ ] v2(타일맵) ↔ v3(플랫폼) 통합 여부 결정 — world_1 을 v3 로 옮길지

### 이전부터 이어지는 것

- [ ] 실기(창모드) 확인: 붓 커서·횟수 숫자 가독성, zone_02 전환 잠금 체감, 슬라임 크기감
- [ ] 신우와 논의: 결정⑬ 구역 클리어 시 자동 회수 여부 (회색 포함?) — 기획서 1-3 추천안 참조
- [ ] 7/15 회의: 아트 결정① — 도형 의견 "1안 선호 + 채도0 셰이더면 2안과 충돌 없음" 상정
- [ ] 타일 이미지 제작·수급 → `assets/textures/tiles/` → 타일셋 등록 + 충돌 설정
	  (명도 기준: 흑 지형 0~10%, 백 지형 90~100%, 회색 감속 45~55% — 기획서 2-3 규칙표)
- [ ] stage_1 실제 플레이 검증 (충돌 없는 타일 밟히는 구간 있는지)
- [ ] 회색으로 칠해진 타일의 게임적 효과 정의 (감속? 통과? — 현재는 색만 바뀜)
- [ ] 흑백 전환 ↔ 지형 상호작용 구현 조율 (검정 상태일 때 검정 지형만 밟는 규칙, OVIVO 방식)
- [ ] stage_2 새 규칙으로 신규 제작 (기승전결 룸 구조 적용)
- [ ] 사망/리스폰(체크포인트) 사양 정리
- [ ] 기억의 조각 수집 요소 프로토타입

# 하수도 내부 접합 마감

- 빨강/노랑: ground_face_distances의 선분 끝 여유(좌우 4px)를 제거해 윗면이 벽 안쪽으로 투영되지 않게 했다.
- 하늘색: 다른 SS2D 다각형에 가려진 윗면은 마감돌·위쪽 시각 확장·사다리꼴 알파 절삭을 함께 비활성화한다. 본체 채움을 유지한다. 위에 다른 색 지형이 있어도 가려짐을 처리하며 색 규칙은 변경하지 않는다.
- 하수도_자연발판.gd: 같은 부모 아래의 보이는 SS2D를 자신의 로컬 좌표로 변환하고 근처 다각형만 전달한다. 방향을 통일한 선분의 winding 방식으로 겹치는 다각형 합집합을 처리한다. 불필요한 경계 없는 직사각형 근삿값으로 계단을 가리지 않는다.
- 에디터/런타임 최대 0.5초 주기로 점과 주변 지형을 확인하고 해시가 바뀌었을 때만 재질 갱신. 에디터는 재질을 복제하여 다른 프리팹을 오염시키지 않는다. 에디터의 선분·bounds도 실제 점으로 갱신한다.
- 윗면표시 옵션 추가. false이면 윗면 없는 기본 지형으로 사용 가능. 기본은 자동 접합.
- 제한: 같은 부모의 SS2D만 검사. 이웃 가림 선분 256 또는 자기 선분 64 초과 시 윗면 전체 숨김. 자동 색칠 충돌 판정은 바꾸지 않는다. 성능/컴파일은 엔진 검증 필요.
- 실제 stage_2-1 텍스트를 파싱해 36개 지형 및 이웃에 가려진 윗면 중앙 샘플 13곳을 확인했다. 방향 반전/중복 닫힘점/내부외부 구분 정적 수치 확인, git diff --check 통과.
- Godot 실행·화면 캡처·착지·색칠·사망·성능 검사는 AGENTS.md 엔진 제한에 따라 미실행. 결과가 시각적으로 완전히 검증됐다는 의미가 아니다.
- 제작 프롬프트 2개 갱신. Graphify는 uv trampoline 오류로 실패. 커밋/푸시 미실행.
